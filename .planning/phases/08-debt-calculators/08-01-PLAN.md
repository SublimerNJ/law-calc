---
phase: 08-debt-calculators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/debt/legal-interest/page.tsx
  - src/app/tools/debt/late-payment/page.tsx
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
must_haves:
  truths:
    - "법정이자 계산기에서 원금, 이율(연 5%), 기간을 입력하면 이자액이 출력된다"
    - "지연손해금 계산기에서 원금, 시작일, 종료일을 입력하면 민사 5% / 소송촉진법 12% 이자가 각각 출력된다"
    - "두 계산기 모두 CalculatorLayout 안에 렌더링되며 breadcrumb, 카테고리 색상이 정상 표시된다"
    - "결과 금액은 한국어 천 단위 쉼표(toLocaleString) 형식으로 표시된다"
  artifacts:
    - path: "src/app/tools/debt/legal-interest/page.tsx"
      provides: "법정이자 계산기 페이지"
      exports: ["default LegalInterestPage"]
    - path: "src/app/tools/debt/late-payment/page.tsx"
      provides: "지연손해금 계산기 페이지"
      exports: ["default LatePaymentPage"]
  key_links:
    - from: "page.tsx"
      to: "CalculatorLayout"
      via: "import CalculatorLayout from '@/components/ui/CalculatorLayout'"
      pattern: "CalculatorLayout"
    - from: "page.tsx"
      to: "tools-data.ts"
      via: "TOOLS.find / CATEGORIES.find"
      pattern: "TOOLS\\.find.*debt"
---

<objective>
법정이자 계산기(DEBT-01)와 지연손해금 계산기(DEBT-02) 두 페이지를 구현한다.

Purpose: 채권자가 법정이율 기반 이자액과 지연이자를 즉시 계산할 수 있도록 한다.
Output: src/app/tools/debt/legal-interest/page.tsx, src/app/tools/debt/late-payment/page.tsx
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- CalculatorLayout props — src/components/ui/CalculatorLayout.tsx -->
```typescript
interface CalculatorLayoutProps {
  tool: Tool;
  category: Category;
  children: React.ReactNode;
}
// Usage: wrap main content in <CalculatorLayout tool={tool} category={category}>
// "premium-card p-6" class for input/result panels
// category.color for button background and result value color
```

<!-- tools-data.ts — debt category entries -->
```typescript
// Category
{ id: "debt", name: "채권/이자", icon: "💰", color: "#06b6d4" }

// Tools
{ id: "legal-interest",    route: "/tools/debt/legal-interest",    name: "법정이자 계산기",    icon: "💰" }
{ id: "late-payment",      route: "/tools/debt/late-payment",      name: "지연손해금 계산기",  icon: "⏳" }
```

<!-- Reference pattern — src/app/tools/court/attorney-fee/page.tsx -->
```typescript
'use client';
import { useState } from 'react';
import CalculatorLayout from '@/components/ui/CalculatorLayout';
import { TOOLS, CATEGORIES } from '@/lib/tools-data';

const tool = TOOLS.find(t => t.id === 'TOOL_ID')!;
const category = CATEGORIES.find(c => c.id === 'CATEGORY_ID')!;
// ... calculate function
// ... formatNumber: (n) => n.toLocaleString('ko-KR')
export default function PageName() { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 법정이자 계산기 (DEBT-01)</name>
  <files>src/app/tools/debt/legal-interest/page.tsx</files>
  <action>
'use client' 파일로 생성. TOOLS.find id='legal-interest', CATEGORIES.find id='debt'.

**계산 로직 — calculateLegalInterest(principal, ratePercent, days):**
- 이자 = 원금 × (연이율 / 100) × (일수 / 365)
- 반환값: Math.floor(이자)

**입력 필드 (premium-card p-6):**
1. 원금 (숫자, 천 단위 포맷 표시, inputMode="numeric")
2. 연이율(%) — 기본값 5, 사용자가 변경 가능 (type="number" min="0" max="100" step="0.1")
3. 기간(일) — 숫자 입력 (type="number" min="1")

**결과 표시 (premium-card p-6):**
- 이자액: 계산 결과 (category.color)
- 원금, 적용 이율, 기간(일), 연환산 이자율 안내
- 법적 근거 텍스트: "법적 근거: 민법 제379조 (법정이율 연 5%), 상사법정이율 연 6%"

**이율 프리셋 버튼 (선택 편의):**
- "민사 5%", "상사 6%", "직접입력" — 클릭 시 ratePercent 세팅
- 선택된 프리셋은 category.color 배경

버튼 "계산하기": category.color 배경, w-full py-3.
</action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep -E "debt/legal-interest" | head -20; echo "TSC exit: $?"</automated>
  </verify>
  <done>
    - /tools/debt/legal-interest 라우트 접근 가능
    - 원금 1,000,000원, 이율 5%, 365일 입력 시 결과 50,000원
    - CalculatorLayout breadcrumb 정상 표시
  </done>
</task>

<task type="auto">
  <name>Task 2: 지연손해금 계산기 (DEBT-02)</name>
  <files>src/app/tools/debt/late-payment/page.tsx</files>
  <action>
'use client' 파일로 생성. TOOLS.find id='late-payment', CATEGORIES.find id='debt'.

**계산 로직 — calculateLatePayment(principal, startDate, endDate, rateType):**
- 지연일수 = (endDate - startDate) / (1000 * 60 * 60 * 24), Math.floor
- 민사법정이율: 연 5% (민법 제397조)
- 소송촉진법 이율: 연 12% (소송촉진 등에 관한 특례법 제3조, 2019.6.1 이후)
- 이자 = 원금 × 이율 × (일수 / 365), Math.floor
- 두 이율 모두 계산하여 함께 표시

**입력 필드:**
1. 원금 (숫자, 천 단위 포맷)
2. 지연 시작일 (type="date")
3. 지연 종료일 (type="date", 기본값 오늘: new Date().toISOString().split('T')[0])
4. 소송 제기 여부 toggle (체크박스) — 체크 시 소송촉진법 이율 12% 강조

**결과 표시:**
두 섹션으로 나눠 표시:
- 민사 법정이자(5%): 금액 (gray 톤)
- 소송촉진법 지연이자(12%): 금액 (category.color, 소송 제기 시 강조)
- 지연일수, 원금 요약
- 법적 근거: "민법 제397조(이행지체), 소송촉진 등에 관한 특례법 제3조(연 12%)"

버튼 "계산하기": category.color 배경, w-full py-3.
startDate > endDate 시 에러 메시지 표시.
</action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep -E "debt/late-payment" | head -20; echo "TSC exit: $?"</automated>
  </verify>
  <done>
    - /tools/debt/late-payment 라우트 접근 가능
    - 원금 1,000,000원, 365일 입력 시 민사 5% = 50,000원, 소촉법 12% = 120,000원
    - 날짜 역전 입력 시 에러 표시
  </done>
</task>

</tasks>

<verification>
npm run build 에러 없음 (해당 두 파일에 한해).
두 라우트 /tools/debt/legal-interest, /tools/debt/late-payment 모두 정상 렌더링.
</verification>

<success_criteria>
- DEBT-01: 법정이자 계산기 — 원금/이율/기간 입력, 이자 출력, 프리셋(민사5%/상사6%)
- DEBT-02: 지연손해금 계산기 — 원금/날짜 입력, 민사5%+소촉법12% 동시 출력
- 두 파일 모두 TypeScript 오류 없음
- CalculatorLayout 정상 사용 (breadcrumb, 카테고리 색상 #06b6d4)
</success_criteria>

<output>
After completion, create `.planning/phases/08-debt-calculators/08-01-SUMMARY.md`
</output>
