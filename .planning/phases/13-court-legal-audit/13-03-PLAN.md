---
phase: 13-court-legal-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/court/civil-mediation/page.tsx
  - src/app/tools/court/family-court/page.tsx
autonomous: true
gap_closure: true
requirements:
  - COURT-04
  - COURT-05

must_haves:
  truths:
    - "civil-mediation calculateLawsuitStampFee()의 소가 경계값이 미만(<)을 사용한다"
    - "family-court calculateStampFee()의 소가 경계값이 미만(<)을 사용한다"
    - "소가 정확히 1,000만원 입력 시 2구간 공식이 적용된다"
  artifacts:
    - path: "src/app/tools/court/civil-mediation/page.tsx"
      provides: "민사조정 인지대 계산 (경계값 수정)"
      contains: "amount < 10_000_000"
    - path: "src/app/tools/court/family-court/page.tsx"
      provides: "가사소송 인지대 계산 (경계값 수정)"
      contains: "amount < 10_000_000"
  key_links:
    - from: "civil-mediation/page.tsx"
      to: "민사소송등인지법 별표"
      via: "소가 구간 경계값 연산자"
      pattern: "amount < \\d"
    - from: "family-court/page.tsx"
      to: "민사소송등인지법 별표"
      via: "소가 구간 경계값 연산자"
      pattern: "amount < \\d"
---

<objective>
Fix boundary value operators in civil-mediation and family-court stamp fee calculations from <= (이하) to < (미만) per 민사소송등인지법 별표.

Purpose: Close 2 verification gaps — boundary values at exactly 10,000,000 / 100,000,000 / 1,000,000,000 원 currently apply wrong tier.
Output: 6 lines changed (3 per file), both calculators match 민사소송등인지법 별표 "미만" basis.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-court-legal-audit/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix civil-mediation boundary operators <= to <</name>
  <files>src/app/tools/court/civil-mediation/page.tsx</files>
  <read_first>src/app/tools/court/civil-mediation/page.tsx lines 15-35</read_first>
  <action>
In calculateLawsuitStampFee(), change 3 boundary operators from <= to < at lines 21, 24, 26:

- Line 21: `amount <= 10_000_000` → `amount < 10_000_000`
- Line 24: `amount <= 100_000_000` → `amount < 100_000_000`
- Line 26: `amount <= 1_000_000_000` → `amount < 1_000_000_000`

This aligns with 민사소송등인지법 별표 which uses "미만" (less than), not "이하" (less than or equal).

Do NOT change any other logic, formulas, or values. Only the 3 comparison operators.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -n "amount <=" src/app/tools/court/civil-mediation/page.tsx | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
- grep "amount <=" returns 0 matches in civil-mediation/page.tsx
- grep "amount < 10_000_000" returns 1 match
- grep "amount < 100_000_000" returns 1 match
- grep "amount < 1_000_000_000" returns 1 match
- npx tsc --noEmit passes with 0 errors
  </acceptance_criteria>
  <done>civil-mediation calculateLawsuitStampFee() uses < (미만) for all 3 boundary values</done>
</task>

<task type="auto">
  <name>Task 2: Fix family-court boundary operators <= to <</name>
  <files>src/app/tools/court/family-court/page.tsx</files>
  <read_first>src/app/tools/court/family-court/page.tsx lines 72-92</read_first>
  <action>
In calculateStampFee(), change 3 boundary operators from <= to < at lines 78, 81, 83:

- Line 78: `amount <= 10_000_000` → `amount < 10_000_000`
- Line 81: `amount <= 100_000_000` → `amount < 100_000_000`
- Line 83: `amount <= 1_000_000_000` → `amount < 1_000_000_000`

This aligns with 민사소송등인지법 별표 which uses "미만" (less than), not "이하" (less than or equal).

Do NOT change any other logic, formulas, or values. Only the 3 comparison operators.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -n "amount <=" src/app/tools/court/family-court/page.tsx | wc -l | tr -d ' '</automated>
  </verify>
  <acceptance_criteria>
- grep "amount <=" returns 0 matches in family-court/page.tsx
- grep "amount < 10_000_000" returns 1 match
- grep "amount < 100_000_000" returns 1 match
- grep "amount < 1_000_000_000" returns 1 match
- npx tsc --noEmit passes with 0 errors
  </acceptance_criteria>
  <done>family-court calculateStampFee() uses < (미만) for all 3 boundary values</done>
</task>

</tasks>

<verification>
1. `grep -rn "amount <=" src/app/tools/court/civil-mediation/page.tsx src/app/tools/court/family-court/page.tsx` returns no matches
2. `grep -rn "amount < " src/app/tools/court/civil-mediation/page.tsx src/app/tools/court/family-court/page.tsx` returns 3 matches per file
3. `npx tsc --noEmit` passes with 0 errors
</verification>

<success_criteria>
- Zero instances of `amount <=` in both files
- Both calculators use `<` (미만) for all stamp fee tier boundaries
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-court-legal-audit/13-03-SUMMARY.md`
</output>
