---
phase: 26-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/labor/severance-pay/page.tsx
  - src/app/tools/labor/dismissal-notice/page.tsx
  - src/app/tools/labor/annual-leave-pay/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "severance-pay에서 입사일 > 퇴사일이면 에러 표시, 잘못된 결과 미출력"
    - "severance-pay에서 근속기간 365일 미만이면 미지급 안내 (eligible=false 처리)"
    - "severance-pay에서 임금 미입력 시 에러 메시지 표시"
    - "dismissal-notice에서 월 통상임금 미입력/0 시 에러 메시지 표시"
    - "dismissal-notice에서 해고예고일수 0~29 범위 벗어나면 에러 표시"
    - "dismissal-notice에서 예고일수 30 이상이면 수당=0 안내"
    - "annual-leave-pay에서 미사용 연차일수 0 이하/비현실값(>25) 시 에러·경고 표시"
    - "annual-leave-pay에서 월 통상임금 미입력 시 에러 표시"
    - "세 계산기 모두 초기 상태에서 0원 결과 미노출"
    - "세 계산기 모두 필수 필드에 * 표시"
  artifacts:
    - path: "src/app/tools/labor/severance-pay/page.tsx"
      provides: "퇴직금 계산기 with date validation, wage validation, error state"
    - path: "src/app/tools/labor/dismissal-notice/page.tsx"
      provides: "해고예고수당 계산기 with range validation, error state"
    - path: "src/app/tools/labor/annual-leave-pay/page.tsx"
      provides: "연차수당 계산기 with day count validation, error state"
  key_links:
    - from: "severance-pay/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "dismissal-notice/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "annual-leave-pay/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
severance-pay, dismissal-notice, annual-leave-pay 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, EDGE) 감사 및 수정

Purpose: 퇴직금·해고예고수당·연차수당 3개 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러 표시 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-ux/26-CONTEXT.md
@src/app/tools/labor/severance-pay/page.tsx
@src/app/tools/labor/dismissal-notice/page.tsx
@src/app/tools/labor/annual-leave-pay/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: severance-pay 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/severance-pay/page.tsx</files>
  <read_first>src/app/tools/labor/severance-pay/page.tsx</read_first>
  <action>
severance-pay/page.tsx를 전체 읽고 16개 요구사항 각각에 대해 코드를 감사한 뒤 문제 발견 즉시 수정 (per D-03):

**현황 파악:**
- `if (!startDate || !endDate) return;` — 조용한 return, 에러 없음
- `if (!monthly || monthly <= 0) return;` — 조용한 return
- `if (totalDays <= 0) return;` — 날짜 역전 무소음 처리
- `error` state 없음 — 추가 필요
- 필수 필드 라벨에 * 없음

**INPUT 검증:**
- INPUT-01: `error` state 추가(`const [error, setError] = useState<string | null>(null);`). 임금이 0 이하면 `setError('임금을 입력해주세요.')`. 월급 모드에서 monthly <= 0이면 setError 호출.
- INPUT-02: 입사일/퇴사일 비어있으면 `setError('입사일과 퇴사일을 모두 입력해주세요.')`.
- INPUT-03: 월 급여 > 100,000,000 (1억) 이면 경고 state 추가(`const [warning, setWarning] = useState<string | null>(null);`). `setWarning('월 급여가 1억원을 초과합니다. 입력값을 확인해주세요.')` — 계산은 허용.
- INPUT-04: 날짜 필드는 type="date" 그대로 유지. 임금 필드는 이미 `handleNumberChange`로 숫자만 허용 — 확인.

**날짜 검증 (EDGE-02):**
- 퇴사일이 입사일보다 이전이면 `setError('퇴사일이 입사일보다 앞설 수 없습니다.')` + `setResult(null)`.
- totalDays <= 0 이면 동일 에러 처리.

**RESULT 표시:**
- RESULT-01: `eligible === false` (근속 < 365일)이면 결과 영역에 "퇴직금 지급 요건 미충족 (근속 1년 미만)" 안내 표시. severancePay가 0이더라도 결과 영역은 표시하되 안내 문구 포함.
- RESULT-02: `formatNumber()` 이미 있음 — 결과 섹션 모든 금액에 적용됐는지 확인, 누락 시 추가.
- RESULT-03: netSeverance = severancePay - retirementTax — 수식 검증 완료.

**FLOW:**
- FLOW-01: `result` state null이면 결과 영역 숨김 — 이미 `useState<CalcResult | null>(null)` 패턴 있음, 조건부 렌더링 확인.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 입사일 라벨을 "입사일 *"로, 퇴사일 라벨을 "퇴사일 *"로, 임금 라벨에도 "*" 추가.

**EDGE:**
- EDGE-01: totalDays = 364 (미지급), = 365 (지급) 경계값 — eligible 로직 `totalDays >= 365` 이미 있음, 확인.
- EDGE-02: 퇴사일 < 입사일 → 에러 처리 (위에서 구현).
- EDGE-03: `threeMonthDays`가 0이면 divide by zero — `if (threeMonthDays <= 0) return;` 가드 추가.

**에러/경고 표시 위치:** 계산 버튼 바로 위에:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```
에러 발생 시 `setResult(null)`, `setWarning(null)` 호출. 성공 시 `setError(null)` 호출.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|입사일과 퇴사일\|퇴사일이 입사일" src/app/tools/labor/severance-pay/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "입사일과 퇴사일\|모두 입력" severance-pay/page.tsx returns match (INPUT-02)
- grep "퇴사일이 입사일\|앞설 수 없" severance-pay/page.tsx returns match (EDGE-02)
- grep "1억\|100000000" severance-pay/page.tsx returns match (INPUT-03)
- grep "text-red-500" severance-pay/page.tsx returns match (에러 스타일)
- grep "text-orange-500" severance-pay/page.tsx returns match (경고 스타일)
- grep "필수\|\\\*" severance-pay/page.tsx returns match (FLOW-03)
- grep "지급 요건 미충족\|1년 미만" severance-pay/page.tsx returns match (RESULT-01)
- npx tsc --noEmit passes for this file (no TypeScript errors)
  </acceptance_criteria>
  <done>severance-pay 계산기가 16개 요구사항 모두 충족. 날짜 검증, 임금 미입력 에러, 비현실값 경고, 근속 미충족 안내, 필수 필드 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: dismissal-notice 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/dismissal-notice/page.tsx</files>
  <read_first>src/app/tools/labor/dismissal-notice/page.tsx, src/app/tools/labor/severance-pay/page.tsx</read_first>
  <action>
dismissal-notice/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. severance-pay에서 확립한 에러/경고 패턴과 동일하게 적용 (per D-03, D-04):

**현황 파악:**
- `if (!wage || wage <= 0 || isNaN(days) || days < 0) return;` — 조용한 return
- `error` state 없음
- `noticeDays` 필드가 `type="number"` — INPUT-04 위반 가능성
- 해고예고일수가 30 이상 입력 시 `unpaidDays = Math.max(0, 30 - days) = 0` → 수당 0, 하지만 사용자에게 설명 없음

**INPUT 검증:**
- INPUT-01: `error` state 추가. 월 통상임금이 비어있거나 0이면 `setError('월 통상임금을 입력해주세요.')`.
- INPUT-02: noticeDays가 비어있으면 `setError('해고예고일수를 입력해주세요.')`.
- INPUT-03: 월 통상임금 > 100,000,000 (1억)이면 `warning` state로 "월 통상임금이 1억원을 초과합니다. 확인해주세요." (계산 허용).
- INPUT-04: noticeDays 필드를 `type="text"` + `inputMode="numeric"`으로 교체. onChange에서 `e.target.value.replace(/[^0-9]/g, '')`. 단, 0~30 범위는 handleCalculate에서 검증.

**범위 검증:**
- noticeDays가 30 이상이면 `setError('해고예고일수는 0~29일 사이여야 합니다. 30일 이상 예고 시 해고예고수당이 발생하지 않습니다.')` — 음수는 INPUT-04에서 이미 차단.

**RESULT 표시:**
- RESULT-01: `allowance === 0`이면 결과에 "해고예고수당이 발생하지 않습니다 (30일 이상 예고)" 안내. unpaidDays=0 조건.
- RESULT-02: `formatNumber()` 이미 있음 — 결과 금액에 적용 확인.
- RESULT-03: `allowance = dailyWage × unpaidDays` — 검증 완료.

**FLOW:**
- FLOW-01: `result` state null이면 결과 영역 숨김 — 조건부 렌더링 확인.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 월 통상임금 라벨을 "월 통상임금 (원) *"로, 해고예고일수 라벨을 "해고예고일수 (0~29일) *"로 변경.

**EDGE:**
- EDGE-01: days=0 (즉시해고, unpaidDays=30), days=29 (1일 예고, unpaidDays=1) — 이미 `Math.max(0, 30 - days)` 처리됨.
- EDGE-02: 날짜 필드 없음 — skip.
- EDGE-03: dailyWage = wage / 30 — wage > 0 보장 시 divide by zero 없음. 확인.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|월 통상임금을 입력\|30일 이상\|필수\|\*" src/app/tools/labor/dismissal-notice/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "월 통상임금을 입력\|입력해주세요" dismissal-notice/page.tsx returns match (INPUT-02)
- grep "30일 이상\|발생하지 않" dismissal-notice/page.tsx returns match (RESULT-01 + 범위 에러)
- grep "text-red-500" dismissal-notice/page.tsx returns match
- grep "필수\|\\\*" dismissal-notice/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>dismissal-notice 계산기가 16개 요구사항 모두 충족. 임금 필수 입력, 예고일수 범위 에러, 수당 0원 안내, 필수 필드 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 3: annual-leave-pay 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/annual-leave-pay/page.tsx</files>
  <read_first>src/app/tools/labor/annual-leave-pay/page.tsx, src/app/tools/labor/severance-pay/page.tsx</read_first>
  <action>
annual-leave-pay/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. 동일 에러/경고 패턴 적용 (per D-03, D-04):

**현황 파악:**
- `if (!wage || wage <= 0 || !days || days <= 0) return;` — 조용한 return
- `unusedDays` 필드가 `type="number"` — INPUT-04 위반
- `error` state 없음
- 연차 상한 25일 초과 검증 없음

**INPUT 검증:**
- INPUT-01: `error` state 추가. 월 통상임금이 0 이하면 `setError('월 통상임금을 입력해주세요.')`.
- INPUT-02: unusedDays가 비어있거나 0 이하면 `setError('미사용 연차일수를 입력해주세요.')`.
- INPUT-03: unusedDays > 25이면 `warning` state로 "미사용 연차일수가 25일을 초과합니다. 근로기준법상 연차 한도(최대 25일)를 확인해주세요." (계산 허용). 월 통상임금 > 100,000,000이면 "임금이 1억원을 초과합니다. 확인해주세요."
- INPUT-04: unusedDays 필드를 `type="text"` + `inputMode="numeric"`으로 교체. onChange: `e.target.value.replace(/[^0-9]/g, '')`.

**RESULT 표시:**
- RESULT-01: totalPay가 0이면 (일반적으로 없지만 hourlyWage=0 케이스) "연차수당이 0원입니다. 임금 정보를 확인해주세요." 안내.
- RESULT-02: `formatNumber()` 적용 확인 — hourlyWage, dailyWage, totalPay 모두.
- RESULT-03: `totalPay = dailyWage × days`, `dailyWage = hourlyWage × 8` — 검증 완료.

**FLOW:**
- FLOW-01: `result` state null이면 결과 영역 숨김 — `useState<... | null>(null)` 패턴 확인.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 미사용 연차일수 라벨을 "미사용 연차일수 (일) *"로, 월 통상임금 라벨을 "월 통상임금 (원) *"로 변경.

**EDGE:**
- EDGE-01: days=1 (최소), days=25 (최대 경계값), days=26 (경고) — 처리 확인.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `monthlyBaseHours`가 209 또는 226 (0 불가) — divide by zero 없음. hourlyWage = wage / monthlyBaseHours, wage > 0 보장.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|미사용 연차일수를 입력\|25일\|필수\|\*" src/app/tools/labor/annual-leave-pay/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "미사용 연차일수를 입력\|입력해주세요" annual-leave-pay/page.tsx returns match (INPUT-02)
- grep "25일\|한도" annual-leave-pay/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" annual-leave-pay/page.tsx returns match
- grep "text-orange-500" annual-leave-pay/page.tsx returns match
- grep "필수\|\\\*" annual-leave-pay/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>annual-leave-pay 계산기가 16개 요구사항 모두 충족. 연차일수 검증, 상한 경고, 임금 필수 입력, 필수 필드 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 세 계산기 모두 에러 메시지 패턴 동일 (text-red-500/text-orange-500)
- grep "setError" 각 파일에서 매치 확인
</verification>

<success_criteria>
- severance-pay, dismissal-notice, annual-leave-pay 세 계산기 모두 음수/0/빈값/비현실값 에러·경고 처리 완료
- 초기 상태에서 0원 결과 미노출
- 필수 필드 * 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/26-ux/26-01-SUMMARY.md`
</output>
