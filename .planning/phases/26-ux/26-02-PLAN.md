---
phase: 26-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/labor/overtime-pay/page.tsx
  - src/app/tools/labor/weekly-holiday-pay/page.tsx
  - src/app/tools/labor/minimum-wage-check/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "overtime-pay에서 월 통상임금 미입력/0 시 에러 표시, 잘못된 결과 미출력"
    - "overtime-pay에서 초과근로시간 합산이 주 12시간 초과 시 경고 표시"
    - "overtime-pay에서 모든 시간 입력 0이면 의미있는 안내 제공"
    - "weekly-holiday-pay에서 주 소정근로시간 0 이하/52 초과 시 에러 표시"
    - "weekly-holiday-pay에서 시간당 통상임금 미입력 시 에러 표시"
    - "weekly-holiday-pay에서 지급 불가(개근 미충족 + 15시간 미만)면 명확한 안내"
    - "minimum-wage-check에서 임금 미입력/0 시 에러 표시"
    - "minimum-wage-check에서 최저임금 위반 여부가 명확한 색상으로 구분"
    - "세 계산기 모두 초기 상태에서 0원 결과 미노출"
    - "세 계산기 모두 필수 필드에 * 표시"
  artifacts:
    - path: "src/app/tools/labor/overtime-pay/page.tsx"
      provides: "연장·야간·휴일근로수당 계산기 with wage validation, hour range warning, error state"
    - path: "src/app/tools/labor/weekly-holiday-pay/page.tsx"
      provides: "주휴수당 계산기 with hour range validation, error state"
    - path: "src/app/tools/labor/minimum-wage-check/page.tsx"
      provides: "최저임금 계산기 with input validation, error state"
  key_links:
    - from: "overtime-pay/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "weekly-holiday-pay/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "minimum-wage-check/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
overtime-pay, weekly-holiday-pay, minimum-wage-check 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, EDGE) 감사 및 수정

Purpose: 연장·야간·휴일수당·주휴수당·최저임금 3개 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러 표시 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-ux/26-CONTEXT.md
@src/app/tools/labor/overtime-pay/page.tsx
@src/app/tools/labor/weekly-holiday-pay/page.tsx
@src/app/tools/labor/minimum-wage-check/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: overtime-pay 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/overtime-pay/page.tsx</files>
  <read_first>src/app/tools/labor/overtime-pay/page.tsx</read_first>
  <action>
overtime-pay/page.tsx를 전체 읽고 16개 요구사항 각각에 대해 코드를 감사한 뒤 문제 발견 즉시 수정 (per D-03):

**현황 파악:**
- `if (!wage || wage <= 0) return;` — 조용한 return
- `ot, nt, hd, hdo` 모두 0이어도 결과 출력 (`total=0`) — RESULT-01 문제
- `error` state 없음
- 시간 입력 필드 `handleDecimalChange` 있음 (소수점 허용) — 근로시간 소수 입력은 허용 맞음

**INPUT 검증:**
- INPUT-01: `error` state 추가. 월 통상임금이 비어있거나 0이면 `setError('월 통상임금을 입력해주세요.')` + `setResult(null)`.
- INPUT-02: 월 통상임금 필드 비어있으면 `setError` 호출.
- INPUT-03: 월 통상임금 > 100,000,000이면 `warning` state 추가 — "월 통상임금이 1억원을 초과합니다. 확인해주세요." (계산 허용). 연장근로시간 합산(`ot + hdo`)이 주 12시간 초과이면 `setWarning('연장근로시간 합산이 주 12시간을 초과합니다. 근로기준법 제53조 한도를 확인해주세요.')` — 계산은 허용.
- INPUT-04: 임금 필드는 이미 `handleNumberChange`로 숫자만 허용. 시간 필드는 `handleDecimalChange` (소수점 허용) — 현재 패턴 유지.

**RESULT 표시:**
- RESULT-01: 모든 시간 입력(ot, nt, hd, hdo)이 모두 0이면 `setError('근로시간을 한 항목 이상 입력해주세요.')`. 계산하지 않음.
- RESULT-02: `formatNumber()` 적용 확인 — hourlyWage, overtimePay, nightPay, holidayPay, total 모두.
- RESULT-03: `total = overtimePay + nightPay + holidayPay` — 합산 검증 완료.

**FLOW:**
- FLOW-01: `result` state null이면 결과 영역 숨김 — `useState<OvertimeResult | null>(null)` 패턴 확인.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 월 통상임금 라벨을 "월 통상임금 (원) *"로 변경. 근로시간 항목들 라벨에 "(시간, 0 = 해당없음)" 명시.

**EDGE:**
- EDGE-01: 모든 시간=0 → RESULT-01에서 처리. ot=0.5 (30분) → 소수 입력 허용.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `monthlyBase`가 209 또는 226 (0 불가), hourlyWage = wage / monthlyBase, wage > 0 보장 → divide by zero 없음.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|월 통상임금을 입력\|근로시간을 한\|12시간" src/app/tools/labor/overtime-pay/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "월 통상임금을 입력\|입력해주세요" overtime-pay/page.tsx returns match (INPUT-02)
- grep "근로시간을 한\|한 항목 이상" overtime-pay/page.tsx returns match (RESULT-01)
- grep "12시간\|제53조" overtime-pay/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" overtime-pay/page.tsx returns match
- grep "text-orange-500" overtime-pay/page.tsx returns match
- grep "필수\|\\\*" overtime-pay/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>overtime-pay 계산기가 16개 요구사항 모두 충족. 임금 필수 입력, 시간 미입력 에러, 12시간 초과 경고, 필수 필드 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: weekly-holiday-pay 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/weekly-holiday-pay/page.tsx</files>
  <read_first>src/app/tools/labor/weekly-holiday-pay/page.tsx, src/app/tools/labor/overtime-pay/page.tsx</read_first>
  <action>
weekly-holiday-pay/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. 동일 에러/경고 패턴 적용 (per D-03, D-04):

**현황 파악:**
- `if (!hours || hours <= 0 || !wage || wage <= 0) return;` — 조용한 return
- `error` state 없음
- 주 소정근로시간 > 52시간 입력 가능 (법정 한도 40+12=52시간) — 검증 없음
- `weeklyHolidayHours = (cappedHours / 40) * 8` — cappedHours = Math.min(weeklyHours, 40) 이미 있음

**INPUT 검증:**
- INPUT-01: `error` state 추가. 시간당 통상임금이 0 이하면 `setError('시간당 통상임금을 입력해주세요.')`.
- INPUT-02: weeklyHours 비어있거나 0이면 `setError('1주 소정근로시간을 입력해주세요.')`.
- INPUT-03: weeklyHours > 52이면 `warning` state로 "1주 소정근로시간이 52시간을 초과합니다. 근로기준법 한도(40+연장12시간)를 확인해주세요." (계산은 허용, Math.min 이미 있음). 시간당 임금 > 1,000,000이면 "시간당 임금이 100만원을 초과합니다. 확인해주세요."
- INPUT-04: weeklyHours 필드는 이미 숫자만 허용 — 확인. hourlyWage 필드도 확인.

**범위 검증:**
- weeklyHours < 1이면 `setError('소정근로시간은 1시간 이상이어야 합니다.')`.

**RESULT 표시:**
- RESULT-01: eligible === false이면 결과 영역에 "주휴수당 지급 요건 미충족" + 조건 설명 ("주 15시간 이상 근무 및 개근 필요") 표시. amount=0 안내.
- RESULT-02: `formatNumber()` 적용 확인 — amount, weeklyHolidayHours 관련.
- RESULT-03: `amount = weeklyHolidayHours × hourlyWage` — 단일 결과값, 합산 검증 해당 없음.

**FLOW:**
- FLOW-01: `result` state null이면 결과 숨김 — `useState<HolidayPayResult | null>(null)` 패턴 확인.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 1주 소정근로시간 라벨을 "1주 소정근로시간 (시간) *"로, 시간당 통상임금 라벨을 "시간당 통상임금 (원) *"로 변경.

**EDGE:**
- EDGE-01: hours=15 (최소 주휴 요건), hours=14 (미충족), hours=40, hours=52 — eligible 로직 `weeklyHours >= 15` 확인.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `cappedHours / 40` — 40으로 나눔 (0 불가), hourlyWage > 0 보장.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|시간당 통상임금을 입력\|주휴수당 지급 요건\|52시간" src/app/tools/labor/weekly-holiday-pay/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "시간당 통상임금을 입력\|입력해주세요" weekly-holiday-pay/page.tsx returns match (INPUT-02)
- grep "52시간\|한도" weekly-holiday-pay/page.tsx returns match (INPUT-03 경고)
- grep "주휴수당 지급 요건\|15시간" weekly-holiday-pay/page.tsx returns match (RESULT-01)
- grep "text-red-500" weekly-holiday-pay/page.tsx returns match
- grep "필수\|\\\*" weekly-holiday-pay/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>weekly-holiday-pay 계산기가 16개 요구사항 모두 충족. 시간/임금 필수 입력, 52시간 초과 경고, 지급 요건 미충족 안내, 필수 필드 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 3: minimum-wage-check 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/minimum-wage-check/page.tsx</files>
  <read_first>src/app/tools/labor/minimum-wage-check/page.tsx, src/app/tools/labor/overtime-pay/page.tsx</read_first>
  <action>
minimum-wage-check/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. 동일 에러/경고 패턴 적용 (per D-03, D-04):

**현황 파악:**
- `if (!val || val <= 0) return;` — 조용한 return
- `error` state 없음
- 결과에 위반/준수 표시 있음 — 색상 구분 여부 확인 필요
- 모드 전환(월급/시급) 시 이전 결과 유지 — FLOW-01 관련 점검 필요

**INPUT 검증:**
- INPUT-01: `error` state 추가. 월급/시급 값이 비어있거나 0이면 `setError('임금을 입력해주세요.')` + `setResult(null)`.
- INPUT-02: 현재 모드에 맞는 입력 필드 비어있을 때 `setError` 호출.
- INPUT-03: 월급 모드에서 monthlySalary > 100,000,000이면 `warning` state로 "월 급여가 1억원을 초과합니다. 확인해주세요." (계산 허용). 시급 모드에서 hourlyWage > 1,000,000이면 "시간당 임금이 100만원을 초과합니다."
- INPUT-04: 임금 입력 필드 `handleNumberChange`로 숫자만 허용 — 확인.

**모드 전환 처리 (FLOW-01):**
- 모드 탭 전환 시 `setResult(null)` + `setError(null)` + `setWarning(null)` 호출하여 이전 결과 제거.

**RESULT 표시:**
- RESULT-01: 결과가 없을 때 (result === null) 결과 영역 숨김 — 확인.
- RESULT-02: `formatNumber()` 적용 확인 — actualHourly, monthlyShortage, annualShortage.
- RESULT-03: 단일 결과 — 합산 검증 해당 없음.
- 위반/준수 색상 구분 확인: `isViolation`이면 결과 배경/텍스트를 `text-red-600` 또는 `bg-red-50`, 준수이면 `text-green-600` 또는 `bg-green-50`. 현재 없으면 추가.

**FLOW:**
- FLOW-01: result null이면 결과 영역 숨김. 모드 전환 시 결과 초기화.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 월급 필드 라벨을 "월 급여 (원) *"로, 시급 필드 라벨을 "시간당 임금 (원) *"로 변경.

**EDGE:**
- EDGE-01: 임금 = 최저임금 정확히 (10,320원) → isViolation=false 확인. 임금 = 10,319원 → isViolation=true 확인.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `monthlySalary / monthlyBaseHours` — monthlyBaseHours는 209 또는 226 (0 불가). monthlySalary > 0 보장.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|임금을 입력\|setResult(null)\|필수\|\*" src/app/tools/labor/minimum-wage-check/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "임금을 입력\|입력해주세요" minimum-wage-check/page.tsx returns match (INPUT-02)
- grep "1억\|100000000\|100만원\|1000000" minimum-wage-check/page.tsx returns match (INPUT-03)
- grep "text-red-500\|text-red-600\|bg-red-50" minimum-wage-check/page.tsx returns match (위반 표시)
- grep "text-green-600\|bg-green-50\|준수" minimum-wage-check/page.tsx returns match (준수 표시)
- grep "필수\|\\\*" minimum-wage-check/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>minimum-wage-check 계산기가 16개 요구사항 모두 충족. 임금 필수 입력, 비현실값 경고, 위반/준수 색상 구분, 모드 전환 시 결과 초기화, 필수 필드 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 세 계산기 모두 에러 메시지 패턴 동일 (text-red-500/text-orange-500)
- grep "setError" 각 파일에서 매치 확인
</verification>

<success_criteria>
- overtime-pay, weekly-holiday-pay, minimum-wage-check 세 계산기 모두 음수/0/빈값/비현실값 에러·경고 처리 완료
- 초기 상태에서 0원 결과 미노출
- 필수 필드 * 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/26-ux/26-02-SUMMARY.md`
</output>
