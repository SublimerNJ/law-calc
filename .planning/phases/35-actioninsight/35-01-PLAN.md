---
phase: 35-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/action-data.ts"]
autonomous: true
requirements: ["ACTION-03"]
must_haves:
  truths:
    - "action-data.ts 파일에 9개 노동/근로 계산기(해고예고, 연차, 연장근로, 주휴, 최저임금, 휴업, 출산휴가, 육아휴직, 실업급여)의 팁과 템플릿이 추가되어 있다."
    - "각 팁은 단호하고 전문적인 노무사 조언 톤으로 3가지씩 제공된다."
    - "템플릿은 당사자 간 원만한 협상 및 요구를 목적으로 하는 텍스트이다."
  artifacts:
    - path: "src/lib/action-data.ts"
      provides: "노동/근로 계산기 9종의 actionData 객체 항목"
  key_links:
    - from: "actionData object keys"
      to: "calculator IDs"
      via: "exact string matching (e.g., 'dismissal-notice')"
---

<objective>
노동/근로(나머지 9개) 계산기에 사용할 ActionInsight 기반 실전 대응 팁 및 액션 플랜 템플릿 데이터를 `action-data.ts`에 추가한다.

Purpose: 실제 계산 결과 확인 후 사용자가 취할 수 있는 현실적인 다음 행동(액션 플랜)을 제시하여 서비스 가치를 높인다.
Output: `action-data.ts` 파일 업데이트
</objective>

<context>
@.planning/phases/35-actioninsight/35-CONTEXT.md
@src/lib/action-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: action-data.ts에 노동/근로 9개 계산기 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `action-data.ts`의 `actionData` 객체에 아래 9개 계산기의 데이터를 추가한다.
    - 대상 ID: `dismissal-notice`, `annual-leave-pay`, `overtime-pay`, `weekly-holiday-pay`, `minimum-wage-check`, `shutdown-allowance`, `maternity-leave`, `parental-leave`, `unemployment-benefit`
    - 각 항목은 `title` (💡 노무사의 팁 형식), `tips` (3개의 배열, 전문적이고 단호한 어조, 법적 근거 언급), `scriptTemplate` (카톡/문자 전송용 협상 템플릿)을 포함해야 한다.
    - 템플릿 텍스트에는 필요시 금액을 나타내는 `{{AMOUNT}}` 변수를 포함할 수 있다.
    - 기존에 구현된 `severance-pay`, `industrial-accident`, `unfair-dismissal` 패턴을 참고하여 일관성을 유지한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>actionData에 9개 계산기 ID가 누락 없이 추가되고, TypeScript 타입 검사를 통과한다.</done>
</task>

</tasks>

<verification>
- `action-data.ts` 파일에 9개의 노동/근로 계산기 키가 모두 추가되었는지 확인
- TypeScript 컴파일 에러가 발생하지 않는지 확인
</verification>

<success_criteria>
- 9개의 노동/근로 계산기에 대한 실전 팁과 템플릿이 `actionData`에 반영되었다.
- 문구가 노무사의 전문적인 조언 톤으로 작성되었다.
</success_criteria>

<output>
After completion, create `.planning/phases/35-actioninsight/35-01-SUMMARY.md`
</output>