---
phase: 35-actioninsight
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified: [
  "src/app/tools/labor/dismissal-notice/page.tsx",
  "src/app/tools/labor/annual-leave-pay/page.tsx",
  "src/app/tools/labor/overtime-pay/page.tsx"
]
autonomous: true
requirements: ["UI-03"]
must_haves:
  truths:
    - "해고예고수당 계산기 하단에 ActionInsight 템플릿이 표시된다."
    - "연차수당 계산기 하단에 ActionInsight 템플릿이 표시된다."
    - "연장근로수당 계산기 하단에 ActionInsight 템플릿이 표시된다."
    - "계산 결과가 존재할 때만 팁이 화면에 렌더링된다."
  artifacts:
    - path: "src/app/tools/labor/dismissal-notice/page.tsx"
      provides: "해고예고수당 ActionInsight UI 통합"
    - path: "src/app/tools/labor/annual-leave-pay/page.tsx"
      provides: "연차수당 ActionInsight UI 통합"
    - path: "src/app/tools/labor/overtime-pay/page.tsx"
      provides: "연장근로수당 ActionInsight UI 통합"
  key_links:
    - from: "ActionInsight component"
      to: "page.tsx"
      via: "import and render with amount prop"
---

<objective>
노동/근로 계산기 중 해고예고수당, 연차수당, 연장근로수당 계산기 페이지에 `ActionInsight` 컴포넌트를 연동한다.

Purpose: 팁과 액션 템플릿을 UI로 노출하여 실무 적용력을 높인다.
Output: 3개의 계산기 페이지 업데이트
</objective>

<context>
@.planning/phases/35-actioninsight/35-CONTEXT.md
@src/lib/action-data.ts
@src/components/ui/ActionInsight.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 해고예고수당 계산기에 ActionInsight 연동</name>
  <files>src/app/tools/labor/dismissal-notice/page.tsx</files>
  <action>
    `src/app/tools/labor/dismissal-notice/page.tsx`에 `ActionInsight` 컴포넌트를 import 하고 적용한다.
    - `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가
    - 결과가 존재하는 블록(`result !== null`) 내부 하단, 기존 `<CalculatorLayout>`이 닫히기 전에 `<ActionInsight calculatorId="dismissal-notice" amount={result.noticePay} />` (또는 해당하는 결과 금액 변수)를 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ActionInsight 컴포넌트가 올바른 calculatorId로 연동되고 빌드 에러가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: 연차수당 계산기에 ActionInsight 연동</name>
  <files>src/app/tools/labor/annual-leave-pay/page.tsx</files>
  <action>
    `src/app/tools/labor/annual-leave-pay/page.tsx`에 `ActionInsight` 컴포넌트를 적용한다.
    - import 구문 추가
    - `<ActionInsight calculatorId="annual-leave-pay" amount={result.totalAllowance} />` (또는 해당하는 총 수당 변수)를 결과 영역 하단에 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ActionInsight가 연동되고 빌드 에러가 없다.</done>
</task>

<task type="auto">
  <name>Task 3: 연장근로수당 계산기에 ActionInsight 연동</name>
  <files>src/app/tools/labor/overtime-pay/page.tsx</files>
  <action>
    `src/app/tools/labor/overtime-pay/page.tsx`에 `ActionInsight` 컴포넌트를 적용한다.
    - import 구문 추가
    - `<ActionInsight calculatorId="overtime-pay" amount={result.totalPay} />` (또는 해당하는 결과 합계 변수)를 결과 영역 하단에 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ActionInsight가 연동되고 빌드 에러가 없다.</done>
</task>

</tasks>

<verification>
- 3개 페이지 모두 `ActionInsight`가 올바른 id와 함께 렌더링된다.
- TypeScript 빌드(`npx tsc --noEmit`)가 통과한다.
</verification>

<success_criteria>
- 3개 계산기 페이지 하단에 실전 대응 팁 UI가 표시된다.
</success_criteria>

<output>
After completion, create `.planning/phases/35-actioninsight/35-02-SUMMARY.md`
</output>