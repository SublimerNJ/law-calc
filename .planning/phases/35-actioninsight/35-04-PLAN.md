---
phase: 35-actioninsight
plan: 04
type: execute
wave: 2
depends_on: ["35-01"]
files_modified: [
  "src/app/tools/labor/maternity-leave/page.tsx",
  "src/app/tools/labor/parental-leave/page.tsx",
  "src/app/tools/labor/unemployment-benefit/page.tsx"
]
autonomous: true
requirements: ["UI-03"]
must_haves:
  truths:
    - "출산전후휴가 계산기 하단에 ActionInsight 템플릿이 표시된다."
    - "육아휴직 계산기 하단에 ActionInsight 템플릿이 표시된다."
    - "실업급여 계산기 하단에 ActionInsight 템플릿이 표시된다."
    - "계산 결과가 존재할 때만 팁이 화면에 렌더링된다."
  artifacts:
    - path: "src/app/tools/labor/maternity-leave/page.tsx"
      provides: "출산전후휴가 ActionInsight UI 통합"
    - path: "src/app/tools/labor/parental-leave/page.tsx"
      provides: "육아휴직 ActionInsight UI 통합"
    - path: "src/app/tools/labor/unemployment-benefit/page.tsx"
      provides: "실업급여 ActionInsight UI 통합"
  key_links:
    - from: "ActionInsight component"
      to: "page.tsx"
      via: "import and render with amount prop"
---

<objective>
노동/근로 계산기 중 출산전후휴가, 육아휴직, 실업급여 계산기 페이지에 `ActionInsight` 컴포넌트를 연동한다.

Purpose: 팁과 액션 템플릿을 UI로 노출하여 실무 적용력을 높인다.
Output: 3개의 계산기 페이지 업데이트
</objective>

<context>
@.planning/phases/35-actioninsight/35-CONTEXT.md
@src/lib/action-data.ts
@src/components/ui/ActionInsight.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 출산전후휴가 계산기에 ActionInsight 연동</name>
  <files>src/app/tools/labor/maternity-leave/page.tsx</files>
  <action>
    `src/app/tools/labor/maternity-leave/page.tsx`에 `ActionInsight` 컴포넌트를 적용한다.
    - import 구문 추가
    - 결과 영역 하단에 `<ActionInsight calculatorId="maternity-leave" amount={result.totalBenefit} />` (또는 해당하는 결과 합계 변수)를 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ActionInsight가 연동되고 빌드 에러가 없다.</done>
</task>

<task type="auto">
  <name>Task 2: 육아휴직 계산기에 ActionInsight 연동</name>
  <files>src/app/tools/labor/parental-leave/page.tsx</files>
  <action>
    `src/app/tools/labor/parental-leave/page.tsx`에 `ActionInsight` 컴포넌트를 적용한다.
    - import 구문 추가
    - 결과 영역 하단에 `<ActionInsight calculatorId="parental-leave" amount={result.totalBenefit} />` (또는 해당하는 결과 합계 변수)를 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ActionInsight가 연동되고 빌드 에러가 없다.</done>
</task>

<task type="auto">
  <name>Task 3: 실업급여 계산기에 ActionInsight 연동</name>
  <files>src/app/tools/labor/unemployment-benefit/page.tsx</files>
  <action>
    `src/app/tools/labor/unemployment-benefit/page.tsx`에 `ActionInsight` 컴포넌트를 적용한다.
    - import 구문 추가
    - 결과 영역 하단에 `<ActionInsight calculatorId="unemployment-benefit" amount={result.totalBenefit} />` (또는 해당하는 결과 합계 변수)를 추가한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ActionInsight가 연동되고 빌드 에러가 없다.</done>
</task>

</tasks>

<verification>
- 3개 페이지 모두 `ActionInsight`가 올바른 id와 함께 렌더링된다.
- TypeScript 빌드(`npx tsc --noEmit`)가 통과한다.
</verification>

<success_criteria>
- 3개 계산기 페이지 하단에 실전 대응 팁 UI가 표시된다.
</success_criteria>

<output>
After completion, create `.planning/phases/35-actioninsight/35-04-SUMMARY.md`
</output>