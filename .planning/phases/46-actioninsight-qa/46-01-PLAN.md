---
phase: 46-actioninsight-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/verify-action-insights.js"]
autonomous: false
requirements: ["QA-01"]
must_haves:
  truths:
    - "모바일 및 PC 환경에서 ActionInsight UI가 깨짐 없이 렌더링된다"
    - "모든 계산기에서 대응 팁과 템플릿 텍스트가 정상적으로 표시된다"
    - "복사 기능 등 ActionInsight의 인터랙션이 오류 없이 동작한다"
  artifacts:
    - path: "scripts/verify-action-insights.js"
      provides: "Automated verification of component integration"
      min_lines: 20
    - path: "src/lib/action-data.ts"
      provides: "ActionInsight data for all calculators"
      contains: "export const actionData"
  key_links:
    - from: "src/app/tools/**/page.tsx"
      to: "src/components/ui/ActionInsight.tsx"
      via: "JSX import and rendering"
      pattern: "<ActionInsight"
---

<objective>
53개(실제 55개) 모든 법률 계산기에서 ActionInsight 기능이 정상적으로 코드에 연동되었는지 검증하고, 실제 브라우저 환경에서 정상 동작하는지 테스트합니다.

Purpose: 
v1.8 마일스톤의 최종 검증 단계로, 사용자가 실무 대응 팁과 복사 템플릿을 문제없이 사용할 수 있는지 보장하기 위함입니다.
Output: 
- 검증 스크립트 작성 및 실행 (`scripts/verify-action-insights.js`)
- 사람에 의한 UI/UX 수동 검증 완료
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/ActionInsight.tsx
@src/lib/action-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 코드 레벨 연동 검증 스크립트 작성 및 실행</name>
  <files>scripts/verify-action-insights.js</files>
  <action>
    모든 계산기 페이지(`src/app/tools/*/page.tsx`)를 스캔하여 다음을 확인하는 Node.js 스크립트를 작성하고 실행합니다:
    1. `src/app/tools` 내의 모든 `page.tsx` 파일 찾기 (동적 라우팅 `[category]/[tool]` 제외).
    2. 각 파일이 `<ActionInsight` 컴포넌트를 임포트하고 렌더링하는지 정규식으로 검사.
    3. `src/lib/action-data.ts`에 각 계산기 ID(폴더명 기반)가 존재하는지 확인.
    
    스크립트를 작성한 후 `node scripts/verify-action-insights.js`를 실행하여 누락된 계산기가 없는지 확인합니다. 누락된 부분이 있다면 수정하지 말고 스크립트 출력 결과만 기록합니다.
  </action>
  <verify>
    <automated>node scripts/verify-action-insights.js</automated>
  </verify>
  <done>검증 스크립트가 성공적으로 실행되고 결과가 콘솔에 출력됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UI/UX 수동 검증</name>
  <files>none</files>
  <action>수동으로 브라우저에서 확인</action>
  <verify><automated>echo "manual check"</automated></verify>
  <done>수동 검증 완료</done>
  <what-built>ActionInsight 코드 통합 자동 검증 완료</what-built>
  <how-to-verify>
    1. 터미널에서 `npm run dev`를 실행하여 개발 서버를 시작합니다.
    2. 브라우저에서 `http://localhost:3000/tools/labor/severance-pay` 등 몇 가지 주요 계산기에 접속합니다.
    3. 결과 페이지 하단에 '💡 노무사의 팁' 영역이 정상적으로 표시되는지 확인합니다.
    4. 팁 내용이 줄바꿈 등 깨짐 없이 나오는지 확인하고 모바일 뷰포트에서도 레이아웃이 유지되는지 체크합니다.
    5. '👉 카톡 전송용 텍스트 복사하기' 버튼을 클릭하여 클립보드에 잘 복사되고 Alert가 뜨는지 확인합니다.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- 모든 55개 계산기 파일에 ActionInsight 컴포넌트가 연동되어 있는지 스크립트로 증명됨
- 브라우저 렌더링 및 복사 기능 인터랙션 수동 검증 완료
</verification>

<success_criteria>
- 55개 계산기 모두 ActionInsight 지원 확인 완료
- 모바일/PC 레이아웃 무결성 확보
</success_criteria>

<output>
After completion, create `.planning/phases/46-actioninsight-qa/46-01-SUMMARY.md`
</output>