---
phase: 06-realty-calculators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/realty/deposit-return/page.tsx
  - src/app/tools/realty/rent-conversion/page.tsx
  - src/app/tools/realty/brokerage-fee/page.tsx
autonomous: true
requirements: [REALTY-01, REALTY-02, REALTY-03]

must_haves:
  truths:
    - "임대차 보증금 반환 계산기: 보증금, 연체이자율, 연체일수 입력 시 반환액과 이자 계산"
    - "전월세 전환율 계산기: 전세금/보증금/월세 입력 시 전환율 또는 전환금액 계산"
    - "중개보수 계산기: 거래유형(매매/전세/월세), 거래금액 입력 시 요율 및 중개보수 계산"
    - "3개 페이지 각각 /tools/realty/{slug} 라우트로 접근 가능"
    - "결과 표시 후 법적 근거 문구 포함"
  artifacts:
    - path: "src/app/tools/realty/deposit-return/page.tsx"
      provides: "임대차 보증금 반환 계산기 페이지"
    - path: "src/app/tools/realty/rent-conversion/page.tsx"
      provides: "전월세 전환율 계산기 페이지"
    - path: "src/app/tools/realty/brokerage-fee/page.tsx"
      provides: "중개보수 계산기 페이지"
  key_links:
    - from: "page.tsx"
      to: "TOOLS/CATEGORIES in tools-data.ts"
      via: "TOOLS.find(t => t.id === '{slug}')  +  CATEGORIES.find(c => c.id === 'realty')"
    - from: "CalculatorLayout"
      to: "tool + category props"
      via: "import CalculatorLayout from '@/components/ui/CalculatorLayout'"
---

<objective>
3개 부동산 계산기 구현: 임대차 보증금 반환(REALTY-01), 전월세 전환율(REALTY-02), 중개보수(REALTY-03).

Purpose: 임대인/임차인이 보증금 반환, 전·월세 전환, 중개보수를 직접 계산할 수 있도록 한다.
Output: 3개 독립 라우트 페이지 (각각 'use client', CalculatorLayout 사용)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- tools-data.ts에서 추출한 realty 도구 ID/route 정보 -->
Tool IDs (category: "realty", color: "#8b5cf6"):
  - { id: "deposit-return", name: "임대차 보증금 반환 계산기", icon: "💰", route: "/tools/realty/deposit-return" }
  - { id: "rent-conversion", name: "전월세 전환율 계산기", icon: "🔄", route: "/tools/realty/rent-conversion" }
  - { id: "brokerage-fee", name: "중개보수 계산기", icon: "🤝", route: "/tools/realty/brokerage-fee" }

Category: { id: "realty", name: "부동산", color: "#8b5cf6" }

CalculatorLayout props: { tool: Tool, category: Category, children: ReactNode }
Pattern: const tool = TOOLS.find(t => t.id === 'XXX')!;
         const category = CATEGORIES.find(c => c.id === 'realty')!;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 임대차 보증금 반환 계산기 (REALTY-01)</name>
  <files>src/app/tools/realty/deposit-return/page.tsx</files>
  <read_first>src/app/tools/court/attorney-fee/page.tsx</read_first>
  <action>
'use client' 컴포넌트. attorney-fee/page.tsx 패턴 그대로 사용.

계산 로직 (주택임대차보호법 기준):
- 입력: 보증금(원), 반환 지연일수, 연체이자율(기본값: 연 5%, 주택임대차보호법상 법정이자율)
- 계산: 지연이자 = 보증금 × (연체이자율/100) × (지연일수/365)
- 출력: 보증금 원금, 지연이자, 합계 반환액

UI 구조 (attorney-fee 패턴 따름):
1. 입력 카드 (premium-card p-6 mb-4):
   - 보증금: 숫자 입력 (천단위 콤마 표시)
   - 지연일수: number 입력
   - 연체이자율: number 입력 (기본값 5, step 0.1)
   - 계산하기 버튼 (category.color 배경)
2. 결과 카드 (premium-card p-6, result !== null 조건):
   - 보증금, 지연이자, 합계 반환액 (합계를 category.color로 강조)
   - 법적 근거: "주택임대차보호법 제3조의2, 민법 제387조"

formatNumber 함수: n.toLocaleString('ko-KR')
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/tools/realty/deposit-return 2>/dev/null || echo "dev server not running - file existence check: $(test -f 'src/app/tools/realty/deposit-return/page.tsx' && echo EXISTS || echo MISSING)"</automated>
  </verify>
  <acceptance_criteria>
    - 보증금 1억원, 지연 30일, 이자율 5% → 지연이자 약 410,958원, 합계 100,410,958원
    - 파일 존재: src/app/tools/realty/deposit-return/page.tsx
    - 'use client' 선언, CalculatorLayout 사용, tool id='deposit-return'
  </acceptance_criteria>
  <done>deposit-return 페이지가 렌더링되고 보증금+지연일수 입력 시 정확한 반환액 계산</done>
</task>

<task type="auto">
  <name>Task 2: 전월세 전환율 계산기 (REALTY-02)</name>
  <files>src/app/tools/realty/rent-conversion/page.tsx</files>
  <action>
'use client' 컴포넌트. 동일 패턴.

계산 로직 (주택임대차보호법 제7조의2):
모드 선택: "전세→월세 전환" 또는 "월세→전세 전환"

전세→월세 전환:
- 입력: 전세금(원), 보증금(원, 월세 전환 후), 전환율(%)
- 월세 = (전세금 - 보증금) × 전환율/100 ÷ 12
- 법정 상한 전환율: 2026년 기준 연 6% (한국은행 기준금리 + 2%p, 기준금리 3.5% 가정 → 5.5% 적용, 표시는 입력값으로)

월세→전세 전환:
- 입력: 보증금(원), 월세(원), 전환율(%)
- 전세금 = 보증금 + 월세 × 12 ÷ (전환율/100)

UI:
1. 입력 카드:
   - 전환 모드 라디오 (전세→월세 / 월세→전세)
   - 모드에 따라 입력 필드 동적 표시
   - 전환율 입력 (기본값 5.5, step 0.1)
   - 계산하기 버튼
2. 결과 카드:
   - 모드별 결과 표시 (월세금액 또는 전세금액)
   - 법적 근거: "주택임대차보호법 제7조의2"
  </action>
  <verify>
    <automated>test -f "/Volumes/NJ SSD 4T/Coding/loop creation 2/src/app/tools/realty/rent-conversion/page.tsx" && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - 전세금 3억, 보증금 5천만, 전환율 5.5% → 월세 약 1,145,833원
    - 파일 존재, 'use client', tool id='rent-conversion'
  </acceptance_criteria>
  <done>rent-conversion 페이지가 렌더링되고 양방향 전환 계산 동작</done>
</task>

<task type="auto">
  <name>Task 3: 중개보수 계산기 (REALTY-03)</name>
  <files>src/app/tools/realty/brokerage-fee/page.tsx</files>
  <action>
'use client' 컴포넌트. 동일 패턴.

계산 로직 (공인중개사법 시행규칙 별표1, 2026년 기준):

거래유형 선택: 매매/교환, 임대차(전세/월세)

매매/교환 요율:
- 5천만원 미만: 0.6% (한도 250,000원)
- 5천만~2억 미만: 0.5% (한도 800,000원)
- 2억~9억 미만: 0.4%
- 9억~12억 미만: 0.5%
- 12억~15억 미만: 0.6%
- 15억 이상: 0.7%

임대차(보증금 기준, 월세는 보증금 + 월세×100으로 환산):
- 5천만원 미만: 0.5% (한도 200,000원)
- 5천만~1억 미만: 0.4% (한도 300,000원)
- 1억~3억 미만: 0.3%
- 3억~6억 미만: 0.4%
- 6억 이상: 0.5% (최대 0.8% 협의)

한도 적용: Math.min(거래금액 × 요율, 한도) (5천만 미만 구간만 한도 적용)

월세 거래 시 환산 보증금: 보증금 + 월세 × 100 (이 금액으로 요율 결정, 실제 중개보수는 거래금액 기준)

UI:
1. 입력 카드:
   - 거래유형 라디오 (매매/교환, 임대차)
   - 임대차 선택 시: 전세/월세 구분 라디오 추가
   - 거래금액/보증금 입력
   - 월세 선택 시 월세 입력 추가
   - 계산하기 버튼
2. 결과 카드:
   - 적용 요율, 중개보수, 부가세(10%) 포함 총액
   - 법적 근거: "공인중개사법 시행규칙 별표1"
  </action>
  <verify>
    <automated>test -f "/Volumes/NJ SSD 4T/Coding/loop creation 2/src/app/tools/realty/brokerage-fee/page.tsx" && echo "EXISTS" || echo "MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - 매매 3억원 → 요율 0.4%, 중개보수 1,200,000원, 부가세 포함 1,320,000원
    - 파일 존재, 'use client', tool id='brokerage-fee'
  </acceptance_criteria>
  <done>brokerage-fee 페이지가 렌더링되고 거래유형/금액별 중개보수 정확 계산</done>
</task>

</tasks>

<verification>
3개 파일 모두 존재:
- src/app/tools/realty/deposit-return/page.tsx
- src/app/tools/realty/rent-conversion/page.tsx
- src/app/tools/realty/brokerage-fee/page.tsx

각 파일 상단 'use client' 선언 확인.
각 파일에서 TOOLS.find(t => t.id === '{correct-id}')! 사용 확인.
npm run build 오류 없음.
</verification>

<success_criteria>
- 3개 페이지 /tools/realty/{slug} 라우트로 접근 가능
- 각 계산기 입력값 → 정확한 계산 결과 출력
- 한국 법령 기준(주택임대차보호법, 공인중개사법) 적용
- CalculatorLayout 사용, category.color (#8b5cf6) 강조색 적용
</success_criteria>

<output>
완료 후 `.planning/phases/06-realty-calculators/06-01-SUMMARY.md` 생성
</output>
