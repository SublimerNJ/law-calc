---
phase: 27-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/labor/parental-leave/page.tsx
  - src/app/tools/labor/unemployment-benefit/page.tsx
  - src/app/tools/labor/shutdown-allowance/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "parental-leave에서 월 통상임금 미입력/0 시 에러 메시지 표시"
    - "parental-leave에서 월 통상임금 > 1억이면 경고 표시"
    - "parental-leave에서 계산식 pre 텍스트가 코드 로직과 일치 (상한 250/200/160만원 기재)"
    - "parental-leave 필수 필드에 * 표시"
    - "unemployment-benefit에서 월임금 미입력/0 시 에러 메시지 표시"
    - "unemployment-benefit에서 월임금 > 1억이면 경고 표시"
    - "unemployment-benefit에서 초기 상태 결과 영역 미노출"
    - "unemployment-benefit 필수 필드에 * 표시"
    - "shutdown-allowance에서 평균임금 미입력/0 시 에러 메시지 표시 (기존 calculated 플래그 방식 개선)"
    - "shutdown-allowance에서 휴업일수 미입력/0 시 에러 메시지 표시"
    - "shutdown-allowance에서 평균임금 > 500,000이면 경고 표시"
    - "shutdown-allowance에서 초기 상태 결과 영역 미노출 (현재 calculated boolean 사용 — result state 방식으로 통일 또는 에러 표시 추가)"
    - "shutdown-allowance 필수 필드에 * 표시"
    - "세 계산기 모두 에러 text-red-500, 경고 text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/labor/parental-leave/page.tsx"
      provides: "육아휴직급여 계산기 with error state, wage validation, corrected formula comment"
    - path: "src/app/tools/labor/unemployment-benefit/page.tsx"
      provides: "실업급여 계산기 with error state, wage validation"
    - path: "src/app/tools/labor/shutdown-allowance/page.tsx"
      provides: "휴업수당 계산기 with error state, wage+days validation"
  key_links:
    - from: "parental-leave/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|text-red-500"
    - from: "unemployment-benefit/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|text-red-500"
    - from: "shutdown-allowance/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|text-red-500"
---

<objective>
parental-leave, unemployment-benefit, shutdown-allowance 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 육아휴직급여·실업급여·휴업수당 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러 표시, 계산식 주석 정확성 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-ux/27-CONTEXT.md
@src/app/tools/labor/parental-leave/page.tsx
@src/app/tools/labor/unemployment-benefit/page.tsx
@src/app/tools/labor/shutdown-allowance/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: parental-leave 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/parental-leave/page.tsx</files>
  <read_first>src/app/tools/labor/parental-leave/page.tsx</read_first>
  <action>
parental-leave/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정 (per D-03):

**현황 파악 (코드 기반):**
- `error` state 없음 — `if (!w || w <= 0) return;` 조용한 return
- 필수 필드 라벨에 * 없음
- 계산식 pre 텍스트: "전 기간: 월 통상임금 × 80% (상한 150만원, 하한 70만원)" — 코드의 실제 상한이 1~3개월 250만원/4~6개월 200만원/7개월~ 160만원인데 "상한 150만원"으로 표기됨 → 수정 필요 (Critical)
- `result` state `useState<... | null>(null)`, `{result && (...)}` 조건부 렌더링 — FLOW-01 ok
- `months` select로 1~12개월 선택 — 음수/0 불가, INPUT-01 자동 충족
- RESULT-03 검증: `total = sum(monthly[].amount)` — 부분합/총합 별도 없음, 총합만 표시. ok

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 재작성:**
1. 시작 시: `setError(null); setWarning(null);`
2. 임금 미입력/0: `if (!w || w <= 0) { setError('월 통상임금을 입력해주세요.'); setResult(null); return; }` (INPUT-02)
3. 임금 상한 경고 (INPUT-03): `if (w > 100_000_000) { setWarning('월 통상임금이 1억원을 초과합니다. 입력값을 확인해주세요.'); }` — 계산 허용
4. 성공 시: `setError(null);`

**에러/경고 표시 위치:** 계산 버튼 바로 위에:
```tsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**계산식 주석 수정 (Critical):**
결과 영역의 pre 태그 텍스트를 코드 실제 로직에 맞게 수정:
```
전 기간: 월 통상임금 × 80% (상한 150만원, 하한 70만원)
```
→
```
1~3개월: 월 통상임금 × 80% (상한 250만원, 하한 70만원)
4~6개월: 월 통상임금 × 80% (상한 200만원, 하한 70만원)
7개월~: 월 통상임금 × 80% (상한 160만원, 하한 70만원)
한부모/장애아동: 첫 3개월 상한 300만원
2024.1.1 개정: 사후지급금 제도 폐지, 전액 즉시 지급
```

**필수 필드 라벨 수정 (FLOW-03):**
- "월 통상임금 (원)" → "월 통상임금 (원) *"
- "육아휴직 기간 (개월)" — select 기본값 있음, * 불필요
- "유형 선택" — radio 기본값 있음, * 불필요

**EDGE-01:** `calcMonthlyBenefit`에서 `Math.min(Math.max(raw, PARENTAL_LOWER), upper)` — 하한/상한 적용 정확. wage가 매우 낮아 raw < 700,000이면 하한 700,000원 적용. ok.

**EDGE-03:** `monthlyWage * PARENTAL_RATE` — w > 0 보장. `(wage * days) / 12` 없음. ok.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|월 통상임금을 입력\|250만원\|상한 250\|\*" src/app/tools/labor/parental-leave/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" parental-leave/page.tsx returns match (에러 state 존재)
- grep "월 통상임금을 입력\|입력해주세요" parental-leave/page.tsx returns match (INPUT-02)
- grep "text-red-500" parental-leave/page.tsx returns match
- grep "text-orange-500" parental-leave/page.tsx returns match
- grep "250만원\|상한 250\|250,000" parental-leave/page.tsx returns match (계산식 주석 수정 확인)
- grep "\\\*" parental-leave/page.tsx returns match (FLOW-03)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "parental-leave" | wc -l → 0
  </acceptance_criteria>
  <done>parental-leave 계산기가 16개 요구사항 모두 충족. 임금 필수 입력 에러, 비현실값 경고, 계산식 주석 수정(150만원→250/200/160만원 단계별), 필수 필드 * 표시, TypeScript 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 2: unemployment-benefit 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/unemployment-benefit/page.tsx</files>
  <read_first>src/app/tools/labor/unemployment-benefit/page.tsx</read_first>
  <action>
unemployment-benefit/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. 동일 에러/경고 패턴 적용 (per D-03, D-04):

**현황 파악 (코드 기반):**
- `error` state 없음 — `if (!w || w <= 0) return;` 조용한 return
- `result` state `useState<... | null>(null)`, `{result && (...)}` 조건부 렌더링 — FLOW-01 ok
- `period`, `ageGroup`, `dailyHours` 모두 select/radio 기본값 있음 — INPUT-01 자동 충족
- 필수 필드 라벨에 * 없음
- RESULT-03: total = dailyPayment × days, 부분합/세부항목 없음 — ok
- RESULT-02: `formatNumber()` 결과 금액 모두 적용됨 — ok
- 상한/하한 적용 여부 표시 이미 있음 — RESULT-01 ok (0원 불가)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 재작성:**
1. 시작 시: `setError(null); setWarning(null);`
2. 임금 미입력/0: `if (!w || w <= 0) { setError('퇴직 전 3개월 평균 월임금을 입력해주세요.'); setResult(null); return; }` (INPUT-02)
3. 임금 상한 경고 (INPUT-03): `if (w > 100_000_000) { setWarning('월임금이 1억원을 초과합니다. 입력값을 확인해주세요.'); }` — 계산 허용
4. 성공 시: `setError(null);`

**에러/경고 표시 위치:** 계산 버튼 바로 위에:
```tsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**필수 필드 라벨 수정 (FLOW-03):**
- "퇴직 전 3개월 평균 월임금 (세전, 원)" → "퇴직 전 3개월 평균 월임금 (세전, 원) *"
- 피보험기간, 연령/장애 구분, 1일 소정근로시간은 select/radio 기본값 있음 — * 불필요

**EDGE-01 확인:** BENEFIT_DAYS 테이블 경계값:
- lt1(1년 미만): 50세 미만 120일, 50세 이상 120일 — 동일 (정상)
- gte10(10년 이상): 50세 미만 240일, 50세 이상 270일 (정상)
- 코드 확인: `const days = BENEFIT_DAYS[ageGroup][period];` — ok

**EDGE-03:** `dailyBase = Math.floor((w * 12) / 365 * 0.6)` — w > 0 보장. `dailyLower = Math.floor(10_320 * 0.8 * dailyHours)` — 상수. ok.

**CONSIST-01 (급여 입력 방식):** 월임금 입력 — 26-ux와 동일 pattern. ok.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|평균 월임금을 입력\|\*" src/app/tools/labor/unemployment-benefit/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" unemployment-benefit/page.tsx returns match (에러 state 존재)
- grep "평균 월임금을 입력\|입력해주세요" unemployment-benefit/page.tsx returns match (INPUT-02)
- grep "text-red-500" unemployment-benefit/page.tsx returns match
- grep "text-orange-500" unemployment-benefit/page.tsx returns match
- grep "\\\*" unemployment-benefit/page.tsx returns match (FLOW-03)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "unemployment-benefit" | wc -l → 0
  </acceptance_criteria>
  <done>unemployment-benefit 계산기가 16개 요구사항 모두 충족. 임금 필수 입력 에러, 비현실값 경고, 필수 필드 * 표시, TypeScript 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 3: shutdown-allowance 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/shutdown-allowance/page.tsx</files>
  <read_first>src/app/tools/labor/shutdown-allowance/page.tsx</read_first>
  <action>
shutdown-allowance/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정 (per D-03, D-04):

**현황 파악 (코드 기반):**
- `error`/`warning` state 없음
- `calculated` boolean 플래그 사용 (다른 계산기와 다른 패턴) — CONSIST-01 위반 소지
- `handleCalculate`에서 `if (avgWage <= 0 || days <= 0) return;` 조용한 return
- 결과 표시: `{calculated && avgWage > 0 && days > 0 && (...)}` — 이중 조건으로 결과 감춤
- 필수 필드 라벨에 * 없음
- `laborBoardApproval=true`일 때 `approvedRate` 필드가 `type="text"` + `inputMode="numeric"` — ok
- `approvedRate` 기본값 "60", 빈값 시 `parseFloat('') || 70` → 70으로 폴백. EDGE-03 ok

**수정 방향:**
`calculated` 플래그는 유지하되 에러 state를 추가. handleCalculate에서 명시적 에러 메시지 표시.
(calculated 플래그 제거 후 result state 방식으로 전환은 과도한 리팩터링 — D-06 원칙 준수)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 수정:**
1. 시작 시: `setError(null); setWarning(null); setCalculated(false);`
2. 평균임금 미입력/0: `if (avgWage <= 0) { setError('1일 평균임금을 입력해주세요.'); return; }` (INPUT-02)
3. 휴업일수 미입력/0: `if (days <= 0) { setError('휴업일수를 입력해주세요.'); return; }` (INPUT-02)
4. 평균임금 상한 경고 (INPUT-03): `if (avgWage > 500_000) { setWarning('1일 평균임금이 50만원을 초과합니다. 확인해주세요.'); }` — 계산 허용
5. 휴업일수 상한 경고 (INPUT-03): `if (days > 365) { setWarning((prev) => prev ? prev + ' 휴업일수가 1년을 초과합니다. 확인해주세요.' : '휴업일수가 1년을 초과합니다. 확인해주세요.'); }` — 계산 허용
6. `laborBoardApproval=true`이고 `approvedRate` 값이 빈값/0 이하면: `setError('승인 지급률(%)을 입력해주세요.')` (INPUT-02)
7. 기존 `setCalculated(true)` 유지

**에러/경고 표시 위치:** 계산 버튼 바로 위에 (checkbox 아래):
```tsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**필수 필드 라벨 수정 (FLOW-03):**
- "1일 평균임금 (원)" → "1일 평균임금 (원) *"
- "휴업일수 (일)" → "휴업일수 (일) *"
- "월 통상임금 (원)" → "월 통상임금 (원, 선택사항)" — 이미 선택사항이고 없어도 계산됨
- "승인 지급률 (%)" — laborBoardApproval=true일 때만 표시, 맥락상 필수 → "승인 지급률 (%) *"

**FLOW-01:** `{calculated && avgWage > 0 && days > 0 && (...)}` — 에러가 표시될 때 `calculated=false`이므로 결과 영역 자동 숨김. ok.

**EDGE-01:** avgWage70 계산: `Math.floor(avgWage * 0.7)` — avgWage > 0 보장. 통상임금이 0일 때 (monthlyOrdinaryWage 미입력) effectiveDaily = avgWage70 (올바름). ok.

**EDGE-03:** `dailyOrdinaryWage = Math.floor(ordinaryWageMonthly / 30)` — ordinaryWageMonthly가 0이면 dailyOrdinaryWage=0. effectiveDaily 계산 시: `avgWage70 > 0 && dailyOrdinaryWage = 0` → `avgWage70 > dailyOrdinaryWage` → `dailyOrdinaryWage = 0` 반환? 아니다: `(avgWage70 > dailyOrdinaryWage && dailyOrdinaryWage > 0) ? dailyOrdinaryWage : avgWage70` — `dailyOrdinaryWage = 0`이면 두 번째 조건 false → avgWage70 반환. ok.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|평균임금을 입력\|휴업일수를 입력\|\*" src/app/tools/labor/shutdown-allowance/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" shutdown-allowance/page.tsx returns match (에러 state 존재)
- grep "평균임금을 입력\|휴업일수를 입력\|입력해주세요" shutdown-allowance/page.tsx returns match (INPUT-02)
- grep "text-red-500" shutdown-allowance/page.tsx returns match
- grep "text-orange-500" shutdown-allowance/page.tsx returns match
- grep "\\\*" shutdown-allowance/page.tsx returns match (FLOW-03)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "shutdown-allowance" | wc -l → 0
  </acceptance_criteria>
  <done>shutdown-allowance 계산기가 16개 요구사항 모두 충족. 평균임금/휴업일수 에러 처리, 비현실값 경고, 필수 필드 * 표시, TypeScript 에러 없음.</done>
</task>

</tasks>

<verification>
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit — 전체 프로젝트 빌드 에러 없음
- 세 계산기 모두 grep "text-red-500" returns match
- 세 계산기 모두 grep "text-orange-500" returns match
- 세 계산기 모두 grep "setError" returns match
- parental-leave grep "250만원\|상한 250" returns match (주석 수정 확인)
</verification>

<success_criteria>
- parental-leave, unemployment-benefit, shutdown-allowance 세 계산기 모두 음수/0/빈값/비현실값 에러·경고 처리 완료
- parental-leave 계산식 주석 수정 (상한 150만원 → 250/200/160만원 단계별)
- 초기 상태에서 결과 영역 미노출
- 필수 필드 * 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/27-ux/27-02-SUMMARY.md`
</output>
