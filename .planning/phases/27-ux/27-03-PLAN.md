---
phase: 27-ux
plan: 03
type: execute
wave: 2
depends_on: [27-01, 27-02]
files_modified:
  - src/app/tools/labor/unfair-dismissal/page.tsx
  - src/app/tools/labor/industrial-accident/page.tsx
  - src/app/tools/labor/maternity-leave/page.tsx
  - src/app/tools/labor/parental-leave/page.tsx
  - src/app/tools/labor/unemployment-benefit/page.tsx
  - src/app/tools/labor/shutdown-allowance/page.tsx
autonomous: true
requirements: [CONSIST-01, CONSIST-02, CONSIST-03, RESULT-02, RESULT-03]

must_haves:
  truths:
    - "6개 계산기 모두 에러 메시지가 text-red-500 스타일, 경고가 text-orange-500 스타일로 통일됨"
    - "6개 계산기 모두 에러 메시지 톤이 '~을(를) 입력해주세요.' 또는 '~을 확인해주세요.' 형식으로 통일됨"
    - "6개 계산기 모두 결과 금액 표시에 formatNumber() 또는 toLocaleString('ko-KR') 적용됨"
    - "6개 계산기 모두 필수 필드 라벨에 * 표시가 통일된 위치에 있음"
    - "6개 계산기 모두 계산 버튼 클래스가 동일한 기본 패턴 사용"
  artifacts:
    - path: "src/app/tools/labor/unfair-dismissal/page.tsx"
      provides: "일관성 검증 완료"
    - path: "src/app/tools/labor/industrial-accident/page.tsx"
      provides: "일관성 검증 완료"
    - path: "src/app/tools/labor/maternity-leave/page.tsx"
      provides: "일관성 검증 완료"
    - path: "src/app/tools/labor/parental-leave/page.tsx"
      provides: "일관성 검증 완료"
    - path: "src/app/tools/labor/unemployment-benefit/page.tsx"
      provides: "일관성 검증 완료"
    - path: "src/app/tools/labor/shutdown-allowance/page.tsx"
      provides: "일관성 검증 완료"
  key_links:
    - from: "6개 계산기 에러 표시 패턴"
      to: "text-red-500/text-orange-500"
      via: "grep 확인"
      pattern: "text-red-500|text-orange-500"
---

<objective>
27-01, 27-02 완료 후 6개 계산기 전체의 CONSIST-01~03, RESULT-02 일관성 최종 점검 및 불일치 수정

Purpose: 같은 카테고리(노동/근로 Part 2) 계산기들이 동일한 UX 패턴을 사용하도록 통일. 사용자가 6개 계산기 간 이동 시 혼란 없음.
Output: 6개 계산기 모두 에러 스타일·메시지 톤·필수필드 표시·결과 포맷이 통일된 상태
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-ux/27-CONTEXT.md
@.planning/phases/27-ux/27-01-SUMMARY.md
@.planning/phases/27-ux/27-02-SUMMARY.md
@src/app/tools/labor/unfair-dismissal/page.tsx
@src/app/tools/labor/industrial-accident/page.tsx
@src/app/tools/labor/maternity-leave/page.tsx
@src/app/tools/labor/parental-leave/page.tsx
@src/app/tools/labor/unemployment-benefit/page.tsx
@src/app/tools/labor/shutdown-allowance/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6개 계산기 일관성 감사 — 에러 스타일·메시지 톤·필수 필드 통일</name>
  <files>
    src/app/tools/labor/unfair-dismissal/page.tsx,
    src/app/tools/labor/industrial-accident/page.tsx,
    src/app/tools/labor/maternity-leave/page.tsx,
    src/app/tools/labor/parental-leave/page.tsx,
    src/app/tools/labor/unemployment-benefit/page.tsx,
    src/app/tools/labor/shutdown-allowance/page.tsx
  </files>
  <read_first>
    src/app/tools/labor/unfair-dismissal/page.tsx,
    src/app/tools/labor/industrial-accident/page.tsx,
    src/app/tools/labor/maternity-leave/page.tsx,
    src/app/tools/labor/parental-leave/page.tsx,
    src/app/tools/labor/unemployment-benefit/page.tsx,
    src/app/tools/labor/shutdown-allowance/page.tsx
  </read_first>
  <action>
6개 파일을 모두 읽고 CONSIST-01~03 항목별로 비교 감사. 불일치 발견 시 즉시 수정 (per D-03):

**감사 항목 1: CONSIST-03 — 에러 메시지 스타일 통일**
각 파일에서 에러/경고 표시 JSX를 확인:
- 통일 기준: `<p className="text-red-500 text-sm mb-3">{error}</p>` (에러)
- 통일 기준: `<p className="text-orange-500 text-sm mb-3">{warning}</p>` (경고)
- 다른 스타일 발견 시 위 기준으로 통일

**감사 항목 2: CONSIST-03 — 에러 메시지 톤 통일**
통일 기준 — "~을(를) 입력해주세요." 또는 "~이(가) ~합니다. 확인해주세요." 형식:
- "입력해 주세요" (띄어쓰기 다름) → "입력해주세요" 로 통일
- "확인 해주세요" → "확인해주세요"
- 경고 메시지 끝: "~을 초과합니다. 입력값을 확인해주세요." 형식 통일
각 파일의 setError/setWarning 인자 문자열 확인 후 불일치 수정

**감사 항목 3: CONSIST-03 — 에러 표시 위치 통일**
통일 기준: 에러/경고 표시 블록이 계산 버튼 `<button onClick={handleCalculate}>` 바로 위에 위치
각 파일에서 에러 표시 위치 확인, 버튼 아래에 있으면 위로 이동

**감사 항목 4: FLOW-03 — 필수 필드 * 표시 위치 통일**
통일 기준: 라벨 텍스트 끝에 ` *` (공백 후 별표)
- "월 통상임금 (원) *" 형식 확인
- " *" 없이 "*월 통상임금"이나 "월 통상임금*" 같은 다른 위치 발견 시 수정

**감사 항목 5: CONSIST-01 — 동일 개념 입력 방식 통일**
6개 계산기 모두 월임금 입력 방식 확인:
- `type="text" inputMode="numeric"` 패턴 — ok
- `onChange`: `e.target.value.replace(/[^0-9]/g, '')` 패턴 — ok
- `value`: `parseInt(state).toLocaleString('ko-KR')` 표시 패턴 — ok
불일치 파일만 수정

**감사 항목 6: RESULT-02 — 결과 금액 formatNumber 적용 확인**
6개 파일의 결과 영역에서 숫자를 직접 렌더링하는 곳 확인:
- `{result.someNumber}원` 형태가 있으면 → `{formatNumber(result.someNumber)}원`으로 수정
- `{someVariable}원` (result 내부가 아닌 파생 변수) 도 동일하게 확인

**감사 항목 7: CONSIST-02 — 결과 표시 형식 통일**
원 단위: `{formatNumber(n)}원` — ok
% 단위: `{(rate * 100).toFixed(0)}%` — parental-leave 확인
일수 단위: `{n}일` 또는 `{formatNumber(n)}일`
3자리 미만의 일수(예: "90일", "120일")에 formatNumber 불필요 — 그대로 유지
1,000일 이상 day 표시에 formatNumber 적용 여부 확인 (unemployment-benefit: `{result.days}일`)
→ 120~270일 범위이므로 formatNumber 없어도 ok. 유지.

수정이 없어도 각 항목에서 "확인 완료" 코멘트 기록 후 다음 항목 진행.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && for f in unfair-dismissal industrial-accident maternity-leave parental-leave unemployment-benefit shutdown-allowance; do echo "=== $f ==="; grep -c "text-red-500\|text-orange-500\|setError\|\*" src/app/tools/labor/$f/page.tsx; done</automated>
  </verify>
  <acceptance_criteria>
- 6개 계산기 모두 grep "text-red-500" returns match (CONSIST-03 에러 스타일)
- 6개 계산기 모두 grep "text-orange-500" returns match (CONSIST-03 경고 스타일)
- 6개 계산기 모두 grep "setError" returns match (에러 state 존재)
- 6개 계산기 모두 grep "\\\*" returns match (FLOW-03 필수 필드)
- 에러 메시지 표시 JSX가 6개 파일 모두 동일 className 사용
  </acceptance_criteria>
  <done>6개 계산기 에러 스타일·메시지 톤·필수 필드 표시 위치 통일 완료. CONSIST-01~03 달성.</done>
</task>

<task type="auto">
  <name>Task 2: 전체 빌드 검증 및 최종 TypeScript 확인</name>
  <files></files>
  <read_first></read_first>
  <action>
6개 파일 수정 완료 후 전체 프로젝트 TypeScript 컴파일 검증:

```bash
cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit
```

에러 발생 시:
1. 에러 메시지에서 파일 경로와 라인 번호 확인
2. 해당 파일 해당 라인 읽기
3. 타입 오류 수정 (주로: useState 타입 추가 누락, 미사용 변수, 타입 불일치)
4. 재실행하여 에러 없음 확인

일반적 TypeScript 수정 패턴:
- `const [error, setError] = useState<string | null>(null);` — 타입 명시
- `const [warning, setWarning] = useState<string | null>(null);` — 타입 명시
- `setError` 호출 인자는 반드시 string 타입

추가 일관성 검사:
```bash
# 6개 계산기에서 에러 state 선언 확인
grep -n "useState.*string.*null\|setError\|setWarning" src/app/tools/labor/unfair-dismissal/page.tsx src/app/tools/labor/industrial-accident/page.tsx src/app/tools/labor/maternity-leave/page.tsx src/app/tools/labor/parental-leave/page.tsx src/app/tools/labor/unemployment-benefit/page.tsx src/app/tools/labor/shutdown-allowance/page.tsx
```

결과를 보고 누락된 파일이 있으면 해당 파일에 state 추가.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- npx tsc --noEmit 출력에 error 없음 (exit 0)
- grep "useState.*string.*null" 6개 파일 모두 match
- 마지막 tsc 출력이 빈 문자열 또는 "Found 0 errors"
  </acceptance_criteria>
  <done>전체 TypeScript 컴파일 에러 없음. Phase 27 6개 계산기 모두 16개 요구사항 충족 완료.</done>
</task>

</tasks>

<verification>
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit — 전체 프로젝트 에러 없음
- 6개 계산기 모두 grep "text-red-500" match
- 6개 계산기 모두 grep "setError" match
- 6개 계산기 모두 grep "\*" (필수 필드) match
- grep "53,060" industrial-accident/page.tsx — 간병급여 주석 수정 확인
- grep "250만원\|250,000" parental-leave/page.tsx — 육아휴직 주석 수정 확인
</verification>

<success_criteria>
- 6개 계산기 CONSIST-01~03 달성: 입력 방식, 결과 포맷, 에러 메시지 스타일·톤 통일
- 6개 계산기 RESULT-02 달성: 모든 결과 금액 formatNumber 적용
- TypeScript 컴파일 에러 없음
- Phase 27 16개 요구사항 전체 충족
</success_criteria>

<output>
After completion, create `.planning/phases/27-ux/27-03-SUMMARY.md`
</output>
