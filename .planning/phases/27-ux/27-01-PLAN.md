---
phase: 27-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/labor/unfair-dismissal/page.tsx
  - src/app/tools/labor/industrial-accident/page.tsx
  - src/app/tools/labor/maternity-leave/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "unfair-dismissal에서 월임금 미입력/0 시 에러 메시지 표시, 조용한 return 없음"
    - "unfair-dismissal에서 날짜 입력 모드일 때 복직일 < 해고일이면 에러 표시"
    - "unfair-dismissal에서 개월수 입력 모드일 때 0 이하면 에러 표시"
    - "unfair-dismissal에서 월임금 > 1억이면 경고 표시 (계산 허용)"
    - "unfair-dismissal에서 초기 상태에서 결과 영역 미노출"
    - "unfair-dismissal 필수 필드에 * 표시"
    - "industrial-accident에서 휴업급여 탭: 평균임금/요양일수 미입력 시 에러 표시"
    - "industrial-accident에서 장해급여 탭: 평균임금 미입력 시 에러 표시"
    - "industrial-accident에서 간병급여 탭: 간병일수 미입력 시 에러 표시"
    - "industrial-accident에서 평균임금 > 500,000(일당)이면 경고 표시"
    - "industrial-accident에서 간병급여 계산식 주석 금액(41,170/27,450)이 코드 상수(53,060/35,370)와 불일치 — 주석 수정"
    - "maternity-leave에서 월 통상임금 미입력/0 시 에러 표시"
    - "maternity-leave에서 월 통상임금 > 1억이면 경고 표시"
    - "maternity-leave에서 초기 상태 결과 영역 미노출"
    - "maternity-leave 필수 필드에 * 표시"
    - "세 계산기 모두 에러 text-red-500, 경고 text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/labor/unfair-dismissal/page.tsx"
      provides: "부당해고 계산기 with error state, date validation, wage validation"
    - path: "src/app/tools/labor/industrial-accident/page.tsx"
      provides: "산재보험 계산기 with per-tab error state, wage validation, nursing formula comment fix"
    - path: "src/app/tools/labor/maternity-leave/page.tsx"
      provides: "출산휴가급여 계산기 with error state, wage validation"
  key_links:
    - from: "unfair-dismissal/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|text-red-500"
    - from: "industrial-accident/page.tsx handleCalculate"
      to: "error state per tab"
      via: "setError + conditional rendering"
      pattern: "setError|text-red-500"
    - from: "maternity-leave/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|text-red-500"
---

<objective>
unfair-dismissal, industrial-accident, maternity-leave 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 부당해고·산재보험·출산휴가급여 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러 표시, 계산식 주석 정확성 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-ux/27-CONTEXT.md
@src/app/tools/labor/unfair-dismissal/page.tsx
@src/app/tools/labor/industrial-accident/page.tsx
@src/app/tools/labor/maternity-leave/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: unfair-dismissal 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/unfair-dismissal/page.tsx</files>
  <read_first>src/app/tools/labor/unfair-dismissal/page.tsx</read_first>
  <action>
unfair-dismissal/page.tsx를 전체 읽고 16개 요구사항 각각에 대해 코드를 감사한 뒤 발견 즉시 수정 (per D-03):

**현황 파악 (코드 기반):**
- `error` state 없음 — `if (!wage || wage <= 0) return;` 조용한 return
- 날짜 모드: `if (!dismissalDate || !reinstatementDate) return;` 조용한 return
- `days <= 0` 조용한 return (복직일 < 해고일 처리 없음)
- 개월수 모드: `if (!months || months <= 0) return;` 조용한 return
- 필수 필드 라벨에 * 없음
- `interimIncome` 공제액이 `grossCompensation`보다 클 때 `Math.max(0, ...)` 처리됨 — RESULT-01 ok

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 재작성:**
1. 함수 시작 시 `setError(null); setWarning(null);`
2. 월임금 검증: `if (!wage || wage <= 0) { setError('퇴직 전 3개월 평균 월임금을 입력해주세요.'); setResult(null); return; }`
3. 월임금 상한 경고 (INPUT-03): `if (wage > 100_000_000) { setWarning('월임금이 1억원을 초과합니다. 입력값을 확인해주세요.'); }` — 계산 계속 허용
4. 날짜 모드 검증:
   - 날짜 미입력: `if (!dismissalDate || !reinstatementDate) { setError('해고 시작일과 판정/복직 예상일을 모두 입력해주세요.'); setResult(null); return; }`
   - 날짜 역전: `if (days <= 0) { setError('판정/복직 예상일이 해고 시작일보다 앞설 수 없습니다.'); setResult(null); return; }` (EDGE-02)
5. 개월수 모드 검증: `if (!months || months <= 0) { setError('해고 경과 개월수를 입력해주세요.'); setResult(null); return; }` — 개월수도 소수점 허용이므로 `parseFloat` 유지, 단 0 이하 차단
6. 성공 시: `setError(null);`

**에러/경고 표시 위치:** 계산 버튼 바로 위에:
```tsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**필수 필드 라벨 수정 (FLOW-03):**
- "퇴직 전 3개월 평균 월임금 (원)" → "퇴직 전 3개월 평균 월임금 (원) *"
- 날짜 모드: "해고 시작일" → "해고 시작일 *", "판정/복직 예상일" → "판정/복직 예상일 *"
- 개월수 모드: "해고 경과 개월수" → "해고 경과 개월수 *"
- "중간수입 (다른 직장 수입, 선택사항)" — 선택사항이므로 * 불필요, 유지

**FLOW-01 확인:** `result` state가 `useState<Result | null>(null)`로 초기화됨 — `{result && (...)}` 조건부 렌더링 이미 있음, 확인 후 유지.

**RESULT-01:** `netCompensation`이 0인 경우 — 중간수입이 임금상당액 이상일 때. 결과 영역에 이미 `Math.max(0, ...)` 처리돼 0원 표시됨. 0원이면 안내 문구 추가: `{result.netCompensation === 0 && <p className="text-sm text-yellow-500 mt-2">중간수입이 임금상당액 이상으로 보상금이 발생하지 않습니다.</p>}`

**EDGE-03:** `dailyAvgWage = Math.floor((wage * 12) / 365)` — wage > 0 보장, divide by zero 없음. 확인.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|월임금을 입력\|앞설 수 없\|\*" src/app/tools/labor/unfair-dismissal/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" unfair-dismissal/page.tsx returns match (에러 state 존재)
- grep "월임금을 입력\|입력해주세요" unfair-dismissal/page.tsx returns match (INPUT-02)
- grep "앞설 수 없\|판정.*복직.*앞" unfair-dismissal/page.tsx returns match (EDGE-02)
- grep "text-red-500" unfair-dismissal/page.tsx returns match (에러 스타일)
- grep "text-orange-500" unfair-dismissal/page.tsx returns match (경고 스타일)
- grep "\\\*" unfair-dismissal/page.tsx returns match (FLOW-03)
- grep "보상금이 발생하지 않" unfair-dismissal/page.tsx returns match (RESULT-01)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "unfair-dismissal" | wc -l → 0
  </acceptance_criteria>
  <done>unfair-dismissal 계산기가 16개 요구사항 모두 충족. 월임금/날짜/개월수 에러 처리, 비현실값 경고, 0원 안내, 필수 필드 * 표시, TypeScript 에러 없음.</done>
</task>

<task type="auto">
  <name>Task 2: industrial-accident 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/industrial-accident/page.tsx</files>
  <read_first>src/app/tools/labor/industrial-accident/page.tsx</read_first>
  <action>
industrial-accident/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. 탭별(휴업/장해/간병) 에러 처리 추가 (per D-03, D-04):

**현황 파악 (코드 기반):**
- `error` state 없음 — 모든 분기에서 조용한 return
- 탭 전환 시 `setResult(null)` 처리됨 — FLOW-01 부분 ok
- 간병급여 계산식 주석 `정액 간병급여(상시: 41,170원 / 수시: 27,450원)`가 실제 상수 `NURSING_DAILY = { fullTime: 53_060, partTime: 35_370 }` 와 다름 → 주석 수정 필요 (Critical)
- 필수 필드에 * 없음
- 요양일수/간병일수 비현실값 상한 검증 없음

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 재작성:**
함수 시작 시 `setError(null); setWarning(null);`

휴업급여(absence) 분기:
1. `if (!wage || wage <= 0) { setError('평균임금(원/일)을 입력해주세요.'); setResult(null); return; }` (INPUT-02)
2. `if (!days || days <= 0) { setError('요양일수를 입력해주세요.'); setResult(null); return; }` (INPUT-02)
3. `if (wage > 500_000) { setWarning('1일 평균임금이 50만원을 초과합니다. 입력값을 확인해주세요.'); }` — 계산 허용 (INPUT-03)
4. `if (days > 1095) { setWarning((prev) => (prev ? prev + ' ' : '') + '요양일수가 3년(1,095일)을 초과합니다. 확인해주세요.'); }` — 계산 허용

장해급여(disability) 분기:
1. `if (!wage || wage <= 0) { setError('평균임금(원/일)을 입력해주세요.'); setResult(null); return; }` (INPUT-02)
2. `if (wage > 500_000) { setWarning('1일 평균임금이 50만원을 초과합니다. 확인해주세요.'); }` (INPUT-03)
3. `if (paymentType === 'pension' && disabilityGrade > 7) { setError('연금은 1~7급에만 적용됩니다.'); setResult(null); return; }` — 이미 UI에서 비활성화되지만 코드 가드도 추가

간병급여(nursing) 분기:
1. `if (!days || days <= 0) { setError('간병일수를 입력해주세요.'); setResult(null); return; }` (INPUT-02)
2. `if (days > 1095) { setWarning('간병일수가 3년을 초과합니다. 확인해주세요.'); }` (INPUT-03)

**계산식 주석 수정 (Critical — 코드와 불일치):**
결과 영역 내 간병급여 pre 태그의 계산식 텍스트:
```
정액 간병급여(상시: 41,170원 / 수시: 27,450원) × 간병일수
```
→
```
정액 간병급여(상시: 53,060원 / 수시: 35,370원) × 간병일수 (2026.1.1~2028.12.31 적용)
```

**필수 필드 라벨 수정 (FLOW-03):**
- 휴업급여 탭: "평균임금 (원/일)" → "평균임금 (원/일) *", "요양일수 (일)" → "요양일수 (일) *"
- 장해급여 탭: "평균임금 (원/일)" → "평균임금 (원/일) *"
- 간병급여 탭: "간병일수 (일)" → "간병일수 (일) *"

**에러/경고 표시 위치:** 계산 버튼 바로 위에 (탭 아래, 버튼 위):
```tsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**FLOW-01 확인:** 탭 전환 시 이미 `setResult(null)` 처리됨 — 에러/경고도 함께 초기화: 탭 전환 버튼 onClick에 `setError(null); setWarning(null);` 추가.

**RESULT-01:** 휴업급여 total이 0이 될 수 없음 (wage > 0, days > 0 보장). 장해급여도 동일. 간병급여도 동일. 0원 결과 없음 — ok.

**EDGE-03:** `dailyBenefit * days` — 나누기 없음. `Math.floor(wage * 0.7)` — wage > 0 보장. `(wage * days) / 12` — wage > 0 보장. ok.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|53,060\|53_060\|\*" src/app/tools/labor/industrial-accident/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" industrial-accident/page.tsx returns match (에러 state 존재)
- grep "평균임금.*입력\|요양일수를 입력\|간병일수를 입력" industrial-accident/page.tsx returns match (INPUT-02)
- grep "text-red-500" industrial-accident/page.tsx returns match
- grep "text-orange-500" industrial-accident/page.tsx returns match
- grep "53,060\|53_060" industrial-accident/page.tsx — 계산식 주석이 53,060원으로 수정됨
- grep "\\\*" industrial-accident/page.tsx returns match (FLOW-03)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "industrial-accident" | wc -l → 0
  </acceptance_criteria>
  <done>industrial-accident 계산기가 16개 요구사항 모두 충족. 탭별 에러 처리, 비현실값 경고, 간병급여 계산식 주석 수정(41,170→53,060), 필수 필드 * 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 3: maternity-leave 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/labor/maternity-leave/page.tsx</files>
  <read_first>src/app/tools/labor/maternity-leave/page.tsx</read_first>
  <action>
maternity-leave/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. Task 1/2와 동일 에러/경고 패턴 적용 (per D-03, D-04):

**현황 파악 (코드 기반):**
- `error` state 없음 — `if (!wage || wage <= 0) return;` 조용한 return
- 필수 필드 라벨에 * 없음
- RESULT-03 검증: `grandTotal = insuranceTotal + employerTotal` — 부분합 = 총합 ok
- RESULT-02: `formatNumber()` 사용됨 — 결과 금액 모두 적용됨 확인
- FLOW-01: `result` state `useState<Result | null>(null)`, `{result && (...)}` 조건부 렌더링 — ok

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 재작성:**
1. 시작 시: `setError(null); setWarning(null);`
2. 임금 미입력/0: `if (!wage || wage <= 0) { setError('월 통상임금을 입력해주세요.'); setResult(null); return; }` (INPUT-02)
3. 음수 차단 — 이미 `handleNumberChange`에서 `replace(/[^0-9]/g, '')` 처리됨 (INPUT-01)
4. 임금 상한 경고 (INPUT-03): `if (wage > 100_000_000) { setWarning('월 통상임금이 1억원을 초과합니다. 입력값을 확인해주세요.'); }` — 계산 허용
5. 성공 시: `setError(null);`

**에러/경고 표시 위치:** 계산 버튼 바로 위에:
```tsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**필수 필드 라벨 수정 (FLOW-03):**
- "월 통상임금 (원)" → "월 통상임금 (원) *"
- 출산 유형, 기업 규모는 버튼 토글 선택 — 항상 기본값 있으므로 * 불필요

**RESULT-01:** 임금이 입력되면 항상 insuranceTotal > 0 (wage > 0 보장 후 계산) — 0원 결과 없음. ok.

**EDGE-01 확인:** 단태아 90일, 다태아 120일 상수 — 경계값 자체가 코드에 하드코딩됨. 중소기업은 전액 고용보험, 대기업은 분담. 양쪽 모두 올바름.

**EDGE-03:** `dailyOrdinaryWage = Math.floor(wage / 30)` — wage > 0 보장. `dailyInsuranceCap = Math.floor(MATERNITY_UPPER / 30)` — 상수. `Math.min(dailyOrdinaryWage, dailyInsuranceCap)` — 0 없음. ok.

**CONSIST-03 선점:** 에러 메시지 톤 — "~을 입력해주세요." 형식으로 통일 (26-ux에서 확립된 패턴).
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|월 통상임금을 입력\|\*" src/app/tools/labor/maternity-leave/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" maternity-leave/page.tsx returns match (에러 state 존재)
- grep "월 통상임금을 입력\|입력해주세요" maternity-leave/page.tsx returns match (INPUT-02)
- grep "text-red-500" maternity-leave/page.tsx returns match
- grep "text-orange-500" maternity-leave/page.tsx returns match
- grep "\\\*" maternity-leave/page.tsx returns match (FLOW-03)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "maternity-leave" | wc -l → 0
  </acceptance_criteria>
  <done>maternity-leave 계산기가 16개 요구사항 모두 충족. 임금 필수 입력 에러, 비현실값 경고, 필수 필드 * 표시, TypeScript 에러 없음.</done>
</task>

</tasks>

<verification>
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit — 전체 프로젝트 빌드 에러 없음
- 세 계산기 모두 grep "text-red-500" returns match
- 세 계산기 모두 grep "text-orange-500" returns match
- 세 계산기 모두 grep "setError" returns match
- industrial-accident grep "53,060" returns match (주석 수정 확인)
</verification>

<success_criteria>
- unfair-dismissal, industrial-accident, maternity-leave 세 계산기 모두 음수/0/빈값/비현실값 에러·경고 처리 완료
- industrial-accident 간병급여 계산식 주석 금액 수정 (41,170→53,060원, 27,450→35,370원)
- 초기 상태에서 0원 결과 미노출
- 필수 필드 * 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/27-ux/27-01-SUMMARY.md`
</output>
