---
phase: 04-labor-calculators
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/labor/parental-leave/page.tsx
  - src/app/tools/labor/unemployment-benefit/page.tsx
  - src/app/tools/labor/ordinary-wage/page.tsx
autonomous: true
requirements: [LABOR-10, LABOR-11, LABOR-12]

must_haves:
  truths:
    - "육아휴직급여 계산기에서 통상임금과 휴직기간을 입력하면 월별 급여와 사후지급분이 계산된다"
    - "실업급여 계산기에서 이직 전 임금과 피보험기간을 입력하면 소정급여일수와 총 급여액이 계산된다"
    - "통상임금 계산기에서 임금 항목을 입력하면 시급 환산 통상임금이 계산된다"
  artifacts:
    - path: src/app/tools/labor/parental-leave/page.tsx
      provides: 육아휴직급여 계산 페이지
    - path: src/app/tools/labor/unemployment-benefit/page.tsx
      provides: 실업급여 계산 페이지
    - path: src/app/tools/labor/ordinary-wage/page.tsx
      provides: 통상임금 계산 페이지
  key_links:
    - from: each page
      to: CalculatorLayout
      via: "import CalculatorLayout from '@/components/ui/CalculatorLayout'"
---

<objective>
Implement three labor calculators: 육아휴직급여 (parental leave pay), 실업급여 (unemployment benefit), and 통상임금 (ordinary wage calculation).

Purpose: Cover LABOR-10 through LABOR-12 — parental support and wage base calculators.
Output: Three independent Next.js page files under src/app/tools/labor/
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-labor-calculators/04-CONTEXT.md

<interfaces>
<!-- From src/lib/tools-data.ts -->
parental-leave       → id 'parental-leave'       icon 👶
unemployment-benefit → id 'unemployment-benefit' icon 📋
ordinary-wage        → id 'ordinary-wage'         icon 💵

<!-- category labor color: '#f59e0b' -->

<!-- 2026 constants:
  육아휴직급여:
    - 통상임금의 80% (상한 월 1,500,000원 → 2024년 상향: 2026년 기준 월 상한 1,500,000원)
    - 하한: 월 700,000원
    - 사후지급분: 급여의 25%를 복직 후 6개월 뒤 지급
    - 첫 3개월: 통상임금 80% (상한 200만원/월 — 2025년 개정 적용)
    - 4개월~: 통상임금 50% (상한 120만원/월)
    NOTE: Use 2026 고용보험법 기준:
      1~3개월: 80%, 상한 200만원, 하한 70만원
      4~6개월: 50%, 상한 120만원, 하한 70만원
      7개월~: 50%, 상한 120만원, 하한 70만원

  실업급여(구직급여):
    - 기초일액 = 이직 전 평균임금의 60%
    - 상한: 66,000원/일 (2026)
    - 하한: 최저임금의 80% × 소정근로시간/일 = 10,030 × 0.8 × 8 = 64,192원 → 하한 = min(기초일액, 64,192원)
    NOTE: 하한 = 이직 당시 최저임금법상 시간급 최저임금의 80% × 1일 소정근로시간
    - 소정급여일수: 연령+피보험기간 기준 표
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 육아휴직급여 계산기 (LABOR-10)</name>
  <files>src/app/tools/labor/parental-leave/page.tsx</files>
  <action>
Create `src/app/tools/labor/parental-leave/page.tsx` with 'use client'.

Tool: `TOOLS.find(t => t.id === 'parental-leave')`, category labor.

2026 고용보험법 기준 육아휴직급여:
- 1~3개월: 통상임금 × 80%, 상한 2,000,000원/월, 하한 700,000원/월
- 4개월 이후: 통상임금 × 50%, 상한 1,200,000원/월, 하한 700,000원/월
- 사후지급분(복직후지급): 각 월 급여의 25%는 복직 후 6개월간 계속근무 후 일시 지급

const PARENTAL_CAPS = { first3: { upper: 2_000_000, lower: 700_000, rate: 0.8 }, rest: { upper: 1_200_000, lower: 700_000, rate: 0.5 } };

Inputs:
1. 월 통상임금 (number, 원)
2. 육아휴직 기간 (number, 1~12개월)
3. 자녀수/유형 (select: 일반 / 한부모·장애아동(추가 지원 있음)) — 한부모는 첫 3개월 상한 300만원

Derived:
- 월별 급여 계산 (3개월 단위 분기):
  - first3months = min(max(월통상임금 × 0.8, 700_000), 2_000_000)
  - restMonths = min(max(월통상임금 × 0.5, 700_000), 1_200_000)
- 총 월 급여 합계 (즉시지급분 75%)
- 사후지급분 합계 (25%)
- 총 급여 = 즉시 + 사후

Results:
- 총 예상 급여 합계 (large)
- 즉시지급분 (복직 전)
- 사후지급분 (복직 후 6개월)
- 월별 breakdown table (월, 급여율, 월급여액)

Legal basis: 고용보험법 제70조(육아휴직급여), 제73조의2(육아기 근로시간 단축급여)
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Page compiles. 월통상임금 3,000,000원, 6개월 → 1~3개월 200만/월, 4~6개월 150만/월(50%), 즉시 총 262.5만, 사후 87.5만, 합계 350만.</done>
</task>

<task type="auto">
  <name>Task 2: 실업급여 계산기 (LABOR-11)</name>
  <files>src/app/tools/labor/unemployment-benefit/page.tsx</files>
  <action>
Create `src/app/tools/labor/unemployment-benefit/page.tsx` with 'use client'.

Tool: `TOOLS.find(t => t.id === 'unemployment-benefit')`, category labor.

Formula (고용보험법 제46조~50조):
const DAILY_UPPER = 66_000; // 2026 상한
const DAILY_LOWER = Math.floor(10_030 * 0.8 * 8); // = 64,192원

소정급여일수 표 (연령 × 피보험기간):
// 피보험기간: 1년미만, 1~3년, 3~5년, 5~10년, 10년이상
// 50세미만/장애인제외: 120, 150, 180, 210, 240일
// 50세이상/장애인:    120, 180, 210, 240, 270일
const BENEFIT_DAYS: Record<string, Record<string, number>> = {
  under50: { lt1: 120, lt3: 150, lt5: 180, lt10: 210, gte10: 240 },
  over50:  { lt1: 120, lt3: 180, lt5: 210, lt10: 240, gte10: 270 },
};

Inputs:
1. 퇴직 전 3개월 평균 월임금 (number, 원)
2. 피보험기간 (select: 1년미만 / 1~3년 / 3~5년 / 5~10년 / 10년이상)
3. 나이 (select: 50세미만 / 50세이상·장애인)
4. 1일 소정근로시간 (select: 8시간 / 6시간 / 4시간 — 시간제 근로자)

Derived:
- 기초일액 = 월임금 × 12 / 365 × 0.6 (이직 전 평균임금의 60%)
- 일일지급액 = max(DAILY_LOWER, min(기초일액, DAILY_UPPER))
- 소정급여일수 = BENEFIT_DAYS[ageKey][periodKey]
- 총 실업급여 = 일일지급액 × 소정급여일수

Results:
- 총 실업급여 예상액 (large)
- 소정급여일수
- 1일 지급액
- 상한/하한 적용 여부

Legal basis: 고용보험법 제46조(구직급여일액), 제50조(소정급여일수)
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Page compiles. 월임금 3,000,000원, 50세미만, 3~5년 → 기초일액 ≈59,178원, 하한 64,192원 적용, 180일 × 64,192 = 11,554,560원.</done>
</task>

<task type="auto">
  <name>Task 3: 통상임금 계산기 (LABOR-12)</name>
  <files>src/app/tools/labor/ordinary-wage/page.tsx</files>
  <action>
Create `src/app/tools/labor/ordinary-wage/page.tsx` with 'use client'.

Tool: `TOOLS.find(t => t.id === 'ordinary-wage')`, category labor.

통상임금 정의 (근로기준법 시행령 제6조): 정기적·일률적·고정적으로 지급되는 임금.

통상임금 해당 항목 예시:
- 기본급 (항상 해당)
- 직무수당, 직책수당 (정기·일률·고정 → 해당)
- 가족수당 (가족 수 관계없이 전원 지급 시 해당, 가족 수에 따른 차등 → 미해당)
- 성과급 (변동 → 미해당)
- 식대/교통비 (전원 균등지급 시 해당)

UI: 임금 항목 동적 추가 (add row)
Each row: 항목명 (text), 금액(월, 원), 해당여부 (checkbox)

Derived:
- 통상임금 산정 기준시간 = (주소정근로시간 + 주휴시간) × 365/7/12
  - 40h제: 209시간
  - 44h제: 226시간
- 통상임금 합계 = sum of checked rows
- 시간급 통상임금 = 통상임금 / 월기준시간
- 1일 통상임금 = 시간급 × 8

Inputs:
1. 1주 소정근로시간 (select: 40 / 44)
2. 임금 항목 목록 (동적 rows): 최초 1행 (기본급) 체크됨
   - "항목 추가" 버튼으로 row 추가

Results:
- 통상임금 합계 (월)
- 시간급 통상임금
- 1일 통상임금 (large, category color)
- 월 기준시간

Legal basis: 근로기준법 시행령 제6조(통상임금), 대법원 2013다4969(전원합의체)
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>Page compiles. 기본급 2,000,000원 + 직무수당 100,000원 (둘 다 체크), 40h제 → 통상임금 2,100,000원, 시급 10,048원, 1일 80,383원.</done>
</task>

</tasks>

<verification>
All three pages pass TypeScript compilation. 실업급여 uses correct 2026 상한(66,000원/일) and 하한(64,192원/일). 육아휴직 uses 2026 요율과 상한.
</verification>

<success_criteria>
- src/app/tools/labor/parental-leave/page.tsx exists and compiles
- src/app/tools/labor/unemployment-benefit/page.tsx exists and compiles
- src/app/tools/labor/ordinary-wage/page.tsx exists and compiles
- 통상임금 계산기 supports dynamic wage item rows
</success_criteria>

<output>
After completion, create `.planning/phases/04-labor-calculators/04-04-SUMMARY.md`
</output>
