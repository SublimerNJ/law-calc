---
phase: 12-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ParallaxLayer.tsx
  - src/components/sections/CategorySection.tsx
autonomous: true
requirements:
  - REVAMP-02
  - REVAMP-04
must_haves:
  truths:
    - "Parallax animations are completely disabled on mobile viewports (< 768px)."
    - "Parallax respects the OS 'prefers-reduced-motion' setting."
    - "Category sections reveal with a smooth, staggered animation."
  artifacts:
    - path: "src/components/ui/ParallaxLayer.tsx"
      provides: "Responsive and accessible parallax container"
    - path: "src/components/sections/CategorySection.tsx"
      provides: "Staggered scroll reveal component"
  key_links:
    - from: "src/components/ui/ParallaxLayer.tsx"
      to: "window"
      via: "matchMedia and innerWidth"
---

<objective>
Update core ParallaxLayer and CategorySection to support high-performance, accessible scroll animations.

Purpose: To ensure all parallax effects meet the 5-core principles (Performance First, Responsive & Accessible) before applying them to visual sections.
Output: An optimized `ParallaxLayer` component and an enhanced `CategorySection` with staggered reveals.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/Users/njparkmacmini/.gemini/skills/parallax-playbook/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Optimize ParallaxLayer for Performance and Accessibility</name>
  <files>src/components/ui/ParallaxLayer.tsx</files>
  <action>
    Update `ParallaxLayer` to strictly adhere to the parallax-playbook principles.
    - Add a state or ref to track if the viewport is mobile (`window.innerWidth < 768`). Listen to `resize` events to update this.
    - Disable the scroll event listener and reset offset if `isMobile` or `prefers-reduced-motion` is true.
    - Change the transform from `translateY` to `translate3d(0, ${offset * speed}px, 0)` to force GPU acceleration.
    - Ensure `will-change: transform` is applied only when animations are active.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>ParallaxLayer uses translate3d and completely disables tracking on mobile and reduced-motion environments.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance CategorySection with Staggered Reveal</name>
  <files>src/components/sections/CategorySection.tsx</files>
  <action>
    Enhance the `IntersectionObserver` logic to create a gentle staggered fade-in/translate-up effect for the section and its children (the tools cards).
    - Maintain the `prefers-reduced-motion` check (if true, bypass animation).
    - When the section intersects, trigger a CSS class or inline styles that not only fade in the section but also apply staggered transition delays to its children.
    - Keep animations performant (only animate `opacity` and `transform`).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>CategorySection and its inner tool cards reveal gracefully without layout thrashing when scrolled into view.</done>
</task>

</tasks>

<verification>
Code compiles cleanly and components adhere to accessibility constraints.
</verification>

<success_criteria>
ParallaxLayer and CategorySection are ready for integration, strictly following GPU-acceleration and accessibility rules.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui/12-01-SUMMARY.md`
</output>
