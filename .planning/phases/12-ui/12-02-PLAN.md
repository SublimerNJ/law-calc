---
phase: 12-ui
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/sections/HeroSection.tsx
autonomous: false
requirements:
  - REVAMP-02
  - REVAMP-04
must_haves:
  truths:
    - "HeroSection features a deep, layered visual effect using ParallaxLayer."
    - "Parallax effect feels premium, smooth, and subtle."
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero visual experience"
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/ui/ParallaxLayer.tsx"
      via: "component composition"
---

<objective>
Implement the optimized ParallaxLayer within the HeroSection to create a premium, depth-rich introduction.

Purpose: To elevate the visual quality of the main entry point without sacrificing performance.
Output: Updated HeroSection and manual verification of the parallax UX.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@/Users/njparkmacmini/.gemini/skills/parallax-playbook/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ParallaxLayer into HeroSection</name>
  <files>src/components/sections/HeroSection.tsx</files>
  <action>
    Import `ParallaxLayer` and use it to wrap specific elements to create depth.
    - Wrap the subtle grid background (`div.absolute.inset-0`) in `<ParallaxLayer speed={0.2} className="absolute inset-0">` (or similar speed) so it moves slower than the scroll.
    - Optionally wrap the eyebrow badge ("70개 무료 법률 계산기") or the subtitle in a `<ParallaxLayer>` with a slightly negative speed (e.g., `-0.1`) so it floats upwards slightly on scroll.
    - Ensure the layout remains perfectly intact and responsive.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>HeroSection utilizes ParallaxLayer for background and/or foreground elements with appropriate speeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Parallax animations in HeroSection and staggered reveals in CategorySection.</what-built>
  <how-to-verify>
    1. Start the dev server (`npm run dev`) and visit http://localhost:3000.
    2. Scroll down and verify the Hero grid background and elements move at different speeds smoothly (60fps).
    3. Scroll into the Categories and verify the cards stagger-fade-in elegantly.
    4. Resize the window below 768px (mobile) and verify parallax is completely disabled.
    5. Test with OS 'prefers-reduced-motion' enabled and ensure animations are skipped.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/performance issues to fix</resume-signal>
</task>

</tasks>

<verification>
Hero section looks visually appealing with depth, and all responsive/accessibility checks pass.
</verification>

<success_criteria>
Premium parallax implementation complete, verified by human for feel and performance.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui/12-02-SUMMARY.md`
</output>
