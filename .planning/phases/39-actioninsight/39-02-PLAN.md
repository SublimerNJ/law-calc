---
phase: 39-actioninsight
plan: 02
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - src/app/tools/debt/late-payment/page.tsx
  - src/app/tools/debt/loan-interest/page.tsx
  - src/app/tools/debt/unjust-enrichment/page.tsx
autonomous: true
requirements:
  - UI-07
must_haves:
  truths:
    - 3개의 채권/이자 계산기 결과 화면 하단에 ActionInsight 컴포넌트가 렌더링된다.
  artifacts:
    - path: src/app/tools/debt/late-payment/page.tsx
      provides: 지연이자 ActionInsight UI 연동
    - path: src/app/tools/debt/loan-interest/page.tsx
      provides: 최고이자율 ActionInsight UI 연동
    - path: src/app/tools/debt/unjust-enrichment/page.tsx
      provides: 부당이득 ActionInsight UI 연동
  key_links:
    - from: src/app/tools/debt/late-payment/page.tsx
      to: src/components/ui/ActionInsight.tsx
      via: ActionInsight 컴포넌트 임포트 및 사용
---

<objective>
채권/이자 도구 3종(late-payment, loan-interest, unjust-enrichment) UI에 ActionInsight 컴포넌트를 연동한다.

Purpose: 계산 결과뿐만 아니라 실제 행동으로 이어질 수 있는 실전 팁과 템플릿을 사용자에게 노출하기 위함.
Output: 각 계산기의 결과 렌더링 영역 하단에 ActionInsight 컴포넌트가 표시됨.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/action-data.ts
@src/components/ui/ActionInsight.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: late-payment (지연이자) UI 연동</name>
  <files>src/app/tools/debt/late-payment/page.tsx</files>
  <action>
    `late-payment` 계산기 컴포넌트에 `ActionInsight` 컴포넌트를 임포트하고 결과 하단에 렌더링한다.
    - `import { ActionInsight } from '@/components/ui/ActionInsight';`
    - `import { actionData } from '@/lib/action-data';`
    - 결과값이 있을 때 하단에 `<ActionInsight data={actionData['late-payment']} calculatedAmount={결과금액(예: totalAmount)} />` 렌더링
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/debt/late-payment/page.tsx</automated>
  </verify>
  <done>late-payment 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

<task type="auto">
  <name>Task 2: loan-interest (최고이자율) UI 연동</name>
  <files>src/app/tools/debt/loan-interest/page.tsx</files>
  <action>
    `loan-interest` 계산기 컴포넌트에 `ActionInsight` 컴포넌트를 임포트하고 결과 하단에 렌더링한다.
    - `import { ActionInsight } from '@/components/ui/ActionInsight';`
    - `import { actionData } from '@/lib/action-data';`
    - 결과값이 있을 때 하단에 `<ActionInsight data={actionData['loan-interest']} calculatedAmount={결과금액(예: totalRefundAmount 또는 totalInterest)} />` 렌더링
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/debt/loan-interest/page.tsx</automated>
  </verify>
  <done>loan-interest 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

<task type="auto">
  <name>Task 3: unjust-enrichment (부당이득) UI 연동</name>
  <files>src/app/tools/debt/unjust-enrichment/page.tsx</files>
  <action>
    `unjust-enrichment` 계산기 컴포넌트에 `ActionInsight` 컴포넌트를 임포트하고 결과 하단에 렌더링한다.
    - `import { ActionInsight } from '@/components/ui/ActionInsight';`
    - `import { actionData } from '@/lib/action-data';`
    - 결과값이 있을 때 하단에 `<ActionInsight data={actionData['unjust-enrichment']} calculatedAmount={결과금액(예: totalAmount)} />` 렌더링
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/debt/unjust-enrichment/page.tsx</automated>
  </verify>
  <done>unjust-enrichment 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

</tasks>

<verification>
- `npm run build` 또는 TypeScript 검증을 통해 컴포넌트 임포트 및 prop 타입 에러가 없는지 확인
</verification>

<success_criteria>
- 3개 채권/이자 계산기 페이지 소스에 ActionInsight 컴포넌트가 적용됨
</success_criteria>

<output>
After completion, create `.planning/phases/39-actioninsight/39-02-SUMMARY.md`
</output>
