---
phase: 39-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/action-data.ts
autonomous: true
requirements:
  - ACTION-07
must_haves:
  truths:
    - actionData 객체에 late-payment, loan-interest, unjust-enrichment 3개 키에 대한 팁과 템플릿 데이터가 존재한다.
  artifacts:
    - path: src/lib/action-data.ts
      provides: 채권/이자 도구용 실전 대응 팁 데이터
  key_links: []
---

<objective>
채권/이자 도구 3종(late-payment, loan-interest, unjust-enrichment)에 대한 ActionInsight 데이터를 `action-data.ts`에 추가한다.

Purpose: 사용자가 채권 및 이자와 관련하여 법적 분쟁 전 단계에서 단호하고 전문적인 톤으로 활용할 수 있는 실전 대응 팁 및 카톡/문자 템플릿을 제공하기 위함.
Output: `src/lib/action-data.ts`에 3개 계산기에 대한 데이터 추가 완료.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/action-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: late-payment (지연이자) 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `actionData` 객체에 `late-payment` 키를 추가한다.
    - title: "💡 채권추심 전문가의 팁 (지연이자 청구)"
    - tips: 3개의 전문적이고 단호한 팁 (예: 소송촉진특례법상 연 12% 지연이자 청구 가능성, 내용증명의 증명력 등)
    - scriptTemplate: 상대방에게 원금 및 지연이자 변제를 요구하는 단호한 내용의 카톡/문자 템플릿 작성
    - 템플릿 내용에는 `{{AMOUNT}}` 플레이스홀더를 사용하여 계산된 금액이 들어갈 수 있도록 구성한다.
  </action>
  <verify>
    <automated>grep -q "late-payment" src/lib/action-data.ts</automated>
  </verify>
  <done>late-payment 데이터가 actionData에 정상적으로 추가됨</done>
</task>

<task type="auto">
  <name>Task 2: loan-interest (최고이자율) 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `actionData` 객체에 `loan-interest` 키를 추가한다.
    - title: "💡 불법채심 방어 및 이자율 전문가 팁"
    - tips: 3개의 단호한 팁 (예: 법정 최고이자율 연 20% 초과분은 무효이며 반환 청구 가능, 불법추심 대처 방법 등)
    - scriptTemplate: 대부업체나 채권자에게 법정 최고이자율 초과분의 이자 지급 거절 및 부당이득 반환을 요구하는 템플릿 작성
    - 템플릿 내용에 `{{AMOUNT}}` 플레이스홀더 적용
  </action>
  <verify>
    <automated>grep -q "loan-interest" src/lib/action-data.ts</automated>
  </verify>
  <done>loan-interest 데이터가 actionData에 정상적으로 추가됨</done>
</task>

<task type="auto">
  <name>Task 3: unjust-enrichment (부당이득) 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `actionData` 객체에 `unjust-enrichment` 키를 추가한다.
    - title: "💡 부당이득 반환청구 실전 팁"
    - tips: 3개의 팁 (예: 악의의 수익자인 경우 법정이자(연 5%) 포함 반환 의무, 내용증명 발송으로 악의 입증 등)
    - scriptTemplate: 착오 송금이나 계약 무효 등으로 인한 부당이득 및 이자 반환을 단호하게 요구하는 템플릿 작성
    - 템플릿 내용에 `{{AMOUNT}}` 플레이스홀더 적용
  </action>
  <verify>
    <automated>grep -q "unjust-enrichment" src/lib/action-data.ts</automated>
  </verify>
  <done>unjust-enrichment 데이터가 actionData에 정상적으로 추가됨</done>
</task>

</tasks>

<verification>
- `action-data.ts` 파일이 TypeScript 문법 에러 없이 컴파일되는지 확인
</verification>

<success_criteria>
- 3종의 채권/이자 계산기용 ActionInsight 데이터가 정확한 포맷으로 추가됨
</success_criteria>

<output>
After completion, create `.planning/phases/39-actioninsight/39-01-SUMMARY.md`
</output>
