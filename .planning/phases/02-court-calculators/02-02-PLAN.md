---
phase: 02-court-calculators
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/court/service-fee/page.tsx
  - src/app/tools/court/small-claims/page.tsx
  - src/app/tools/court/payment-order/page.tsx
autonomous: true
requirements:
  - COURT-04
  - COURT-05
  - COURT-06

must_haves:
  truths:
    - "송달료 계산기에서 당사자 수와 회수 입력 시 총 송달료가 계산된다"
    - "소액사건 계산기에서 3천만원 이하 소가에 대해 특례 비용이 계산된다"
    - "지급명령 계산기에서 인지대(1/10)와 송달료 합산 비용이 계산된다"
  artifacts:
    - path: "src/app/tools/court/service-fee/page.tsx"
      provides: "송달료 계산기 페이지"
      exports: ["default (page component)"]
    - path: "src/app/tools/court/small-claims/page.tsx"
      provides: "소액사건 재판비용 계산기 페이지"
      exports: ["default (page component)"]
    - path: "src/app/tools/court/payment-order/page.tsx"
      provides: "지급명령 비용 계산기 페이지"
      exports: ["default (page component)"]
  key_links:
    - from: "each page.tsx"
      to: "CalculatorLayout"
      via: "import from @/components/ui/CalculatorLayout"
      pattern: "CalculatorLayout"
---

<objective>
Implement 3 court calculators: service fee (송달료), small claims cost (소액사건), and payment order cost (지급명령).

Purpose: These calculators serve users filing smaller or expedited court proceedings.
Output: 3 fully functional 'use client' calculator pages.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/tools-data.ts
@src/components/ui/CalculatorLayout.tsx
@src/app/tools/[category]/[tool]/page.tsx

<interfaces>
From src/lib/tools-data.ts — court tool slugs for this plan:
- service-fee    → route: /tools/court/service-fee
- small-claims   → route: /tools/court/small-claims
- payment-order  → route: /tools/court/payment-order

Court category color: "#3b82f6"

Stamp fee formula (needed in payment-order and small-claims):
- ≤ 10,000,000: amount × 0.005 (min 1,000)
- 10,000,001 ~ 100,000,000: amount × 0.0045 + 5,000
- 100,000,001 ~ 1,000,000,000: amount × 0.004 + 55,000
- > 1,000,000,000: amount × 0.0035 + 555,000
- Round up to nearest 100원 (Math.ceil(x/100)*100)

Service fee unit: 4,500원/회 (2026 기준)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 송달료 계산기 (service-fee)</name>
  <read_first>
    - src/components/ui/CalculatorLayout.tsx
    - src/lib/tools-data.ts (service-fee entry)
  </read_first>
  <files>src/app/tools/court/service-fee/page.tsx</files>
  <action>
Create `src/app/tools/court/service-fee/page.tsx` as a 'use client' component.

**Korean law basis:** 민사소송규칙 제19조, 대법원 규칙 — 송달료는 당사자 수 × 회수 × 단가로 계산.

**계산 공식:**
총 송달료 = 당사자 수 × 송달 회수 × 4,500원

**입력 필드:**
1. 당사자 수 (원고+피고 합산, number, min 2, default 2)
2. 송달 예정 회수 (number, default 10)
   - 안내 텍스트: "민사 1심 기본 10회, 항소심 8회 권장"
3. 직접 입력 체크박스 vs 자동 계산 (기본값 자동)

**자동 계산 모드 안내 (심급별 기본 회수):**
- 1심: 10회 × 당사자 수 × 4,500원
- 항소심: 8회 × 당사자 수 × 4,500원
- 상고심: 5회 × 당사자 수 × 4,500원

**결과 표시:**
- 1인당 송달료: XXX원
- 총 송달료: XXX원 (강조)
- 계산식 표시: "2명 × 10회 × 4,500원 = 90,000원"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/tools/court/service-fee/page.tsx
    - Contains 'use client' on line 1
    - grep -q "4500\|4,500" src/app/tools/court/service-fee/page.tsx
    - Shows calculation formula string in result
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>당사자 수 + 회수 입력 → 총 송달료 + 계산식 표시. TypeScript 오류 없음.</done>
</task>

<task type="auto">
  <name>Task 2: 소액사건 재판비용 계산기 (small-claims)</name>
  <read_first>
    - src/app/tools/court/service-fee/page.tsx (pattern reference)
    - src/lib/tools-data.ts (small-claims entry)
  </read_first>
  <files>src/app/tools/court/small-claims/page.tsx</files>
  <action>
Create `src/app/tools/court/small-claims/page.tsx` as a 'use client' component.

**Korean law basis:** 소액사건심판법 — 소가 3,000만원 이하 사건의 특례 절차.

**특징:** 소액사건은 일반 민사와 동일한 인지대+송달료 공식 적용하되, 소가 상한이 3,000만원이므로 입력 검증 필요.

**계산 로직:**
1. 인지대 = 소가 × 0.005 (소가 ≤ 1,000만원) 또는 소가 × 0.0045 + 5,000 (1,000만원 < 소가 ≤ 3,000만원)
   → 100원 올림
2. 송달료 = 당사자 수 × 10회 × 4,500원 (소액 기본 10회)
3. 합계 = 인지대 + 송달료

**입력 필드:**
1. 청구금액 (원, max 30,000,000, validation: 3천만원 초과 시 경고 메시지 "소액사건은 3,000만원 이하 사건입니다")
2. 당사자 수 (number, default 2, min 2)

**결과 표시:**
- 인지대: XXX원
- 송달료: XXX원
- 합계: XXX원 (강조)
- 안내: "소액사건심판법에 따른 간이 절차 비용입니다"
- 소액사건 특징 안내 (소액사건은 변호사 없이 본인 진행 가능, 1회 변론으로 종결 원칙)
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/tools/court/small-claims/page.tsx
    - Contains 'use client' on line 1
    - grep -q "30000000\|3,000만\|소액" src/app/tools/court/small-claims/page.tsx
    - Validation message for amounts over 30,000,000
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>청구금액(≤3천만) + 당사자 수 → 인지대/송달료/합계 표시. 3천만 초과 시 경고. TypeScript 오류 없음.</done>
</task>

<task type="auto">
  <name>Task 3: 지급명령 비용 계산기 (payment-order)</name>
  <read_first>
    - src/app/tools/court/small-claims/page.tsx (pattern reference)
    - src/lib/tools-data.ts (payment-order entry)
  </read_first>
  <files>src/app/tools/court/payment-order/page.tsx</files>
  <action>
Create `src/app/tools/court/payment-order/page.tsx` as a 'use client' component.

**Korean law basis:** 민사소송법 제462조 이하 독촉절차 — 지급명령 인지대는 소송 인지대의 1/10.

**계산 로직:**
1. 소송 인지대 계산 (일반 공식):
   - ≤ 10,000,000: amount × 0.005
   - 10,000,001 ~ 100,000,000: amount × 0.0045 + 5,000
   - 100,000,001 ~ 1,000,000,000: amount × 0.004 + 55,000
   - > 1,000,000,000: amount × 0.0035 + 555,000
   → 100원 올림
2. 지급명령 인지대 = 소송 인지대 × 0.1 → 100원 올림 (최소 1,000원)
3. 송달료 = 당사자 수 × 3회 × 4,500원 (독촉절차 기본 3회)
4. 합계 = 지급명령 인지대 + 송달료

**입력 필드:**
1. 청구금액 (원)
2. 채무자 수 (number, default 1, min 1) — 지급명령은 채무자별 별도 신청

**결과 표시:**
- 지급명령 인지대: XXX원 (소송 인지대의 1/10)
- 송달료: XXX원
- 합계: XXX원 (강조)
- 비교 정보: "일반 소송 대비 인지대 절약액: XXX원"
- 안내: "지급명령 이의 시 소송으로 전환, 차액 인지대 납부 필요"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/app/tools/court/payment-order/page.tsx
    - Contains 'use client' on line 1
    - grep -q "0\.1\|1/10\|독촉\|지급명령" src/app/tools/court/payment-order/page.tsx
    - Shows savings comparison vs regular lawsuit
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>청구금액+채무자 수 → 지급명령 인지대(1/10)+송달료+합계+절약액 표시. TypeScript 오류 없음.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `npx tsc --noEmit` — zero errors
2. `npm run build` — 3 routes compile
3. Known value checks:
   - 송달료: 2명 × 10회 × 4,500 = 90,000원
   - 소액 청구 1,000만원: 인지대 = 1,000만 × 0.005 = 50,000원
   - 지급명령 청구 1,000만원: 인지대 = 50,000 × 0.1 = 5,000원
</verification>

<success_criteria>
- 3개 페이지 /tools/court/service-fee, /tools/court/small-claims, /tools/court/payment-order 각각 접근 가능
- 각 페이지 입력 → 계산 → 결과 흐름 동작
- TypeScript 오류 없음
</success_criteria>

<output>
After completion, create `.planning/phases/02-court-calculators/02-02-SUMMARY.md`
</output>
