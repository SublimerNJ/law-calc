---
phase: 09-damages-calculators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/damages/damages-general/page.tsx
  - src/app/tools/damages/defamation/page.tsx
  - src/app/tools/damages/medical-malpractice/page.tsx
autonomous: true
requirements:
  - DAMAGES-01
  - DAMAGES-02
  - DAMAGES-03

must_haves:
  truths:
    - "손해배상 계산기에서 재산상 손해 + 정신적 손해(위자료) 합산 결과가 표시된다"
    - "명예훼손 계산기에서 훼손 유형·매체·피해 정도 입력 후 위자료 예상 범위가 표시된다"
    - "의료사고 계산기에서 과실비율·치료비·후유장해 입력 후 배상액이 표시된다"
    - "세 페이지 모두 CalculatorLayout을 사용하고 damages 카테고리 색상(#f97316)이 적용된다"
    - "세 페이지 모두 독립 라우트로 직접 접근 가능하다"
  artifacts:
    - path: "src/app/tools/damages/damages-general/page.tsx"
      provides: "일반 손해배상액 계산기"
      exports: ["default function DamagesGeneralPage"]
    - path: "src/app/tools/damages/defamation/page.tsx"
      provides: "명예훼손 손해배상 계산기"
      exports: ["default function DefamationPage"]
    - path: "src/app/tools/damages/medical-malpractice/page.tsx"
      provides: "의료사고 손해배상 계산기"
      exports: ["default function MedicalMalpracticePage"]
  key_links:
    - from: "각 page.tsx"
      to: "src/components/ui/CalculatorLayout"
      via: "import CalculatorLayout"
      pattern: "import CalculatorLayout"
    - from: "각 page.tsx"
      to: "src/lib/tools-data"
      via: "TOOLS.find(t => t.id === '...')!"
      pattern: "TOOLS\\.find"
---

<objective>
손해배상 6개 계산기 중 첫 3개(DAMAGES-01~03)를 구현한다.

Purpose: 일반 손해배상, 명예훼손, 의료사고 — 세 가지 손해배상 유형별 계산기를 제공해 한국 판례 기준에 따른 배상액 산정을 지원한다.
Output: 세 개의 독립 페이지 파일, 각각 계산 로직 + CalculatorLayout UI 포함.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- CalculatorLayout: src/components/ui/CalculatorLayout.tsx -->
interface CalculatorLayoutProps {
  tool: Tool;
  category: Category;
  children: React.ReactNode;
}
export default function CalculatorLayout({ tool, category, children }: CalculatorLayoutProps)

<!-- tools-data.ts 손해배상 카테고리 -->
{ id: "damages", name: "손해배상", color: "#f97316" }

<!-- tools-data.ts 해당 도구 항목 -->
{ id: "damages-general", name: "손해배상 계산기", route: "/tools/damages/damages-general", icon: "📋", category: "damages" }
{ id: "defamation", name: "명예훼손 손해배상 계산기", route: "/tools/damages/defamation", icon: "📢", category: "damages" }
{ id: "medical-malpractice", name: "의료사고 손해배상 계산기", route: "/tools/damages/medical-malpractice", icon: "🏥", category: "damages" }

<!-- 패턴 (attorney-fee/page.tsx 참고) -->
'use client';
const tool = TOOLS.find(t => t.id === 'xxx')!;
const category = CATEGORIES.find(c => c.id === 'damages')!;
// 입력 폼: bg-[#0d1424] border border-[#1e2d4a] rounded-lg
// 버튼: style={{ backgroundColor: category.color }}
// 결과 금액: style={{ color: category.color }}
// premium-card p-6
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 손해배상 계산기 (DAMAGES-01) — damages-general</name>
  <files>src/app/tools/damages/damages-general/page.tsx</files>
  <action>
'use client' 클라이언트 컴포넌트로 생성. TOOLS.find(t => t.id === 'damages-general')!, CATEGORIES.find(c => c.id === 'damages')! 로 tool/category 참조.

계산 로직 (한국 판례 기준):
- 입력: 재산상 손해액(치료비+일실이익+기타 재산손해, 원), 과실비율(피해자, 0~100%), 위자료 청구 여부(체크박스), 위자료 기준액(원 — 사망 1억, 중상해 3천만 기본값 제공)
- 계산:
  - 과실상계 후 재산상 손해 = 재산상 손해액 × (1 - 과실비율/100)
  - 위자료 = 위자료 기준액 × (1 - 과실비율/100)
  - 총 배상액 = 과실상계 후 재산상 손해 + 위자료
- 결과 표시: 재산상 손해(과실상계 후), 위자료, 총 배상액 각각 별도 행으로 표시

UI: premium-card p-6, 숫자 입력은 toLocaleString('ko-KR') 포맷, 계산하기 버튼(category.color), 법적 근거: "민법 제750조(불법행위), 제763조(과실상계)"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "damages-general" || echo "no type errors in damages-general"</automated>
  </verify>
  <done>
    - /tools/damages/damages-general 라우트 접근 시 계산기 렌더링
    - 재산상 손해 1000만원, 과실비율 30% 입력 시 700만원 표시
    - 위자료 포함 시 합산 총액 표시
  </done>
</task>

<task type="auto">
  <name>Task 2: 명예훼손 손해배상 계산기 (DAMAGES-02) — defamation</name>
  <files>src/app/tools/damages/defamation/page.tsx</files>
  <action>
'use client' 컴포넌트. tool id: 'defamation', category: 'damages'.

계산 로직 (한국 판례 기준 위자료 산정):
- 입력:
  - 훼손 유형: select — "사실 적시"(기본) / "허위 사실 적시" / "모욕"
  - 매체: select — "온라인/SNS" / "방송" / "신문·잡지" / "대화·구두"
  - 피해자 유형: select — "일반인" / "공인(공직자·연예인 등)"
  - 유포 기간(개월, 숫자 입력)
  - 정신적 고통 정도: select — "경미" / "보통" / "심각"
- 계산 (판례 위자료 범위):
  - 기준액 설정:
    - 모욕: 100만~300만, 사실 적시: 300만~1000만, 허위 사실: 500만~3000만
    - 중간값을 기준으로 사용
  - 가중치:
    - 온라인/SNS: ×1.5, 방송: ×2.0, 신문: ×1.3, 구두: ×1.0
    - 공인이면 ×0.7 (공인은 비판 감수), 일반인은 ×1.0
    - 심각: ×1.5, 보통: ×1.0, 경미: ×0.7
    - 유포 기간 1개월 초과 시 +10% per 월 (최대 +100%)
  - 최종 위자료 = 기준액 × 매체가중치 × 피해자가중치 × 고통가중치 × 기간가중치
  - 최소/최대 범위도 함께 표시 (최종값 ±30%)
- 결과: 예상 위자료 중간값, 예상 범위(최소~최대)

UI: 법적 근거 "형법 제307조·제311조, 민법 제750조, 정보통신망법 제70조"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "defamation" || echo "no type errors in defamation"</automated>
  </verify>
  <done>
    - /tools/damages/defamation 접근 시 계산기 렌더링
    - 허위 사실 + 온라인 + 일반인 + 심각 선택 시 높은 범위 위자료 표시
    - 예상 범위(최소/최대) 표시 확인
  </done>
</task>

<task type="auto">
  <name>Task 3: 의료사고 손해배상 계산기 (DAMAGES-03) — medical-malpractice</name>
  <files>src/app/tools/damages/medical-malpractice/page.tsx</files>
  <action>
'use client' 컴포넌트. tool id: 'medical-malpractice', category: 'damages'.

계산 로직 (한국 의료사고 판례 기준):
- 입력:
  - 치료비(원): 기지출 치료비
  - 향후 치료비(원): 향후 발생 예상 치료비
  - 일실수입(원): 사고 기간 중 잃은 수입 (직접 입력)
  - 의사 과실비율(%): 0~100
  - 후유장해 여부: 체크박스
  - 후유장해율(%): 0~100 (후유장해 체크 시 활성)
  - 위자료 기준액(원): 기본값 — 사망 1억, 중상해 3천만 (select: "사망(1억)", "중상해(3천만)", "경상해(1천만)", "직접 입력")
- 계산:
  - 과실상계 후 치료비 = (치료비 + 향후 치료비) × (과실비율/100)
    - 단, 의사 과실비율을 적용: 환자 기여 과실이 아닌 의료인 과실이므로 그대로 사용
    - 배상액 = (치료비 + 향후치료비 + 일실수입) × (의사 과실비율/100)
  - 후유장해 위자료 = 위자료 기준액 × (후유장해율/100) × (의사 과실비율/100)
  - 총 배상액 = 재산상 배상액 + 후유장해 위자료
- 결과 표시: 재산상 손해 배상액, 위자료, 합계

UI: 후유장해 체크 시 후유장해율 입력란 동적 표시. 법적 근거: "의료법, 민법 제750조, 의료사고 피해구제 및 의료분쟁 조정 등에 관한 법률"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep "medical-malpractice" || echo "no type errors in medical-malpractice"</automated>
  </verify>
  <done>
    - /tools/damages/medical-malpractice 접근 시 계산기 렌더링
    - 치료비 1000만, 의사 과실 70% 입력 시 700만 재산상 손해 표시
    - 후유장해 체크 시 위자료 입력란 활성화 및 합산 표시
  </done>
</task>

</tasks>

<verification>
세 파일 모두 생성 후:
- `npx tsc --noEmit` — TypeScript 오류 없음
- 각 라우트 curl 또는 브라우저 접근 시 렌더링 확인
- damages 카테고리 색상 #f97316 적용 확인
</verification>

<success_criteria>
- DAMAGES-01~03 세 계산기 페이지 파일 존재
- CalculatorLayout 래핑, 'use client' 선언
- 한국 판례 기준 계산 로직 적용
- 과실비율 입력 및 과실상계 계산 정상 동작
- TypeScript 컴파일 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/09-damages-calculators/09-01-SUMMARY.md` 생성.
</output>
