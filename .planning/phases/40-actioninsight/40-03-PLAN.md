---
phase: 40-actioninsight
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified: [
  "src/app/tools/misc/certified-letter/page.tsx",
  "src/app/tools/misc/legal-aid/page.tsx",
  "src/app/tools/misc/public-defender/page.tsx",
  "src/app/tools/misc/statute-of-limitations/page.tsx"
]
autonomous: true
requirements: ["UI-08"]
must_haves:
  truths:
    - "misc 4개 도구 화면 하단에 ActionInsight 컴포넌트가 렌더링된다."
    - "ActionInsight 컴포넌트가 각 도구의 calculatorId를 올바르게 전달받는다."
  artifacts:
    - path: "src/app/tools/misc/certified-letter/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"certified-letter\""
    - path: "src/app/tools/misc/legal-aid/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"legal-aid\""
    - path: "src/app/tools/misc/public-defender/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"public-defender\""
    - path: "src/app/tools/misc/statute-of-limitations/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"statute-of-limitations\""
  key_links:
    - from: "src/app/tools/misc/*/page.tsx"
      to: "src/components/ui/ActionInsight.tsx"
      via: "import and render"
      pattern: "<ActionInsight"
---

<objective>
기타 법률(misc) 4개 도구 UI 하단에 `ActionInsight` 컴포넌트를 연동한다.

Purpose: 사용자가 내용증명, 법률구조, 국선변호인 판별 및 소멸시효 계산 결과를 확인한 후 활용할 수 있는 실무 팁을 제공하여 사용성을 극대화한다.
Output: 4개 도구 화면 하단에 ActionInsight 컴포넌트 렌더링 코드 추가
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From `src/components/ui/ActionInsight.tsx`:
```typescript
interface ActionInsightProps {
  calculatorId: string;
  amount?: number;
}
export function ActionInsight({ calculatorId, amount }: ActionInsightProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: certified-letter 및 legal-aid 연동</name>
  <files>
    src/app/tools/misc/certified-letter/page.tsx
    src/app/tools/misc/legal-aid/page.tsx
  </files>
  <action>
    `ActionInsight` 컴포넌트를 import하고 렌더링 최하단(보통 결과 또는 주요 내용 직후)에 추가한다.
    - `certified-letter`: `<ActionInsight calculatorId="certified-letter" />`
    - `legal-aid`: `<ActionInsight calculatorId="legal-aid" />`
    (금액 필드가 없는 도구의 경우 amount props 생략 가능)
    적절한 mt-8 등의 여백을 주어 디자인 일관성을 유지한다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight calculatorId=\"certified-letter\"" src/app/tools/misc/certified-letter/page.tsx && grep -q "ActionInsight calculatorId=\"legal-aid\"" src/app/tools/misc/legal-aid/page.tsx</automated>
  </verify>
  <done>2개 도구 UI 연동 완료</done>
</task>

<task type="auto">
  <name>Task 2: public-defender 및 statute-of-limitations 연동</name>
  <files>
    src/app/tools/misc/public-defender/page.tsx
    src/app/tools/misc/statute-of-limitations/page.tsx
  </files>
  <action>
    `ActionInsight` 컴포넌트를 import하고 렌더링 최하단에 추가한다.
    - `public-defender`: `<ActionInsight calculatorId="public-defender" />`
    - `statute-of-limitations`: `<ActionInsight calculatorId="statute-of-limitations" />`
    적절한 mt-8 여백을 주어 디자인 일관성을 유지한다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight calculatorId=\"public-defender\"" src/app/tools/misc/public-defender/page.tsx && grep -q "ActionInsight calculatorId=\"statute-of-limitations\"" src/app/tools/misc/statute-of-limitations/page.tsx</automated>
  </verify>
  <done>2개 도구 UI 연동 완료</done>
</task>

</tasks>

<verification>
npm run build가 에러 없이 통과하는지 확인.
각 파일에서 ActionInsight가 올바르게 import 되었는지 확인.
</verification>

<success_criteria>
misc 4개 도구 하단에 ActionInsight 컴포넌트가 올바르게 연동되고 렌더링됨.
</success_criteria>

<output>
After completion, create `.planning/phases/40-actioninsight/40-03-SUMMARY.md`
</output>