---
phase: 40-actioninsight
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified: [
  "src/app/tools/damages/damages-general/page.tsx",
  "src/app/tools/damages/defamation/page.tsx",
  "src/app/tools/damages/medical-malpractice/page.tsx",
  "src/app/tools/damages/lost-income/page.tsx"
]
autonomous: true
requirements: ["UI-08"]
must_haves:
  truths:
    - "damages 4개 도구 화면 하단에 ActionInsight 컴포넌트가 렌더링된다."
    - "ActionInsight 컴포넌트가 각 도구의 calculatorId를 올바르게 전달받는다."
  artifacts:
    - path: "src/app/tools/damages/damages-general/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"damages-general\""
    - path: "src/app/tools/damages/defamation/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"defamation\""
    - path: "src/app/tools/damages/medical-malpractice/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"medical-malpractice\""
    - path: "src/app/tools/damages/lost-income/page.tsx"
      provides: "ActionInsight UI"
      contains: "ActionInsight calculatorId=\"lost-income\""
  key_links:
    - from: "src/app/tools/damages/*/page.tsx"
      to: "src/components/ui/ActionInsight.tsx"
      via: "import and render"
      pattern: "<ActionInsight"
---

<objective>
손해배상(damages) 4개 계산기 UI 하단에 `ActionInsight` 컴포넌트를 연동한다.

Purpose: 사용자가 손해배상 계산 결과를 확인한 후, 곧바로 활용할 수 있는 실무 팁과 액션 플랜 템플릿을 제공하여 사용성을 극대화한다.
Output: 4개 계산기 화면 하단에 ActionInsight 컴포넌트 렌더링 코드 추가
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From `src/components/ui/ActionInsight.tsx`:
```typescript
interface ActionInsightProps {
  calculatorId: string;
  amount?: number;
}
export function ActionInsight({ calculatorId, amount }: ActionInsightProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: damages-general 및 defamation 연동</name>
  <files>
    src/app/tools/damages/damages-general/page.tsx
    src/app/tools/damages/defamation/page.tsx
  </files>
  <action>
    `ActionInsight` 컴포넌트를 import하고 렌더링 최하단(보통 결과 컨테이너 직후)에 추가한다.
    - `damages-general`: `<ActionInsight calculatorId="damages-general" amount={totalDamages} />` (amount는 해당 파일에 정의된 총 손해액 변수 사용)
    - `defamation`: `<ActionInsight calculatorId="defamation" amount={totalDamages} />`
    적절한 mt-8 등의 여백을 주어 디자인 일관성을 유지한다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight calculatorId=\"damages-general\"" src/app/tools/damages/damages-general/page.tsx && grep -q "ActionInsight calculatorId=\"defamation\"" src/app/tools/damages/defamation/page.tsx</automated>
  </verify>
  <done>2개 도구 UI 연동 완료</done>
</task>

<task type="auto">
  <name>Task 2: medical-malpractice 및 lost-income 연동</name>
  <files>
    src/app/tools/damages/medical-malpractice/page.tsx
    src/app/tools/damages/lost-income/page.tsx
  </files>
  <action>
    `ActionInsight` 컴포넌트를 import하고 렌더링 최하단에 추가한다.
    - `medical-malpractice`: `<ActionInsight calculatorId="medical-malpractice" amount={totalDamages} />`
    - `lost-income`: `<ActionInsight calculatorId="lost-income" amount={totalLostIncome} />`
    적절한 mt-8 여백을 주어 디자인 일관성을 유지한다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight calculatorId=\"medical-malpractice\"" src/app/tools/damages/medical-malpractice/page.tsx && grep -q "ActionInsight calculatorId=\"lost-income\"" src/app/tools/damages/lost-income/page.tsx</automated>
  </verify>
  <done>2개 도구 UI 연동 완료</done>
</task>

</tasks>

<verification>
npm run build가 에러 없이 통과하는지 확인.
각 파일에서 ActionInsight가 올바르게 import 되었는지 확인.
</verification>

<success_criteria>
damages 4개 도구 하단에 ActionInsight 컴포넌트가 올바르게 연동되고 렌더링됨.
</success_criteria>

<output>
After completion, create `.planning/phases/40-actioninsight/40-02-SUMMARY.md`
</output>