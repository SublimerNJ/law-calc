---
phase: 40-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/action-data.ts"]
autonomous: true
requirements: ["ACTION-08"]
must_haves:
  truths:
    - "action-data.ts 내에 damages 4개 및 misc 4개 계산기에 대한 데이터 객체가 존재한다."
    - "각 데이터 객체는 title, tips (3개), scriptTemplate 형식을 갖춘다."
  artifacts:
    - path: "src/lib/action-data.ts"
      provides: "ActionInsight 데이터 객체"
      contains: "'damages-general', 'defamation', 'medical-malpractice', 'lost-income', 'certified-letter', 'legal-aid', 'public-defender', 'statute-of-limitations'"
  key_links:
    - from: "src/lib/action-data.ts"
      to: "ActionInsight component"
      via: "actionData export"
      pattern: "export const actionData"
---

<objective>
손해배상(damages) 4개 도구와 기타(misc) 4개 도구를 위한 ActionInsight 데이터를 `action-data.ts`에 추가한다.

Purpose: 이후 진행될 UI 연동(Plan 40-02, 40-03)을 위해 각 계산기 맥락에 맞는 구체적이고 전문적인 실전 팁/템플릿 데이터를 미리 준비한다.
Output: `action-data.ts` 내에 8개의 새로운 키가 추가됨
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From `src/types/action-insights.ts`:
```typescript
export interface ScriptTemplate {
  title: string;
  text: string;
  buttonLabel: string;
}

export interface ActionInsightData {
  title: string;
  tips: string[];
  scriptTemplate?: ScriptTemplate;
}

export type CalculatorActionData = Record<string, ActionInsightData>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: damages 4개 도구 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `actionData` 객체에 다음 4개 키를 추가한다:
    - `damages-general` (일반 손해배상)
    - `defamation` (명예훼손)
    - `medical-malpractice` (의료사고)
    - `lost-income` (일실수입)
    
    각 데이터는 전문적인 '전문가 조언' 톤을 유지하며, 팁은 3개로 구성하고, 당사자 간 합의/요구를 위한 `scriptTemplate`을 포함해야 한다. 기존 데이터 객체의 형식을 그대로 따른다.
  </action>
  <verify>
    <automated>grep -q "damages-general" src/lib/action-data.ts && grep -q "defamation" src/lib/action-data.ts</automated>
  </verify>
  <done>damages 4개 키가 actionData 객체에 포함됨</done>
</task>

<task type="auto">
  <name>Task 2: misc 4개 도구 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `actionData` 객체에 다음 4개 키를 추가한다:
    - `certified-letter` (내용증명)
    - `legal-aid` (법률구조)
    - `public-defender` (국선변호인)
    - `statute-of-limitations` (소멸시효)
    
    마찬가지로 전문적이고 단호한 어조로 작성하되, `scriptTemplate`의 경우 내용증명이나 기타 분쟁 전 단계의 통지 용도로 적절히 작성한다. 
  </action>
  <verify>
    <automated>grep -q "certified-letter" src/lib/action-data.ts && grep -q "statute-of-limitations" src/lib/action-data.ts</automated>
  </verify>
  <done>misc 4개 키가 actionData 객체에 포함됨</done>
</task>

</tasks>

<verification>
npm run build가 에러 없이 통과하는지 확인.
TypeScript 린트 오류가 발생하지 않는지 확인.
</verification>

<success_criteria>
`action-data.ts`에 8개 도구의 실무 팁과 템플릿이 추가되고 TypeScript 타입 에러가 없음.
</success_criteria>

<output>
After completion, create `.planning/phases/40-actioninsight/40-01-SUMMARY.md`
</output>