---
phase: 36-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/action-data.ts"]
autonomous: true
requirements: ["ACTION-04"]
must_haves:
  truths:
    - "세금 관련 10개 도구에 대해 전문가 팁 데이터가 존재한다"
    - "세금 관련 10개 도구에 대해 카톡/메시지 템플릿이 제공된다"
  artifacts:
    - path: "src/lib/action-data.ts"
      provides: "Tax calculator action data"
      contains: "acquisition-tax"
  key_links:
    - from: "src/lib/action-data.ts"
      to: "Tax pages"
      via: "export actionData object"
---

<objective>
세금 계산기 10종에 대한 ActionInsight 데이터 추가

Purpose: 사용자가 세금 계산 결과를 확인한 후, 세무사 수준의 조언을 얻고 관련자(임대인, 세무서 등)에게 협조/요청을 할 수 있는 템플릿을 제공하여 도구의 가치를 높인다.
Output: `action-data.ts`에 10개 세금 계산기 데이터 추가
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: actionData에 10개 세금 도구 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `src/lib/action-data.ts` 파일의 `actionData` 객체에 세금 10종 도구의 키를 추가하고, 각 도구의 특성에 맞는 `title`, `tips`(3개), `scriptTemplate`을 정의합니다.
    추가 대상: `acquisition-tax`, `capital-gains-tax`, `comprehensive-income-tax`, `comprehensive-property-tax`, `four-insurances`, `registration-tax`, `rent-tax-credit`, `securities-tax`, `vat`, `year-end-tax`.
    
    팁 작성 시 Tone은 전문적이어야 하며, 템플릿은 실무에서 사용할 수 있는 양식(문자/카톡)이어야 합니다.
    기존 노동/근로 계산기 팁 작성 방식을 참고하여 포맷을 유지합니다. 금액 치환자인 `{{AMOUNT}}`를 적절히 활용하세요.
  </action>
  <verify>
    <automated>grep -q "acquisition-tax" src/lib/action-data.ts</automated>
  </verify>
  <done>10개 세금 도구 키와 데이터가 action-data.ts에 모두 등록됨</done>
</task>

</tasks>

<verification>
npm run type-check를 실행하여 TypeScript 오류가 없는지 확인합니다.
</verification>

<success_criteria>
`action-data.ts`가 성공적으로 업데이트되고 컴파일 에러가 발생하지 않습니다.
</success_criteria>

<output>
After completion, create `.planning/phases/36-actioninsight/36-01-SUMMARY.md`
</output>