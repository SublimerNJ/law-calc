---
phase: 36-actioninsight
plan: 05
type: execute
wave: 2
depends_on: ["36-01"]
files_modified: [
  "src/app/tools/tax/vat/page.tsx",
  "src/app/tools/tax/year-end-tax/page.tsx"
]
autonomous: true
requirements: ["UI-04"]
must_haves:
  truths:
    - "부가세, 연말정산 결과 화면 하단에 ActionInsight 컴포넌트가 노출된다"
  artifacts:
    - path: "src/app/tools/tax/vat/page.tsx"
      provides: "ActionInsight rendering"
      contains: "<ActionInsight"
    - path: "src/app/tools/tax/year-end-tax/page.tsx"
      provides: "ActionInsight rendering"
      contains: "<ActionInsight"
  key_links:
    - from: "Tax pages"
      to: "ActionInsight component"
      via: "import and render"
---

<objective>
세금 계산기(부가세, 연말정산) 화면에 ActionInsight 연동

Purpose: 계산 결과를 확인한 사용자에게 즉각적인 실행 방안(ActionInsight)을 화면 하단에 렌더링.
Output: 대상 페이지 2개에 UI 추가 완료
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 부가세 계산기 ActionInsight 적용</name>
  <files>src/app/tools/tax/vat/page.tsx</files>
  <action>
    `ActionInsight` 컴포넌트와 `actionData`를 임포트합니다.
    계산 결과 화면 하단에 `<ActionInsight data={actionData['vat']} amount={result.payableTax || result.supplyVat} />` (모드에 따라 적절한 세액 변수) 형태로 컴포넌트를 추가합니다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/tax/vat/page.tsx</automated>
  </verify>
  <done>부가세 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

<task type="auto">
  <name>Task 2: 연말정산 계산기 ActionInsight 적용</name>
  <files>src/app/tools/tax/year-end-tax/page.tsx</files>
  <action>
    `ActionInsight` 컴포넌트와 `actionData`를 임포트합니다.
    계산 결과 화면 하단에 `<ActionInsight data={actionData['year-end-tax']} amount={Math.abs(result.finalTax)} />` (또는 적절한 세액 변수) 형태로 컴포넌트를 추가합니다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/tax/year-end-tax/page.tsx</automated>
  </verify>
  <done>연말정산 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

</tasks>

<verification>
npm run type-check를 실행하여 TypeScript 오류가 없는지 확인합니다.
</verification>

<success_criteria>
2개 계산기 페이지에 ActionInsight 컴포넌트 렌더링이 에러 없이 통합되었습니다.
</success_criteria>

<output>
After completion, create `.planning/phases/36-actioninsight/36-05-SUMMARY.md`
</output>