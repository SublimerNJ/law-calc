---
phase: 36-actioninsight
plan: 04
type: execute
wave: 2
depends_on: ["36-01"]
files_modified: [
  "src/app/tools/tax/rent-tax-credit/page.tsx",
  "src/app/tools/tax/securities-tax/page.tsx"
]
autonomous: true
requirements: ["UI-04"]
must_haves:
  truths:
    - "월세세액공제, 증권거래세 결과 화면 하단에 ActionInsight 컴포넌트가 노출된다"
  artifacts:
    - path: "src/app/tools/tax/rent-tax-credit/page.tsx"
      provides: "ActionInsight rendering"
      contains: "<ActionInsight"
    - path: "src/app/tools/tax/securities-tax/page.tsx"
      provides: "ActionInsight rendering"
      contains: "<ActionInsight"
  key_links:
    - from: "Tax pages"
      to: "ActionInsight component"
      via: "import and render"
---

<objective>
세금 계산기(월세세액공제, 증권거래세) 화면에 ActionInsight 연동

Purpose: 계산 결과를 확인한 사용자에게 즉각적인 실행 방안(ActionInsight)을 화면 하단에 렌더링.
Output: 대상 페이지 2개에 UI 추가 완료
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 월세세액공제 계산기 ActionInsight 적용</name>
  <files>src/app/tools/tax/rent-tax-credit/page.tsx</files>
  <action>
    `ActionInsight` 컴포넌트와 `actionData`를 임포트합니다.
    계산 결과 화면 하단에 `<ActionInsight data={actionData['rent-tax-credit']} amount={result.creditAmount} />` (또는 적절한 세액 변수) 형태로 컴포넌트를 추가합니다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/tax/rent-tax-credit/page.tsx</automated>
  </verify>
  <done>월세세액공제 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

<task type="auto">
  <name>Task 2: 증권거래세 계산기 ActionInsight 적용</name>
  <files>src/app/tools/tax/securities-tax/page.tsx</files>
  <action>
    `ActionInsight` 컴포넌트와 `actionData`를 임포트합니다.
    계산 결과 화면 하단에 `<ActionInsight data={actionData['securities-tax']} amount={result.totalTax} />` (또는 적절한 세액 변수) 형태로 컴포넌트를 추가합니다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/tax/securities-tax/page.tsx</automated>
  </verify>
  <done>증권거래세 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

</tasks>

<verification>
npm run type-check를 실행하여 TypeScript 오류가 없는지 확인합니다.
</verification>

<success_criteria>
2개 계산기 페이지에 ActionInsight 컴포넌트 렌더링이 에러 없이 통합되었습니다.
</success_criteria>

<output>
After completion, create `.planning/phases/36-actioninsight/36-04-SUMMARY.md`
</output>