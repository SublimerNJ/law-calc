---
phase: 36-actioninsight
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified: [
  "src/app/tools/tax/acquisition-tax/page.tsx",
  "src/app/tools/tax/capital-gains-tax/page.tsx",
  "src/app/tools/tax/comprehensive-income-tax/page.tsx"
]
autonomous: true
requirements: ["UI-04"]
must_haves:
  truths:
    - "취득세, 양도소득세, 종합소득세 결과 화면 하단에 ActionInsight 컴포넌트가 노출된다"
  artifacts:
    - path: "src/app/tools/tax/acquisition-tax/page.tsx"
      provides: "ActionInsight rendering"
      contains: "<ActionInsight"
    - path: "src/app/tools/tax/capital-gains-tax/page.tsx"
      provides: "ActionInsight rendering"
      contains: "<ActionInsight"
    - path: "src/app/tools/tax/comprehensive-income-tax/page.tsx"
      provides: "ActionInsight rendering"
      contains: "<ActionInsight"
  key_links:
    - from: "Tax pages"
      to: "ActionInsight component"
      via: "import and render"
---

<objective>
세금 계산기(취득세, 양도소득세, 종합소득세) 화면에 ActionInsight 연동

Purpose: 계산 결과를 확인한 사용자에게 즉각적인 실행 방안(ActionInsight)을 화면 하단에 렌더링.
Output: 대상 페이지 3개에 UI 추가 완료
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 취득세 계산기 ActionInsight 적용</name>
  <files>src/app/tools/tax/acquisition-tax/page.tsx</files>
  <action>
    `ActionInsight` 컴포넌트와 `actionData`를 임포트합니다.
    계산 결과가 렌더링되는 블록(`result`가 존재하는 조건 내 하단 또는 전체 래퍼 하단)에 `<ActionInsight data={actionData['acquisition-tax']} amount={result.totalTax} />` 형태로 컴포넌트를 추가합니다.
    금액(amount) 프로퍼티는 계산된 대표 금액으로 전달합니다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/tax/acquisition-tax/page.tsx</automated>
  </verify>
  <done>취득세 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

<task type="auto">
  <name>Task 2: 양도소득세 계산기 ActionInsight 적용</name>
  <files>src/app/tools/tax/capital-gains-tax/page.tsx</files>
  <action>
    `ActionInsight` 컴포넌트와 `actionData`를 임포트합니다.
    계산 결과 화면 하단에 `<ActionInsight data={actionData['capital-gains-tax']} amount={result.payableTax} />` (또는 적절한 총 세액 변수) 형태로 컴포넌트를 추가합니다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/tax/capital-gains-tax/page.tsx</automated>
  </verify>
  <done>양도소득세 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

<task type="auto">
  <name>Task 3: 종합소득세 계산기 ActionInsight 적용</name>
  <files>src/app/tools/tax/comprehensive-income-tax/page.tsx</files>
  <action>
    `ActionInsight` 컴포넌트와 `actionData`를 임포트합니다.
    계산 결과 화면 하단에 `<ActionInsight data={actionData['comprehensive-income-tax']} amount={result.payableTax} />` (또는 적절한 세액 변수) 형태로 컴포넌트를 추가합니다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/tax/comprehensive-income-tax/page.tsx</automated>
  </verify>
  <done>종합소득세 계산기에 ActionInsight가 정상적으로 연동됨</done>
</task>

</tasks>

<verification>
npm run type-check를 실행하여 TypeScript 오류가 없는지 확인합니다.
</verification>

<success_criteria>
3개 계산기 페이지에 ActionInsight 컴포넌트 렌더링이 에러 없이 통합되었습니다.
</success_criteria>

<output>
After completion, create `.planning/phases/36-actioninsight/36-02-SUMMARY.md`
</output>