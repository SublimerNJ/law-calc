---
phase: 43-tax-realty-actioninsight
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/ui/ActionInsight.tsx", "src/lib/action-data.ts"]
autonomous: true
requirements: ["DATA-04", "COMP-04", "DATA-05", "COMP-05"]

must_haves:
  truths:
    - "User can see ActionInsight on tax calculators"
    - "User can see ActionInsight on realty calculators"
    - "User sees a blue theme for the ActionInsight component"
    - "User gets an alert when copying the template to clipboard"
  artifacts:
    - path: "src/components/ui/ActionInsight.tsx"
      provides: "ActionInsight component rendering"
    - path: "src/lib/action-data.ts"
      provides: "Tax and realty tool specific data"
  key_links:
    - from: "src/components/ui/ActionInsight.tsx"
      to: "src/lib/action-data.ts"
      via: "imports and reads actionData"
      pattern: "actionData\\[calculatorId\\]"
---

<objective>
Update ActionInsight UI to match the required blue theme and alert feedback, and verify the tax/realty data integration.

Purpose: To ensure the UI aligns with the context requirements (bg-blue-50, alert feedback) and to formally satisfy Phase 43 requirements for Tax and Realty calculators (which were largely preemptively implemented in the codebase).
Output: Updated ActionInsight.tsx and verified integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/ActionInsight.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ActionInsight Component Theme and Feedback</name>
  <files>src/components/ui/ActionInsight.tsx</files>
  <action>
    Per Phase 43 context (Implementation Decisions), modify `ActionInsight.tsx` to:
    1. Change the theme from amber to blue:
       - Replace `bg-amber-50` -> `bg-blue-50`
       - Replace `border-amber-200` -> `border-blue-200`
       - Replace `text-amber-900` -> `text-blue-900`
       - Replace `text-amber-600` -> `text-blue-600`
       - Replace `ring-amber-500` -> `ring-blue-500`
    2. Change the clipboard feedback to use `alert` instead of inline text:
       - In `handleCopy`, after `writeText`, remove `setCopied(true)` and instead call `window.alert('클립보드에 복사되었습니다.');`.
       - Update the button text to just show `data.scriptTemplate.buttonLabel || '텍스트 복사하기'` without checking `copied` state.
  </action>
  <verify>
    <automated>grep "bg-blue-50" src/components/ui/ActionInsight.tsx</automated>
  </verify>
  <done>ActionInsight component uses blue theme and window.alert for copy feedback</done>
</task>

<task type="auto">
  <name>Task 2: Verify Tax & Realty Calculators ActionInsight Data</name>
  <files>src/lib/action-data.ts</files>
  <action>
    Since the action-data.ts file has already been populated with tax and realty keys in previous commits, simply verify that the 17 keys ('acquisition-tax', 'brokerage-fee', etc.) are present and properly formatted. No functional code changes are required here unless syntax or structure errors are found. This step formally satisfies DATA-04, COMP-04, DATA-05, and COMP-05.
  </action>
  <verify>
    <automated>grep "acquisition-tax" src/lib/action-data.ts</automated>
  </verify>
  <done>Tax and Realty ActionInsight data verified and requirements formally satisfied</done>
</task>

</tasks>

<verification>
1. Check that the ActionInsight.tsx component has the blue theme classes applied.
2. Check that window.alert is used for copy feedback.
3. Tax and Realty calculations continue to function without type errors.
</verification>

<success_criteria>
- ActionInsight UI renders in blue.
- Clicking the copy button triggers an alert.
- Tax and Realty calculators successfully show ActionInsight data.
</success_criteria>

<output>
After completion, create `.planning/phases/43-tax-realty-actioninsight/43-01-SUMMARY.md`
</output>
