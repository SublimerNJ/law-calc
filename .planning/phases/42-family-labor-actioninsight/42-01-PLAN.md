---
phase: 42-family-labor-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/app/tools/family/child-support/page.tsx"]
autonomous: true
requirements: ["DATA-02", "COMP-02", "DATA-03", "COMP-03"]
must_haves:
  truths:
    - "User can see practical tips and templates for child support"
    - "ActionInsight data is available and rendered for family and labor calculators"
  artifacts:
    - path: "src/app/tools/family/child-support/page.tsx"
      provides: "ActionInsight component rendering"
      contains: "<ActionInsight"
  key_links:
    - from: "src/app/tools/family/child-support/page.tsx"
      to: "ActionInsight"
      via: "component render"
      pattern: "<ActionInsight"
---

<objective>
가사/가족법 및 노동/근로 관련 계산기에서 ActionInsight 컴포넌트를 연동합니다.
대부분의 계산기는 이미 연동되어 있으며, 누락된 `child-support` (양육비 계산기)에만 추가 연동 작업을 진행합니다.

Purpose: 실전 대응 팁 및 템플릿 제공을 통한 사용자 가치 창출
Output: ActionInsight가 연동된 child-support 계산기 페이지
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/action-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: child-support 계산기에 ActionInsight 연동</name>
  <files>src/app/tools/family/child-support/page.tsx</files>
  <action>
    - `src/app/tools/family/child-support/page.tsx`에 `ActionInsight` 컴포넌트가 import 되어 있으나 렌더링되지 않고 있습니다.
    - 결과 화면(`result !== null`) 영역 하단, "양육비 청구 방법" 안내 박스 직전에 다음 코드를 추가합니다:
      `{result !== null && ( <ActionInsight calculatorId="child-support" amount={result.monthlyTotal} /> )}`
  </action>
  <verify>
    <automated>grep -q "&lt;ActionInsight calculatorId=\"child-support\"" src/app/tools/family/child-support/page.tsx</automated>
  </verify>
  <done>결과값 도출 시 ActionInsight 영역이 정상적으로 표시됨</done>
</task>

</tasks>

<verification>
- 모든 가족/노동 계산기가 ActionInsight를 포함하고 있는지 확인 (기존 적용분 포함).
- 양육비 계산기 결과 도출 후 팁과 템플릿이 잘 나타나는지 확인.
</verification>

<success_criteria>
- child-support 페이지 소스코드에 ActionInsight 컴포넌트 렌더링 코드가 존재함.
- 빌드 에러 없이 정상적으로 렌더링됨.
</success_criteria>

<output>
After completion, create `.planning/phases/42-family-labor-actioninsight/42-01-SUMMARY.md`
</output>