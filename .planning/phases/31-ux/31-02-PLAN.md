---
phase: 31-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/debt/late-payment/page.tsx
  - src/app/tools/debt/loan-interest/page.tsx
  - src/app/tools/debt/unjust-enrichment/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "late-payment에서 원금 미입력 시 에러 표시 후 결과 미출력"
    - "late-payment에서 시작일 미입력 시 에러 표시 후 결과 미출력"
    - "late-payment에서 원금 999조 초과 시 경고 표시 (계산 허용)"
    - "late-payment에서 지연일수 0일 결과 시 의미 있는 안내 표시"
    - "late-payment에서 에러 박스가 dark 색상(bg-red-500/10 text-red-400) → light 색상(bg-red-50 text-red-600) 수정"
    - "late-payment에서 필수 필드 라벨에 * 표시"
    - "loan-interest에서 원금 미입력 시 에러 표시 후 결과 미출력"
    - "loan-interest에서 이율·기간 미입력 시 에러 표시 후 결과 미출력"
    - "loan-interest에서 이율 필드 type=number → type=text + inputMode=decimal 변경"
    - "loan-interest에서 기간 필드 type=number → type=text + inputMode=numeric 변경"
    - "loan-interest에서 원금 999조 초과 시 경고 표시"
    - "loan-interest에서 필수 필드 라벨에 * 표시"
    - "unjust-enrichment에서 원금 미입력 시 에러 표시 후 결과 미출력"
    - "unjust-enrichment에서 취득일 미입력 시 에러 표시 후 결과 미출력"
    - "unjust-enrichment에서 원금 999조 초과 시 경고 표시"
    - "unjust-enrichment에서 에러 메시지 스타일 text-red-400 → text-red-500/bg-red-50 박스로 통일"
    - "unjust-enrichment에서 선의 수익자 선택 시 결과 섹션 안내 박스 dark 색상(cyan-400) → light 색상 수정"
    - "unjust-enrichment에서 악의 수익자 안내 박스 dark 색상(text-red-400) → light 색상 수정"
    - "3개 계산기 모두 에러: text-red-500, 경고: text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/debt/late-payment/page.tsx"
      provides: "지연손해금 계산기 with 원금/날짜 에러 처리, light theme 에러 박스, 필수 *, 0일 안내"
    - path: "src/app/tools/debt/loan-interest/page.tsx"
      provides: "대여금이자 계산기 with 에러/경고 state, text input 변환, 필수 *"
    - path: "src/app/tools/debt/unjust-enrichment/page.tsx"
      provides: "부당이득 계산기 with 에러/경고 state, light theme 안내 박스, 필수 *"
  key_links:
    - from: "late-payment handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "loan-interest handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "unjust-enrichment handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
late-payment, loan-interest, unjust-enrichment 3개 채권/이자 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 채권·이자 관련 계산기의 입력 검증, 결과 표시, UX 흐름 표준화 — dark 테마 색상 잔재 제거 및 에러 표시 일관성 확보 (per D-01, D-02, D-03)
Output: 3개 계산기 모두 에러/경고 표시, 필수 필드 구분, 숫자 입력 정규화, dark 테마 잔재 수정 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-ux/31-CONTEXT.md
@src/app/tools/debt/late-payment/page.tsx
@src/app/tools/debt/loan-interest/page.tsx
@src/app/tools/debt/unjust-enrichment/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: late-payment 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/debt/late-payment/page.tsx</files>
  <read_first>src/app/tools/debt/late-payment/page.tsx</read_first>
  <action>
late-payment/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03):

**현황 파악 (실제 코드 기준):**
- `error` state 이미 있음 (`const [error, setError] = useState('')`)
- `handleCalculate`: `if (!p || p <= 0 || !startDate || !endDate) return;` — 조용한 return, 에러 메시지 없음 (INPUT-02 위반)
- 날짜 역순 에러: `setError('종료일은 시작일 이후여야 합니다.')` — 이미 OK (EDGE-02 처리)
- **CONSIST-03 버그:** 에러 박스 `bg-red-500/10 border border-red-500/30`, 텍스트 `text-red-400` — dark 테마 색상
- 원금 비현실값 상한 없음 (INPUT-03)
- 필드 라벨에 * 없음 (FLOW-03)
- RESULT-01: `days=0`이면 (같은 날 시작·종료) 이자=0원 → 안내 없음
- RESULT-03: 3개 이자 항목이 각각 독립 계산 (합산 구조 아님) — OK
- EDGE-01: days=1 (최솟값) → 정상 계산
- EDGE-03: 나누기 0 없음 (365는 상수)

**`warning` state 추가:**
```tsx
const [warning, setWarning] = useState<string | null>(null);
```
기존 `error` state 타입을 `string | null`로 통일:
```tsx
const [error, setError] = useState<string | null>(null);
```

**handleCalculate 수정 — 조용한 return → 에러 메시지로 교체:**
```tsx
const handleCalculate = () => {
  setError(null);
  setWarning(null);
  const p = parseInt(principal, 10);
  // INPUT-02: 원금 필수 체크
  if (!principal || !p || p <= 0) {
    setError('원금을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-02: 시작일 필수 체크
  if (!startDate) {
    setError('지연 시작일을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-03: 원금 비현실값 경고 (999조 = 999_000_000_000_000)
  if (p > 999_000_000_000_000) {
    setWarning('원금이 999조원을 초과합니다. 입력값을 확인해주세요.');
  }
  // ... 기존 날짜 검증 유지
  const start = new Date(startDate);
  const end = new Date(endDate);
  if (start > end) {
    setError('종료일은 시작일 이후여야 합니다.');
    setResult(null);
    return;
  }
  const days = Math.floor((end.getTime() - start.getTime()) / (1000 * 60 * 60 * 24));
  setResult({ principal: p, days, ... });
};
```

**RESULT-01: days=0 안내 (결과 렌더 상단에 추가):**
```jsx
{result.days === 0 && (
  <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">시작일과 종료일이 같아 지연일수가 0일입니다. 이자가 발생하지 않습니다.</p>
  </div>
)}
```

**CONSIST-03: 에러 박스 light 색상 수정:**
기존: `className="mb-4 p-3 rounded-lg bg-red-500/10 border border-red-500/30"`
수정: `className="mb-4 p-3 rounded-lg bg-red-50 border border-red-200"`
기존 텍스트: `className="text-sm text-red-400"`
수정: `className="text-sm text-red-600"`

**에러/경고 표시:** 계산하기 버튼 바로 위에 추가(기존 에러 박스 위치 대체 또는 보완):
```jsx
{error && (
  <div className="mb-4 p-3 rounded-lg bg-red-50 border border-red-200">
    <p className="text-sm text-red-600">{error}</p>
  </div>
)}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**FLOW-03: 필수 필드 * 추가:**
- "원금 (원)" → "원금 (원) *"
- "지연 시작일" → "지연 시작일 *"
- "지연 종료일" — 기본값 오늘로 설정되어 있으므로 선택으로 봐도 되나, 계산에 필수이므로 "지연 종료일 *"

**EDGE-02 확인:** 시작일 > 종료일 에러 이미 처리됨. 미래 날짜는 허용 (소송 예상 기간 계산 가능) — OK.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "원금을 입력\|지연 시작일을 입력\|bg-red-50\|999조\|0일입니다\|text-orange-500" src/app/tools/debt/late-payment/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "원금을 입력\|시작일을 입력" late-payment/page.tsx returns match (INPUT-02 에러 메시지)
- grep "999조\|999_000_000_000_000" late-payment/page.tsx returns match (INPUT-03 경고)
- grep "bg-red-50" late-payment/page.tsx returns match (CONSIST-03 light 박스)
- grep "0일입니다\|지연일수가 0" late-payment/page.tsx returns match (RESULT-01 안내)
- grep "text-orange-500" late-payment/page.tsx returns match (경고 스타일)
- grep "\*" late-payment/page.tsx returns match (FLOW-03 필수 표시)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>late-payment 계산기가 16개 요구사항 충족. 원금/시작일 에러, 비현실값 경고, 에러 박스 light 색상, 0일 안내, 필수 * 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: loan-interest + unjust-enrichment 계산기 UX·논리 감사 및 수정</name>
  <files>
    src/app/tools/debt/loan-interest/page.tsx
    src/app/tools/debt/unjust-enrichment/page.tsx
  </files>
  <read_first>
    src/app/tools/debt/loan-interest/page.tsx
    src/app/tools/debt/unjust-enrichment/page.tsx
  </read_first>
  <action>
2개 파일을 순서대로 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

---

### loan-interest/page.tsx

**현황 파악:**
- `handleCalculate`: `if (!p || p <= 0 || isNaN(r) || r < 0 || !d || d < 1) return;` — 조용한 return, 에러 없음 (INPUT-02 위반)
- `error` state 없음 — 추가 필요
- `warning` state 없음 — 추가 필요
- 이율 필드: `type="number"` + `min="0"` + `max="200"` + `step="0.1"` — INPUT-04 위반
- 기간 필드: `type="number"` + `min="1"` — INPUT-04 위반
- 원금 비현실값 상한 없음 (INPUT-03)
- 필드 라벨에 * 없음 (FLOW-03)
- **CONSIST-03 버그:** 이율 초과 경고 박스 `bg-red-500/10 border border-red-500/30`, 텍스트 `text-red-400` — dark 색상
- FLOW-01: 초기 result=null → OK
- RESULT-03: `interest = principal × effectiveRate × days/365` — 단일 항목 (합산 구조 아님) OK
- EDGE-01: rate=20 (법정 상한) → effectiveRate=20, isExceeded=false → 정상
- EDGE-03: `effectiveRate / 100`: effectiveRate는 `Math.min(agreedRatePercent, 20)`, agreedRatePercent가 0인 경우 → interest=0, 안내 필요

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 수정:**
```tsx
const handleCalculate = () => {
  setError(null);
  setWarning(null);
  const p = parseInt(principal, 10);
  const r = parseFloat(rate);
  const d = parseInt(days, 10);

  // INPUT-02: 원금 필수
  if (!principal || !p || p <= 0) {
    setError('대여 원금을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-02: 이율 필수
  if (!rate || rate.trim() === '' || isNaN(r)) {
    setError('약정 이율을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-01: 이율 음수 차단
  if (r < 0) {
    setError('이율은 0% 이상이어야 합니다.');
    setResult(null);
    return;
  }
  // INPUT-02: 기간 필수
  if (!days || days.trim() === '' || isNaN(d) || d < 1) {
    setError('대여 기간을 1일 이상으로 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-03: 원금 비현실값 경고
  if (p > 999_000_000_000_000) {
    setWarning('원금이 999조원을 초과합니다. 입력값을 확인해주세요.');
  }

  setResult(calculateLoanInterest(p, r, d));
};
```

**INPUT-04: 이율 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="decimal"`
- `min="0"` + `max="200"` + `step="0.1"` 제거
- `onChange={e => setRate(e.target.value.replace(/[^0-9.]/g, ''))}` 로 변경

**INPUT-04: 기간 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="numeric"`
- `min="1"` 제거
- `onChange={e => setDays(e.target.value.replace(/[^0-9]/g, ''))}` 로 변경

**RESULT-01: interest=0 안내 (결과 렌더에 추가):**
rate=0이거나 days=1이어서 interest=0이면:
```jsx
{result.interest === 0 && (
  <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">이자액이 0원으로 계산되었습니다. 이율 또는 기간을 확인해주세요.</p>
  </div>
)}
```

**CONSIST-03: 이율 초과 경고 박스 light 색상 수정:**
기존: `className="mb-4 p-3 rounded-lg bg-red-500/10 border border-red-500/30"`
수정: `className="mb-4 p-3 rounded-lg bg-red-50 border border-red-200"`
기존 텍스트: `className="text-sm text-red-400 font-semibold"`
수정: `className="text-sm text-red-600 font-semibold"`

**FLOW-03: 필수 필드 * 추가:**
- "대여 원금 (원)" → "대여 원금 (원) *"
- "약정 이율 (%)" → "약정 이율 (%) *"
- "대여 기간 (일)" → "대여 기간 (일) *"

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && (
  <div className="mb-4 p-3 rounded-lg bg-red-50 border border-red-200">
    <p className="text-sm text-red-600">{error}</p>
  </div>
)}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

---

### unjust-enrichment/page.tsx

**현황 파악:**
- `error` state 이미 있음 (`const [error, setError] = useState('')`)
- `handleCalculate`: `if (!p || p <= 0 || !acquiredDate || !returnDate) return;` — 조용한 return (INPUT-02 위반)
- 날짜 역순 에러: `setError('취득일이 반환 기준일보다 늦을 수 없습니다.')` — 이미 OK (EDGE-02)
- **CONSIST-03 버그 1:** 에러 메시지 `className="mt-3 text-sm text-red-400 font-semibold"` — dark 색상이며 박스 없음
- **CONSIST-03 버그 2:** 결과 섹션 안내 박스 `style={{ backgroundColor: beneficiaryType === 'good' ? '#06b6d41a' : '#ef44441a' }}` — 인라인 스타일, 통일 불량
  - 선의 수익자 텍스트: `text-sm text-cyan-400` — dark 색상
  - 악의 수익자 텍스트: `text-sm text-red-400 font-semibold` — dark 색상
- 원금 비현실값 상한 없음 (INPUT-03)
- `warning` state 없음 — 추가 필요
- 필드 라벨에 * 없음 (FLOW-03)
- FLOW-01: 초기 result=null → OK
- RESULT-03: `total = principal + interest` — 부분합 맞음 OK
- EDGE-01: principal=1 → interest=Math.floor(1 × 0.05 × days/365) = 0 → RESULT-01 안내 불필요 (interest=0이어도 principal이 반환 합계로 표시)
- EDGE-03: 나누기 0 없음

**`warning` state 추가:**
```tsx
const [warning, setWarning] = useState<string | null>(null);
```
기존 `error` state 타입을 `string | null`로 통일.

**handleCalculate 수정 — 조용한 return → 에러 메시지로 교체:**
```tsx
const handleCalculate = () => {
  setError(null);
  setWarning(null);
  const p = parseInt(principal, 10);

  // INPUT-02: 원금 필수
  if (!principal || !p || p <= 0) {
    setError('부당이득 금액을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-02: 취득일 필수
  if (!acquiredDate) {
    setError('부당이득 취득일을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-03: 비현실값 경고
  if (p > 999_000_000_000_000) {
    setWarning('금액이 999조원을 초과합니다. 입력값을 확인해주세요.');
  }
  // ... 기존 날짜 검증 유지
  const acq = new Date(acquiredDate);
  const ret = new Date(returnDate);
  if (acq > ret) {
    setError('취득일이 반환 기준일보다 늦을 수 없습니다.');
    setResult(null);
    return;
  }
  setResult(calculateUnjustEnrichment(p, acq, ret));
};
```

**CONSIST-03: 에러 메시지를 박스 형태로 수정:**
기존: `{error && <p className="mt-3 text-sm text-red-400 font-semibold">{error}</p>}`
수정: 버튼 위(또는 버튼 아래)에 박스 형태로:
```jsx
{error && (
  <div className="mt-3 p-3 rounded-lg bg-red-50 border border-red-200">
    <p className="text-sm text-red-600">{error}</p>
  </div>
)}
{warning && <p className="text-orange-500 text-sm mt-2">{warning}</p>}
```

**CONSIST-03: 결과 섹션 안내 박스 light 색상 수정:**
기존 인라인 스타일 제거 후 Tailwind 클래스로 교체:
- 선의 수익자: `className="mb-4 p-3 rounded-lg bg-cyan-50 border border-cyan-200"`
  - 텍스트: `className="text-sm text-cyan-700"` (text-cyan-400 → text-cyan-700)
- 악의 수익자: `className="mb-4 p-3 rounded-lg bg-red-50 border border-red-200"`
  - 텍스트: `className="text-sm text-red-600 font-semibold"` (text-red-400 → text-red-600)

**FLOW-03: 필수 필드 * 추가:**
- "부당이득 금액 (원)" → "부당이득 금액 (원) *"
- "부당이득 취득일" → "부당이득 취득일 *"
- "반환 기준일" — 기본값 오늘이므로 "반환 기준일 *"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "원금을 입력\|이율을 입력\|기간을 입력\|bg-red-50\|bg-red-200\|inputMode.*decimal\|inputMode.*numeric\|999조\|이자액이 0원\|금액을 입력\|취득일을 입력\|bg-cyan-50\|cyan-700" src/app/tools/debt/loan-interest/page.tsx src/app/tools/debt/unjust-enrichment/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "원금을 입력\|이율을 입력\|기간을 입력" loan-interest/page.tsx returns match (INPUT-02 에러 메시지)
- grep "bg-red-50" loan-interest/page.tsx returns match (CONSIST-03 light 박스)
- grep "inputMode.*decimal" loan-interest/page.tsx returns match (INPUT-04 이율)
- grep "inputMode.*numeric" loan-interest/page.tsx count >= 2 (원금 + 기간 필드)
- grep "999조\|999_000_000_000_000" loan-interest/page.tsx returns match (INPUT-03 경고)
- grep "이자액이 0원" loan-interest/page.tsx returns match (RESULT-01 안내)
- grep "\*" loan-interest/page.tsx returns match (FLOW-03)
- grep "금액을 입력\|취득일을 입력" unjust-enrichment/page.tsx returns match (INPUT-02 에러 메시지)
- grep "bg-red-50\|bg-cyan-50" unjust-enrichment/page.tsx returns match (CONSIST-03 light 박스)
- grep "cyan-700\|text-red-600" unjust-enrichment/page.tsx returns match (CONSIST-03 light 색상)
- grep "\*" unjust-enrichment/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>loan-interest, unjust-enrichment 2개 계산기 16개 요구사항 충족. 에러 처리, type=text 입력 변환, dark 테마 잔재 light 색상 수정, 필수 * 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 3개 계산기 모두 에러 박스 패턴: bg-red-50 + border-red-200 + text-red-600 통일
- dark 색상 잔재(bg-red-500/10, text-red-400, text-cyan-400, 인라인 color 스타일) → Tailwind light 클래스로 수정 확인
- grep "setError" 각 파일에서 매치 확인
</verification>

<success_criteria>
- late-payment: 원금/시작일 에러, 비현실값 경고, 에러 박스 light 색상, 0일 안내, 필수 * 완료
- loan-interest: 에러 state 추가, type=text 입력 변환, dark 박스 수정, 필수 * 완료
- unjust-enrichment: 조용한 return → 에러 메시지, dark 안내 박스 → light, 필수 * 완료
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/31-ux/31-02-SUMMARY.md`
</output>
