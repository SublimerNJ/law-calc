---
phase: 31-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/traffic/accident-settlement/page.tsx
  - src/app/tools/traffic/drunk-driving/page.tsx
  - src/app/tools/traffic/fine-penalty/page.tsx
  - src/app/tools/traffic/bail/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "accident-settlement에서 치료비 미입력 시 에러 표시 후 결과 미출력"
    - "accident-settlement에서 치료비 100억 초과 시 경고 표시 (계산 허용)"
    - "accident-settlement에서 치료비·입원·통원 필드 라벨에 * 표시"
    - "accident-settlement에서 결과가 0원일 때 의미 있는 안내 표시"
    - "drunk-driving에서 BAC 미입력/빈 문자열 제출 시 에러 표시 후 결과 미출력"
    - "drunk-driving에서 BAC 0.4 초과 입력 시 에러 표시"
    - "drunk-driving에서 BAC 필드 type=number → type=text + inputMode=decimal 변경"
    - "drunk-driving에서 결과 섹션 경고 박스 dark 색상(bg-red-900/30 text-red-300) → light 색상 수정"
    - "fine-penalty에서 형사처벌 항목 선택 시 '최종 납부 금액 0원' 방지 — 안내 메시지 표시"
    - "fine-penalty에서 미납 기간 음수 입력 차단 (INPUT-01)"
    - "bail에서 재산 입력 없이 계산하기 시 에러 표시 후 결과 미출력"
    - "bail에서 재산 100억 초과 시 경고 표시"
    - "bail에서 결과 섹션 경고 박스 dark 색상(bg-yellow-900/30 text-yellow-400) → light 색상 수정"
    - "4개 계산기 모두 에러: text-red-500, 경고: text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/traffic/accident-settlement/page.tsx"
      provides: "교통사고 합의금 계산기 with error/warning state, 필수 필드 *, 0원 안내"
    - path: "src/app/tools/traffic/drunk-driving/page.tsx"
      provides: "음주운전 처벌 계산기 with BAC 에러 처리, text input, light theme 경고 박스"
    - path: "src/app/tools/traffic/fine-penalty/page.tsx"
      provides: "범칙금/과태료 계산기 with 형사처벌 항목 안내, 음수 미납기간 차단"
    - path: "src/app/tools/traffic/bail/page.tsx"
      provides: "보석금 계산기 with 재산 에러/경고, light theme 안내 박스"
  key_links:
    - from: "accident-settlement handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "drunk-driving handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "fine-penalty result render"
      to: "형사처벌 0원 안내"
      via: "result.finalAmount === 0 && result.violation.note"
      pattern: "finalAmount.*0|형사처벌"
    - from: "bail handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
accident-settlement, drunk-driving, fine-penalty, bail 4개 교통/형사 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 교통사고·형사 관련 계산기의 입력 검증, 결과 표시, UX 흐름 표준화 — 사용자가 잘못된 값 입력 시 명확한 안내 제공 (per D-01, D-02, D-03)
Output: 4개 계산기 모두 에러/경고 표시, 필수 필드 구분, 숫자 입력 정규화, dark 테마 잔재 수정 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-ux/31-CONTEXT.md
@src/app/tools/traffic/accident-settlement/page.tsx
@src/app/tools/traffic/drunk-driving/page.tsx
@src/app/tools/traffic/fine-penalty/page.tsx
@src/app/tools/traffic/bail/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: accident-settlement 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/traffic/accident-settlement/page.tsx</files>
  <read_first>src/app/tools/traffic/accident-settlement/page.tsx</read_first>
  <action>
accident-settlement/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03):

**현황 파악 (실제 코드 기준):**
- `handleCalculate`에서 입력 미검증 — `medicalCost`가 비어있어도 0으로 처리하고 결과 출력 (INPUT-02 위반)
- `error` state 없음 — 추가 필요
- `warning` state 없음 — 추가 필요
- 치료비, 입원일수, 통원일수 라벨에 * 없음 (FLOW-03)
- 치료비 비현실값 상한 없음 (INPUT-03)
- FLOW-01: 초기 상태 result=null → 결과 미출력 (이미 OK)
- RESULT-01: `finalAmount === 0`이면 (치료비 0 + 일수 0 + 장해 없음 + 과실 100%) 안내 필요
- RESULT-03: `subtotal = medical + lost + totalSolatium` → `faultDeduction = subtotal * (fault/100)` → `finalAmount = subtotal - faultDeduction` — 부분합 맞음
- EDGE-01: fault=100% → finalAmount=0 — 논리상 맞음, RESULT-01 안내로 처리
- EDGE-03: 나누기 0 없음 (100은 상수)
- 모든 숫자 입력 필드는 이미 type=text + inputMode=numeric (INPUT-04 OK)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 리팩터 (handleCalculate 내부 시작):**
1. `setError(null); setWarning(null);`
2. INPUT-02: `if (!medicalCost || !medical || medical <= 0)` → `setError('치료비를 입력해주세요.')` + `setResult(null)` + return
3. INPUT-03: `if (medical > 10_000_000_000)` → `setWarning('치료비가 100억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용, return 없음)
4. INPUT-01: `if (hospDays < 0 || outDays < 0)` — 이미 `parseInt(value, 10) || 0`이라 음수 입력해도 0 처리됨. 그러나 직접 음수 숫자 입력 방지를 위해 handleNumberInput에서 replace(/[^0-9]/g, '') 처리 — 이미 OK.

**RESULT-01: finalAmount=0 안내 (결과 렌더 섹션에 추가):**
결과 카드 상단(h2 바로 아래)에:
```jsx
{result.finalAmount === 0 && (
  <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">합의금이 0원으로 계산되었습니다. 과실비율 또는 손해항목을 확인해주세요.</p>
  </div>
)}
```

**FLOW-03: 필수 필드 * 추가:**
- "치료비 (원)" → "치료비 (원) *"
- "입원일수 (일)" → "입원일수 (일) *" (위자료 산정 필수)
- "통원일수 (일)" → "통원일수 (일) *"
- "일실수입 / 휴업손해 (원, 선택)" — 그대로 (선택)
- "후유장해 등급" — 그대로 (선택, 없음 옵션 있음)
- "피해자 과실비율" — 슬라이더, 선택

**에러/경고 표시:** 계산하기 버튼 바로 위에 추가:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**CONSIST-02:** 결과 표시 모두 `formatNumber()원` 형식 — 이미 OK.
**CONSIST-03:** 에러 text-red-500, 경고 text-orange-500 통일 (per 이번 수정).
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|치료비를 입력\|100억\|합의금이 0원" src/app/tools/traffic/accident-settlement/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" accident-settlement/page.tsx returns match (에러 state 사용)
- grep "치료비를 입력" accident-settlement/page.tsx returns match (INPUT-02 에러 메시지)
- grep "100억\|10_000_000_000" accident-settlement/page.tsx returns match (INPUT-03 경고)
- grep "합의금이 0원" accident-settlement/page.tsx returns match (RESULT-01 안내)
- grep "text-red-500" accident-settlement/page.tsx returns match (에러 스타일)
- grep "text-orange-500" accident-settlement/page.tsx returns match (경고 스타일)
- grep "\*" accident-settlement/page.tsx returns match (FLOW-03 필수 표시)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>accident-settlement 계산기가 16개 요구사항 충족. 치료비 필수 에러, 100억 경고, 0원 합의금 안내, 필수 * 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: drunk-driving + fine-penalty + bail 계산기 UX·논리 감사 및 수정</name>
  <files>
    src/app/tools/traffic/drunk-driving/page.tsx
    src/app/tools/traffic/fine-penalty/page.tsx
    src/app/tools/traffic/bail/page.tsx
  </files>
  <read_first>
    src/app/tools/traffic/drunk-driving/page.tsx
    src/app/tools/traffic/fine-penalty/page.tsx
    src/app/tools/traffic/bail/page.tsx
  </read_first>
  <action>
3개 파일을 순서대로 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

---

### drunk-driving/page.tsx

**현황 파악:**
- `handleCalculate`: `if (isNaN(bacValue) || bacValue < 0 || bacValue > 0.4) return;` — BAC 빈 문자열 시 `parseFloat('') = NaN` → 조용한 return, 에러 없음 (INPUT-02 위반)
- BAC 필드: `type="number"` — INPUT-04 위반 (문자 입력 가능, 모바일 UX 불량)
- `error` state 없음 — 추가 필요
- 결과 섹션 경고 박스: `bg-red-900/30 border border-red-800`, 텍스트 `text-red-300/text-red-200` — dark 테마 색상 (CONSIST-03 위반, 라이트 테마 프로젝트에서 가독성 불량)
- FLOW-01: 초기 result=null → 결과 미출력 (OK)
- FLOW-03: "혈중알코올농도 BAC (%)" 라벨에 * 없음
- CONSIST-01: BAC 입력 방식 통일 필요 (type=text)
- EDGE-01: bacValue=0.03 (경계값) → `level='면허정지'` — 정상
- EDGE-03: 나누기 0 없음

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
```

**handleCalculate 수정:**
```tsx
const handleCalculate = () => {
  setError(null);
  if (!bac || bac.trim() === '') {
    setError('혈중알코올농도(BAC)를 입력해주세요.');
    setResult(null);
    return;
  }
  const bacValue = parseFloat(bac);
  if (isNaN(bacValue) || bacValue < 0) {
    setError('BAC는 0 이상의 숫자로 입력해주세요.');
    setResult(null);
    return;
  }
  if (bacValue > 0.4) {
    setError('BAC는 최대 0.400%까지 입력 가능합니다.');
    setResult(null);
    return;
  }
  setResult(getPenalty(bacValue, method, hasInjury, hasPropertyDamage, priorRecord));
};
```

**INPUT-04: BAC 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="decimal"`
- `step="0.001"` + `min="0"` + `max="0.400"` 제거
- `onChange={e => setBac(e.target.value.replace(/[^0-9.]/g, ''))}` 로 변경

**FLOW-03: BAC 라벨 * 추가:**
- "혈중알코올농도 BAC (%)" → "혈중알코올농도 BAC (%) *"

**에러 표시 위치:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
```

**CONSIST-03: 결과 섹션 경고 박스 라이트 색상 수정 (2개 박스 모두):**
- 전력 가중 박스: `bg-red-900/30 border border-red-800` → `bg-red-50 border border-red-200`
- 텍스트: `text-red-300 font-semibold` → `text-red-600 font-semibold`
- 텍스트: `text-red-200` → `text-red-600`
- 사고 가중 박스: 동일하게 수정

---

### fine-penalty/page.tsx

**현황 파악:**
- 모든 입력은 select(위반항목), radio(단속방법), text(미납기간) — 위반항목은 항상 선택값 있음 (INPUT-02 N/A)
- `unpaidMonths`: `parseInt(unpaidMonths, 10) || 0` → 음수 입력 시 `parseInt('-3') = -3` → surcharge가 음수 가능 (INPUT-01 위반)
- **핵심 버그:** 음주/약물 + 보험미가입 항목은 `fine=null, penalty=null` → `baseAmount=0` → `finalAmount=0` — "최종 납부 금액 0원" 표시되지만, note에 "형사처벌 대상"이라고 설명됨. 결과 섹션에서 note 표시는 있으나 `finalAmount=0`일 때 별도 안내 없음 (RESULT-01 위반)
- FLOW-01: 초기 result=null → OK
- FLOW-03: 필드 자체가 select/radio이므로 * 불필요

**미납기간 음수 차단 (INPUT-01):**
기존 onChange: `e => setUnpaidMonths(e.target.value.replace(/[^0-9]/g, ''))`
— 이미 `[^0-9]` 패턴이 '-'를 제거하므로 음수 입력 불가. 현재 코드 확인 필수. 만약 replace 없다면 추가.

**RESULT-01: finalAmount=0 형사처벌 안내 (결과 렌더에 추가):**
`result.finalAmount === 0 && result.violation.note`인 경우, 현재 note 박스는 이미 표시됨 (`result.violation.note && ...`). 다만 "최종 납부 금액 0원" 표시가 혼란스러움 — 결과 납부금액 섹션에 추가 안내:
```jsx
{result.finalAmount === 0 && result.violation.note && (
  <div className="mb-3 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">이 위반 항목은 범칙금/과태료가 없는 형사처벌 대상입니다. 납부 금액이 없습니다.</p>
  </div>
)}
```

**CONSIST-03: violation.note 박스 dark → light 색상 수정:**
결과 섹션의 note 박스: `bg-red-900/30 border border-red-800` → `bg-red-50 border border-red-200`
텍스트: `text-sm text-red-400` → `text-sm text-red-600`

---

### bail/page.tsx

**현황 파악:**
- `handleCalculate`: `const assetVal = parseInt(assets.replace(/,/g, ''), 10) || 0;` → 재산 미입력 시 0으로 처리 (INPUT-02 — 재산 0도 의미 있는 값이므로 필수는 아니지만 사용자가 의도치 않게 빈 채 계산 가능)
- `error` state 없음
- `warning` state 없음
- 재산 비현실값 상한 없음 (INPUT-03)
- **CONSIST-03 버그:** 결과 섹션 안내 박스 `bg-yellow-900/30 border border-yellow-800`, 텍스트 `text-yellow-400` — dark 테마 색상, 라이트 테마에서 가독성 불량
- FLOW-03: "피의자/피고인 재산 (원)" 라벨에 * 없음 — 재산은 필수가 아니지만 0이면 의미 있는 결과를 위해 입력 권장 → "(선택)" 명시 추가
- FLOW-01: 초기 result=null → OK
- RESULT-02: `formatNumber()원` 형식 — 이미 OK
- EDGE-01: assets=0, crime=fraud → `assetBased=0`, `baseBail=16_500_000` → finalBail 계산 정상
- EDGE-03: 나누기 0 없음

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 수정 (재산 경고 추가):**
```tsx
const handleCalculate = () => {
  setError(null);
  setWarning(null);
  const assetVal = parseInt(assets.replace(/,/g, ''), 10) || 0;
  if (assets && assetVal > 10_000_000_000) {
    setWarning('재산이 100억원을 초과합니다. 입력값을 확인해주세요.');
  }
  setResult(calculateBail(crimeId, assetVal, priorRecord, flightRisk, evidenceRisk));
};
```

**FLOW-03: 재산 라벨 수정:**
- "피의자/피고인 재산 (원)" → "피의자/피고인 재산 (원, 선택)"
- 입력 힌트 추가 (input 아래): `<p className="text-xs text-gray-500 mt-1">재산이 없거나 불명확한 경우 비워두면 죄종 기준값으로 계산합니다.</p>`

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**CONSIST-03: 결과 섹션 안내 박스 라이트 색상 수정:**
기존: `className="bg-yellow-900/30 border border-yellow-800 rounded-lg p-3"`
수정: `className="bg-yellow-50 border border-yellow-200 rounded-lg p-3"`
기존 텍스트: `text-sm text-yellow-400 font-semibold`
수정: `text-sm text-yellow-700 font-semibold`
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|BAC.*입력\|inputMode.*decimal\|bg-red-50\|bg-yellow-50\|yellow-700\|형사처벌 대상\|bg-blue-50" src/app/tools/traffic/drunk-driving/page.tsx src/app/tools/traffic/fine-penalty/page.tsx src/app/tools/traffic/bail/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" drunk-driving/page.tsx returns match (에러 state 사용)
- grep "BAC.*입력\|입력해주세요" drunk-driving/page.tsx returns match (INPUT-02 에러 메시지)
- grep "bg-red-50\|text-red-600" drunk-driving/page.tsx returns match (CONSIST-03 light 색상)
- grep "inputMode.*decimal\|decimal.*inputMode" drunk-driving/page.tsx returns match (INPUT-04)
- grep "형사처벌 대상\|납부 금액이 없습니다" fine-penalty/page.tsx returns match (RESULT-01 안내)
- grep "bg-red-50" fine-penalty/page.tsx returns match (CONSIST-03 light 색상)
- grep "setWarning\|100억\|10_000_000_000" bail/page.tsx returns match (INPUT-03 경고)
- grep "bg-yellow-50\|yellow-700" bail/page.tsx returns match (CONSIST-03 light 색상)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>drunk-driving, fine-penalty, bail 3개 계산기 16개 요구사항 충족. BAC 에러 처리, dark 테마 잔재 수정, 형사처벌 항목 0원 안내, 재산 경고 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 4개 계산기 모두 에러/경고 패턴: text-red-500 / text-orange-500 통일
- dark 색상 잔재(bg-red-900/30, bg-yellow-900/30, text-red-300, text-yellow-400) → light 색상(bg-red-50, bg-yellow-50, text-red-600, text-yellow-700) 수정 확인
- grep "setError" 각 파일에서 매치 확인
</verification>

<success_criteria>
- accident-settlement: 치료비 필수 에러, 100억 경고, 합의금 0원 안내, 필수 * 표시 완료
- drunk-driving: BAC 에러 처리, text+inputMode=decimal 변경, 경고 박스 라이트 색상 완료
- fine-penalty: 형사처벌 항목 0원 안내, note 박스 라이트 색상, 미납기간 음수 차단 완료
- bail: 재산 경고, 안내 박스 라이트 색상 완료
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/31-ux/31-01-SUMMARY.md`
</output>
