---
phase: 24-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/court/lawsuit-cost/page.tsx
  - src/app/tools/court/payment-order/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "lawsuit-cost에서 음수/0 소가 입력 시 에러 메시지 표시되고 잘못된 결과 미출력"
    - "payment-order에서 음수/0 청구금액 입력 시 에러 메시지 표시"
    - "두 계산기 모두 필수 입력 비어있으면 계산 불가 또는 안내 표시"
    - "초기 상태에서 0원 결과 미노출"
    - "비현실 값 입력 시 경고 표시"
    - "나누기 0 등 수학적 예외에서 크래시 없음"
  artifacts:
    - path: "src/app/tools/court/lawsuit-cost/page.tsx"
      provides: "소송비용 계산기 with input validation and edge case handling"
    - path: "src/app/tools/court/payment-order/page.tsx"
      provides: "지급명령 계산기 with input validation and edge case handling"
  key_links:
    - from: "lawsuit-cost/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "payment-order/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
lawsuit-cost와 payment-order 2개 계산기에 대해 16개 요구사항(INPUT, RESULT, FLOW, EDGE) 감사 및 수정

Purpose: 소송비용(가장 큰 341줄)과 지급명령(가장 작은 145줄) 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리를 표준화
Output: 두 계산기 모두 음수/0/비현실값 처리, 초기상태 정리, 결과 포맷팅 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-ux/24-CONTEXT.md
@src/app/tools/court/lawsuit-cost/page.tsx
@src/app/tools/court/payment-order/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: lawsuit-cost 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/court/lawsuit-cost/page.tsx</files>
  <read_first>src/app/tools/court/lawsuit-cost/page.tsx</read_first>
  <action>
lawsuit-cost/page.tsx를 전체 읽고, 아래 16개 요구사항 각각에 대해 코드를 감사한 뒤 문제 발견 즉시 수정 (per D-03):

**INPUT 검증:**
- INPUT-01: 소가(금액) 입력에 음수 값 입력 시 "금액은 0보다 커야 합니다" 에러 표시. handleCalculate 시작부에 validation 추가. 0도 차단.
- INPUT-02: 필수 필드(소가) 비어있으면 계산 버튼 클릭 시 "소가를 입력해주세요" 안내. 빈 문자열/NaN 체크.
- INPUT-03: 소가 > 100,000,000,000 (1000억) 시 "입력값이 비현실적으로 큽니다. 확인해주세요." 경고 표시 (계산은 허용).
- INPUT-04: 숫자 필드에 문자 입력 방지. onChange에서 숫자+콤마만 허용하는 필터 적용. 천단위 콤마 자동 포맷팅.

**RESULT 표시:**
- RESULT-01: 계산 결과가 0원이면 "해당 조건에서는 비용이 발생하지 않습니다" 안내.
- RESULT-02: 모든 금액 결과에 toLocaleString('ko-KR') 적용하여 천단위 콤마 표시.
- RESULT-03: 부분합(인지대+송달료+변호사보수 등) 합산이 총합과 일치하는지 코드 로직 확인. 불일치 시 수정.

**FLOW:**
- FLOW-01: 초기 렌더링 시 result state가 null이면 결과 영역 숨김. 0원 결과 미노출.
- FLOW-02: 버튼 클릭 방식이면 그대로 유지 (실시간 계산 아님 per 기존 패턴).
- FLOW-03: 필수 필드에 시각적 표시 추가 (필드 라벨에 * 또는 "(필수)" 텍스트).

**EDGE:**
- EDGE-01: 소가=0, 소가=1원, 소가=최대값(Number.MAX_SAFE_INTEGER 근처)에서 정상 동작 확인.
- EDGE-02: 날짜 입력이 있다면 미래/과거 제한 확인. 없으면 skip.
- EDGE-03: 소가 기반 구간 계산에서 나누기 0 가능성 점검. 구간 경계값(예: 정확히 1000만원, 1억원)에서 계산 확인.

에러 메시지 스타일: 빨간색 텍스트, `text-red-500` Tailwind 클래스 사용 (per D-01 Critical/Warning 분류).
경고 메시지 스타일: 주황색 텍스트, `text-orange-500`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|toLocaleString\|formatNumber" src/app/tools/court/lawsuit-cost/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "0보다 커야" lawsuit-cost/page.tsx returns match (INPUT-01)
- grep "입력해주세요" lawsuit-cost/page.tsx returns match (INPUT-02)
- grep "비현실적" lawsuit-cost/page.tsx returns match (INPUT-03)
- grep "toLocaleString\|formatNumber" lawsuit-cost/page.tsx returns match (RESULT-02)
- grep "null" lawsuit-cost/page.tsx shows result null check for initial state (FLOW-01)
- No TypeScript compilation errors: npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>lawsuit-cost 계산기가 16개 요구사항 모두 충족. 음수/0 차단, 비현실값 경고, 초기상태 정리, 결과 포맷팅, 엣지케이스 처리 완료.</done>
</task>

<task type="auto">
  <name>Task 2: payment-order 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/court/payment-order/page.tsx</files>
  <read_first>src/app/tools/court/payment-order/page.tsx, src/app/tools/court/lawsuit-cost/page.tsx</read_first>
  <action>
payment-order/page.tsx를 전체 읽고, lawsuit-cost에서 적용한 동일 패턴으로 16개 요구사항 감사 및 수정 (per D-03, D-04):

**INPUT 검증 (lawsuit-cost와 동일 패턴 적용):**
- INPUT-01: 청구금액 음수/0 → "금액은 0보다 커야 합니다" 에러
- INPUT-02: 필수 필드 비어있으면 → "청구금액을 입력해주세요" 안내
- INPUT-03: 청구금액 > 100,000,000,000 → "입력값이 비현실적으로 큽니다" 경고
- INPUT-04: 숫자+콤마만 허용, 천단위 콤마 자동 포맷팅

**RESULT 표시:**
- RESULT-01: 결과 0원 시 의미 있는 안내
- RESULT-02: 모든 금액에 천단위 콤마
- RESULT-03: 부분합=총합 일치 확인

**FLOW:**
- FLOW-01: 초기상태 result=null이면 결과 영역 숨김
- FLOW-02: 버튼 클릭 방식 유지
- FLOW-03: 필수 필드 시각적 표시

**EDGE:**
- EDGE-01: 청구금액=0, =1원, =최대값 테스트
- EDGE-02: 날짜 있으면 제한 확인
- EDGE-03: 나누기 0 가능성 점검

lawsuit-cost Task 1에서 사용한 에러/경고 메시지 문구와 Tailwind 클래스를 동일하게 적용하여 일관성 확보.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|toLocaleString\|formatNumber" src/app/tools/court/payment-order/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "0보다 커야" payment-order/page.tsx returns match
- grep "입력해주세요" payment-order/page.tsx returns match
- grep "비현실적" payment-order/page.tsx returns match
- grep "toLocaleString\|formatNumber" payment-order/page.tsx returns match
- No TypeScript compilation errors for this file
  </acceptance_criteria>
  <done>payment-order 계산기가 16개 요구사항 모두 충족. lawsuit-cost와 동일한 검증 패턴 적용 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 두 계산기 모두 에러 메시지 패턴 동일 (grep으로 확인)
</verification>

<success_criteria>
- lawsuit-cost, payment-order 두 계산기 모두 음수/0/빈값/비현실값에 대한 에러/경고 처리 완료
- 초기 상태에서 0원 결과 미노출
- 모든 금액 천단위 콤마 표시
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/24-ux/24-01-SUMMARY.md`
</output>
