---
phase: 24-ux
plan: 03
type: execute
wave: 2
depends_on: [24-01, 24-02]
files_modified:
  - src/app/tools/court/attorney-fee/page.tsx
  - src/app/tools/court/lawsuit-cost/page.tsx
  - src/app/tools/court/payment-order/page.tsx
  - src/app/tools/court/civil-mediation/page.tsx
  - src/app/tools/court/family-court/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "attorney-fee에서 음수/0 입력 시 에러 메시지 표시 (기존 silent return 제거)"
    - "5개 계산기 모두 동일한 에러 메시지 문구와 스타일 사용"
    - "5개 계산기 모두 동일한 입력 포맷팅 방식 사용"
    - "5개 계산기 모두 동일한 결과 표시 형식 사용"
  artifacts:
    - path: "src/app/tools/court/attorney-fee/page.tsx"
      provides: "변호사보수 계산기 with full validation"
    - path: "src/app/tools/court/lawsuit-cost/page.tsx"
      provides: "일관성 통일 적용된 소송비용 계산기"
  key_links:
    - from: "attorney-fee handleCalculate"
      to: "error state"
      via: "setError (replacing silent return)"
      pattern: "setError"
    - from: "all 5 calculators"
      to: "error message pattern"
      via: "동일 문구: 0보다 커야, 입력해주세요, 비현실적"
      pattern: "0보다 커야|입력해주세요|비현실적"
---

<objective>
attorney-fee 계산기 감사 + 5개 계산기 전체 일관성(CONSIST) 통일 패스

Purpose: 마지막 계산기 감사 완료 후, Plan 01/02에서 수정된 4개 + attorney-fee까지 5개 전체의 에러 메시지, 입력 방식, 결과 형식 일관성 확보
Output: 5개 계산기 모두 동일한 UX 패턴으로 통일
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-ux/24-CONTEXT.md
@.planning/phases/24-ux/24-01-SUMMARY.md
@.planning/phases/24-ux/24-02-SUMMARY.md
@src/app/tools/court/attorney-fee/page.tsx
@src/app/tools/court/lawsuit-cost/page.tsx
@src/app/tools/court/payment-order/page.tsx
@src/app/tools/court/civil-mediation/page.tsx
@src/app/tools/court/family-court/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: attorney-fee 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/court/attorney-fee/page.tsx</files>
  <read_first>src/app/tools/court/attorney-fee/page.tsx</read_first>
  <action>
attorney-fee/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. 특히 CONTEXT.md에서 발견된 문제 수정:
- 기존: 에러 시 조용히 return (CONSIST-03 위반) → setError로 에러 메시지 UI 표시로 변경

**INPUT 검증:**
- INPUT-01: 소가/금액 음수/0 → "금액은 0보다 커야 합니다" (text-red-500). 기존 `if (!val || val <= 0) return;` 패턴을 `setError("금액은 0보다 커야 합니다")` + return으로 변경.
- INPUT-02: 필수 필드 비어있으면 → "소가를 입력해주세요"
- INPUT-03: 금액 > 100,000,000,000 → "입력값이 비현실적으로 큽니다" (text-orange-500)
- INPUT-04: 숫자+콤마만 허용. 기존 handleNumberChange 활용 가능하면 유지, 다른 계산기와 패턴 통일.

**RESULT/FLOW/EDGE:** Plan 01/02와 동일 패턴 적용.

error state가 없으면 useState 추가: `const [error, setError] = useState<string | null>(null);`
warning state 추가: `const [warning, setWarning] = useState<string | null>(null);`
결과 영역 위에 에러/경고 표시 JSX 추가.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500" src/app/tools/court/attorney-fee/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" attorney-fee/page.tsx returns match (에러 state 존재)
- grep "0보다 커야" attorney-fee/page.tsx returns match
- grep "입력해주세요" attorney-fee/page.tsx returns match
- grep "비현실적" attorney-fee/page.tsx returns match
- attorney-fee에 silent return 없음: grep -c "return;" 의 결과가 validation 외 silent return 제거 확인
  </acceptance_criteria>
  <done>attorney-fee 계산기 에러 표시 방식이 다른 4개 계산기와 동일. silent return 제거, setError 방식으로 통일.</done>
</task>

<task type="auto">
  <name>Task 2: 5개 계산기 일관성 통일 패스 (CONSIST-01/02/03)</name>
  <files>src/app/tools/court/lawsuit-cost/page.tsx, src/app/tools/court/payment-order/page.tsx, src/app/tools/court/civil-mediation/page.tsx, src/app/tools/court/family-court/page.tsx, src/app/tools/court/attorney-fee/page.tsx</files>
  <read_first>src/app/tools/court/lawsuit-cost/page.tsx, src/app/tools/court/payment-order/page.tsx, src/app/tools/court/civil-mediation/page.tsx, src/app/tools/court/family-court/page.tsx, src/app/tools/court/attorney-fee/page.tsx</read_first>
  <action>
5개 계산기를 모두 읽고 일관성 검사. Plan 01/02에서 개별 수정된 내용이 계산기 간 통일되지 않은 부분을 찾아 수정:

**CONSIST-01 (입력 방식 통일):**
- 금액 입력: 모든 계산기에서 동일한 onChange 핸들러 패턴 사용 (숫자만 허용 + 천단위 콤마)
- 동일 개념(소가, 청구금액, 조정금액)의 입력 필드가 동일한 type, placeholder 패턴 사용

**CONSIST-02 (결과 표시 통일):**
- 모든 금액: toLocaleString('ko-KR') + "원" 표시 형식 통일
- 결과 카드/테이블의 레이아웃 패턴 통일 (같은 Tailwind 클래스)

**CONSIST-03 (에러 메시지 통일):**
- 에러 메시지 문구 통일: "금액은 0보다 커야 합니다", "[필드명]을 입력해주세요", "입력값이 비현실적으로 큽니다"
- 에러 표시 위치 통일: 결과 영역 상단
- 에러 스타일 통일: text-red-500 (에러), text-orange-500 (경고)
- 에러 초기화 타이밍 통일: 새 계산 시작 시 setError(null)

3개 이상 계산기에서 동일 패턴 발견 시 공통 헬퍼 추출 고려 (per D-05, D-06). 단 과도한 추상화 지양.

최종적으로 5개 파일을 grep으로 비교하여 동일 패턴 사용 확인.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && for f in lawsuit-cost payment-order civil-mediation family-court attorney-fee; do echo "=== $f ==="; grep -c "0보다 커야\|입력해주세요\|비현실적\|text-red-500\|text-orange-500" src/app/tools/court/$f/page.tsx; done</automated>
  </verify>
  <acceptance_criteria>
- 5개 계산기 모두 grep "0보다 커야" 매치 (CONSIST-03)
- 5개 계산기 모두 grep "text-red-500" 매치 (CONSIST-03 스타일)
- 5개 계산기 모두 grep "toLocaleString\|formatNumber" 매치 (CONSIST-02)
- npx tsc --noEmit 전체 빌드 에러 없음
- npm run build 성공
  </acceptance_criteria>
  <done>5개 court 계산기 간 입력 방식, 결과 표시, 에러 메시지가 완전히 통일됨. CONSIST-01/02/03 충족.</done>
</task>

</tasks>

<verification>
- npm run build 성공 (전체 프로젝트 빌드)
- 5개 계산기 모두 동일한 에러/경고 패턴 사용 (grep 비교)
- 5개 계산기 모두 동일한 금액 포맷팅 사용
</verification>

<success_criteria>
- attorney-fee 계산기 silent return 제거, 에러 표시 방식 통일
- CONSIST-01/02/03: 5개 계산기 간 입력/결과/에러 패턴 완전 통일
- 전체 프로젝트 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/24-ux/24-03-SUMMARY.md`
</output>
