---
phase: 33-actioninsight
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified: [
  "src/app/tools/court/lawsuit-cost/page.tsx",
  "src/app/tools/court/payment-order/page.tsx",
  "src/app/tools/court/civil-mediation/page.tsx"
]
autonomous: true
requirements: ["UI-01"]
must_haves:
  truths:
    - "세 도구(lawsuit-cost, payment-order, civil-mediation)의 결과 영역 하단에 실전 대응 팁이 렌더링된다"
    - "ActionInsight 컴포넌트에 올바른 calculatorId와 소가/청구금액(amount)이 전달된다"
  artifacts:
    - path: "src/app/tools/court/lawsuit-cost/page.tsx"
      provides: "ActionInsight UI 렌더링"
    - path: "src/app/tools/court/payment-order/page.tsx"
      provides: "ActionInsight UI 렌더링"
    - path: "src/app/tools/court/civil-mediation/page.tsx"
      provides: "ActionInsight UI 렌더링"
  key_links:
    - from: "src/app/tools/court/*/page.tsx"
      to: "ActionInsight"
      via: "import { ActionInsight }"
---

<objective>
민사 소송 관련 3개 도구(lawsuit-cost, payment-order, civil-mediation)의 결과 화면에 ActionInsight 컴포넌트를 연동한다.

Purpose: 사용자가 계산 결과를 확인한 후 바로 실전 대응을 준비할 수 있도록 팁과 템플릿을 제공한다.
Output: 3개 페이지 컴포넌트 JSX에 ActionInsight 배치
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: lawsuit-cost UI 연동</name>
  <files>src/app/tools/court/lawsuit-cost/page.tsx</files>
  <action>
    - `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가.
    - 결과가 렌더링되는 블록(`result && ( ... )` 내부의 최하단, 보통 다시 계산하기 버튼 위 또는 합계 표 아래)에 `<ActionInsight calculatorId="lawsuit-cost" amount={Number(amount)} />` 추가. (amount는 사용자가 입력한 소가/청구금액)
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/court/lawsuit-cost/page.tsx</automated>
  </verify>
  <done>lawsuit-cost 결과 영역에 ActionInsight가 추가됨.</done>
</task>

<task type="auto">
  <name>Task 2: payment-order UI 연동</name>
  <files>src/app/tools/court/payment-order/page.tsx</files>
  <action>
    - `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가.
    - 결과 영역 하단에 `<ActionInsight calculatorId="payment-order" amount={Number(amountRaw.replace(/[^0-9]/g, ''))} />` 추가.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/court/payment-order/page.tsx</automated>
  </verify>
  <done>payment-order 결과 영역에 ActionInsight가 추가됨.</done>
</task>

<task type="auto">
  <name>Task 3: civil-mediation UI 연동</name>
  <files>src/app/tools/court/civil-mediation/page.tsx</files>
  <action>
    - `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가.
    - 결과 영역 하단에 `<ActionInsight calculatorId="civil-mediation" amount={Number(amount)} />` 추가. (amount 변수 형태에 맞게 숫자 파싱)
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/court/civil-mediation/page.tsx</automated>
  </verify>
  <done>civil-mediation 결과 영역에 ActionInsight가 추가됨.</done>
</task>

</tasks>

<verification>
- 3개 페이지가 문법 에러 없이 컴파일되어야 한다.
- `calculatorId` 값이 `action-data.ts`에 정의된 키와 정확히 일치해야 한다.
</verification>

<success_criteria>
3개 계산기 하단에 팁과 카톡 템플릿 컴포넌트가 올바르게 렌더링되고 작동한다.
</success_criteria>

<output>
After completion, create `.planning/phases/33-actioninsight/33-02-SUMMARY.md`
</output>
