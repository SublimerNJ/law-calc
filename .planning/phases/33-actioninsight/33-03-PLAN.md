---
phase: 33-actioninsight
plan: 03
type: execute
wave: 2
depends_on: ["33-01"]
files_modified: [
  "src/app/tools/court/family-court/page.tsx",
  "src/app/tools/court/attorney-fee/page.tsx"
]
autonomous: true
requirements: ["UI-01"]
must_haves:
  truths:
    - "가사소송비용 및 변호사보수 계산기의 결과 영역 하단에 실전 대응 팁이 렌더링된다"
    - "ActionInsight 컴포넌트에 올바른 calculatorId와 관련 금액이 전달된다"
  artifacts:
    - path: "src/app/tools/court/family-court/page.tsx"
      provides: "ActionInsight UI 렌더링"
    - path: "src/app/tools/court/attorney-fee/page.tsx"
      provides: "ActionInsight UI 렌더링"
---

<objective>
가사 소송 및 변호사 보수 관련 2개 도구(family-court, attorney-fee)의 결과 화면에 ActionInsight 컴포넌트를 연동한다.

Purpose: 법적 분쟁이나 변호사 선임을 고려하는 사용자에게 합리적인 대응 팁을 제공한다.
Output: 2개 페이지 컴포넌트 JSX에 ActionInsight 배치
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: family-court UI 연동</name>
  <files>src/app/tools/court/family-court/page.tsx</files>
  <action>
    - `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가.
    - 결과 영역 하단(`result && ( ... )` 내부)에 `<ActionInsight calculatorId="family-court" />` 추가.
    - 가사소송비용의 경우 금액 산정이 다양할 수 있으므로, 적절한 대표 금액 변수가 있다면 `amount` 프롭스로 넘기고 없으면 생략한다.
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/court/family-court/page.tsx</automated>
  </verify>
  <done>family-court 결과 영역에 ActionInsight가 추가됨.</done>
</task>

<task type="auto">
  <name>Task 2: attorney-fee UI 연동</name>
  <files>src/app/tools/court/attorney-fee/page.tsx</files>
  <action>
    - `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가.
    - 결과 영역 하단에 `<ActionInsight calculatorId="attorney-fee" amount={Number(amount)} />` 추가. (amount: 사용자가 입력한 소가 변수)
  </action>
  <verify>
    <automated>grep -q "ActionInsight" src/app/tools/court/attorney-fee/page.tsx</automated>
  </verify>
  <done>attorney-fee 결과 영역에 ActionInsight가 추가됨.</done>
</task>

</tasks>

<verification>
- 2개 페이지가 컴파일되고 ActionInsight가 삽입된 JSX 문법이 올바르다.
</verification>

<success_criteria>
2개 계산기 하단에 팁과 카톡 템플릿 컴포넌트가 올바르게 렌더링되고 작동한다.
</success_criteria>

<output>
After completion, create `.planning/phases/33-actioninsight/33-03-SUMMARY.md`
</output>
