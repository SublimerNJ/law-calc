---
phase: 33-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/action-data.ts"]
autonomous: true
requirements: ["ACTION-01"]
must_haves:
  truths:
    - "`action-data.ts` 파일에 소송/법원 5개 계산기용 실전 대응 팁과 템플릿 데이터가 정의된다"
    - "팁은 전문가 조언 톤으로 작성되며 당사자 간 협상 목적의 카톡 템플릿이 포함된다"
  artifacts:
    - path: "src/lib/action-data.ts"
      provides: "ActionInsight 데이터 객체"
  key_links:
    - from: "src/lib/action-data.ts"
      to: "ActionInsight 컴포넌트"
      via: "actionData exports"
---

<objective>
소송/법원 5개 도구(lawsuit-cost, payment-order, civil-mediation, family-court, attorney-fee)에 사용할 ActionInsight 팁과 템플릿 데이터를 `action-data.ts`에 추가한다.

Purpose: 이후 진행될 UI 연동(Wave 2)에서 컴포넌트가 참조할 데이터를 미리 구축(DRY)하여 컨플릭트를 방지한다.
Output: `action-data.ts` 업데이트 (5개 키 추가)
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: action-data.ts 5개 도구 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    기존 `actionData` 객체에 소송/법원 5개 도구 특화 데이터를 추가한다.
    
    대상 키 (calculatorId):
    - `lawsuit-cost`
    - `payment-order`
    - `civil-mediation`
    - `family-court`
    - `attorney-fee`
    
    데이터 작성 지침 (D-01 등 기획 기준 반영):
    - 팁은 각 3개씩, "단호하고 전문적인 '전문가 조언' 톤"을 유지한다.
    - 템플릿(`scriptTemplate`)은 "당사자 간의 원만한 협상/요구 목적 텍스트 (법적 분쟁 전 단계)"로 작성한다.
    - 템플릿 텍스트 내 금액 치환자 `{{AMOUNT}}`를 적절히 사용한다. (예: "청구 금액인 {{AMOUNT}}원을 확인해주시기 바랍니다.")
    - 다른 기존 데이터(severance-pay 등)는 건드리지 않고, 객체 안에 5개 키를 이어서 추가한다.
  </action>
  <verify>
    <automated>grep -q "lawsuit-cost" src/lib/action-data.ts && grep -q "payment-order" src/lib/action-data.ts && grep -q "attorney-fee" src/lib/action-data.ts</automated>
  </verify>
  <done>actionData 객체에 5개 소송/법원 도구 키와 데이터가 성공적으로 추가됨.</done>
</task>

</tasks>

<verification>
- `action-data.ts` 파일에 문법적 에러가 없어야 하며 export가 정상적으로 유지되어야 한다.
- `npm run build` 시 TypeScript 에러가 발생하지 않아야 한다.
</verification>

<success_criteria>
5개 계산기 식별자가 `actionData` 객체의 키로 추가되었고 데이터 형태가 인터페이스에 부합한다.
</success_criteria>

<output>
After completion, create `.planning/phases/33-actioninsight/33-01-SUMMARY.md`
</output>
