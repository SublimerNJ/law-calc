---
phase: 41-court-actioninsight
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ActionInsight.tsx
  - src/lib/action-data.ts
autonomous: true
requirements:
  - DATA-01
  - COMP-01
must_haves:
  truths:
    - "ActionInsight component has a blue theme (not amber) for UI consistency."
    - "Action data for all 5 court calculators exists and contains 3 tips and a script template."
  artifacts:
    - path: "src/components/ui/ActionInsight.tsx"
      provides: "ActionInsight UI Component"
    - path: "src/lib/action-data.ts"
      provides: "ActionInsight content data"
  key_links:
    - from: "src/components/ui/ActionInsight.tsx"
      to: "src/lib/action-data.ts"
      via: "import actionData"
---

<objective>
Update the ActionInsight UI component to follow the required blue theme and ensure that the action data for the court domain calculators is properly defined.

Purpose: To provide a consistent blue-themed UI for the actionable tips section and ensure all court calculators have data.
Output: Updated ActionInsight component and action-data.ts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-court-actioninsight/41-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ActionInsight Theme</name>
  <files>src/components/ui/ActionInsight.tsx</files>
  <action>
    Update the ActionInsight component styling to use a blue theme as specified in the context ("기존 ActionInsight 디자인(예: bg-blue-50 등 기본 파란색 계열)을 유지").
    Change all amber-related Tailwind classes to blue-related ones.
    For example:
    - `bg-amber-50` -> `bg-blue-50`
    - `border-amber-200` -> `border-blue-200`
    - `text-amber-900` -> `text-blue-900`
    - `text-amber-600` -> `text-blue-600`
    - `focus:ring-amber-500` -> `focus:ring-blue-500`
    Keep all other functionality (copying, prop passing) intact.
  </action>
  <verify>
    <automated>grep -q "bg-blue-50" src/components/ui/ActionInsight.tsx</automated>
  </verify>
  <done>ActionInsight component uses blue theme classes instead of amber.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and Add Court Action Data</name>
  <files>src/lib/action-data.ts</files>
  <action>
    Verify that the `actionData` object contains entries for the 5 court calculators:
    `attorney-fee`, `lawsuit-cost`, `payment-order`, `civil-mediation`, `family-court`.
    If any are missing or do not have 3 tips and a script template, add/update them using the domain knowledge context.
    If they already exist and conform to the structure, no changes are needed.
  </action>
  <verify>
    <automated>grep -q "attorney-fee" src/lib/action-data.ts && grep -q "lawsuit-cost" src/lib/action-data.ts</automated>
  </verify>
  <done>All 5 court calculators have their ActionInsight data properly defined in action-data.ts.</done>
</task>

</tasks>

<verification>
Ensure that `src/components/ui/ActionInsight.tsx` does not have residual `amber` classes and successfully compiles.
</verification>

<success_criteria>
ActionInsight component correctly uses the blue theme and `action-data.ts` contains the complete court data.
</success_criteria>

<output>
After completion, create `.planning/phases/41-court-actioninsight/41-01-SUMMARY.md`
</output>