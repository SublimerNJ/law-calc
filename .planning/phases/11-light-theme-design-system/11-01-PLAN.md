---
phase: 11-light-theme-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/layout/Header.tsx
  - src/components/ui/ThemeToggle.tsx
autonomous: true
requirements:
  - REVAMP-01
  - REVAMP-03
must_haves:
  truths:
    - "사용자는 다크 테마나 테마 전환 버튼 없이 항상 밝고 전문적인 라이트 테마를 경험한다"
    - "전역 CSS 변수가 라이트 테마(흰색 배경, 딥블루/황동색 포인트)로 고정된다"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Light theme CSS variables and utility classes"
    - path: "src/app/layout.tsx"
      provides: "Root layout without next-themes ThemeProvider"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS import"
      pattern: "import './globals.css'"
---

<objective>
Phase 11: Foundation & Theme Configuration
Purpose: 다크 모드를 완전히 제거하고, 전문적이고 신뢰감을 주는 라이트 테마(딥블루/황동 포인트, 깨끗한 흰색 배경)의 기반을 마련한다.
Output: 전역 CSS 변수 수정, next-themes 제거, Header 컴포넌트의 테마 토글 버튼 제거 및 라이트 테마 스타일 적용.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/app/layout.tsx
@src/components/layout/Header.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Global CSS Variables & Utility Classes</name>
  <files>src/app/globals.css</files>
  <action>
    Modify `src/app/globals.css` to completely remove dark mode overrides (`.light` selectors, `color-scheme: dark`).
    - Set `:root` variables to the new professional light theme: `color-scheme: light;`
    - `--color-background`: crisp white (`#ffffff` or `#f8f9fc`)
    - `--color-foreground`: dark text (`#1a1a2e` or slate-900)
    - `--color-surface-*`: light grays (`#f1f3f8`, `#e4e7ef`)
    - `--color-brand-primary`: deep blue (`#1e3a8a` or similar) and brass accents.
    - Remove the glassmorphism feel: Update `.glass-panel` to have a solid background (`#ffffff`), solid border (`rgba(0,0,0,0.1)`), and minimal shadow. 
    - Update `.premium-card` to a flat design with solid borders, removing dark gradients.
  </action>
  <verify>
    <automated>grep -q "color-scheme: light" src/app/globals.css && ! grep -q "\.light" src/app/globals.css</automated>
  </verify>
  <done>globals.css variables are updated to light mode colors and glassmorphism is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Remove next-themes from Root Layout</name>
  <files>src/app/layout.tsx, package.json</files>
  <action>
    - Remove `ThemeProvider` import from `next-themes` and its wrapper around the app in `src/app/layout.tsx`.
    - Ensure the body tag just has standard light mode classes (`bg-[var(--color-background)] text-[var(--color-foreground)]`).
    - Uninstall `next-themes` using `npm uninstall next-themes`.
  </action>
  <verify>
    <automated>! grep -q "next-themes" src/app/layout.tsx</automated>
  </verify>
  <done>next-themes is removed and app is forced into light theme mode.</done>
</task>

<task type="auto">
  <name>Task 3: Update Header & Remove Theme Toggle</name>
  <files>src/components/layout/Header.tsx, src/components/ui/ThemeToggle.tsx</files>
  <action>
    - Delete `src/components/ui/ThemeToggle.tsx`.
    - In `src/components/layout/Header.tsx`: Remove `ThemeToggle` import and instances.
    - Update Header styling: Change `text-white`/`text-gray-300` to dark colors (e.g. `text-slate-900`/`text-slate-600`).
    - Change `glass-panel border-white/5` to `bg-white border-b border-slate-200`.
    - Ensure mobile menu drops the dark mode background (`bg-white/5`) in favor of light mode styles.
  </action>
  <verify>
    <automated>! grep -q "ThemeToggle" src/components/layout/Header.tsx</automated>
  </verify>
  <done>ThemeToggle is removed and Header styled for light mode.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds.
- No references to `.light` class in globals.css.
- `next-themes` is uninstalled.
</verification>

<success_criteria>
- The foundational files restrict the app to a clean, professional light theme without a dark mode option.
</success_criteria>

<output>
After completion, create `.planning/phases/11-light-theme-design-system/11-01-SUMMARY.md`
</output>
