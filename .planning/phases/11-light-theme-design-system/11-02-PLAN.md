---
phase: 11-light-theme-design-system
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - src/components/ui/Card.tsx
  - src/components/ui/CalculatorLayout.tsx
  - src/components/layout/Footer.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/CategorySection.tsx
autonomous: true
requirements:
  - REVAMP-01
  - REVAMP-03
must_haves:
  truths:
    - "공통 UI 컴포넌트(카드, 레이아웃, 푸터, 메인 섹션)가 새로운 라이트 테마 기반으로 렌더링된다"
    - "AI 느낌의 글로우 효과와 어두운 배경이 제거되고 신뢰감 있는 솔리드 디자인이 적용된다"
  artifacts:
    - path: "src/components/ui/CalculatorLayout.tsx"
      provides: "Layout wrapper for all 70 calculators"
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Main landing page hero section"
  key_links:
    - from: "src/components/ui/CalculatorLayout.tsx"
      to: "src/app/tools"
      via: "Layout wrapper"
      pattern: "CalculatorLayout"
---

<objective>
Phase 11: Core Components & Sections Light Theme Update
Purpose: 앱의 주요 공통 UI 컴포넌트와 레이아웃을 전문적인 라이트 테마 디자인 시스템(플랫 디자인, 솔리드 보더)으로 마이그레이션한다.
Output: Card, CalculatorLayout, Footer, HeroSection, CategorySection의 하드코딩된 다크 테마 클래스 제거 및 라이트 테마 클래스 적용.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
@~/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/Card.tsx
@src/components/ui/CalculatorLayout.tsx
@src/components/layout/Footer.tsx
@src/components/sections/HeroSection.tsx
@src/components/sections/CategorySection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Card & Calculator Layouts</name>
  <files>src/components/ui/Card.tsx, src/components/ui/CalculatorLayout.tsx</files>
  <action>
    - `Card.tsx`: Ensure any hardcoded dark borders/colors are replaced with CSS variables or Tailwind light mode equivalents.
    - `CalculatorLayout.tsx`: Remove `tool-page-bg` (which has a dark gradient). Use a clean background like `bg-slate-50`. Change text colors from `text-white` to `text-slate-900`, `text-gray-500` to `text-slate-500`, and `text-gray-400` to `text-slate-600`. Update breadcrumb hover states. Replace dark borders in the "광고 영역" with `bg-white border border-slate-200 text-slate-500`.
  </action>
  <verify>
    <automated>! grep -q "text-white" src/components/ui/CalculatorLayout.tsx</automated>
  </verify>
  <done>CalculatorLayout and Card use light theme color palette.</done>
</task>

<task type="auto">
  <name>Task 2: Update Footer</name>
  <files>src/components/layout/Footer.tsx</files>
  <action>
    - `Footer.tsx`: Change the dark root background `bg-[#080d1a]` to a light theme color like `bg-slate-50` or `bg-slate-100`. Change `border-white/10` to `border-slate-200`. 
    - Change text colors: `text-white` -> `text-slate-900`, `text-gray-400` -> `text-slate-600`.
  </action>
  <verify>
    <automated>! grep -q "bg-\[#080d1a\]" src/components/layout/Footer.tsx</automated>
  </verify>
  <done>Footer components are clearly readable on a light background.</done>
</task>

<task type="auto">
  <name>Task 3: Update Main Page Sections</name>
  <files>src/components/sections/HeroSection.tsx, src/components/sections/CategorySection.tsx, src/app/page.tsx</files>
  <action>
    - In `HeroSection.tsx` & `CategorySection.tsx` & `page.tsx`: Strip out all `bg-white/5`, `border-white/10`, `text-white`, `text-gray-400`.
    - Replace with flat, professional light theme variants (e.g., `bg-white`, `border-slate-200`, `text-slate-900`, `text-slate-600`).
    - Remove AI glowing effects, deep shadows, and glassmorphism backdrops (`backdrop-blur`, etc.). The design should feel like a solid, trustworthy legal document/tool.
  </action>
  <verify>
    <automated>grep -q "text-slate-900" src/components/sections/HeroSection.tsx</automated>
  </verify>
  <done>Hero and Category sections reflect the new flat light design.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors.
- The specified UI components do not contain hardcoded dark mode colors like `text-white` or `bg-gray-800`.
</verification>

<success_criteria>
- All common UI components successfully render with the professional light theme.
</success_criteria>

<output>
After completion, create `.planning/phases/11-light-theme-design-system/11-02-SUMMARY.md`
</output>
