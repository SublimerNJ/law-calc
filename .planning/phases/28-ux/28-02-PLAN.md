---
phase: 28-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/tax/acquisition-tax/page.tsx
  - src/app/tools/tax/comprehensive-property-tax/page.tsx
  - src/app/tools/tax/registration-tax/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "acquisition-tax에서 취득가액 미입력/0 시 에러 표시"
    - "acquisition-tax에서 취득가액 100억 초과 시 경고 표시"
    - "acquisition-tax에서 합계 = 취득세 + 농특세 + 교육세 검증"
    - "acquisition-tax에서 초기 상태에서 0원 결과 미노출 (이미 null 패턴이나 확인)"
    - "acquisition-tax에서 필수 필드 * 표시"
    - "comprehensive-property-tax에서 공시가격 미입력/0 시 에러 표시"
    - "comprehensive-property-tax에서 공시가격 100억 초과 시 경고 표시"
    - "comprehensive-property-tax에서 비과세 판정 시 '비과세' 안내 이미 있음 — 확인"
    - "comprehensive-property-tax에서 연령 0 입력 시 고령자공제 미적용 확인"
    - "registration-tax에서 금액 미입력/0 시 에러 표시 (면허 유형 제외)"
    - "registration-tax에서 금액 100억 초과 시 경고 표시"
    - "registration-tax에서 면허 유형 선택 시 금액 입력 불필요 → 에러 없음"
    - "세 계산기 모두 에러: text-red-500, 경고: text-orange-500 스타일"
  artifacts:
    - path: "src/app/tools/tax/acquisition-tax/page.tsx"
      provides: "취득세 계산기 with price validation, error/warning state, required field markers"
    - path: "src/app/tools/tax/comprehensive-property-tax/page.tsx"
      provides: "종합부동산세 계산기 with assessed value validation, error/warning state, required field markers"
    - path: "src/app/tools/tax/registration-tax/page.tsx"
      provides: "등록세 계산기 with amount validation (non-license), error/warning state, required field markers"
  key_links:
    - from: "acquisition-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "comprehensive-property-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "registration-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering (isLicense 분기)"
      pattern: "setError|error &&"
---

<objective>
acquisition-tax, comprehensive-property-tax, registration-tax 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 취득세·종합부동산세·등록세 3개 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러 표시 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-ux/28-CONTEXT.md
@src/app/tools/tax/acquisition-tax/page.tsx
@src/app/tools/tax/comprehensive-property-tax/page.tsx
@src/app/tools/tax/registration-tax/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: acquisition-tax 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/acquisition-tax/page.tsx</files>
  <read_first>src/app/tools/tax/acquisition-tax/page.tsx</read_first>
  <action>
acquisition-tax/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03):

**현황 파악 (실제 코드 기준):**
- `handleCalculate`에서 `if (!val || val <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음 — 추가 필요
- `{result !== null && ...}` 패턴 — FLOW-01 이미 충족
- 취득가액 상한 검증 없음 (INPUT-03)
- 라벨에 * 없음 (FLOW-03)
- `total = acquisitionTax + ruralSpecialTax + localEducationTax` — RESULT-03 확인 필요

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 (handleCalculate 내):**
- INPUT-01/02: `price`가 비어있거나 0 이하면 `setError('취득가액을 입력해주세요.')` + return
- INPUT-03: `val > 10_000_000_000` (100억) 이면 `setWarning('취득가액이 100억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**RESULT-03 확인:**
- `total: acquisitionTax + ruralSpecialTax + localEducationTax` 수식 검증
- 결과 화면에 부분합 항목(취득세 + 농특세 + 교육세)이 합계와 일치하는지 렌더링 확인
- 이미 `calculateAcquisitionTax()` 함수에서 `total: acquisitionTax + ruralSpecialTax + localEducationTax` — 맞음

**RESULT-01:** 취득가액이 유효하면 세액이 항상 0 이상 (taxRate > 0) — 단, 최소세액 로직 없음. 세액이 1원 이상임을 확인. 문제 없음.

**FLOW-01:** `{result !== null && ...}` — 이미 충족, 확인만.

**FLOW-03: 필수 필드 * 추가:**
- "취득가액 (원)" → "취득가액 (원) *"

**에러/경고 표시 위치:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**에러 초기화:** handleCalculate 시작 시 `setError(null); setWarning(null);` 호출.
성공 시 `setError(null)` 호출. 에러 발생 시 `setResult(null)` 호출.

**EDGE-01:** price=600_000_000 (경계: 1%), price=600_000_001 (보간 시작), price=900_000_000 (3%) — `calculateAcquisitionTax()` 함수 내 보간 로직 확인. 이미 올바르게 처리됨.
**EDGE-03:** `acquisitionTax = Math.floor(price * taxRate)` — price > 0, taxRate > 0 보장 시 divide by zero 없음. 확인.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|취득가액을 입력\|100억\|\*" src/app/tools/tax/acquisition-tax/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" acquisition-tax/page.tsx returns match (에러 state 추가)
- grep "취득가액을 입력\|입력해주세요" acquisition-tax/page.tsx returns match (INPUT-02)
- grep "100억\|10_000_000_000\|10000000000" acquisition-tax/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" acquisition-tax/page.tsx returns match (에러 스타일)
- grep "text-orange-500" acquisition-tax/page.tsx returns match (경고 스타일)
- grep "\*" acquisition-tax/page.tsx returns match (FLOW-03 필수 필드)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>acquisition-tax 계산기가 16개 요구사항 모두 충족. 취득가액 필수 에러, 비현실값 경고, 필수 필드 * 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: comprehensive-property-tax + registration-tax 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/comprehensive-property-tax/page.tsx, src/app/tools/tax/registration-tax/page.tsx</files>
  <read_first>src/app/tools/tax/comprehensive-property-tax/page.tsx, src/app/tools/tax/registration-tax/page.tsx, src/app/tools/tax/acquisition-tax/page.tsx</read_first>
  <action>
comprehensive-property-tax/page.tsx와 registration-tax/page.tsx를 각각 전체 읽고 감사 및 수정. acquisition-tax에서 확립한 에러/경고 패턴 동일 적용 (per D-03, D-04, D-05):

---

## comprehensive-property-tax 감사 및 수정

**현황 파악 (실제 코드 기준):**
- `handleCalculate`에서 `if (!val || val <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음
- `{result !== null && ...}` 패턴 — FLOW-01 이미 충족
- 비과세(exempt: true) 시 "비과세" 안내 — RESULT-01 이미 충족
- `age` 필드가 이미 `type="text" inputMode="numeric"` — INPUT-04 충족
- 공시가격 상한 검증 없음
- 라벨에 * 없음 (FLOW-03)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 (handleCalculate 내):**
- INPUT-01/02: `val <= 0`이면 `setError('주택 공시가격을 입력해주세요.')` + return
- INPUT-03: `val > 10_000_000_000` (100억) 이면 `setWarning('공시가격이 100억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**연령 검증 (EDGE-01):**
- `singleHousehold && age` 입력 시: `ageVal > 120`이면 `setWarning('연령이 120세를 초과합니다. 확인해주세요.')` (계산 허용)

**RESULT-03 확인:**
- `total: finalTax + ruralSpecialTax`
- `finalTax = tax - seniorDeduction`
- `ruralSpecialTax = finalTax * 0.2`
- 부분합 검증 완료

**FLOW-03: 필수 필드 * 추가:**
- "주택 공시가격 합산 (원)" → "주택 공시가격 합산 (원) *"

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**에러 초기화:** handleCalculate 시작 시 `setError(null); setWarning(null);` 호출.

---

## registration-tax 감사 및 수정

**현황 파악 (실제 코드 기준):**
- 비면허 유형에서 `if (!raw || raw <= 0) return;` — 조용한 return, 에러 없음
- 면허 유형(isLicense)에서는 금액 입력 불필요 — 즉시 계산 가능, 에러 처리 불필요
- `error` state 없음
- `{result && ...}` 패턴 — FLOW-01 이미 충족
- 금액 상한 검증 없음
- 라벨에 * 없음 (FLOW-03)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 (handleCalculate 내, isLicense가 false일 때만):**
- INPUT-01/02: `!isLicense && (!raw || raw <= 0)`이면 `setError('금액을 입력해주세요.')` + return
- INPUT-03: `!isLicense && raw > 10_000_000_000` (100억) 이면 `setWarning('입력 금액이 100억원을 초과합니다. 확인해주세요.')` (계산 허용)

**RESULT-03 확인:**
- `total = registrationTax + educationTax`
- `educationTax = registrationTax * 0.2`
- 수식 검증 완료

**FLOW-03: 필수 필드 * 추가 (비면허 유형에만):**
- 비면허 유형일 때 표시되는 금액 라벨 `{currentType.amountLabel}` 뒤에 `*` 추가:
  - `{currentType.amountLabel} *` (단, amountLabel이 빈 문자열인 license 유형 제외)
  - 조건부 렌더링: `{!isLicense && <label>...{currentType.amountLabel} *...</label>}`

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**에러 초기화:** handleCalculate 시작 시 `setError(null); setWarning(null);` 호출. 등록 유형 변경 시 `setResult(null)`은 이미 있음 — 에러도 초기화: `onChange={e => { setRegType(...); setResult(null); setError(null); setWarning(null); }}`
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500" src/app/tools/tax/comprehensive-property-tax/page.tsx src/app/tools/tax/registration-tax/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" comprehensive-property-tax/page.tsx returns match
- grep "주택 공시가격을 입력\|입력해주세요" comprehensive-property-tax/page.tsx returns match (INPUT-02)
- grep "100억\|10_000_000_000\|10000000000" comprehensive-property-tax/page.tsx returns match (INPUT-03)
- grep "text-red-500" comprehensive-property-tax/page.tsx returns match
- grep "text-orange-500" comprehensive-property-tax/page.tsx returns match
- grep "\*" comprehensive-property-tax/page.tsx returns match (FLOW-03)
- grep "setError" registration-tax/page.tsx returns match
- grep "금액을 입력\|입력해주세요" registration-tax/page.tsx returns match (INPUT-02)
- grep "text-red-500" registration-tax/page.tsx returns match
- grep "text-orange-500" registration-tax/page.tsx returns match
- npx tsc --noEmit passes for both files
  </acceptance_criteria>
  <done>comprehensive-property-tax와 registration-tax 계산기가 각각 16개 요구사항 모두 충족. 필수 입력 에러, 비현실값 경고, 필수 필드 * 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 세 계산기(acquisition-tax, comprehensive-property-tax, registration-tax) 모두 에러 패턴 동일 (text-red-500 / text-orange-500)
- grep "setError" 각 파일에서 매치 확인
</verification>

<success_criteria>
- acquisition-tax, comprehensive-property-tax, registration-tax 세 계산기 모두 음수/0/빈값/비현실값 에러·경고 처리 완료
- 초기 상태에서 0원 결과 미노출
- 필수 필드 * 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/28-ux/28-02-SUMMARY.md`
</output>
