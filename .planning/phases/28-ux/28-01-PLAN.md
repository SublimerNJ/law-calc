---
phase: 28-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/tax/capital-gains-tax/page.tsx
  - src/app/tools/tax/comprehensive-income-tax/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "capital-gains-tax에서 취득일 > 양도일이면 에러 표시, 결과 미출력"
    - "capital-gains-tax에서 취득가액/양도가액 미입력 시 에러 표시"
    - "capital-gains-tax에서 양도차익이 0 이하일 때 '양도차익이 없습니다' 안내"
    - "capital-gains-tax에서 양도가액 또는 취득가액이 100억 초과 시 경고 표시"
    - "capital-gains-tax에서 초기 상태에서 0원 결과 미노출"
    - "capital-gains-tax에서 필수 필드에 * 표시"
    - "comprehensive-income-tax에서 종합소득금액 미입력/0 시 에러 표시"
    - "comprehensive-income-tax에서 공제액 합계가 소득을 초과할 때 과세표준 0원 + 안내"
    - "comprehensive-income-tax에서 소득이 10억 초과 시 경고 표시"
    - "comprehensive-income-tax에서 공제액 합계 > 총 납부세액 합산 RESULT-03 검증"
    - "두 계산기 모두 에러: text-red-500, 경고: text-orange-500 스타일"
  artifacts:
    - path: "src/app/tools/tax/capital-gains-tax/page.tsx"
      provides: "양도소득세 계산기 with date validation, price validation, error/warning state"
    - path: "src/app/tools/tax/comprehensive-income-tax/page.tsx"
      provides: "종합소득세 계산기 with income validation, deduction overflow notice, error/warning state"
  key_links:
    - from: "capital-gains-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "comprehensive-income-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
capital-gains-tax, comprehensive-income-tax 2개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 양도소득세·종합소득세 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 2개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러 표시 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-ux/28-CONTEXT.md
@src/app/tools/tax/capital-gains-tax/page.tsx
@src/app/tools/tax/comprehensive-income-tax/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: capital-gains-tax 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/capital-gains-tax/page.tsx</files>
  <read_first>src/app/tools/tax/capital-gains-tax/page.tsx</read_first>
  <action>
capital-gains-tax/page.tsx를 전체 읽고 16개 요구사항 각각에 대해 감사 후 즉시 수정 (per D-03):

**현황 파악 (실제 코드 기준):**
- `handleCalculate`에서 `if (!acq || !trn || !acquisitionDate || !transferDate) return;` — 조용한 return, 에러 없음
- `error` state 없음 — 추가 필요
- `residenceYears` 필드가 `type="number"` — INPUT-04 위반
- 양도일 < 취득일 시 holdingYears가 음수가 되어 잘못된 세율 적용 — 검증 필요
- 양도차익이 0 이하(손실)일 때 결과가 totalTax=0으로 표시되나 사용자에게 설명 없음
- 모든 필드 라벨에 * 없음

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 (handleCalculate 내):**
- INPUT-01/02: 취득가액이 비어있거나 0이면 `setError('취득가액을 입력해주세요.')` + return
- INPUT-01/02: 양도가액이 비어있거나 0이면 `setError('양도가액을 입력해주세요.')` + return
- INPUT-02: acquisitionDate 또는 transferDate가 비어있으면 `setError('취득일과 양도일을 모두 입력해주세요.')` + return
- INPUT-03: `if (acq > 10_000_000_000 || trn > 10_000_000_000)` → `setWarning('입력 금액이 100억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용, return하지 않음)

**날짜 검증 (EDGE-02):**
- `const acqMs = new Date(acquisitionDate).getTime(); const trnMs = new Date(transferDate).getTime();`
- `if (trnMs <= acqMs)` → `setError('양도일이 취득일보다 앞서거나 같을 수 없습니다.')` + `setResult(null)` + return

**양도차익 0/음수 처리 (RESULT-01):**
- `calculate()` 호출 후: `if (calculated.gain <= 0) { setResult(calculated); }` 는 허용하되
- 결과 렌더 시 `result.gain <= 0`이면 결과 영역 상단에 info 박스 추가:
  ```jsx
  {result.gain <= 0 && (
    <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
      <p className="text-sm text-blue-600">양도차익이 없거나 손실이 발생한 경우 양도소득세가 부과되지 않습니다.</p>
    </div>
  )}
  ```

**INPUT-04: residenceYears 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="numeric"`
- `onChange={e => setResidenceYears(e.target.value.replace(/[^0-9]/g, ''))}`
- `value={residenceYears}` 유지

**FLOW-01:** `result` state null이면 결과 영역 숨김 — 이미 `{result && ...}` 패턴 있음, 확인만.

**FLOW-03: 필수 필드 * 추가:**
- "취득가액 (원)" → "취득가액 (원) *"
- "양도가액 (원)" → "양도가액 (원) *"
- "취득일" → "취득일 *"
- "양도일" → "양도일 *"

**에러/경고 표시 위치:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**에러 초기화:** handleCalculate 시작 시 `setError(null); setWarning(null);` 호출.
성공 시 `setError(null)` 호출. 에러 발생 시 `setResult(null)` 호출.

**RESULT-03 확인:** `totalTax = computedTax + localTax`. `localTax = computedTax * 0.1`. 수식 맞음, 확인만.

**EDGE-01:** holdingYears=0 (단기), holdingYears=1 (60%), holdingYears=2 (기본세율) 경계값 — `calculate()` 함수 내 분기 확인. 날짜 역전 가드가 위에서 처리되므로 holdingYears >= 0 보장됨.

**EDGE-03:** `taxBase = Math.max(taxableGain - longTermDeduction - basicDeduction, 0)` — 음수 방지 이미 있음. 확인.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|양도일이 취득일\|양도가액을 입력\|취득가액을 입력" src/app/tools/tax/capital-gains-tax/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" capital-gains-tax/page.tsx returns match (에러 state 추가)
- grep "양도일이 취득일\|앞서거나" capital-gains-tax/page.tsx returns match (EDGE-02 날짜 역전 방지)
- grep "취득가액을 입력\|양도가액을 입력" capital-gains-tax/page.tsx returns match (INPUT-02)
- grep "100억\|10_000_000_000\|10000000000" capital-gains-tax/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" capital-gains-tax/page.tsx returns match (에러 스타일)
- grep "text-orange-500" capital-gains-tax/page.tsx returns match (경고 스타일)
- grep "양도차익이 없거나\|\\.gain <= 0" capital-gains-tax/page.tsx returns match (RESULT-01)
- grep "\*" capital-gains-tax/page.tsx returns match (FLOW-03 필수 필드)
- npx tsc --noEmit passes for this file (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>capital-gains-tax 계산기가 16개 요구사항 모두 충족. 날짜 역전 방지, 필수 입력 에러, 비현실값 경고, 양도차익 0 안내, 필수 필드 * 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: comprehensive-income-tax 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/comprehensive-income-tax/page.tsx</files>
  <read_first>src/app/tools/tax/comprehensive-income-tax/page.tsx, src/app/tools/tax/capital-gains-tax/page.tsx</read_first>
  <action>
comprehensive-income-tax/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. capital-gains-tax에서 확립한 에러/경고 패턴과 동일하게 적용 (per D-03, D-04):

**현황 파악 (실제 코드 기준):**
- `if (!income || income <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음
- `dependents` 필드가 `type="number"` — INPUT-04 위반
- 공제액 합계가 소득 초과 시 taxBase=0 처리는 있으나 사용자에게 안내 없음
- 소득금액 비현실값 상한 검증 없음
- 모든 필드 라벨에 * 없음

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 (handleCalculate 내):**
- INPUT-01/02: `income`이 0 이하이면 `setError('종합소득금액을 입력해주세요.')` + return
- INPUT-03: `income > 1_000_000_000` (10억) 이면 `setWarning('종합소득금액이 10억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)
- INPUT-03: `pensionPaid > income || healthPaid > income` 이면 `setWarning('공제액이 소득금액을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**INPUT-04: dependents 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="numeric"`
- `onChange={e => setDependents(e.target.value.replace(/[^0-9]/g, ''))}`
- 0 입력 시 1로 처리하는 `parseInt(dependents) || 1` 로직 유지

**RESULT-01: 공제 초과 시 taxBase=0 안내:**
- 결과 렌더 시 `result.taxBase === 0 && result.totalDeduction >= result.totalIncome`이면:
  ```jsx
  <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">소득공제 합계가 종합소득금액과 같거나 커서 과세표준이 0원입니다.</p>
  </div>
  ```

**RESULT-03 확인:**
- `totalTax = determinedTax + localTax`
- `determinedTax = computedTax - standardCredit` (최소 0)
- `localTax = determinedTax * 0.1`
- 수식 검증: rows 배열에서 소득공제 합계 하위 항목(기본공제 + 연금공제 + 건보공제) = totalDeduction 확인. `calculate()` 함수 내 `totalDeduction = basicDeduction + pensionDeduction + healthDeduction` — 맞음.

**FLOW-01:** `{result && ...}` 패턴 — 이미 있음. 확인.

**FLOW-03: 필수 필드 * 추가:**
- "종합소득금액 (원)" → "종합소득금액 (원) *"
- "인적공제 대상 인원수 (본인 포함)" → "인적공제 대상 인원수 (본인 포함) *"

**EDGE-01:** taxBase=0 (공제 초과) → determinedTax=0, totalTax=0 — `Math.max(..., 0)` 이미 있음.
**EDGE-03:** `taxBase = Math.max(totalIncome - totalDeduction, 0)` — 음수 방지 있음. calcTax(0)=0 — 정상.

**에러/경고 표시 위치:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**에러 초기화:** handleCalculate 시작 시 `setError(null); setWarning(null);` 호출.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|종합소득금액을 입력\|10억\|과세표준이 0원" src/app/tools/tax/comprehensive-income-tax/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" comprehensive-income-tax/page.tsx returns match (에러 state 추가)
- grep "종합소득금액을 입력\|입력해주세요" comprehensive-income-tax/page.tsx returns match (INPUT-02)
- grep "10억\|1_000_000_000\|1000000000" comprehensive-income-tax/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" comprehensive-income-tax/page.tsx returns match (에러 스타일)
- grep "text-orange-500" comprehensive-income-tax/page.tsx returns match (경고 스타일)
- grep "과세표준이 0원\|taxBase === 0" comprehensive-income-tax/page.tsx returns match (RESULT-01)
- grep "\*" comprehensive-income-tax/page.tsx returns match (FLOW-03 필수 필드)
- npx tsc --noEmit passes for this file (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>comprehensive-income-tax 계산기가 16개 요구사항 모두 충족. 소득 미입력 에러, 비현실값 경고, 공제 초과 안내, 필수 필드 * 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 두 계산기 모두 에러 메시지 패턴 동일 (text-red-500 / text-orange-500)
- grep "setError" 각 파일에서 매치 확인
- grep "\*" 각 파일 라벨에서 매치 확인 (FLOW-03)
</verification>

<success_criteria>
- capital-gains-tax, comprehensive-income-tax 두 계산기 모두 음수/0/빈값/비현실값 에러·경고 처리 완료
- 초기 상태에서 0원 결과 미노출
- 필수 필드 * 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/28-ux/28-01-SUMMARY.md`
</output>
