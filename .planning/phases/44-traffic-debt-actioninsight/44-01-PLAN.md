---
phase: 44-traffic-debt-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/traffic/accident-settlement/page.tsx
  - src/app/tools/traffic/drunk-driving/page.tsx
  - src/app/tools/traffic/fine-penalty/page.tsx
  - src/app/tools/traffic/bail/page.tsx
  - src/app/tools/debt/late-payment/page.tsx
  - src/app/tools/debt/loan-interest/page.tsx
  - src/app/tools/debt/unjust-enrichment/page.tsx
autonomous: true
requirements: [DATA-06, COMP-06, DATA-07, COMP-07]
must_haves:
  truths:
    - "User only sees the ActionInsight tip and template after performing a calculation (result !== null)"
    - "User sees formatted amount inside the template for calculators where the amount is computable (fine-penalty, debt 3종)"
    - "User sees the {{AMOUNT}} placeholder for calculators where the amount should be filled manually by the user (bail, drunk-driving, accident-settlement)"
    - "ActionInsight section is consistently placed at the very bottom of the page (after the static guide blocks)"
  artifacts:
    - path: "src/app/tools/traffic/fine-penalty/page.tsx"
      provides: "Conditional ActionInsight with amount prop"
    - path: "src/app/tools/debt/late-payment/page.tsx"
      provides: "ActionInsight at the bottom"
  key_links:
    - from: "result state"
      to: "ActionInsight amount prop"
      via: "JSX conditional rendering"
---

<objective>
교통/형사 4종, 채권/이자 3종 계산기에 ActionInsight를 올바르게 연동하고 위치를 통일합니다.

Purpose: 사용자가 계산 결과를 확인한 후 실무 대응 팁과 템플릿을 볼 수 있도록 하며, 계산기 성격에 따라 템플릿 내의 금액을 자동 치환하거나 사용자가 직접 입력할 수 있도록 보존합니다.
Output: 일관된 위치(결과 박스 및 가이드 하단)에 조건부로 렌더링되는 ActionInsight 컴포넌트가 적용된 7개의 page.tsx 파일
</objective>

<context>
@src/components/ui/ActionInsight.tsx
@src/lib/action-data.ts
@src/app/tools/traffic/fine-penalty/page.tsx
@src/app/tools/debt/late-payment/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 교통/형사 4종 ActionInsight 조건부 렌더링 및 amount 적용</name>
  <files>
    - src/app/tools/traffic/accident-settlement/page.tsx
    - src/app/tools/traffic/drunk-driving/page.tsx
    - src/app/tools/traffic/fine-penalty/page.tsx
    - src/app/tools/traffic/bail/page.tsx
  </files>
  <action>
    현재 무조건적으로 하단에 렌더링되고 있는 `<ActionInsight calculatorId="..." />` 컴포넌트를 계산 후(`result !== null`)에만 보이도록 수정합니다.
    
    1. `<ActionInsight>` 컴포넌트를 `{result !== null && <div className="mt-6"><ActionInsight ... /></div>}` 로 감싸주세요.
    2. 모든 파일에서 이 블록이 `</CalculatorLayout>` 바로 앞(가이드 블록 하단)에 위치하도록 합니다.
    3. `fine-penalty/page.tsx` 에서는 `amount={result.finalAmount}` 프롭을 추가로 전달하여 과태료 금액이 템플릿에 자동 치환되도록 합니다.
    4. `accident-settlement`, `drunk-driving`, `bail` 에서는 `amount` 프롭을 전달하지 않습니다. (이 계산기들은 결과값이 템플릿의 맥락과 다르거나, 사용자가 직접 금액을 기입해야 하므로 템플릿의 `{{AMOUNT}}` 문자열을 그대로 유지해야 합니다.)
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>교통 4종 계산기가 계산 후에만 ActionInsight를 표시하며, fine-penalty만 amount 프롭을 전달함</done>
</task>

<task type="auto">
  <name>Task 2: 채권/이자 3종 ActionInsight 위치 하단 통일</name>
  <files>
    - src/app/tools/debt/late-payment/page.tsx
    - src/app/tools/debt/loan-interest/page.tsx
    - src/app/tools/debt/unjust-enrichment/page.tsx
  </files>
  <action>
    현재 채권 3종 계산기들은 ActionInsight를 조건부로 렌더링하고 amount 프롭도 올바르게 전달하고 있으나, 그 위치가 "가이드 블록(예: 지연손해금 청구 방법)" 이전(위)에 있습니다.
    
    1. `{result !== null && <div className="mt-6"><ActionInsight ... /></div>}` 전체 블록을 잘라내어 정적인 가이드 블록(`<div className="premium-card p-6 mt-4">...</div>`) 아래, 즉 `</CalculatorLayout>` 바로 앞으로 이동시킵니다.
    2. `amount` 프롭 전달 로직은 이미 올바르게 작성되어 있으므로 기존 코드를 그대로 유지합니다.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>채권 3종 계산기의 ActionInsight 블록이 가이드 블록 하단으로 이동되어 전체 페이지의 가장 아래에 위치함</done>
</task>

</tasks>

<verification>
- 7개 파일 모두 `<ActionInsight>`가 `{result !== null}` 조건 안에 감싸져 있는지 확인
- 7개 파일 모두 `<ActionInsight>`가 페이지 최하단(가이드 블록 아래)에 위치하는지 확인
- `amount` 프롭이 의도한 대로 전달/생략되었는지 확인
</verification>

<success_criteria>
- 교통 4종, 채권 3종 계산기에서 ActionInsight UI가 올바른 위치에, 올바른 데이터 상태로 노출됩니다.
- Typescript 및 Lint 에러가 발생하지 않습니다.
</success_criteria>

<output>
After completion, create `.planning/phases/44-traffic-debt-actioninsight/44-01-SUMMARY.md`
</output>
