---
phase: 34-actioninsight
plan: 03
type: execute
wave: 2
depends_on: ["34-01"]
files_modified: [
  "src/app/tools/family/inheritance-tax/page.tsx",
  "src/app/tools/family/forced-heirship/page.tsx",
  "src/app/tools/family/inheritance-order/page.tsx"
]
autonomous: true
requirements: ["UI-02"]
must_haves:
  truths:
    - "상속 관련 계산기 페이지 하단에 ActionInsight 컴포넌트가 렌더링된다"
  artifacts:
    - path: "src/app/tools/family/inheritance-tax/page.tsx"
      provides: "ActionInsight integration"
      contains: "<ActionInsight"
    - path: "src/app/tools/family/forced-heirship/page.tsx"
      provides: "ActionInsight integration"
      contains: "<ActionInsight"
    - path: "src/app/tools/family/inheritance-order/page.tsx"
      provides: "ActionInsight integration"
      contains: "<ActionInsight"
  key_links:
    - from: "src/app/tools/family/inheritance-tax/page.tsx"
      to: "src/components/ui/ActionInsight.tsx"
      via: "import and render"
      pattern: "ActionInsight"
---

<objective>
상속 관련 가족법 계산기 3종(inheritance-tax, forced-heirship, inheritance-order) 하단에 ActionInsight 컴포넌트를 연동한다.

Purpose: 사용자가 상속세, 유류분, 상속순위 계산 후 해당 결과를 바탕으로 실질적인 조치를 취할 수 있도록 가이드를 제공
Output: 3개 계산기의 페이지 컴포넌트 파일에 ActionInsight 컴포넌트 통합 완료
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/ActionInsight.tsx
@src/lib/action-data.ts
@src/app/tools/family/inheritance-tax/page.tsx
@src/app/tools/family/forced-heirship/page.tsx
@src/app/tools/family/inheritance-order/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: inheritance-tax, forced-heirship, inheritance-order 페이지에 ActionInsight 연동</name>
  <files>
    src/app/tools/family/inheritance-tax/page.tsx,
    src/app/tools/family/forced-heirship/page.tsx,
    src/app/tools/family/inheritance-order/page.tsx
  </files>
  <action>
    각 페이지(page.tsx) 하단 결과 섹션 아래에 `ActionInsight` 컴포넌트를 추가한다.
    `src/components/ui/ActionInsight.tsx`와 `src/lib/action-data.ts`를 import하여 연동한다.
    
    예시:
    ```tsx
    import ActionInsight from '@/components/ui/ActionInsight';
    import { actionData } from '@/lib/action-data';
    
    // 렌더링 부 하단
    <ActionInsight 
      data={actionData['inheritance-tax']} 
      calculatedAmount={resultAmount} // 각 계산기의 최종 결과 금액 변수 매핑
    />
    ```
    
    각 계산기마다 산출된 주요 금액(calculatedAmount) 변수를 찾아 매핑한다. 금액 변수가 없다면 (예: inheritance-order 등) prop을 적절히 생략하거나 0으로 넘긴다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>3개 파일에 ActionInsight 컴포넌트가 올바르게 import되고 렌더링 로직이 추가되었다.</done>
</task>

</tasks>

<verification>
- 각 계산기의 구조에 맞게 `calculatedAmount` prop이 올바르게 전달되었는지 확인
</verification>

<success_criteria>
- TypeScript 빌드가 성공적으로 통과한다.
- 3개 계산기 페이지 코드에 `<ActionInsight>` 컴포넌트 렌더링이 포함되어 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/34-actioninsight/34-03-SUMMARY.md`
</output>