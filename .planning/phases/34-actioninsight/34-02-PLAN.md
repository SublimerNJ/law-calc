---
phase: 34-actioninsight
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified: [
  "src/app/tools/family/alimony/page.tsx",
  "src/app/tools/family/child-support/page.tsx",
  "src/app/tools/family/property-division/page.tsx"
]
autonomous: true
requirements: ["UI-02"]
must_haves:
  truths:
    - "위자료, 양육비, 재산분할 계산기 페이지 하단에 ActionInsight 컴포넌트가 렌더링된다"
  artifacts:
    - path: "src/app/tools/family/alimony/page.tsx"
      provides: "ActionInsight integration"
      contains: "<ActionInsight"
    - path: "src/app/tools/family/child-support/page.tsx"
      provides: "ActionInsight integration"
      contains: "<ActionInsight"
    - path: "src/app/tools/family/property-division/page.tsx"
      provides: "ActionInsight integration"
      contains: "<ActionInsight"
  key_links:
    - from: "src/app/tools/family/alimony/page.tsx"
      to: "src/components/ui/ActionInsight.tsx"
      via: "import and render"
      pattern: "ActionInsight"
---

<objective>
이혼 관련 가사법 계산기 3종(alimony, child-support, property-division) 하단에 ActionInsight 컴포넌트를 연동한다.

Purpose: 사용자가 위자료, 양육비, 재산분할 계산 후 해당 결과를 바탕으로 실질적인 조치를 취할 수 있도록 가이드를 제공
Output: 3개 계산기의 페이지 컴포넌트 파일에 ActionInsight 컴포넌트 통합 완료
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/ui/ActionInsight.tsx
@src/lib/action-data.ts
@src/app/tools/family/alimony/page.tsx
@src/app/tools/family/child-support/page.tsx
@src/app/tools/family/property-division/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: alimony, child-support, property-division 페이지에 ActionInsight 연동</name>
  <files>
    src/app/tools/family/alimony/page.tsx,
    src/app/tools/family/child-support/page.tsx,
    src/app/tools/family/property-division/page.tsx
  </files>
  <action>
    각 페이지(page.tsx) 하단 결과 섹션 아래에 `ActionInsight` 컴포넌트를 추가한다.
    `src/components/ui/ActionInsight.tsx`와 `src/lib/action-data.ts`를 import하여 연동한다.
    
    예시:
    ```tsx
    import ActionInsight from '@/components/ui/ActionInsight';
    import { actionData } from '@/lib/action-data';
    
    // 렌더링 부 하단
    <ActionInsight 
      data={actionData['alimony']} 
      calculatedAmount={resultAmount} // 각 계산기의 최종 결과 금액 변수 매핑
    />
    ```
    
    각 계산기마다 산출된 주요 금액(calculatedAmount) 변수를 정확히 찾아 매핑한다. 금액 변수가 여러 개인 경우, 가장 대표적인 총액을 사용한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>3개 파일에 ActionInsight 컴포넌트가 올바르게 import되고 렌더링 로직이 추가되었다.</done>
</task>

</tasks>

<verification>
- 각 계산기의 주요 금액 변수가 `calculatedAmount` prop으로 올바르게 전달되었는지 확인
</verification>

<success_criteria>
- TypeScript 빌드가 성공적으로 통과한다.
- 3개 계산기 페이지 코드에 `<ActionInsight>` 컴포넌트 렌더링이 포함되어 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/34-actioninsight/34-02-SUMMARY.md`
</output>