---
phase: 34-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/action-data.ts"]
autonomous: true
requirements: ["ACTION-02"]
must_haves:
  truths:
    - "action-data.ts 파일에 가사/가족법 6개 도구에 대한 ActionInsight 데이터가 존재한다"
  artifacts:
    - path: "src/lib/action-data.ts"
      provides: "ActionInsight content for family law calculators"
      contains: "alimony"
  key_links:
    - from: "src/lib/action-data.ts"
      to: "src/app/tools/family/*/page.tsx"
      via: "exported actionData object"
      pattern: "export const actionData"
---

<objective>
가사/가족법 6개 계산기(alimony, child-support, property-division, inheritance-tax, forced-heirship, inheritance-order)에 특화된 ActionInsight 실전 대응 팁 및 액션 플랜 데이터를 `action-data.ts`에 추가한다.

Purpose: 사용자가 계산 결과를 확인한 후 실질적인 행동을 취할 수 있도록 돕는 데이터를 구축하기 위함
Output: `src/lib/action-data.ts` 파일 내 `actionData` 객체에 6개 키 추가
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/action-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: action-data.ts에 가사/가족법 6개 도구 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `src/lib/action-data.ts` 파일의 `actionData` 객체에 다음 6개 키에 해당하는 데이터를 추가한다:
    - `alimony` (위자료)
    - `child-support` (양육비)
    - `property-division` (재산분할)
    - `inheritance-tax` (상속세)
    - `forced-heirship` (유류분)
    - `inheritance-order` (상속순위/비율)
    
    각 데이터는 `CalculatorActionData` 타입(`title`, `tips`, `scriptTemplate`)을 준수해야 한다.
    팁의 어조는 단호하고 전문적인 '전문가 조언' 톤으로 작성하며, 카톡 템플릿은 당사자 간의 원만한 협상/요구 목적 텍스트로 구성한다. 팁은 각 도구당 3가지로 제한한다.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>action-data.ts 파일이 문법 오류 없이 저장되고 6개 도구 데이터가 모두 포함되어 있다.</done>
</task>

</tasks>

<verification>
- `action-data.ts`에 추가된 데이터가 `CalculatorActionData` 타입에 맞게 정확히 작성되었는지 확인
</verification>

<success_criteria>
- TypeScript 빌드(`npx tsc --noEmit`)가 성공적으로 통과한다.
- 6개 도구에 대한 ActionInsight 데이터가 `action-data.ts`에 존재한다.
</success_criteria>

<output>
After completion, create `.planning/phases/34-actioninsight/34-01-SUMMARY.md`
</output>