---
phase: 25-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/family/inheritance-tax/page.tsx
  - src/app/tools/family/forced-heirship/page.tsx
  - src/app/tools/family/inheritance-order/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "inheritance-tax에서 상속재산 0 이하 입력 시 에러 표시, 잘못된 결과 미출력"
    - "inheritance-tax에서 상속재산 > 1조원 입력 시 경고 표시"
    - "forced-heirship에서 상속재산 0 이하 또는 법정상속분율 0/100% 초과 시 에러"
    - "inheritance-order에서 상속인 0명(모든 항목이 0)인 경우 '상속인 없음' 결과 표시"
    - "세 계산기 모두 초기 상태에서 0원 결과 미노출"
    - "세 계산기 모두 필수 필드에 시각적 구분 표시"
  artifacts:
    - path: "src/app/tools/family/inheritance-tax/page.tsx"
      provides: "상속세 계산기 with input validation and edge case handling"
    - path: "src/app/tools/family/forced-heirship/page.tsx"
      provides: "유류분 계산기 with input validation and edge case handling"
    - path: "src/app/tools/family/inheritance-order/page.tsx"
      provides: "상속순위 계산기 with input validation and edge case handling"
  key_links:
    - from: "inheritance-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "forced-heirship/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "inheritance-order/page.tsx"
      to: "result display"
      via: "determineOrder result conditional rendering"
      pattern: "determineOrder|results.length"
---

<objective>
inheritance-tax, forced-heirship, inheritance-order 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, EDGE) 감사 및 수정

Purpose: 상속 관련 3개 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 유효성 검증, 비현실값 경고, 초기 상태 정리, 결과 포맷팅 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-ux/25-CONTEXT.md
@src/app/tools/family/inheritance-tax/page.tsx
@src/app/tools/family/forced-heirship/page.tsx
@src/app/tools/family/inheritance-order/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: inheritance-tax 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/family/inheritance-tax/page.tsx</files>
  <read_first>src/app/tools/family/inheritance-tax/page.tsx</read_first>
  <action>
inheritance-tax/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정 (per D-03):

**현황 파악 (80줄까지 확인한 내용):**
- handleCalculate에서 `if (estate <= 0) return;` — 조용한 return
- `error` state 없음
- `formatNumber()`, `parseInput()`, `displayValue()` 이미 있음
- 상속세 계산 로직 올바름

**INPUT 검증:**
- INPUT-01: `error` state 추가. `estate <= 0`이면 `setError('상속재산 총액은 0보다 커야 합니다.')`.
- INPUT-02: `grossEstate === ''`이면 `setError('상속재산 총액을 입력해주세요.')`.
- INPUT-03: 상속재산 > 1,000,000,000,000 (1조)이면 `warning` state로 "입력값이 1조원을 초과합니다. 입력 단위(원)를 확인해주세요." (`text-orange-500`). 계산은 허용.
- INPUT-04: `handleNumberInput`으로 숫자만 허용 — 이미 있음, 확인만. 장례비, 채무 필드도 동일 패턴 적용됐는지 확인.

**RESULT 표시:**
- RESULT-01: `result.tax === 0`이면 "납부할 상속세가 없습니다 (공제액이 과세가액 이상)" 안내.
- RESULT-02: 모든 금액에 `formatNumber()` 적용 확인.
- RESULT-03: `taxableBase - totalDeduction = taxableAmount` 수식 확인 — 현재 `Math.max(0, taxableBase - totalDeduction)` 이미 처리됨.

**FLOW:**
- FLOW-01: `result` null 체크 후 결과 영역 표시 — `useState<TaxResult | null>(null)` 패턴 확인.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 상속재산 총액 라벨에 "(필수)" 또는 "*" 표시. 배우자 체크박스 라벨도 확인.

**EDGE:**
- EDGE-01: 재산=0 → 에러. 재산=100억 → 상속세 0원(공제 이하) → RESULT-01 안내. 채무 > 재산 → `taxableBase = Math.max(0, ...)` 이미 처리됨.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `progressiveTax(0)` → `return { tax: 0, rate: '0%' }` 이미 처리됨.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|상속재산 총액을 입력\|납부할 상속세" src/app/tools/family/inheritance-tax/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "상속재산 총액을 입력\|입력해주세요" inheritance-tax/page.tsx returns match (INPUT-02)
- grep "0보다 커야" inheritance-tax/page.tsx returns match (INPUT-01)
- grep "1조\|1,000,000,000,000" inheritance-tax/page.tsx returns match (INPUT-03)
- grep "text-red-500" inheritance-tax/page.tsx returns match
- grep "납부할 상속세가 없습니다" inheritance-tax/page.tsx returns match (RESULT-01)
- grep "필수\|\*" inheritance-tax/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>inheritance-tax 계산기가 16개 요구사항 모두 충족. 상속재산 검증, 1조 초과 경고, 세액 0원 안내, 필수 필드 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: forced-heirship 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/family/forced-heirship/page.tsx</files>
  <read_first>src/app/tools/family/forced-heirship/page.tsx, src/app/tools/family/inheritance-tax/page.tsx</read_first>
  <action>
forced-heirship/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. inheritance-tax에서 확립한 패턴과 동일하게 적용 (per D-03, D-04):

**현황 파악 (파일 전체 읽어 확인):**
- handleCalculate: `if (estate <= 0) return;` + `if (isNaN(pct) || pct <= 0 || pct > 100) return;` — 조용한 return
- `error` state 없음
- `formatNumber()`, `parseInput()`, `displayValue()` 이미 있음
- 유류분 기초재산 = estateAtDeath + giftsWithinYear - debts

**INPUT 검증:**
- INPUT-01: `estate <= 0`이면 `setError('상속 개시 시 재산은 0보다 커야 합니다.')`.
- INPUT-02: `estateAtDeath === ''`이면 `setError('상속 개시 시 재산을 입력해주세요.')`. 법정상속분율이 빈 문자열이면 `setError('법정상속분율을 입력해주세요.')`.
- INPUT-03: 총재산(estateAtDeath) > 1,000,000,000,000 (1조)이면 경고 `text-orange-500`.
- INPUT-04: 숫자 입력 패턴 확인 — `handleNumberInput`으로 처리됐는지 확인, `statutorySharePct` 필드는 별도 처리 필요 (소수점 허용: `[^0-9.]`).

**RESULT 표시:**
- RESULT-01: `result.shortfall === 0`이면 "유류분 부족액이 없습니다 (이미 충분히 수령)" 안내.
- RESULT-02: 모든 금액에 `formatNumber()` 적용 확인.
- RESULT-03: `statutoryShareAmount × forcedRatio = forcedHeirshipAmount` 확인 — 수식 검증.

**FLOW:**
- FLOW-01: `result` null 체크 후 결과 영역 표시.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 상속재산, 법정상속분율 라벨에 "(필수)" 표시. 생전증여, 채무는 선택 필드 라벨 "(선택)" 추가.

**EDGE:**
- EDGE-01: 재산=0 → 에러. 법정상속분율=100% → 결과 확인. 채무 > 재산 → `baseEstate = Math.max(0, ...)` 이미 처리됨.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `statutoryShareAmount = Math.floor(baseEstate * (statutorySharePct / 100))` — pct가 0이면 이미 차단됨.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|재산을 입력\|부족액이 없습니다" src/app/tools/family/forced-heirship/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "재산을 입력\|입력해주세요" forced-heirship/page.tsx returns match (INPUT-02)
- grep "0보다 커야" forced-heirship/page.tsx returns match (INPUT-01)
- grep "text-red-500" forced-heirship/page.tsx returns match
- grep "부족액이 없습니다\|충분히 수령" forced-heirship/page.tsx returns match (RESULT-01)
- grep "필수\|\*" forced-heirship/page.tsx returns match (FLOW-03)
- grep "선택" forced-heirship/page.tsx returns match (FLOW-03 선택 필드)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>forced-heirship 계산기가 16개 요구사항 모두 충족. 재산 검증, 유류분 부족액 0원 안내, 필수/선택 필드 구분 완료.</done>
</task>

<task type="auto">
  <name>Task 3: inheritance-order 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/family/inheritance-order/page.tsx</files>
  <read_first>src/app/tools/family/inheritance-order/page.tsx</read_first>
  <action>
inheritance-order/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정 (per D-03):

**이 계산기의 특성:**
- 금액 입력 없음 — 상속인 수(숫자)와 배우자 체크박스를 입력
- 결과는 금액이 아닌 상속인 목록과 지분율 (텍스트)
- `determineOrder()` 함수로 결과 계산

**INPUT 검증:**
- INPUT-01: 금액 필드 없음 — 상속인 수 음수 입력 방지. `numChildren`, `numParents`, `numSiblings`, `numGrandchildren` 필드에서 음수 입력 시 에러 `setError('상속인 수는 0 이상이어야 합니다.')`.
- INPUT-02: 모든 상속인 수가 0이고 배우자도 없으면 버튼 클릭 시 안내 → 실제로는 `determineOrder`에서 "상속인 없음" 결과를 반환하므로, 이 경우 에러 없이 "상속인 없음(국가 귀속)" 결과를 표시하는 것이 맞음. 에러 불필요.
- INPUT-03: 상속인 수 > 20명이면 경고 "상속인이 20명을 초과합니다. 입력값을 확인해주세요." (`text-orange-500`). 계산 허용.
- INPUT-04: 숫자 입력 방지 — `type="number"` 필드의 onChange에서 음수 차단. `Math.max(0, ...)` 적용 또는 `min="0"` 속성 추가.

**RESULT 표시:**
- RESULT-01: `determineOrder` 결과에서 모든 그룹의 `isInheriting === false`이면 "상속인이 없습니다 (국가 귀속)" 안내 — 이미 `determineOrder`에서 "상속인 없음" 항목 반환함. 렌더링에서 이를 눈에 띄게 표시.
- RESULT-02: 지분율은 분수/텍스트 형태 — 금액이 아니므로 천단위 콤마 불필요.
- RESULT-03: 각 그룹의 지분 합산 = 100% 확인 — `formatFraction` 함수 확인.

**FLOW:**
- FLOW-01: 결과를 실시간으로 계산하는지 또는 버튼 방식인지 확인. 버튼 방식이 아니면 버튼 방식으로 변경 (per 프로젝트 패턴). `result` state null이면 결과 영역 숨김.
- FLOW-02: 버튼 클릭 방식 확인.
- FLOW-03: 상속인 수 필드는 숫자 입력 — 모든 필드가 선택적이므로 "(선택)" 또는 기본값 0 표시.

**EDGE:**
- EDGE-01: 모든 상속인 수=0, 배우자=false → "상속인 없음" 결과 확인.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `totalPortions = firstPriorityCount + spousePortions` — `firstPriorityCount + spousePortions = 0`이면 나누기 0 가능. `formatFraction(1, 0)` 호출 시 크래시 여부 확인하고 방어 코드 추가.

에러: `text-red-500`, 경고: `text-orange-500`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|상속인이 없습니다\|20명\|min=" src/app/tools/family/inheritance-order/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "0 이상\|음수\|min=" inheritance-order/page.tsx returns match (INPUT-01/04)
- grep "20명\|초과" inheritance-order/page.tsx returns match (INPUT-03)
- grep "text-orange-500" inheritance-order/page.tsx returns match
- grep "국가 귀속\|상속인이 없\|상속인 없음" inheritance-order/page.tsx returns match (RESULT-01)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>inheritance-order 계산기가 16개 요구사항 모두 충족. 음수 상속인 수 차단, 비현실값 경고, 상속인 없음 결과 명확 표시, 나누기 0 방어 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 세 계산기 모두 에러 메시지 패턴 동일 (text-red-500/text-orange-500)
- grep "setError\|text-red-500" 각 파일에서 매치 확인
</verification>

<success_criteria>
- inheritance-tax, forced-heirship, inheritance-order 세 계산기 모두 유효성 검증, 비현실값 경고, 에러·안내 처리 완료
- 초기 상태에서 불필요한 결과 미노출
- 필수/선택 필드 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux/25-02-SUMMARY.md`
</output>
