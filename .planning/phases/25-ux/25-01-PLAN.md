---
phase: 25-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/family/alimony/page.tsx
  - src/app/tools/family/child-support/page.tsx
  - src/app/tools/family/property-division/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "alimony에서 혼인기간 0년 미만 입력 시 에러 메시지 표시, 잘못된 결과 미출력"
    - "child-support에서 자녀 수, 나이 구분, 소득 필수 입력 누락 시 명확한 안내"
    - "child-support에서 양육자/비양육자 소득 모두 0이면 에러 처리"
    - "property-division에서 총재산 0 이하 시 에러, 기여율 20~80% 범위 벗어나면 에러"
    - "세 계산기 모두 초기 상태에서 0원 결과 미노출"
    - "세 계산기 모두 필수 필드에 시각적 구분 표시"
  artifacts:
    - path: "src/app/tools/family/alimony/page.tsx"
      provides: "위자료 계산기 with input validation and edge case handling"
    - path: "src/app/tools/family/child-support/page.tsx"
      provides: "양육비 계산기 with required field validation and edge case handling"
    - path: "src/app/tools/family/property-division/page.tsx"
      provides: "재산분할 계산기 with input validation and edge case handling"
  key_links:
    - from: "alimony/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "child-support/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "property-division/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
alimony, child-support, property-division 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, EDGE) 감사 및 수정

Purpose: 가사/가족법 가장 많이 사용되는 3개 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 결과 포맷팅 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-ux/25-CONTEXT.md
@src/app/tools/family/alimony/page.tsx
@src/app/tools/family/child-support/page.tsx
@src/app/tools/family/property-division/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: alimony 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/family/alimony/page.tsx</files>
  <read_first>src/app/tools/family/alimony/page.tsx</read_first>
  <action>
alimony/page.tsx를 전체 읽고 16개 요구사항 각각에 대해 코드를 감사한 뒤 문제 발견 즉시 수정 (per D-03):

**현황 파악:**
- handleCalculate에서 `if (isNaN(y) || y < 0 || faults.length === 0) return;` — 조용한 return (에러 표시 없음)
- `error` state 없음 — 추가 필요
- 필수 필드 시각적 구분 없음
- 혼인기간 0년은 유효할 수 있으나 음수는 차단

**INPUT 검증:**
- INPUT-01: `error` state 추가. handleCalculate에서 혼인기간이 음수이면 `setError('혼인기간은 0년 이상이어야 합니다.')` 표시. 0년은 허용 (미혼 기간 중 이혼 사례).
- INPUT-02: 혼인기간 필드가 비어있으면 `setError('혼인기간을 입력해주세요.')` 안내. `years === ''` 체크.
- INPUT-03: 혼인기간 > 80년이면 `setError` 대신 경고(`warning` state 추가) — "혼인기간이 80년을 초과합니다. 입력값을 확인해주세요." (계산은 허용, `text-orange-500`).
- INPUT-04: 현재 `type="number"` — `type="text"` + `inputMode="numeric"` + onChange에서 숫자만 허용하는 패턴으로 교체. `e.target.value.replace(/[^0-9]/g, '')` 적용.

**RESULT 표시:**
- RESULT-01: 결과가 0원이면 "해당 조건에서는 위자료가 인정되지 않을 수 있습니다" 안내. `result.estimate === 0` 체크.
- RESULT-02: `formatNumber()` 이미 있음 — 결과 섹션에서 모든 금액에 적용됐는지 확인, 누락 시 추가.
- RESULT-03: 결과에 min/max/estimate 3개 항목 — 합산 검증 없음 (범위 표시라 해당 없음, skip).

**FLOW:**
- FLOW-01: `result` state가 null인지 확인 후 결과 영역 표시 — 이미 `useState<... | null>(null)` 패턴이면 조건부 렌더링 확인.
- FLOW-02: 버튼 클릭 방식 — 현재 그대로 유지.
- FLOW-03: 혼인기간 라벨을 "혼인기간 (년) *" 로 변경 (필수 표시). 유책사유 라벨도 "유책사유 (1개 이상 선택) *".

**EDGE:**
- EDGE-01: 혼인기간=0, =1, =80에서 정상 동작 확인. 유책사유 최소 1개 강제 (이미 `next.length === 0 ? prev : next` 있음).
- EDGE-02: 날짜 입력 없음 — skip.
- EDGE-03: `getBaseRange`에서 Infinity 비교 — 안전함. `range = baseMax - baseMin`에서 divide by zero 없음.

**에러 표시 위치:** 계산 버튼 위에 `{error && <p className="text-red-500 text-sm mb-3">{error}</p>}`.
**경고 표시 위치:** 같은 위치에 `{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}`.
에러 발생 시 `setResult(null)` 호출하여 이전 결과 제거.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|혼인기간을 입력\|0년 이상" src/app/tools/family/alimony/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "혼인기간을 입력해주세요" alimony/page.tsx returns match (INPUT-02)
- grep "0년 이상\|음수" alimony/page.tsx returns match (INPUT-01)
- grep "80년\|비현실" alimony/page.tsx returns match (INPUT-03)
- grep "text-red-500" alimony/page.tsx returns match (에러 스타일)
- grep "text-orange-500" alimony/page.tsx returns match (경고 스타일)
- grep "\*\|필수" alimony/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file (no TypeScript errors)
  </acceptance_criteria>
  <done>alimony 계산기가 16개 요구사항 모두 충족. 혼인기간 검증, 비현실값 경고, 필수 필드 표시, 초기 상태 정리 완료.</done>
</task>

<task type="auto">
  <name>Task 2: child-support 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/family/child-support/page.tsx</files>
  <read_first>src/app/tools/family/child-support/page.tsx, src/app/tools/family/alimony/page.tsx</read_first>
  <action>
child-support/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. alimony에서 확립한 에러/경고 패턴과 동일하게 적용 (per D-03, D-04):

**현황 파악 (80줄까지 확인한 내용):**
- `ChildCount: '1'|'2'|'3'` — select로 이미 제한됨 (자녀 수 0명 불가)
- `AgeGroup: 'infant'|'elementary'|'teen'` — select로 제한됨
- `combinedIncome > 0`가 이미 체크됨 (divide by zero 방어)
- 소득 입력 필드 검증은 확인 필요

**INPUT 검증:**
- INPUT-01: 양육자 소득, 비양육자 소득 모두 0이면 `setError('소득 정보를 입력해주세요. 양육자 또는 비양육자 소득 중 하나 이상이 필요합니다.')`.
- INPUT-02: 소득 필드가 모두 빈 문자열이면 `setError('소득 정보를 입력해주세요.')`. 자녀 수, 나이 구분은 select이므로 항상 선택됨.
- INPUT-03: 월 소득 합산 > 3,000만원(30,000,000 → 3,000만원 단위로 환산: > 30,000,000)이면 경고 "소득 합산이 3,000만원을 초과합니다. 입력 단위를 확인해주세요." (만원 단위 입력 여부 확인 후 적용).
- INPUT-04: 숫자+콤마만 허용 — onChange 패턴 확인, `handleNumberInput` 있으면 그대로, 없으면 추가.

**RESULT 표시:**
- RESULT-01: `monthlyTotal === 0`이면 "양육비가 산정되지 않았습니다" 안내.
- RESULT-02: 모든 결과 금액에 `toLocaleString('ko-KR')` 적용 확인.
- RESULT-03: `noncustodialPayment + custodialPayment === monthlyTotal` 검증 확인. 불일치 시 수정.

**FLOW:**
- FLOW-01: `result` state null이면 결과 영역 숨김.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 소득 입력 필드 라벨에 "(필수)" 또는 "*" 표시. 자녀 수, 나이 구분도 필수이므로 라벨에 표시.

**EDGE:**
- EDGE-01: 양육자 소득=0, 비양육자=0, 두 소득=0 케이스.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `noncustodialShare = combinedIncome > 0 ? ... : 0.5` — 이미 방어됨.

alimony와 동일한 에러 메시지 스타일: `text-red-500`, 경고: `text-orange-500`.
에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|소득 정보를 입력\|필수" src/app/tools/family/child-support/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "소득 정보를 입력\|입력해주세요" child-support/page.tsx returns match (INPUT-02)
- grep "text-red-500" child-support/page.tsx returns match
- grep "필수\|\*" child-support/page.tsx returns match (FLOW-03)
- grep "null" child-support/page.tsx shows result null check (FLOW-01)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>child-support 계산기가 16개 요구사항 모두 충족. 소득 필수 입력 안내, 필수 필드 표시, 초기 상태 정리 완료.</done>
</task>

<task type="auto">
  <name>Task 3: property-division 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/family/property-division/page.tsx</files>
  <read_first>src/app/tools/family/property-division/page.tsx, src/app/tools/family/alimony/page.tsx</read_first>
  <action>
property-division/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. 동일 에러/경고 패턴 적용 (per D-03, D-04):

**현황 파악 (80줄까지 확인한 내용):**
- `handleCalculate`에서 `if (total <= 0 || isNaN(contribRate) || contribRate < 20 || contribRate > 80) return;` — 조용한 return
- `error` state 없음
- `formatNumber()`, `parseKoreanNumber()` 이미 있음
- `getDisplayValue()` — 천단위 콤마 표시 이미 있음

**INPUT 검증:**
- INPUT-01: 총재산 필드 값이 음수는 `handleNumberInput`에서 이미 차단 (`[^0-9]` 제거). 0은 아래 처리.
- INPUT-02: 총재산 `totalAssets`이 비어있으면 `setError('재산 총액을 입력해주세요.')`. 기여율이 범위 밖이면 `setError('기여율은 20~80% 범위에서 입력해주세요.')`.
- INPUT-03: 총재산 > 100,000,000,000 (1000억)이면 경고 "입력값이 비현실적으로 큽니다. 확인해주세요." (`text-orange-500`).
- INPUT-04: 숫자 입력 이미 처리됨 — 혼인기간 필드도 숫자만 허용하는지 확인하고 필요 시 추가.

**RESULT 표시:**
- RESULT-01: `result.paymentFromOpponent === 0 && result.claimantShare <= claimantAssets`이면 "상대방으로부터 받을 금액이 없습니다" 안내.
- RESULT-02: `formatNumber()` 적용 확인.
- RESULT-03: `claimantShare = totalAssets * (contributionRate / 100)`, `paymentFromOpponent = max(0, claimantShare - claimantAssets)` — 수식 검증 완료.

**FLOW:**
- FLOW-01: `result` state null일 때 결과 영역 숨김 — 이미 `useState<DivisionResult | null>(null)` 패턴 있음, 조건부 렌더링 확인.
- FLOW-02: 버튼 클릭 방식 유지.
- FLOW-03: 총재산 라벨에 "(필수)" 또는 "*" 표시. 기여율 라벨도 필수 표시.

**EDGE:**
- EDGE-01: 총재산=0 → 에러. 기여율=20, =80 경계값 → 정상. 청구인 재산 > 총재산 → `paymentFromOpponent = Math.max(0, ...)` 이미 처리됨.
- EDGE-02: 날짜 없음 — skip.
- EDGE-03: `contributionRate / 100` — 0 불가 (20~80 제한). 나누기 0 없음.

에러: `text-red-500`, 경고: `text-orange-500`. 에러 발생 시 `setResult(null)`.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|재산 총액을 입력\|필수\|\*" src/app/tools/family/property-division/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "재산 총액을 입력\|입력해주세요" property-division/page.tsx returns match (INPUT-02)
- grep "비현실\|1000억\|100000000000" property-division/page.tsx returns match (INPUT-03)
- grep "text-red-500" property-division/page.tsx returns match
- grep "필수\|\*" property-division/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes for this file
  </acceptance_criteria>
  <done>property-division 계산기가 16개 요구사항 모두 충족. 총재산 검증, 기여율 범위 에러, 비현실값 경고, 필수 필드 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 세 계산기 모두 에러 메시지 패턴 동일 (text-red-500/text-orange-500)
- grep "setError" 각 파일에서 매치 확인
</verification>

<success_criteria>
- alimony, child-support, property-division 세 계산기 모두 음수/0/빈값/비현실값 에러·경고 처리 완료
- 초기 상태에서 0원 결과 미노출
- 필수 필드 시각적 구분
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/25-ux/25-01-SUMMARY.md`
</output>
