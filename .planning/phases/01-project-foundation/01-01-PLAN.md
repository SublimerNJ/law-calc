---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/tools-data.ts
autonomous: true
requirements: [INFRA-01, INFRA-03, DESIGN-02]

must_haves:
  truths:
    - "npm run dev starts without errors"
    - "Next.js 16 + React 19 + Tailwind 4 + TypeScript configured"
    - "9 legal categories with 70 tools defined in tools-data.ts"
    - "Navy/gold color theme tokens defined in globals.css"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies"
      contains: "next"
    - path: "src/lib/tools-data.ts"
      provides: "Category and tool data"
      exports: ["CATEGORIES", "TOOLS", "getToolsByCategory"]
    - path: "src/app/globals.css"
      provides: "Design tokens and base styles"
      contains: "--color-brand-primary"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals.css"
---

<objective>
Scaffold the Next.js 16 project with all dependencies, Tailwind 4 config, legal-themed design tokens, and the complete 70-tool data structure across 9 categories.

Purpose: Foundation that all subsequent plans build upon.
Output: Working Next.js dev server with design tokens and tool data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and configure design tokens</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs, src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/package.json
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/app/globals.css
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/app/layout.tsx
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias --yes` in the project root (if package.json already exists, delete it first).

    2. Install additional deps: `npm install next-themes recharts`

    3. Ensure package.json has: next@16, react@19, react-dom@19, tailwindcss@4, @tailwindcss/postcss@4, next-themes, recharts. Match the reference project's versions.

    4. Create `src/app/globals.css` modeled on reference but with legal-domain branding:
       - `@import "tailwindcss";`
       - @theme block with:
         - `--font-sans: "Pretendard Variable", Pretendard, -apple-system, BlinkMacSystemFont, system-ui, sans-serif;`
         - `--color-background: #0a0f1c;` (deep navy, NOT pure black — legal trust feel)
         - `--color-foreground: #f0f0f5;`
         - `--color-brand-primary: #c9a84c;` (muted gold)
         - `--color-brand-secondary: #e8d48b;`
         - `--color-brand-accent: #3b82f6;` (blue accent for legal)
         - `--color-surface-50: #0f1628;`
         - `--color-surface-100: #151d32;`
         - `--color-surface-200: #1c2540;`
         - `--color-surface-300: #2a3556;`
         - `--color-border-subtle: rgba(255,255,255,0.05);`
         - `--color-border-default: rgba(255,255,255,0.1);`
         - `--color-border-hover: rgba(201,168,76,0.3);`
         - `--ease-spring: cubic-bezier(0.175, 0.885, 0.32, 1.275);`
         - `--ease-smooth: cubic-bezier(0.4, 0, 0.2, 1);`
       - Same utility classes as reference: `.glass-panel`, `.premium-card`, `.text-gradient-gold`, `.parallax-layer`, `.tool-page-bg`, `.recharts-dark`
       - Custom scrollbar, focus-visible, smooth scroll, prefers-reduced-motion

    5. Create `src/app/layout.tsx`:
       - `lang="ko"`, `suppressHydrationWarning`
       - Pretendard font CDN link in head (same URL as reference)
       - ThemeProvider from next-themes wrapping children (attribute="class", defaultTheme="dark", enableSystem)
       - Body: `min-h-screen flex flex-col bg-[var(--color-background)] text-[var(--color-foreground)] font-sans antialiased selection:bg-[#c9a84c] selection:text-black`
       - metadata export: title="법률 계산기 | 70개 무료 법률 도구 모음", description with Korean SEO keywords, openGraph

    6. Create minimal `src/app/page.tsx` that just renders `<div>법률 계산기</div>` as placeholder (will be replaced in Plan 04).

    7. Verify `npm run dev` starts clean.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "next" package.json
    - grep -q "next-themes" package.json
    - grep -q "recharts" package.json
    - grep -q "tailwindcss" package.json
    - grep -q "@import" src/app/globals.css
    - grep -q "color-brand-primary" src/app/globals.css
    - grep -q "0a0f1c" src/app/globals.css
    - grep -q "Pretendard" src/app/layout.tsx
    - grep -q "ThemeProvider" src/app/layout.tsx
    - grep -q "lang=\"ko\"" src/app/layout.tsx
    - grep -q "법률" src/app/layout.tsx
  </acceptance_criteria>
  <done>Next.js 16 project builds successfully with navy/gold legal theme tokens, Pretendard font, and dark theme provider</done>
</task>

<task type="auto">
  <name>Task 2: Create complete tools-data.ts with 9 legal categories and 70 tools</name>
  <files>src/lib/tools-data.ts</files>
  <read_first>
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/lib/tools-data.ts
    /Volumes/NJ SSD 4T/Coding/loop creation 2/.planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `src/lib/tools-data.ts` with same interface pattern as reference:

    ```typescript
    export interface Tool { id: string; name: string; description: string; icon: string; category: string; route: string; }
    export interface Category { id: string; name: string; icon: string; description: string; color: string; }
    ```

    Note: Category has an extra `color` field for per-category accent color (DESIGN-03).

    9 CATEGORIES with colors:
    - { id: "court", name: "소송/법원", icon: "⚖️", description: "소송비용, 인지대, 송달료 계산", color: "#3b82f6" }
    - { id: "family", name: "가사/가족법", icon: "👨‍👩‍👧", description: "위자료, 양육비, 상속 계산", color: "#ec4899" }
    - { id: "labor", name: "노동/근로", icon: "💼", description: "퇴직금, 수당, 임금 계산", color: "#f59e0b" }
    - { id: "tax", name: "세금", icon: "🏛️", description: "소득세, 양도세, 취득세 계산", color: "#10b981" }
    - { id: "realty", name: "부동산", icon: "🏠", description: "보증금, 중개보수, DSR 계산", color: "#8b5cf6" }
    - { id: "traffic", name: "교통/형사", icon: "🚗", description: "합의금, 벌금, 과실비율 계산", color: "#ef4444" }
    - { id: "debt", name: "채권/이자", icon: "💰", description: "법정이자, 지연손해금 계산", color: "#06b6d4" }
    - { id: "damages", name: "손해배상", icon: "📋", description: "손해배상, 일실수입 계산", color: "#f97316" }
    - { id: "misc", name: "기타 법률도구", icon: "🔧", description: "소멸시효, 법률구조 확인", color: "#6b7280" }

    70 TOOLS array — one entry per requirement (COURT-01 through MISC-04). Each tool:
    - id: kebab-case slug (e.g., "attorney-fee")
    - name: Korean name from REQUIREMENTS.md
    - description: brief 1-line Korean description
    - icon: relevant emoji
    - category: matching category id
    - route: `/tools/{category}/{id}` (e.g., `/tools/court/attorney-fee`)

    All 70 tools mapped exactly to requirements:
    - court: 9 tools (COURT-01~09)
    - family: 8 tools (FAMILY-01~08)
    - labor: 14 tools (LABOR-01~14)
    - tax: 12 tools (TAX-01~12)
    - realty: 7 tools (REALTY-01~07)
    - traffic: 6 tools (TRAFFIC-01~06)
    - debt: 4 tools (DEBT-01~04)
    - damages: 6 tools (DAMAGES-01~06)
    - misc: 4 tools (MISC-01~04)

    Export helper functions:
    - `getToolsByCategory(categoryId: string): Tool[]`
    - `searchTools(query: string): Tool[]`
    - `getToolByRoute(route: string): Tool | undefined`
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && node -e "const t = require('./src/lib/tools-data.ts'); console.log(t.TOOLS.length)" 2>&1 || npx tsx -e "import {TOOLS,CATEGORIES} from './src/lib/tools-data'; console.log('tools:'+TOOLS.length+' cats:'+CATEGORIES.length)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export interface Tool" src/lib/tools-data.ts
    - grep -q "export interface Category" src/lib/tools-data.ts
    - grep -q "color:" src/lib/tools-data.ts
    - grep -c "route:" src/lib/tools-data.ts returns 70
    - grep -q "getToolsByCategory" src/lib/tools-data.ts
    - grep -q "searchTools" src/lib/tools-data.ts
    - grep -q "getToolByRoute" src/lib/tools-data.ts
    - grep -q "court" src/lib/tools-data.ts
    - grep -q "family" src/lib/tools-data.ts
    - grep -q "labor" src/lib/tools-data.ts
    - grep -q "misc" src/lib/tools-data.ts
  </acceptance_criteria>
  <done>tools-data.ts exports 9 categories with colors and 70 tools matching all requirement IDs, with helper functions</done>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- tools-data.ts has exactly 70 tool entries and 9 categories
- globals.css has navy/gold legal theme tokens
- layout.tsx has Korean lang, Pretendard font, ThemeProvider
</verification>

<success_criteria>
- Project scaffolded with Next.js 16 + React 19 + Tailwind 4
- Design tokens establish legal navy/gold brand
- 70 tools across 9 categories defined with routes
- Dev server runs cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-01-SUMMARY.md`
</output>
