---
phase: 01-project-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/ui/ThemeToggle.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [INFRA-02, INFRA-05]

must_haves:
  truths:
    - "Header shows site logo, category navigation, mobile hamburger menu"
    - "Footer shows categories, copyright, legal links"
    - "Dark/light theme toggle works and persists"
    - "Layout is responsive across mobile/tablet/desktop"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "Fixed header with nav and mobile menu"
      exports: ["default"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer with category links"
      exports: ["default"]
    - path: "src/components/ui/ThemeToggle.tsx"
      provides: "Dark/light theme switch button"
      exports: ["default"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import Header"
      pattern: "import.*Header"
    - from: "src/components/layout/Header.tsx"
      to: "src/lib/tools-data.ts"
      via: "import CATEGORIES"
      pattern: "import.*CATEGORIES.*tools-data"
---

<objective>
Create shared layout components (Header, Footer, ThemeToggle) and wire them into the root layout. Header has fixed positioning, category dropdown, mobile menu. Footer has category grid. Theme toggle switches dark/light.

Purpose: Common chrome for all pages.
Output: Header.tsx, Footer.tsx, ThemeToggle.tsx wired into layout.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Header with category nav and mobile menu</name>
  <files>src/components/layout/Header.tsx, src/components/ui/ThemeToggle.tsx</files>
  <read_first>
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/components/layout/Header.tsx
    src/lib/tools-data.ts
    src/app/globals.css
  </read_first>
  <action>
    1. Create `src/components/ui/ThemeToggle.tsx` (client component):
       - Uses `useTheme()` from next-themes
       - Renders a button with sun/moon SVG icons
       - Toggles between "dark" and "light"
       - Styled: `w-10 h-10 rounded-full bg-white/5 hover:bg-white/10 border border-white/10 text-gray-300 transition-all`
       - Uses `mounted` state to avoid hydration mismatch

    2. Create `src/components/layout/Header.tsx` (client component) modeled on reference:
       - Fixed top, z-50, glass-panel background
       - Logo: gold circle with "LT" (Legal Tools) + "법률 계산기" text with text-gradient-gold
       - Desktop nav: "카테고리" dropdown (hover-open, shows 9 categories from CATEGORIES with icons and colors), "전체 도구" link
       - ThemeToggle button in desktop nav
       - Mobile: hamburger button, expandable grid of 9 categories (2 columns)
       - ThemeToggle also in mobile menu
       - Brand name: "법률 계산기"
       - All category links point to `/#${cat.id}`
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit src/components/layout/Header.tsx src/components/ui/ThemeToggle.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/components/layout/Header.tsx
    - grep -q "use client" src/components/ui/ThemeToggle.tsx
    - grep -q "useTheme" src/components/ui/ThemeToggle.tsx
    - grep -q "CATEGORIES" src/components/layout/Header.tsx
    - grep -q "glass-panel" src/components/layout/Header.tsx
    - grep -q "법률 계산기" src/components/layout/Header.tsx
    - grep -q "isMenuOpen" src/components/layout/Header.tsx
    - grep -q "ThemeToggle" src/components/layout/Header.tsx
  </acceptance_criteria>
  <done>Header renders with logo, category dropdown, theme toggle, and responsive mobile menu</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer and wire layout components</name>
  <files>src/components/layout/Footer.tsx, src/app/layout.tsx</files>
  <read_first>
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/components/layout/Footer.tsx
    src/app/layout.tsx
  </read_first>
  <action>
    1. Create `src/components/layout/Footer.tsx` modeled on reference:
       - Dark background (#080d1a), top border with gold gradient accent line
       - Grid: brand column (gold circle "LT" + "법률 계산기" + description "70개 법률 계산기를 무료로..."), categories column (3-col grid of 9 categories linking to `/#${cat.id}`)
       - Bottom bar: copyright "2026 법률 계산기. 모든 권리 보유." + links to /privacy and /about
       - Uses CATEGORIES from tools-data.ts
       - Decorative gradient orb at top (gold blur)

    2. Update `src/app/layout.tsx`:
       - Import and render Header above main, Footer below main
       - main gets `flex-1 relative z-10 pt-20` (pt-20 for fixed header offset)
       - Ensure ThemeProvider wraps Header + main + Footer

    3. Update globals.css to add light theme overrides:
       - Add `.light` class overrides for --color-background (#f8f9fc), --color-foreground (#1a1a2e), surface colors (white-ish), border colors, and adjust .glass-panel, .premium-card, .text-gradient-gold for light mode
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "CATEGORIES" src/components/layout/Footer.tsx
    - grep -q "법률 계산기" src/components/layout/Footer.tsx
    - grep -q "2026" src/components/layout/Footer.tsx
    - grep -q "Header" src/app/layout.tsx
    - grep -q "Footer" src/app/layout.tsx
    - grep -q "pt-20" src/app/layout.tsx
    - grep -q ".light" src/app/globals.css
  </acceptance_criteria>
  <done>Header and Footer render in layout, light/dark theme switching works with proper color overrides</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- Header, Footer, ThemeToggle components exist and compile
- Layout.tsx imports and renders Header + Footer
- Light theme CSS overrides exist in globals.css
</verification>

<success_criteria>
- Fixed header with logo, category nav, theme toggle, mobile menu
- Footer with categories and copyright
- Dark/light theme toggle persists via next-themes
- Responsive layout works
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-02-SUMMARY.md`
</output>
