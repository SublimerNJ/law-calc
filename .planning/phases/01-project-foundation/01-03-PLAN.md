---
phase: 01-project-foundation
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/ui/ParallaxLayer.tsx
  - src/components/sections/HeroSection.tsx
  - src/components/sections/CategorySection.tsx
autonomous: true
requirements: [DESIGN-01, DESIGN-05]

must_haves:
  truths:
    - "Hero section displays with parallax background layers on scroll"
    - "Hero shows site title, subtitle, tool count badge, category pills"
    - "Scroll-based fade-in animations on category sections"
  artifacts:
    - path: "src/components/ui/ParallaxLayer.tsx"
      provides: "Reusable parallax scroll component"
      exports: ["default"]
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Parallax hero with title and category pills"
      exports: ["default"]
    - path: "src/components/sections/CategorySection.tsx"
      provides: "Scroll-animated category section wrapper"
      exports: ["default"]
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/components/ui/ParallaxLayer.tsx"
      via: "import ParallaxLayer"
      pattern: "import.*ParallaxLayer"
    - from: "src/components/sections/HeroSection.tsx"
      to: "src/lib/tools-data.ts"
      via: "import CATEGORIES"
      pattern: "import.*CATEGORIES.*tools-data"
---

<objective>
Build the parallax hero section and scroll-based animation components. Hero has multi-layer parallax with legal branding. CategorySection wraps each category with fade-in-on-scroll animation.

Purpose: Visual polish and premium feel matching reference project.
Output: HeroSection.tsx, ParallaxLayer.tsx, CategorySection.tsx.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ParallaxLayer and HeroSection with legal branding</name>
  <files>src/components/ui/ParallaxLayer.tsx, src/components/sections/HeroSection.tsx</files>
  <read_first>
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/components/sections/HeroSection.tsx
    src/app/globals.css
    src/lib/tools-data.ts
  </read_first>
  <action>
    1. Create `src/components/ui/ParallaxLayer.tsx` (client component):
       - Props: `{ speed: number; className?: string; children: React.ReactNode }`
       - Uses useEffect + window scroll listener to apply translateY transform based on scrollY * speed
       - Uses requestAnimationFrame for performance
       - Applies `.parallax-layer` class for GPU acceleration
       - Respects prefers-reduced-motion (disable parallax if reduced motion)

    2. Create `src/components/sections/HeroSection.tsx` (client component) modeled on reference but with legal branding:
       - Props: `{ totalTools: number }`
       - min-h-[85vh], flex items-center justify-center, overflow-hidden
       - Background layers with ParallaxLayer:
         - Layer 1 (speed -0.15): Gold and blue ambient glow orbs (gold=#c9a84c, blue=#3b82f6)
         - Layer 2: Fine grain dot texture overlay (same as reference)
         - Layer 3 (speed -0.05): Subtle vertical lines with gold tint
         - Layer 4 (speed -0.12): Decorative scale-of-justice inspired geometric shapes (two rotated rounded squares)
         - Layer 5 (speed -0.03): Horizontal gold accent line
       - Content (z-10):
         - Eyebrow badge: gold dot + "{totalTools}개 무료 법률 계산기" in pill
         - Main heading: "법률 계산기" in text-7xl to text-[10rem] responsive, font-black, white
         - Subheading: "정확하고 빠른 법률 비용 계산" in gold (#c9a84c)
         - Subtitle: "변호사 비용부터 세금까지, 70개 법률 계산기를 무료로 이용하세요"
         - Category pills: 9 categories from CATEGORIES, each as an anchor to `#${cat.id}`, showing icon + name + tool count, with hover:border-gold effect
       - Scroll hint at bottom: "Scroll" + vertical line
       - Bottom gradient fade to background color
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit src/components/sections/HeroSection.tsx src/components/ui/ParallaxLayer.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/components/ui/ParallaxLayer.tsx
    - grep -q "use client" src/components/sections/HeroSection.tsx
    - grep -q "requestAnimationFrame" src/components/ui/ParallaxLayer.tsx
    - grep -q "prefers-reduced-motion" src/components/ui/ParallaxLayer.tsx
    - grep -q "ParallaxLayer" src/components/sections/HeroSection.tsx
    - grep -q "법률 계산기" src/components/sections/HeroSection.tsx
    - grep -q "CATEGORIES" src/components/sections/HeroSection.tsx
    - grep -q "totalTools" src/components/sections/HeroSection.tsx
    - grep -q "85vh" src/components/sections/HeroSection.tsx
  </acceptance_criteria>
  <done>HeroSection renders with multi-layer parallax, legal branding, and category pills</done>
</task>

<task type="auto">
  <name>Task 2: Create scroll-animated CategorySection wrapper</name>
  <files>src/components/sections/CategorySection.tsx</files>
  <read_first>
    src/app/globals.css
    src/lib/tools-data.ts
  </read_first>
  <action>
    Create `src/components/sections/CategorySection.tsx` (client component):
    - Props: `{ category: Category; children: React.ReactNode; toolCount: number }`
    - Uses IntersectionObserver to detect when section enters viewport
    - Applies fade-in + slide-up animation (opacity 0->1, translateY 30px->0) when visible
    - Transition: 0.6s ease-out, with stagger delay based on index if needed
    - Section element with `id={category.id}` and `scroll-mt-24` for anchor offset
    - Category header row: icon (in rounded box with category.color background at 10% opacity), name (h2, text-lg, font-semibold), description (text-xs, gray), tool count badge ("N tools" mono text)
    - Gradient divider line below header
    - Children slot renders below divider (will receive the tool card grid)
    - Respects prefers-reduced-motion: skip animation, show immediately
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit src/components/sections/CategorySection.tsx 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "use client" src/components/sections/CategorySection.tsx
    - grep -q "IntersectionObserver" src/components/sections/CategorySection.tsx
    - grep -q "category.id" src/components/sections/CategorySection.tsx
    - grep -q "category.color" src/components/sections/CategorySection.tsx
    - grep -q "scroll-mt-24" src/components/sections/CategorySection.tsx
    - grep -q "opacity" src/components/sections/CategorySection.tsx
    - grep -q "prefers-reduced-motion" src/components/sections/CategorySection.tsx
  </acceptance_criteria>
  <done>CategorySection fades in on scroll with category header, color accent, and children slot</done>
</task>

</tasks>

<verification>
- All 3 components compile with tsc --noEmit
- ParallaxLayer respects reduced motion
- HeroSection has parallax layers and legal branding
- CategorySection uses IntersectionObserver for scroll animation
</verification>

<success_criteria>
- Parallax hero with multi-layer depth effect
- Legal branding (title, subtitle, gold accents)
- Category pills with tool counts
- Scroll-based fade-in animation on category sections
- Accessibility: reduced motion respected
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-03-SUMMARY.md`
</output>
