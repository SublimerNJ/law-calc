---
phase: 01-project-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/components/ui/Card.tsx
  - src/components/ui/CalculatorLayout.tsx
  - src/app/page.tsx
  - src/app/tools/[category]/[tool]/page.tsx
  - src/app/tools/[category]/[tool]/layout.tsx
autonomous: true
requirements: [INFRA-04, INFRA-06, DESIGN-03, DESIGN-04]

must_haves:
  truths:
    - "Main page shows hero + 9 category sections with tool cards in responsive grid"
    - "Tool cards show icon, name, description with category color accent"
    - "Grid is 2-col mobile, 3-col tablet, 5-col desktop"
    - "Each tool card links to /tools/{category}/{id}"
    - "Calculator layout component exists with input/result sections"
    - "SEO metadata generated per tool page"
  artifacts:
    - path: "src/components/ui/Card.tsx"
      provides: "Reusable card with hover effects"
      exports: ["default"]
    - path: "src/components/ui/CalculatorLayout.tsx"
      provides: "Shared calculator page layout"
      exports: ["default"]
    - path: "src/app/page.tsx"
      provides: "Main page with hero + categories"
      exports: ["default"]
    - path: "src/app/tools/[category]/[tool]/page.tsx"
      provides: "Dynamic tool page with SEO"
      exports: ["default", "generateMetadata"]
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/HeroSection.tsx"
      via: "import HeroSection"
      pattern: "import.*HeroSection"
    - from: "src/app/page.tsx"
      to: "src/components/sections/CategorySection.tsx"
      via: "import CategorySection"
      pattern: "import.*CategorySection"
    - from: "src/app/page.tsx"
      to: "src/lib/tools-data.ts"
      via: "import CATEGORIES, TOOLS"
      pattern: "import.*CATEGORIES.*tools-data"
    - from: "src/app/tools/[category]/[tool]/page.tsx"
      to: "src/components/ui/CalculatorLayout.tsx"
      via: "import CalculatorLayout"
      pattern: "import.*CalculatorLayout"
---

<objective>
Build the main page assembling hero + category sections + tool card grid, create the reusable Card and CalculatorLayout components, and set up dynamic tool page routing with SEO metadata.

Purpose: Complete the visual main page and provide the calculator shell for all future phases.
Output: Working main page with all 70 tools displayed, plus calculator layout template.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md
@.planning/phases/01-project-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Card component and assemble main page</name>
  <files>src/components/ui/Card.tsx, src/app/page.tsx</files>
  <read_first>
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/components/ui/Card.tsx
    /Volumes/NJ SSD 4T/Coding/loop creation/tool-collection/src/app/page.tsx
    src/components/sections/HeroSection.tsx
    src/components/sections/CategorySection.tsx
    src/lib/tools-data.ts
  </read_first>
  <action>
    1. Create `src/components/ui/Card.tsx` modeled on reference:
       - Props: `{ children: React.ReactNode; className?: string; href?: string }`
       - Uses `.premium-card` base class
       - If href provided, wraps in Next.js Link (block, group, cursor-pointer)
       - Otherwise renders as div

    2. Rewrite `src/app/page.tsx` to assemble the full main page:
       - Import HeroSection, CategorySection, Card, CATEGORIES, TOOLS, getToolsByCategory
       - Render HeroSection with totalTools={TOOLS.length}
       - For each category, render CategorySection with:
         - Inside: responsive grid `grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-3` (per DESIGN-04: 2/3/5 columns; md:4 is intermediate)
         - Each tool as Card with href={tool.route}:
           - Tool icon (text-2xl, hover:scale-110 transition)
           - Arrow SVG icon (top-right, gray -> category color on hover)
           - Tool name (text-sm, font-medium)
           - Tool description (text-[11px], gray, line-clamp-2)
       - Bottom CTA section: "찾고 있는 법률 계산기가 있나요?" + "{totalTools}개의 법률 계산기가 준비되어 있어요."
       - max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 container for content sections
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "premium-card" src/components/ui/Card.tsx
    - grep -q "Link" src/components/ui/Card.tsx
    - grep -q "HeroSection" src/app/page.tsx
    - grep -q "CategorySection" src/app/page.tsx
    - grep -q "getToolsByCategory" src/app/page.tsx
    - grep -q "grid-cols-2" src/app/page.tsx
    - grep -q "lg:grid-cols-5" src/app/page.tsx
    - grep -q "line-clamp-2" src/app/page.tsx
  </acceptance_criteria>
  <done>Main page renders hero + 9 category sections with 70 tool cards in responsive grid</done>
</task>

<task type="auto">
  <name>Task 2: Create CalculatorLayout and dynamic tool pages with SEO</name>
  <files>src/components/ui/CalculatorLayout.tsx, src/app/tools/[category]/[tool]/page.tsx, src/app/tools/[category]/[tool]/layout.tsx</files>
  <read_first>
    src/lib/tools-data.ts
    src/app/globals.css
  </read_first>
  <action>
    1. Create `src/components/ui/CalculatorLayout.tsx`:
       - Props: `{ tool: Tool; category: Category; children: React.ReactNode }`
       - Uses `.tool-page-bg` class for background
       - Layout structure:
         - Breadcrumb: Home > Category > Tool name
         - Header: tool icon + tool name (h1) + tool description
         - Two-column layout on desktop (lg:grid-cols-3):
           - Left column (col-span-2): children slot (calculator form/results)
           - Right column (col-span-1): placeholder sidebar for future ad/related tools
         - max-w-5xl mx-auto container
       - Back link to `/#${category.id}`

    2. Create `src/app/tools/[category]/[tool]/page.tsx`:
       - Server component
       - Extract params.category and params.tool
       - Look up tool via getToolByRoute(`/tools/${category}/${tool}`)
       - If not found, call notFound()
       - Export `generateMetadata` function:
         - title: `${tool.name} | 법률 계산기`
         - description: tool.description + " - 무료 온라인 법률 계산기"
         - openGraph: title, description, type: "website"
       - Render CalculatorLayout wrapping a placeholder: "이 계산기는 준비 중입니다" message in a styled card
       - Export `generateStaticParams` returning all 70 tool routes for static generation

    3. Create `src/app/tools/[category]/[tool]/layout.tsx`:
       - Simple pass-through layout (just renders children)
       - This file exists so Next.js properly nests the route
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "tool: Tool" src/components/ui/CalculatorLayout.tsx
    - grep -q "category: Category" src/components/ui/CalculatorLayout.tsx
    - grep -q "tool-page-bg" src/components/ui/CalculatorLayout.tsx
    - grep -q "Breadcrumb\|breadcrumb\|Home\|홈" src/components/ui/CalculatorLayout.tsx
    - grep -q "generateMetadata" "src/app/tools/[category]/[tool]/page.tsx"
    - grep -q "generateStaticParams" "src/app/tools/[category]/[tool]/page.tsx"
    - grep -q "notFound" "src/app/tools/[category]/[tool]/page.tsx"
    - grep -q "CalculatorLayout" "src/app/tools/[category]/[tool]/page.tsx"
    - grep -q "법률 계산기" "src/app/tools/[category]/[tool]/page.tsx"
  </acceptance_criteria>
  <done>Dynamic tool pages render with SEO metadata, breadcrumbs, calculator layout shell, and 70 static paths generated</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds (including static generation of 70 tool pages)
- Main page shows hero + 9 categories with tool cards
- Clicking a tool card navigates to /tools/{category}/{tool}
- Tool pages have correct meta title/description
- Responsive grid: 2-col mobile, 5-col desktop
</verification>

<success_criteria>
- Main page fully assembled with all components
- 70 tool cards displayed in category-grouped responsive grids
- Calculator layout template ready for future phases
- SEO metadata on every tool page
- All 70 tool routes statically generated
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-04-SUMMARY.md`
</output>
