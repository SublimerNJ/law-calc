---
phase: 30-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/realty/deposit-return/page.tsx
  - src/app/tools/realty/rent-conversion/page.tsx
  - src/app/tools/realty/brokerage-fee/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "deposit-return에서 보증금·지연일수·이자율 미입력 시 에러 표시, 결과 미출력"
    - "deposit-return에서 보증금 100억 초과 시 경고 표시 (계산 허용)"
    - "deposit-return에서 지연일수 type=number → type=text + inputMode=numeric 변경"
    - "deposit-return에서 초기 상태에서 0원 결과 미노출 (result null)"
    - "deposit-return에서 필수 필드 레이블에 * 표시"
    - "rent-conversion에서 전세금 미입력 또는 보증금 >= 전세금 시 에러 표시"
    - "rent-conversion에서 전환율 0 이하 시 에러 표시"
    - "rent-conversion에서 전환율 type=number → type=text + inputMode=numeric 변경"
    - "rent-conversion에서 전세금 10억 초과 시 경고 표시"
    - "brokerage-fee에서 거래금액 미입력 시 에러 표시"
    - "brokerage-fee에서 월세 모드에서 월세 미입력 시 에러 표시"
    - "brokerage-fee에서 거래금액 50억 초과 시 경고 표시"
    - "brokerage-fee에서 부가세 안내 박스의 dark 색상(yellow-400 텍스트) → light 색상(yellow-700) 수정"
    - "3개 계산기 모두 에러: text-red-500, 경고: text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/realty/deposit-return/page.tsx"
      provides: "보증금반환 계산기 with error/warning state, numeric input fix, 필수 필드 *"
    - path: "src/app/tools/realty/rent-conversion/page.tsx"
      provides: "전월세전환 계산기 with error/warning state, numeric input fix"
    - path: "src/app/tools/realty/brokerage-fee/page.tsx"
      provides: "중개보수 계산기 with error/warning state, vat box color fix"
  key_links:
    - from: "deposit-return handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "rent-conversion handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "brokerage-fee handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
deposit-return, rent-conversion, brokerage-fee 3개 부동산 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 임대차 관련 계산기의 입력 검증, 결과 표시, UX 흐름 표준화 — 사용자가 잘못된 값 입력 시 명확한 안내 제공
Output: 3개 계산기 모두 에러/경고 표시, 필수 필드 구분, 숫자 입력 정규화 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-ux/30-CONTEXT.md
@src/app/tools/realty/deposit-return/page.tsx
@src/app/tools/realty/rent-conversion/page.tsx
@src/app/tools/realty/brokerage-fee/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: deposit-return 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/realty/deposit-return/page.tsx</files>
  <read_first>src/app/tools/realty/deposit-return/page.tsx</read_first>
  <action>
deposit-return/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03):

**현황 파악 (실제 코드 기준):**
- `handleCalculate`에서 `if (!depositVal || depositVal <= 0 || !daysVal || daysVal <= 0 || !rateVal || rateVal <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음 — 추가 필요
- `days` 필드: `type="number"` — INPUT-04 위반 (문자 입력 가능)
- `rate` 필드: `type="number"` — INPUT-04 위반
- 보증금 비현실값 상한 검증 없음 (INPUT-03)
- 필드 라벨에 * 없음 (FLOW-03)
- 초기 상태에서 result null → FLOW-01 이미 OK (`{result !== null && ...}` 패턴)
- RESULT-03: `total = deposit + interest` — 부분합 맞음, 확인만

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 리팩터 (handleCalculate 내):**
1. 시작 시 `setError(null); setWarning(null);` 호출
2. INPUT-01/02: `if (!deposit || !depositVal || depositVal <= 0)` → `setError('보증금을 입력해주세요.')` + `setResult(null)` + return
3. INPUT-01/02: `if (!days || !daysVal || daysVal <= 0)` → `setError('반환 지연일수를 입력해주세요.')` + `setResult(null)` + return
4. INPUT-01: `if (!rateVal || rateVal <= 0)` → `setError('연체이자율을 입력해주세요.')` + `setResult(null)` + return
5. INPUT-03: `if (depositVal > 10_000_000_000)` → `setWarning('보증금이 100억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용, return하지 않음)

**INPUT-04: days 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="numeric"`
- `onChange` 수정: `e => setDays(e.target.value.replace(/[^0-9]/g, ''))`
- `min="1"` 제거 (type=text이므로 무의미)
- value 그대로 유지 (`value={days}`)

**INPUT-04: rate 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="numeric"` + `pattern="[0-9]*\.?[0-9]*"`
- `onChange` 수정: `e => setRate(e.target.value.replace(/[^0-9.]/g, ''))`
- `step="0.1"` + `min="0"` 제거
- value 그대로 유지 (`value={rate}`)

**FLOW-03: 필수 필드 * 추가:**
- "보증금 (원)" → "보증금 (원) *"
- "반환 지연일수 (일)" → "반환 지연일수 (일) *"
- "연체이자율 (%)" → "연체이자율 (%)" (default 5% 있어서 선택 아님이지만, 변경 가능하므로 필수로 표시) → "연체이자율 (%) *"

**에러/경고 표시 위치:** 계산하기 버튼 바로 위에 추가:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**EDGE-01:** daysVal=1 (최솟값), rateVal=5 (기본값) → interest = Math.floor(depositVal * 0.05 * 1/365) — 정상 작동. 확인만.
**EDGE-03:** 나누기 0 없음 (365는 상수). rateVal/100은 0 이하 검증으로 처리.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|보증금을 입력\|지연일수를 입력\|100억\|inputMode" src/app/tools/realty/deposit-return/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" deposit-return/page.tsx returns match (에러 state 사용)
- grep "보증금을 입력\|지연일수를 입력" deposit-return/page.tsx returns match (INPUT-02 에러 메시지)
- grep "100억\|10_000_000_000\|10000000000" deposit-return/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" deposit-return/page.tsx returns match (에러 스타일)
- grep "text-orange-500" deposit-return/page.tsx returns match (경고 스타일)
- grep "inputMode" deposit-return/page.tsx count >= 3 (모든 숫자 필드 numeric inputMode)
- grep "\*" deposit-return/page.tsx returns match (FLOW-03 필수 표시)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>deposit-return 계산기가 16개 요구사항 충족. 필수 입력 에러, 100억 경고, 숫자 필드 text 타입, 필수 * 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: rent-conversion 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/realty/rent-conversion/page.tsx</files>
  <read_first>src/app/tools/realty/rent-conversion/page.tsx</read_first>
  <action>
rent-conversion/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

**현황 파악 (실제 코드 기준):**
- `handleCalculate`에서 모두 조용한 return — 에러 없음
- 전세→월세 모드: `if (depositVal >= jeonseVal) return;` — 보증금이 전세금 이상일 때 조용히 fail
- `conversionRate` 필드: `type="number"` — INPUT-04 위반
- 전세금 비현실값 상한 없음 (INPUT-03)
- 필드 라벨에 * 없음 (FLOW-03)
- RESULT-01: 결과가 0원인 경우 → (전세금 - 보증금)이 0에 매우 가까울 때 `Math.floor(...) = 0` 가능, 안내 없음
- CONSIST-02: 결과 표시에 /월 단위 있음 (OK)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 리팩터 (handleCalculate 내):**
1. 시작 시 `setError(null); setWarning(null);`
2. 전환율 검증: `if (!rateVal || rateVal <= 0)` → `setError('전환율을 입력해주세요.')` + `setResult(null)` + return
3. 전환율 상한 검증 (EDGE-01): `if (rateVal > 100)` → `setError('전환율은 100% 이하여야 합니다.')` + `setResult(null)` + return
4. **jeonse-to-wolse 모드:**
   - `if (!jeonseVal || jeonseVal <= 0)` → `setError('전세금을 입력해주세요.')` + `setResult(null)` + return
   - `if (depositVal >= jeonseVal)` → `setError('전환 후 보증금이 전세금 이상이면 전환할 금액이 없습니다.')` + `setResult(null)` + return (기존 조용한 return 교체)
   - INPUT-03: `if (jeonseVal > 1_000_000_000)` → `setWarning('전세금이 10억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)
5. **wolse-to-jeonse 모드:**
   - `if (!wolseVal || wolseVal <= 0)` → `setError('월세를 입력해주세요.')` + `setResult(null)` + return
   - INPUT-03: `if (wolseVal > 10_000_000)` → `setWarning('월세가 1,000만원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**RESULT-01: 월세 0원 안내:**
결과 렌더 시 `result.mode === 'jeonse-to-wolse' && result.amount === 0`이면:
```jsx
<div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
  <p className="text-sm text-blue-600">전환 월세가 0원입니다. 전환할 금액 차이가 매우 작거나 전환율이 낮습니다.</p>
</div>
```

**INPUT-04: conversionRate 필드 수정:**
- `type="number"` → `type="text"` + `inputMode="decimal"`
- `onChange` 수정: `e => setConversionRate(e.target.value.replace(/[^0-9.]/g, ''))`
- `step="0.1"` + `min="0"` 제거

**FLOW-03: 필수 필드 * 추가:**
- 전세→월세 모드: "전세금 (원)" → "전세금 (원) *"
- 월세→전세 모드: "월세 (원)" → "월세 (원) *"
- "전환율 (%)" → "전환율 (%) *"
- 보증금은 선택 (0 가능) — * 없음

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**EDGE-03 (나누기 0):** `rateVal / 100`에서 rateVal=0은 위에서 차단. `wolse-to-jeonse` 모드: `jeonseAmount = depositVal + wolseVal * 12 / (rateVal / 100)` — rateVal > 0 보장됨.

**CONSIST-01:** deposit 필드가 jeonse-to-wolse와 wolse-to-jeonse 모두에서 동일한 입력 패턴 (text + inputMode=numeric) — 이미 OK.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|전세금을 입력\|월세를 입력\|전환율을 입력\|10억\|보증금이 전세금" src/app/tools/realty/rent-conversion/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" rent-conversion/page.tsx returns match (에러 state 사용)
- grep "전세금을 입력\|월세를 입력\|전환율을 입력" rent-conversion/page.tsx returns match (INPUT-02)
- grep "보증금이 전세금" rent-conversion/page.tsx returns match (논리 에러 처리)
- grep "10억\|1_000_000_000\|1000000000" rent-conversion/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" rent-conversion/page.tsx returns match (에러 스타일)
- grep "text-orange-500" rent-conversion/page.tsx returns match (경고 스타일)
- grep "\*" rent-conversion/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>rent-conversion 계산기가 16개 요구사항 충족. 필수 입력 에러, 보증금>=전세금 에러, 비현실값 경고, 전환율 text 입력, 필수 * 표시 완료.</done>
</task>

<task type="auto">
  <name>Task 3: brokerage-fee 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/realty/brokerage-fee/page.tsx</files>
  <read_first>src/app/tools/realty/brokerage-fee/page.tsx</read_first>
  <action>
brokerage-fee/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

**현황 파악 (실제 코드 기준):**
- `handleCalculate`에서 `if (!amountVal || amountVal <= 0) return;` — 조용한 return
- 월세 모드: `if (!rentVal || rentVal <= 0) return;` — 조용한 return
- `error` state 없음 — 추가 필요
- 비현실값 상한 없음 (INPUT-03)
- 필드 라벨에 * 없음 (FLOW-03)
- **버그 발견:** 결과 영역의 부가세 안내 박스가 dark 테마 색상 (`bg-yellow-500/10 border border-yellow-500/30 text-yellow-400`) — 라이트 테마 프로젝트에서 가독성 불량 (CONSIST-03)
- RESULT-03: `total = fee + vat`, `vat = Math.floor(fee * 0.1)` — 맞음. 확인만.
- RESULT-02: 모든 금액 `toLocaleString('ko-KR')` — 이미 OK

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 리팩터 (handleCalculate 내):**
1. 시작 시 `setError(null); setWarning(null);`
2. INPUT-01/02: `if (!amount || !amountVal || amountVal <= 0)` → `setError('거래금액(보증금)을 입력해주세요.')` + `setResult(null)` + return
3. 월세 모드 INPUT-01/02: `if (!monthlyRent || !rentVal || rentVal <= 0)` → `setError('월세를 입력해주세요.')` + `setResult(null)` + return
4. INPUT-03: `if (amountVal > 5_000_000_000)` (50억) → `setWarning('거래금액이 50억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)
5. INPUT-03: 월세 모드 `if (rentVal > 10_000_000)` (1,000만원) → `setWarning('월세가 1,000만원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**CONSIST-03: 부가세 안내 박스 색상 수정:**
기존: `className="mb-4 p-3 rounded-lg bg-yellow-500/10 border border-yellow-500/30"`
수정: `className="mb-4 p-3 rounded-lg bg-yellow-50 border border-yellow-200"`
기존 텍스트: `className="text-xs text-yellow-400"`
수정: `className="text-xs text-yellow-700"`

**FLOW-03: 필수 필드 * 추가:**
- "거래금액 (원)" → "거래금액 (원) *" (sale 모드)
- "보증금 (원)" → "보증금 (원) *" (lease 모드)
- 월세 모드: "월세 (원)" → "월세 (원) *"
- 단, label이 조건부 렌더링 (`txType === 'sale' ? '거래금액 (원)' : '보증금 (원)'`) — 두 케이스 모두 * 추가

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**EDGE-01:** amountVal=1 (매우 작은 금액) → `getSaleRate(1)` returns `{ rate: 0.006, cap: 250_000 }`, `rawFee = Math.floor(1 * 0.006) = 0`, `capApplied = false` (0 < 250000). fee=0. 결과 표시에 0원이 나오는데 RESULT-01 처리 필요.
  - 결과 렌더 시 `result.fee === 0`이면 안내:
  ```jsx
  {result.fee === 0 && (
    <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
      <p className="text-sm text-blue-600">거래금액이 매우 작아 중개보수가 0원으로 계산되었습니다.</p>
    </div>
  )}
  ```

**EDGE-03:** 나누기 0 없음. `Math.floor(transactionAmount * rateInfo.rate)` — transactionAmount > 0 보장됨.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|거래금액.*입력\|월세를 입력\|50억\|yellow-50\|yellow-700" src/app/tools/realty/brokerage-fee/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" brokerage-fee/page.tsx returns match (에러 state 사용)
- grep "거래금액.*입력\|입력해주세요" brokerage-fee/page.tsx returns match (INPUT-02)
- grep "50억\|5_000_000_000\|5000000000" brokerage-fee/page.tsx returns match (INPUT-03 경고)
- grep "text-red-500" brokerage-fee/page.tsx returns match (에러 스타일)
- grep "text-orange-500" brokerage-fee/page.tsx returns match (경고 스타일)
- grep "yellow-50\|yellow-700" brokerage-fee/page.tsx returns match (CONSIST-03 박스 색상 수정)
- grep "\*" brokerage-fee/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>brokerage-fee 계산기가 16개 요구사항 충족. 필수 입력 에러, 비현실값 경고, 부가세 박스 라이트 테마 색상 수정, 중개보수 0원 안내, 필수 * 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 3개 계산기 모두 에러 메시지 패턴 동일 (text-red-500 / text-orange-500)
- grep "setError" 각 파일에서 매치 확인
- grep "inputMode" 각 파일에서 숫자 필드 모두 numeric/decimal inputMode 확인
</verification>

<success_criteria>
- deposit-return, rent-conversion, brokerage-fee 3개 계산기 모두 필수 입력 에러·경고 처리 완료
- 숫자 필드 type=text + inputMode=numeric 통일 (INPUT-04)
- 초기 상태 0원 미노출 (FLOW-01)
- 필수 필드 * 시각적 구분 (FLOW-03)
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/30-ux/30-01-SUMMARY.md`
</output>
