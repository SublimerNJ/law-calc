---
phase: 30-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/realty/subscription-score/page.tsx
  - src/app/tools/realty/dsr/page.tsx
  - src/app/tools/realty/ltv/page.tsx
  - src/app/tools/realty/dti/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "subscription-score에서 초기 상태(모든 select 0번 index)에서 계산하기를 누르면 정상 결과(가장 낮은 점수) 표시"
    - "subscription-score에서 합계가 무주택기간+부양가족+통장기간 합산과 일치 (RESULT-03)"
    - "subscription-score에서 FLOW-01: 초기 로드 시 결과 미노출 확인"
    - "dsr에서 연소득 미입력 시 에러 표시, 결과 미출력"
    - "dsr에서 연소득 10억 초과 시 경고 표시"
    - "dsr에서 연소득 필드에 * 표시"
    - "dsr에서 dark 테마 색상(green-900/30, red-900/30, green-400, red-400) → light 색상으로 수정"
    - "ltv에서 주택가격 또는 대출금액 미입력 시 에러 표시"
    - "ltv에서 대출금액이 주택가격 초과 시 경고 표시"
    - "ltv에서 주택가격 50억 초과 시 경고 표시"
    - "ltv에서 light 테마: 적합/초과 배지 dark 색상 수정 (green-500/20 text-green-400 → bg-green-100 text-green-700)"
    - "dti에서 연소득 미입력 시 에러 표시"
    - "dti에서 주담대 월 원리금 미입력 시 에러 표시"
    - "dti에서 연소득 10억 초과 시 경고 표시"
    - "dti에서 필수 필드 * 표시"
    - "4개 계산기 모두 에러: text-red-500, 경고: text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/realty/subscription-score/page.tsx"
      provides: "청약가점 계산기 with FLOW-01 확인, RESULT-03 합산 검증"
    - path: "src/app/tools/realty/dsr/page.tsx"
      provides: "DSR 계산기 with error/warning state, light 테마 색상 수정"
    - path: "src/app/tools/realty/ltv/page.tsx"
      provides: "LTV 계산기 with error/warning state, light 테마 배지 수정"
    - path: "src/app/tools/realty/dti/page.tsx"
      provides: "DTI 계산기 with error/warning state, 필수 필드 *"
  key_links:
    - from: "dsr handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "ltv handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "dti handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
subscription-score, dsr, ltv, dti 4개 부동산 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 금융(대출/청약) 관련 계산기의 입력 검증, 결과 표시, UX 흐름 표준화 — dark 테마 잔재 색상 제거, 에러/경고 통일
Output: 4개 계산기 모두 에러/경고 표시, 라이트 테마 색상 통일, 필수 필드 구분 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-ux/30-CONTEXT.md
@src/app/tools/realty/subscription-score/page.tsx
@src/app/tools/realty/dsr/page.tsx
@src/app/tools/realty/ltv/page.tsx
@src/app/tools/realty/dti/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: subscription-score 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/realty/subscription-score/page.tsx</files>
  <read_first>src/app/tools/realty/subscription-score/page.tsx</read_first>
  <action>
subscription-score/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03):

**현황 파악 (실제 코드 기준):**
- 입력 방식: 3개 select (index 기반) — 항상 유효한 값 선택됨. INPUT-01/02 위반 없음.
- `handleCalculate`: `homelessOptions[homelessIdx].score` — 항상 숫자. 에러 가능성 없음.
- FLOW-01: `result` 초기값 null → `{result !== null && ...}` — 이미 OK
- RESULT-03: `total = homeless + dependent + savings` — 합산 정확. 결과 테이블에서 합계행 = `result.total` — OK
- INPUT-04: select 필드이므로 숫자 입력 이슈 없음
- EDGE-01: 최솟값 (homelessIdx=0 → 0점, dependentIdx=0 → 5점, savingsIdx=0 → 1점) = 6점. 최댓값 (32+35+17) = 84점. 모두 논리적.
- **발견된 문제:**
  1. FLOW-03: 3개 select 라벨에 모두 * 없음. select 필드는 항상 값이 있으므로 필수/선택 구분이 의미 없어 보이나, 실제 계산에 3개 항목 모두 필요 → * 추가
  2. CONSIST-03: 에러 메시지 스타일 — 이 계산기는 에러 없음. 하지만 다른 계산기와 통일을 위해 pattern 확인 필요
  3. FLOW-02: 입력 변경 시 결과가 즉시 업데이트되지 않음 (버튼 클릭 패턴 — 일관성은 OK, 모든 계산기가 동일 패턴)

**수정 항목:**

1. **FLOW-03: 필수 필드 * 추가:**
   - "무주택기간 (최대 32점)" → "무주택기간 (최대 32점) *"
   - "부양가족수 (최대 35점)" → "부양가족수 (최대 35점) *"
   - "청약통장 가입기간 (최대 17점)" → "청약통장 가입기간 (최대 17점) *"

2. **RESULT-01: 합계 0점 불가능** (최솟값 6점) — 별도 안내 불필요.

3. **RESULT-03 확인 표시:** 결과 테이블 합계행이 `result.homeless + result.dependent + result.savings`와 일치하는지 코드로 확인. 현재 `{result.total}점` — result.total = homeless + dependent + savings (handleCalculate에서 설정). 맞음.

4. **EDGE-01: 경계값 확인:** homelessIdx=0 ('만 30세 미만 미혼' → score: 0). dependentOptions[0] = '0명' → score: 5 (부양가족 0명도 5점). savingsOptions[0] = '6개월 미만' → score: 1. 총 6점. 84점 만점 표시 OK.

5. **CONSIST-02: 결과 단위 "점" 표시** — 이미 일관성 있음. 확인만.

**변경이 필요없는 항목:**
- INPUT-01~04: select 기반이므로 해당 없음 (유효값만 선택 가능)
- EDGE-02: 날짜 입력 없음
- EDGE-03: 나누기 없음
- FLOW-01: result null 초기값 이미 OK

**결론:** subscription-score는 select 기반으로 대부분 요구사항이 자연스럽게 충족. 필수 * 추가만 수정.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "\*" src/app/tools/realty/subscription-score/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "\*" subscription-score/page.tsx returns count >= 3 (3개 라벨 모두 * 추가)
- grep "32점.*\*\|35점.*\*\|17점.*\*\|무주택기간.*\*\|부양가족수.*\*\|청약통장.*\*" subscription-score/page.tsx returns match (FLOW-03)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>subscription-score 계산기 감사 완료. 선택 기반 입력으로 INPUT 이슈 없음. 필수 필드 * 추가 완료. 합산 논리 정확.</done>
</task>

<task type="auto">
  <name>Task 2: dsr + ltv + dti 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/realty/dsr/page.tsx, src/app/tools/realty/ltv/page.tsx, src/app/tools/realty/dti/page.tsx</files>
  <read_first>src/app/tools/realty/dsr/page.tsx, src/app/tools/realty/ltv/page.tsx, src/app/tools/realty/dti/page.tsx</read_first>
  <action>
dsr, ltv, dti 3개 파일을 각각 전체 읽고 16개 요구사항 감사 후 즉시 수정. 각 파일 순서대로 수정 (per D-03, D-04):

---

### DSR (dsr/page.tsx) 수정:

**현황:**
- `if (annualIncome <= 0) return;` — 조용한 return
- `error` state 없음
- 필드 라벨에 * 없음
- **버그:** 결과 영역에 dark 테마 잔재 색상:
  - 적합 박스: `bg-green-900/30 border border-green-700 text-green-400`
  - 초과 박스: `bg-red-900/30 border border-red-700 text-red-400`
- 연소득 비현실값 상한 없음 (INPUT-03)
- RESULT-03: `annualRepayment` 세부 항목 없음 (총 합산만 표시) — 합계가 단순 표시라 확인 불가, 하지만 계산식 표시에 각 항목 있음 → OK

**dsr 추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**dsr INPUT 검증 (handleCalculate 내):**
1. 시작 시 `setError(null); setWarning(null);`
2. INPUT-01/02: `if (!income || parseNum(income) <= 0)` → `setError('연소득을 입력해주세요.')` + `setResult(null)` + return
3. INPUT-03: `if (parseNum(income) > 1_000_000_000)` → `setWarning('연소득이 10억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)
4. INPUT-03: `if (parseNum(mortgage) + parseNum(credit) + parseNum(other) > parseNum(income))` → 월 상환액이 월 소득(연소득÷12) 초과: `setWarning('월 상환액 합계가 월 소득을 초과합니다. 입력값을 확인해주세요.')` (계산 허용, DSR > 100% 나올 수 있음)

**dsr CONSIST-03: dark 테마 색상 수정:**
- 적합 박스: `bg-green-900/30 border border-green-700` → `bg-green-50 border border-green-300`
- 적합 텍스트: `text-green-400` → `text-green-700`
- 초과 박스: `bg-red-900/30 border border-red-700` → `bg-red-50 border border-red-300`
- 초과 텍스트: `text-red-400` → `text-red-600`

**dsr FLOW-03: 필수 필드 *:**
- "연소득 (원)" → "연소득 (원) *"
- 대출 상환액 3개 필드: 선택(없으면 0) — * 없음

**dsr 에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**dsr EDGE-03:** `monthlyTotal * 12 / annualIncome` — annualIncome > 0 보장됨.

---

### LTV (ltv/page.tsx) 수정:

**현황:**
- `if (!price || price <= 0 || !loan || loan <= 0) return;` — 조용한 return
- `error` state 없음
- 필드 라벨에 * 없음
- **버그:** 결과 영역 dark 테마 잔재:
  - 적합: `bg-green-500/20 text-green-400`
  - 초과: `bg-red-500/20 text-red-400`
- 비현실값 상한 없음
- **논리 버그 발견:** 대출금액이 주택가격보다 클 때 LTV > 100% → 계산 허용되나 경고 없음 (EDGE-01)

**ltv 추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**ltv INPUT 검증 (handleCalculate 내):**
1. 시작 시 `setError(null); setWarning(null);`
2. INPUT-01/02: `if (!housePrice || price <= 0)` → `setError('주택 가격을 입력해주세요.')` + `setResult(null)` + return
3. INPUT-01/02: `if (!loanAmount || loan <= 0)` → `setError('대출 희망 금액을 입력해주세요.')` + `setResult(null)` + return
4. EDGE-01: `if (loan > price)` → `setWarning('대출 희망 금액이 주택 가격을 초과합니다. LTV가 100%를 넘을 수 있습니다.')` (계산 허용)
5. INPUT-03: `if (price > 5_000_000_000)` → `setWarning('주택 가격이 50억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용, 경고 중 warning을 덮어쓰지 않도록 이미 warning이 있으면 추가 안 함)

**ltv CONSIST-03: dark 테마 배지 색상 수정:**
- 적합: `bg-green-500/20 text-green-400` → `bg-green-100 text-green-700`
- 초과: `bg-red-500/20 text-red-400` → `bg-red-100 text-red-700`

**ltv FLOW-03: 필수 필드 *:**
- "주택 가격 (원)" → "주택 가격 (원) *"
- "대출 희망 금액 (원)" → "대출 희망 금액 (원) *"
- "지역 구분"은 select (기본값 있음) — * 선택사항, 추가하지 않음

**ltv 에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

---

### DTI (dti/page.tsx) 수정:

**현황:**
- `if (!income || income <= 0 || !mortgage || mortgage < 0) return;` — 조용한 return (mortgage < 0도 체크하지만 에러 없음)
- `error` state 없음
- 필드 라벨에 * 없음
- 비현실값 상한 없음
- **버그:** `!mortgage || mortgage < 0` 조건 — mortgage=0이면 `!mortgage`가 true → return. 하지만 주담대 0원인 경우도 DTI 계산 가능 (기타 대출 이자만 있을 때). 조건 수정 필요.
- RESULT-03: `dti = (annualMortgage + annualOther) / income * 100` — 세부 항목 합산 표시 OK (결과 영역에 연간 주담대 원리금 + 연간 기타 이자 각각 표시)

**dti 추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**dti INPUT 검증 (handleCalculate 내):**
1. 시작 시 `setError(null); setWarning(null);`
2. INPUT-01/02: `if (!annualIncome || income <= 0)` → `setError('연소득을 입력해주세요.')` + `setResult(null)` + return
3. INPUT-01/02: **버그 수정** — `if (!monthlyMortgage || mortgage <= 0)` 를 `if (mortgage < 0)` 으로 수정 (0원 허용). 단 완전히 비어있으면 안내: `if (!monthlyMortgage)` → `setError('주택담보대출 월 원리금을 입력해주세요. (없으면 0을 입력해주세요.)')` + return
4. INPUT-03: `if (income > 1_000_000_000)` → `setWarning('연소득이 10억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)
5. EDGE-01: `if ((mortgage + (parseInt(monthlyOtherInterest, 10) || 0)) * 12 > income)` → `setWarning('연간 상환액이 연소득을 초과합니다. DTI가 100%를 넘을 수 있습니다.')` (계산 허용)

**dti FLOW-03: 필수 필드 *:**
- "연소득 (원)" → "연소득 (원) *"
- "주택담보대출 월 원리금 상환액 (원)" → "주택담보대출 월 원리금 상환액 (원) *"
- "기타 대출 월 이자" — 선택 (없으면 0) → * 없음

**dti 에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**dti EDGE-03:** `dti = ... / income * 100` — income > 0 보장됨.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; for f in dsr ltv dti; do echo "=== $f ==="; grep -c "setError\|text-red-500\|text-orange-500\|입력해주세요\|green-50\|green-100\|red-50\|red-100" "src/app/tools/realty/$f/page.tsx"; done</automated>
  </verify>
  <acceptance_criteria>
- dsr/page.tsx: grep "setError" returns match, grep "연소득을 입력" returns match (INPUT-02)
- dsr/page.tsx: grep "green-50\|green-700" returns match (dark→light 색상 수정)
- dsr/page.tsx: grep "red-50\|red-600" returns match (dark→light 색상 수정)
- ltv/page.tsx: grep "setError" returns match, grep "주택 가격을 입력\|대출.*입력" returns match (INPUT-02)
- ltv/page.tsx: grep "green-100\|text-green-700" returns match (dark→light 배지 수정)
- ltv/page.tsx: grep "대출.*주택 가격을 초과\|LTV.*100%" returns match (EDGE-01 경고)
- dti/page.tsx: grep "setError" returns match, grep "연소득을 입력\|원리금을 입력" returns match (INPUT-02)
- dti/page.tsx: grep "\*" returns match (FLOW-03)
- 3개 파일 모두 grep "text-red-500" returns match (에러 스타일)
- 3개 파일 모두 grep "text-orange-500" returns match (경고 스타일)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>dsr, ltv, dti 3개 계산기가 16개 요구사항 충족. 연소득·주택가격 필수 입력 에러, dark 테마 잔재 색상 라이트 테마로 수정, LTV 초과 경고, DTI mortgage=0 버그 수정, 필수 * 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 4개 계산기 모두 에러 메시지 패턴 동일 (text-red-500 / text-orange-500)
- dsr, ltv 결과 영역의 dark 테마 색상(green-900/30, red-900/30, green-400, red-400) 미사용 확인
- grep "setError" 각 파일에서 매치 확인 (subscription-score 제외)
</verification>

<success_criteria>
- subscription-score: 필수 * 추가, 논리 검증 통과
- dsr: 에러/경고 state 추가, dark 테마 적합/초과 박스 색상 수정, 연소득 * 표시
- ltv: 에러/경고 state 추가, dark 테마 배지 색상 수정, 대출>주택가격 경고, 필수 * 표시
- dti: 에러/경고 state 추가, mortgage=0 버그 수정, 연소득/원리금 * 표시
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/30-ux/30-02-SUMMARY.md`
</output>
