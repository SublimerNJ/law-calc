---
phase: 30-ux
plan: 03
type: execute
wave: 2
depends_on: [30-01, 30-02]
files_modified:
  - src/app/tools/realty/deposit-return/page.tsx
  - src/app/tools/realty/rent-conversion/page.tsx
  - src/app/tools/realty/brokerage-fee/page.tsx
  - src/app/tools/realty/subscription-score/page.tsx
  - src/app/tools/realty/dsr/page.tsx
  - src/app/tools/realty/ltv/page.tsx
  - src/app/tools/realty/dti/page.tsx
autonomous: true
requirements: [CONSIST-01, CONSIST-02, CONSIST-03, RESULT-02, FLOW-03]

must_haves:
  truths:
    - "7개 계산기 모두 에러 메시지가 text-red-500 클래스 사용 (CONSIST-03)"
    - "7개 계산기 모두 경고 메시지가 text-orange-500 클래스 사용 (CONSIST-03)"
    - "7개 계산기 모두 숫자 금액 필드에 천 단위 콤마 표시 (RESULT-02)"
    - "7개 계산기 중 에러/경고 state가 있는 모든 파일에서 setError(null) + setWarning(null)이 handleCalculate 시작 시 호출"
    - "7개 계산기의 법적 근거 텍스트가 동일한 text-xs text-gray-500 스타일"
  artifacts:
    - path: "src/app/tools/realty/deposit-return/page.tsx"
      provides: "일관성 최종 검증 완료"
    - path: "src/app/tools/realty/rent-conversion/page.tsx"
      provides: "일관성 최종 검증 완료"
    - path: "src/app/tools/realty/brokerage-fee/page.tsx"
      provides: "일관성 최종 검증 완료"
    - path: "src/app/tools/realty/subscription-score/page.tsx"
      provides: "일관성 최종 검증 완료"
    - path: "src/app/tools/realty/dsr/page.tsx"
      provides: "일관성 최종 검증 완료"
    - path: "src/app/tools/realty/ltv/page.tsx"
      provides: "일관성 최종 검증 완료"
    - path: "src/app/tools/realty/dti/page.tsx"
      provides: "일관성 최종 검증 완료"
  key_links:
    - from: "7개 계산기 모두"
      to: "에러/경고 표시"
      via: "text-red-500 / text-orange-500 패턴"
      pattern: "text-red-500|text-orange-500"
---

<objective>
30-01, 30-02에서 수정된 7개 부동산 계산기의 일관성을 최종 검증하고 남은 불일치를 수정

Purpose: 7개 계산기 전체에서 에러 스타일, 경고 스타일, 법적 근거 표시, 숫자 포맷이 통일되도록 마무리
Output: Phase 30 완료 — 7개 계산기 CONSIST-01~03 완전 충족
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-ux/30-CONTEXT.md
@.planning/phases/30-ux/30-01-SUMMARY.md
@.planning/phases/30-ux/30-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 7개 계산기 일관성 점검 및 잔여 수정</name>
  <files>src/app/tools/realty/deposit-return/page.tsx, src/app/tools/realty/rent-conversion/page.tsx, src/app/tools/realty/brokerage-fee/page.tsx, src/app/tools/realty/subscription-score/page.tsx, src/app/tools/realty/dsr/page.tsx, src/app/tools/realty/ltv/page.tsx, src/app/tools/realty/dti/page.tsx</files>
  <read_first>
src/app/tools/realty/deposit-return/page.tsx,
src/app/tools/realty/rent-conversion/page.tsx,
src/app/tools/realty/brokerage-fee/page.tsx,
src/app/tools/realty/subscription-score/page.tsx,
src/app/tools/realty/dsr/page.tsx,
src/app/tools/realty/ltv/page.tsx,
src/app/tools/realty/dti/page.tsx
  </read_first>
  <action>
7개 파일 모두 읽고 아래 체크리스트를 파일별로 확인. 불일치 발견 시 즉시 수정 (per D-03):

**CONSIST-03 체크리스트 (에러/경고 스타일 통일):**
각 파일에서 에러/경고 표시 패턴 확인:
- 에러: `<p className="text-red-500 text-sm mb-3">{error}</p>` 형식
- 경고: `<p className="text-orange-500 text-sm mb-3">{warning}</p>` 형식
- subscription-score는 에러/경고 state 없음 — 해당 없음 (select 기반)
- 나머지 6개: 모두 동일 패턴이어야 함

**CONSIST-03 체크리스트 (법적 근거 텍스트):**
7개 파일의 법적 근거 표시 방식 확인:
- 모두 `text-xs text-gray-500` 클래스인지 확인
- 불일치 발견 시 통일

**CONSIST-01 체크리스트 (숫자 입력 방식):**
금액 필드 입력 패턴 확인:
- 모든 금액 필드: `type="text" inputMode="numeric"` + `.replace(/[^0-9]/g, '')` 패턴
- 비율(%) 필드: `type="text" inputMode="decimal"` + `.replace(/[^0-9.]/g, '')` 패턴
- 불일치 발견 시 수정

**RESULT-02 체크리스트 (숫자 포맷):**
7개 파일에서 `formatNumber()` 또는 `.toLocaleString('ko-KR')` 사용 여부 확인:
- 모든 결과 금액 표시에 천 단위 콤마 적용 여부 확인
- DSR의 `annualRepayment` 등 중간 계산값도 포맷 적용되는지 확인

**FLOW-01 체크리스트 (초기 0원 미노출):**
7개 파일 모두 `{result !== null && (...)}` 또는 `{result && (...)}` 패턴 사용 확인
- 초기 state `null`로 설정되어 있는지 확인

**EDGE-03 체크리스트 (나누기 0 방지):**
각 파일의 계산 함수에서 나누기 사용 부분 확인:
- deposit-return: `daysVal / 365` — daysVal > 0 보장
- rent-conversion: `rateVal / 100`, `12 / (rateVal / 100)` — rateVal > 0 보장
- brokerage-fee: 나누기 없음
- subscription-score: 나누기 없음
- dsr: `annualRepayment / annualIncome` — annualIncome > 0 보장
- ltv: `loan / price * 100` — price > 0 보장
- dti: `(annualMortgage + annualOther) / income * 100` — income > 0 보장
- 불일치 발견 시 수정

**일관성 수정 우선순위:**
1. Critical: 에러 표시 없이 조용한 return이 남아있는 케이스 → setError 추가
2. Warning: 스타일 클래스 불일치 → 통일
3. Info: 주석 또는 레이블 표현 미세 차이 → 기록만 (D-02)

**30-01, 30-02 SUMMARY 확인:**
각 SUMMARY에서 "남은 이슈" 또는 "TODO" 항목이 있으면 우선 처리.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; for f in deposit-return rent-conversion brokerage-fee dsr ltv dti; do echo "=== $f: error/warning ==="; grep -c "text-red-500\|text-orange-500" "src/app/tools/realty/$f/page.tsx"; done</automated>
  </verify>
  <acceptance_criteria>
- 6개 계산기(subscription-score 제외) 모두 grep "text-red-500" returns match (에러 스타일 CONSIST-03)
- 6개 계산기(subscription-score 제외) 모두 grep "text-orange-500" returns match (경고 스타일 CONSIST-03)
- 7개 계산기 모두 grep "setError(null)\|setWarning(null)\|null.*setError\|null.*setWarning" 에러/경고 state 있는 파일에서 확인
- 7개 파일 모두 `result !== null` 또는 `result &&` 패턴으로 초기 결과 숨김
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>7개 계산기 일관성 최종 점검 완료. CONSIST-01~03 모두 충족. 에러/경고 스타일 통일, 나누기 0 방지 확인, 숫자 포맷 통일.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript 빌드 및 최종 검증</name>
  <files></files>
  <read_first></read_first>
  <action>
7개 계산기 수정 완료 후 전체 프로젝트 TypeScript 컴파일 및 빌드 검증:

1. **TypeScript 타입 검사:**
   ```bash
   cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1
   ```
   에러 발생 시 해당 파일 수정 후 재실행.

2. **대표 grep 검증 (7개 파일 모두):**
   ```bash
   # 에러 state 사용 여부 (subscription-score 제외)
   for f in deposit-return rent-conversion brokerage-fee dsr ltv dti; do
     echo "=== $f ==="
     grep -n "setError\|text-red-500\|text-orange-500\|inputMode\|\*" "src/app/tools/realty/$f/page.tsx" | head -10
   done
   ```

3. **dark 테마 잔재 색상 미사용 확인:**
   ```bash
   grep -r "green-900\|red-900\|green-400\|red-400\|yellow-400" src/app/tools/realty/ 2>/dev/null
   ```
   결과가 있으면 해당 파일에서 라이트 테마 색상으로 수정.

4. **필수 필드 * 확인:**
   ```bash
   for f in deposit-return rent-conversion brokerage-fee dsr ltv dti; do
     echo "=== $f: 필수 * ==="; grep -c '"\*\|) \*"' "src/app/tools/realty/$f/page.tsx"
   done
   ```

5. 빌드 에러 없으면 완료.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- npx tsc --noEmit exits with code 0, 에러 없음
- grep "green-900\|red-900" src/app/tools/realty/ 결과 없음 (dark 테마 잔재 제거)
- Phase 30 전체 16개 요구사항(INPUT-01~04, RESULT-01~03, FLOW-01~03, CONSIST-01~03, EDGE-01~03) 7개 계산기 모두 충족
  </acceptance_criteria>
  <done>Phase 30 부동산 UX·논리 감사 완료. 7개 계산기 모두 16개 요구사항 충족. TypeScript 에러 없음. 빌드 성공.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes for entire project
- 7개 계산기 파일 모두 에러/경고 표시 패턴 동일 (text-red-500 / text-orange-500)
- dark 테마 잔재 색상 (green-900/30, red-900/30, yellow-400 등) 미존재
- grep "result !== null\|result &&" 7개 파일 모두 매치 (FLOW-01)
- grep "\*" 에러/경고 state 있는 6개 파일 모두 매치 (FLOW-03)
</verification>

<success_criteria>
- 7개 계산기 전체 일관성 점검 완료
- CONSIST-01: 숫자 입력 방식 통일 (text + inputMode=numeric)
- CONSIST-02: 결과 표시 형식 통일 (원, %, 점 단위)
- CONSIST-03: 에러/경고 스타일 통일 (text-red-500 / text-orange-500)
- TypeScript 컴파일 에러 없음
- Phase 30 완료
</success_criteria>

<output>
After completion, create `.planning/phases/30-ux/30-03-SUMMARY.md`
</output>
