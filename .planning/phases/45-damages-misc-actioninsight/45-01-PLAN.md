---
phase: 45-damages-misc-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/damages/damages-general/page.tsx
  - src/app/tools/damages/defamation/page.tsx
  - src/app/tools/damages/lost-income/page.tsx
  - src/app/tools/damages/medical-malpractice/page.tsx
  - src/app/tools/misc/certified-letter/page.tsx
  - src/app/tools/misc/legal-aid/page.tsx
  - src/app/tools/misc/public-defender/page.tsx
  - src/app/tools/misc/statute-of-limitations/page.tsx
autonomous: true
requirements: [DATA-08, COMP-08, DATA-09, COMP-09]
must_haves:
  truths:
    - "User sees the ActionInsight tip and template ONLY after performing a calculation (result !== null)"
    - "ActionInsight section is properly spaced with mt-6 margin and placed at the very bottom of the page (before </CalculatorLayout>)"
    - "Computed amounts (total, mid, lostIncomeAfterFault) are passed as the amount prop to ActionInsight in damages calculators"
  artifacts:
    - path: "src/app/tools/damages/damages-general/page.tsx"
      provides: "ActionInsight component placed at the bottom with amount prop"
    - path: "src/app/tools/misc/certified-letter/page.tsx"
      provides: "ActionInsight component placed at the bottom without amount prop"
  key_links:
    - from: "result state"
      to: "ActionInsight render condition"
      via: "JSX conditional rendering {result !== null && (...)}"
---

<objective>
손해배상 4종, 기타 4종 계산기에 ActionInsight UI를 보완하고 전체적인 위치와 여백(mt-6)을 통일합니다.

Purpose: 사용자가 계산 결과를 확인한 후 실전 대응 팁과 템플릿을 쉽게 확인할 수 있도록 UI를 정돈합니다.
Output: 일관된 위치(페이지 최하단)와 여백으로 조건부 렌더링되는 ActionInsight 컴포넌트가 적용된 8개의 page.tsx 파일
</objective>

<context>
@src/components/ui/ActionInsight.tsx
@src/lib/action-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 손해배상 4종 ActionInsight 위치 및 여백 보완</name>
  <files>
    - src/app/tools/damages/damages-general/page.tsx
    - src/app/tools/damages/defamation/page.tsx
    - src/app/tools/damages/lost-income/page.tsx
    - src/app/tools/damages/medical-malpractice/page.tsx
  </files>
  <action>
    현재 4개 계산기 모두 `<ActionInsight calculatorId="..." amount={...} />`가 `{result !== null}` 조건으로 렌더링되고 있으나, 위치나 여백이 일관되지 않습니다.
    
    1. `damages-general` 및 `defamation`: `<ActionInsight>`가 가이드 블록(예: 손해배상 청구 절차) 이전에 위치하고 있습니다. 이를 잘라내어 페이지 최하단, 즉 `</CalculatorLayout>` 바로 앞으로 이동시킵니다.
    2. 모든 4개 파일에서 `<ActionInsight>` 컴포넌트를 `<div className="mt-6">...</div>`로 감싸 위쪽 여백을 확보합니다.
    3. 기존에 전달되고 있던 `amount` 프롭(예: `result.total`, `result.mid`, `result.lostIncomeAfterFault`)은 그대로 유지합니다.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>손해배상 4종 계산기의 ActionInsight가 mt-6 여백과 함께 페이지 최하단(가이드 블록 아래)에 위치함</done>
</task>

<task type="auto">
  <name>Task 2: 기타 4종 ActionInsight 위치 및 여백 보완</name>
  <files>
    - src/app/tools/misc/certified-letter/page.tsx
    - src/app/tools/misc/legal-aid/page.tsx
    - src/app/tools/misc/public-defender/page.tsx
    - src/app/tools/misc/statute-of-limitations/page.tsx
  </files>
  <action>
    현재 기타 4개 계산기 모두 계산 결과 블록 내부 또는 가이드 블록 이전에 `<ActionInsight>`가 불완전하게 배치되어 있습니다. (예: `<> ... <ActionInsight /> </>` 형태로 묶여 있음)
    
    1. `<ActionInsight calculatorId="..." />` 컴포넌트를 계산 결과 블록이나 프래그먼트 내부에서 빼내어 페이지 최하단(`</CalculatorLayout>` 바로 앞)으로 이동시킵니다.
    2. 이동한 컴포넌트를 `{result !== null && <div className="mt-6"><ActionInsight calculatorId="..." /></div>}` 형태로 감싸 조건부로 렌더링되게 하고 위쪽 여백을 줍니다.
    3. 기존 컴포넌트 위치에 남아있는 불필요한 프래그먼트(`<></>`)나 구조적 오류가 있다면 정리합니다.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>기타 4종 계산기의 ActionInsight가 mt-6 여백과 함께 페이지 최하단(가이드 블록 아래)에 위치함</done>
</task>

</tasks>

<verification>
- 8개 파일 모두 `<ActionInsight>`가 `{result !== null}` 조건 안에 감싸져 있고 `<div className="mt-6">`로 둘러싸여 있는지 확인
- 8개 파일 모두 `<ActionInsight>` 렌더링 블록이 페이지 최하단(모든 가이드 블록 아래, `</CalculatorLayout>` 바로 위)에 위치하는지 확인
</verification>

<success_criteria>
- 손해배상 4종, 기타 4종 계산기에서 ActionInsight UI가 올바른 여백과 위치에 노출됩니다.
- Typescript 및 Lint 에러가 발생하지 않습니다.
</success_criteria>

<output>
After completion, create `.planning/phases/45-damages-misc-actioninsight/45-01-SUMMARY.md`
</output>
