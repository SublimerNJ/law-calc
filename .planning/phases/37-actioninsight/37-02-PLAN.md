---
phase: 37-actioninsight
plan: 2
type: execute
wave: 2
depends_on: ["37-01"]
files_modified: [
  "src/app/tools/realty/deposit-return/page.tsx",
  "src/app/tools/realty/rent-conversion/page.tsx",
  "src/app/tools/realty/brokerage-fee/page.tsx",
  "src/app/tools/realty/subscription-score/page.tsx"
]
autonomous: true
requirements: ["UI-05"]
must_haves:
  truths:
    - "보증금 반환, 전월세 전환율, 중개보수, 청약가점 계산 결과 화면에서 ActionInsight 팁이 표시된다"
  artifacts:
    - path: "src/app/tools/realty/deposit-return/page.tsx"
      provides: "ActionInsight component rendering"
    - path: "src/app/tools/realty/rent-conversion/page.tsx"
      provides: "ActionInsight component rendering"
    - path: "src/app/tools/realty/brokerage-fee/page.tsx"
      provides: "ActionInsight component rendering"
    - path: "src/app/tools/realty/subscription-score/page.tsx"
      provides: "ActionInsight component rendering"
  key_links:
    - from: "UI components"
      to: "ActionInsight component"
      via: "prop passing (calculatorId, amount)"
---

<objective>
부동산 4개 계산기(보증금 반환, 전월세 전환율, 중개보수, 청약가점)의 결과 화면에 ActionInsight 컴포넌트를 연동하여 실전 팁과 템플릿을 표시한다.

Purpose: 사용자가 계산 결과를 확인한 후, 그 결과(금액/점수)를 바탕으로 즉각적인 법적/실무적 대응을 할 수 있도록 지원한다.
Output: 각 `page.tsx`에 `<ActionInsight>` 컴포넌트 렌더링.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/37-actioninsight/37-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 보증금 반환, 전월세 전환율 UI 연동</name>
  <files>
    src/app/tools/realty/deposit-return/page.tsx
    src/app/tools/realty/rent-conversion/page.tsx
  </files>
  <action>
    1. 각 파일 상단에 `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가
    2. `result !== null` 블록 내부 하단(법적 근거 문구 아래 등)에 `<ActionInsight>` 컴포넌트 추가
    3. `calculatorId="deposit-return"` 및 `calculatorId="rent-conversion"` 지정
    4. `amount` prop에 주요 결과값 전달 (예: deposit-return은 보증금 또는 우선변제금액, rent-conversion은 `result.amount`)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>두 계산기에 ActionInsight 컴포넌트가 추가되고 빌드 성공.</done>
</task>

<task type="auto">
  <name>Task 2: 중개보수, 청약가점 UI 연동</name>
  <files>
    src/app/tools/realty/brokerage-fee/page.tsx
    src/app/tools/realty/subscription-score/page.tsx
  </files>
  <action>
    1. 각 파일 상단에 `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가
    2. `result !== null` 블록 내부 하단에 `<ActionInsight>` 컴포넌트 추가
    3. `calculatorId="brokerage-fee"` 및 `calculatorId="subscription-score"` 지정
    4. `amount` prop에 주요 결과값 전달 (예: brokerage-fee는 `result.fee`, subscription-score는 `result.total`)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>두 계산기에 ActionInsight 컴포넌트가 추가되고 빌드 성공.</done>
</task>

</tasks>

<verification>
수정된 4개의 페이지 컴포넌트가 타입 에러 없이 빌드되며, 기존 UI 흐름을 깨뜨리지 않는다.
</verification>

<success_criteria>
4개의 부동산 계산기 결과 섹션에 ActionInsight 컴포넌트가 정상적으로 렌더링된다.
</success_criteria>

<output>
After completion, create `.planning/phases/37-actioninsight/37-02-SUMMARY.md`
</output>