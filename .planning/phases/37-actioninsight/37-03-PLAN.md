---
phase: 37-actioninsight
plan: 3
type: execute
wave: 2
depends_on: ["37-01"]
files_modified: [
  "src/app/tools/realty/dsr/page.tsx",
  "src/app/tools/realty/ltv/page.tsx",
  "src/app/tools/realty/dti/page.tsx"
]
autonomous: true
requirements: ["UI-05"]
must_haves:
  truths:
    - "DSR, LTV, DTI 계산 결과 화면에서 ActionInsight 팁이 표시된다"
  artifacts:
    - path: "src/app/tools/realty/dsr/page.tsx"
      provides: "ActionInsight component rendering"
    - path: "src/app/tools/realty/ltv/page.tsx"
      provides: "ActionInsight component rendering"
    - path: "src/app/tools/realty/dti/page.tsx"
      provides: "ActionInsight component rendering"
  key_links:
    - from: "UI components"
      to: "ActionInsight component"
      via: "prop passing (calculatorId, amount)"
---

<objective>
대출 규제 3종 계산기(DSR, LTV, DTI)의 결과 화면에 ActionInsight 컴포넌트를 연동하여 실전 팁과 템플릿을 표시한다.

Purpose: 사용자가 대출 한도 및 규제 결과를 확인한 후, 예외 적용 또는 추가 한도 상담 등을 은행과 진행할 때 유용한 템플릿을 제공한다.
Output: 3개의 `page.tsx`에 `<ActionInsight>` 컴포넌트 렌더링.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/37-actioninsight/37-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DSR, LTV, DTI UI 연동</name>
  <files>
    src/app/tools/realty/dsr/page.tsx
    src/app/tools/realty/ltv/page.tsx
    src/app/tools/realty/dti/page.tsx
  </files>
  <action>
    1. 각 파일 상단에 `import { ActionInsight } from '@/components/ui/ActionInsight';` 추가
    2. `result !== null` 블록 내부 하단(법적 근거 문구 등 아래)에 `<ActionInsight>` 컴포넌트 추가
    3. `calculatorId="dsr"`, `calculatorId="ltv"`, `calculatorId="dti"` 지정
    4. `amount` prop에 주요 결과값(백분율 결과 또는 최대 대출 가능 금액 등 의미 있는 값)을 전달 (예: `result.dsr`, `result.ltv`, `result.dti` 등)
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>3개의 대출 계산기에 ActionInsight 컴포넌트가 추가되고 빌드 성공.</done>
</task>

</tasks>

<verification>
수정된 3개의 페이지 컴포넌트가 타입 에러 없이 빌드되며, 기존 UI 흐름을 깨뜨리지 않는다.
</verification>

<success_criteria>
3개의 대출 규제 계산기 결과 섹션에 ActionInsight 컴포넌트가 정상적으로 렌더링된다.
</success_criteria>

<output>
After completion, create `.planning/phases/37-actioninsight/37-03-SUMMARY.md`
</output>