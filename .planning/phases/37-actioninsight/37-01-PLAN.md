---
phase: 37-actioninsight
plan: 1
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/action-data.ts"]
autonomous: true
requirements: ["ACTION-05"]
must_haves:
  truths:
    - "부동산 7개 도구에 해당하는 ActionInsight 데이터가 존재한다"
  artifacts:
    - path: "src/lib/action-data.ts"
      provides: "7개 도구의 actionData entries"
  key_links:
    - from: "src/lib/action-data.ts"
      to: "ActionInsight component"
      via: "calculatorId lookup"
---

<objective>
부동산 7개 도구(보증금 반환, 전월세 전환율, 중개보수, 청약가점, DSR, LTV, DTI)를 위한 ActionInsight 데이터를 `action-data.ts`에 추가한다.

Purpose: 부동산 법률 및 금융 분쟁에서 사용자가 실질적으로 활용할 수 있는 전문가 팁과 협상용 카톡 템플릿을 제공한다.
Output: `actionData` 객체 내에 7개의 새로운 키-값 쌍 추가.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/37-actioninsight/37-CONTEXT.md
@src/lib/action-data.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 부동산 7종 ActionInsight 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `src/lib/action-data.ts` 파일의 `actionData` 객체에 다음 7개 키를 추가하고, 부동산 전문 변호사/세무사/공인중개사의 단호하고 전문적인 톤으로 팁(3개)과 카톡 템플릿(1개)을 작성합니다.

    추가할 키:
    1. `deposit-return` (보증금 반환): 내용증명 전 임대인에게 보내는 반환 독촉 템플릿
    2. `rent-conversion` (전월세 전환율): 법정 전환율 초과 시 임대인에게 이의 제기 템플릿
    3. `brokerage-fee` (중개보수): 법정 한도 초과 요구 시 공인중개사에게 법적 근거 제시 템플릿
    4. `subscription-score` (청약가점): 부적격 당첨 소명 자료 준비 안내 템플릿
    5. `dsr` (DSR): 은행/대출상담사에게 추가 한도/예외 적용 확인 템플릿
    6. `ltv` (LTV): 잔금 대출 실행 전 은행에 생애최초 등 우대 LTV 적용 요청 템플릿
    7. `dti` (DTI): 소득 증빙 서류 보완 관련 은행/회사 요청 템플릿

    템플릿에는 동적으로 값이 삽입될 수 있도록 `{{AMOUNT}}` 플레이스홀더를 적절히 활용합니다 (금액이나 비율 등에 사용 가능).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>actionData 객체에 7개의 부동산 키가 정상적으로 추가되고 타입 에러가 없다.</done>
</task>

</tasks>

<verification>
`action-data.ts`에 추가된 7개의 데이터가 `CalculatorActionData` 타입 규칙을 준수하는지 확인합니다.
</verification>

<success_criteria>
7개의 새로운 도구 데이터가 `action-data.ts`에 존재하며, 문법 오류 없이 빌드된다.
</success_criteria>

<output>
After completion, create `.planning/phases/37-actioninsight/37-01-SUMMARY.md`
</output>