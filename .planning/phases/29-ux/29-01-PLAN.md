---
phase: 29-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/tax/vat/page.tsx
  - src/app/tools/tax/securities-tax/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "vat supply 모드에서 매출/매입 모두 비어있으면 에러 메시지 표시 후 결과 미출력"
    - "vat supply 모드에서 매출 또는 매입이 100억 초과 시 경고 표시 (계산은 허용)"
    - "vat total 모드에서 공급대가 미입력 시 에러 표시"
    - "vat 초기 상태에서 결과 영역이 보이지 않음 (FLOW-01 충족)"
    - "vat 필수 필드에 * 시각 표시 (FLOW-03)"
    - "securities-tax에서 양도가액 미입력/0 시 에러 표시"
    - "securities-tax에서 양도가액 500억 초과 시 경고 표시 (계산 허용)"
    - "securities-tax에서 합계 세액 = 증권거래세 + 농어촌특별세 (RESULT-03)"
    - "securities-tax 초기 상태에서 결과 미노출 (FLOW-01)"
    - "securities-tax 필수 필드에 * 시각 표시 (FLOW-03)"
    - "두 계산기 에러: text-red-500, 경고: text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/tax/vat/page.tsx"
      provides: "부가가치세 계산기 with error/warning state, input validation, * required markers"
      exports: ["VatPage"]
    - path: "src/app/tools/tax/securities-tax/page.tsx"
      provides: "증권거래세 계산기 with error/warning state, input validation, * required markers"
      exports: ["SecuritiesTaxPage"]
  key_links:
    - from: "vat/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "securities-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
vat, securities-tax 2개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 부가가치세·증권거래세 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 2개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러/경고 표시 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-ux/29-CONTEXT.md
@src/app/tools/tax/vat/page.tsx
@src/app/tools/tax/securities-tax/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: vat 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/vat/page.tsx</files>
  <read_first>src/app/tools/tax/vat/page.tsx</read_first>
  <action>
vat/page.tsx를 전체 읽고 16개 요구사항 각각에 대해 감사 후 즉시 수정 (per D-03):

**현황 파악 (코드 분석 결과):**
- `handleCalculate` supply 모드: `if (sales <= 0 && purchase <= 0) return;` — 조용한 return, 에러 없음
- `handleCalculate` total 모드: `if (total <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음 — 추가 필요
- `warning` state 없음 — 추가 필요
- `handleChange` 패턴은 숫자만 입력 허용 — INPUT-04 이미 충족
- 모든 필드 라벨에 * 없음 — FLOW-03 위반
- 비현실값 상한 검증 없음 — INPUT-03 위반
- 초기 `{result && ...}` 패턴 — FLOW-01 이미 충족, 확인만
- supply 모드 환급세액 음수 처리: `Math.abs(result.payableTax!)` + "(환급)" 표시 — RESULT-01 충족

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 — supply 모드 (handleCalculate 내):**
- INPUT-01/02: `if (sales <= 0 && purchase <= 0)` → `setError('매출 또는 매입 공급가액 중 하나 이상을 입력해주세요.')` + `setResult(null)` + return
- INPUT-03: `if (sales > 50_000_000_000 || purchase > 50_000_000_000)` → `setWarning('입력 금액이 500억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용, return 안 함)

**INPUT 검증 — total 모드 (handleCalculate 내):**
- INPUT-01/02: `if (total <= 0)` → `setError('공급대가(VAT 포함 금액)를 입력해주세요.')` + `setResult(null)` + return
- INPUT-03: `if (total > 50_000_000_000)` → `setWarning('입력 금액이 500억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**에러 초기화:** `handleCalculate` 시작 시 `setError(null); setWarning(null);` 호출. 성공 시에도 error null 유지.

**RESULT-03 확인 (supply 모드):**
- `payableTax = salesVat - purchaseVat`. 개별 항목 합산과 일치하는지: salesVat과 purchaseVat이 각각 표시되고, payableTax = salesVat - purchaseVat — 수식 맞음.
- total 모드: `supplyVat = total - supplyBase`. 계산식 섹션에서 `supplyVat = supplyTotal - supplyBase` — 맞음.

**EDGE-01:** supply 모드에서 sales=0, purchase=0 → 위 에러 가드에서 걸림. sales>0, purchase=0 → payableTax=salesVat (정상). payableTax<0 → 환급 표시 — 경계값 처리 정상.
**EDGE-03:** `payableTax = salesVat - purchaseVat` — 음수 가능(환급), `Math.abs`로 표시. `supplyBase = Math.floor(total / 1.1)` — 0 나누기 없음. 정상.

**FLOW-03: 필수 필드 * 추가:**
- supply 모드: "매출 공급가액 (원)" → "매출 공급가액 (원) *"
- total 모드: "공급대가 (VAT 포함 금액, 원)" → "공급대가 (VAT 포함 금액, 원) *"
- 매입 공급가액은 optional이므로 * 없음 (supply 모드에서 매출 또는 매입 중 하나만 있어도 계산 가능)

**에러/경고 표시 위치:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**모드 전환 시 상태 초기화:** 라디오 버튼 onChange에 `setError(null); setWarning(null);` 추가.

**securities-tax 참고 정보 박스 스타일 수정 (CONSIST-02):**
- 결과 내 `bg-blue-500/10 border border-blue-500/30` 안내 박스의 텍스트가 `text-blue-400` (어두운 배경 전제) — 라이트 테마에서 가독성 확인. `text-blue-600`으로 변경 필요 시 변경.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|입력해주세요\|500억\|50_000_000_000" src/app/tools/tax/vat/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" vat/page.tsx returns match (에러 state 추가됨)
- grep "text-red-500" vat/page.tsx returns match (에러 스타일)
- grep "text-orange-500" vat/page.tsx returns match (경고 스타일)
- grep "입력해주세요\|입력해 주세요" vat/page.tsx returns match (INPUT-02 에러 메시지)
- grep "50_000_000_000\|500억" vat/page.tsx returns match (INPUT-03 비현실값 경고)
- grep "\*" vat/page.tsx returns match for label with asterisk (FLOW-03)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>vat 계산기가 16개 요구사항 모두 충족. 빈값 에러, 500억 초과 경고, 필수 필드 * 표시, 에러/경고 스타일 통일 완료.</done>
</task>

<task type="auto">
  <name>Task 2: securities-tax 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/securities-tax/page.tsx</files>
  <read_first>src/app/tools/tax/securities-tax/page.tsx, src/app/tools/tax/vat/page.tsx</read_first>
  <action>
securities-tax/page.tsx를 전체 읽고 16개 요구사항 감사 및 수정. vat에서 확립한 에러/경고 패턴과 동일하게 적용 (per D-03, D-04):

**현황 파악 (코드 분석 결과):**
- `handleCalculate`: `if (!raw || raw <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음 — 추가 필요
- `warning` state 없음 — 추가 필요
- `amount` 필드: `type="text"` + `inputMode="numeric"` + `replace(/[^0-9]/g, '')` — INPUT-04 이미 충족
- 결과 내 `{result && ...}` 패턴 — FLOW-01 이미 충족
- 비현실값 상한 없음 — INPUT-03 위반
- 모든 라벨에 * 없음 — FLOW-03 위반
- 결과 내 `bg-blue-500/10 border border-blue-500/30` + `text-blue-400` 안내 박스 — 라이트 테마에서 색상 부적합 가능성 (CONSIST-02)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT 검증 (handleCalculate 내):**
- INPUT-01/02: `if (!raw || raw <= 0)` → `setError('양도(매도)가액을 입력해주세요.')` + `setResult(null)` + return
- INPUT-03: `if (raw > 50_000_000_000)` → `setWarning('양도가액이 500억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**에러 초기화:** `handleCalculate` 시작 시 `setError(null); setWarning(null);` 호출.

**RESULT-03 확인:**
- `total = securitiesTax + agriTax` — 수식 맞음, `total`이 result에 포함됨.
- 결과 UI: securitiesTax, agriTax 개별 표시 후 total 표시 — 합산 검증 통과.

**RESULT-01:** `total`이 항상 >= 0 (세율 * 양도금액). 0원이 되려면 `raw * rate = 0`인 경우인데 raw > 0이 보장되면 total > 0. 별도 안내 불필요.

**결과 안내 박스 스타일 수정 (CONSIST-02):**
- `bg-blue-500/10 border border-blue-500/30` 안의 `text-blue-400` → `text-blue-600` 으로 변경 (라이트 테마 가독성)
- 내용 텍스트가 "2025년 기준: 코스피..."로 표시되나 파일 상단에 `// 2026년 기준 세율` 주석이 있음. 안내 박스 내용을 2026년 기준으로 업데이트:
  - 기존: "2025년 기준: 코스피 증권거래세 0% (농어촌특별세 0.15% 별도), 코스닥·K-OTC 0.15%, 코넥스 0.10%, 비상장 0.35%"
  - 변경: "2026년 기준: 코스피 증권거래세 0.05% + 농어촌특별세 0.15%, 코스닥·K-OTC 0.20%, 코넥스 0.10%, 비상장 0.35%"
- 법적 근거 텍스트도 "2025년 기준" → "2026년 기준"으로 수정.

**FLOW-03: 필수 필드 * 추가:**
- "양도(매도)가액 (원)" → "양도(매도)가액 (원) *"

**에러/경고 표시 위치:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**EDGE-01:** raw 최솟값 1원 → securitiesTax = Math.floor(1 * 0.0005) = 0. total=0이 가능하나 1원 양도는 비현실적. 에러 가드 raw > 0으로 충분.
**EDGE-03:** `Math.floor(raw * market.rate)` — 0 나누기 없음. 정상.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|양도.*가액을 입력\|500억\|50_000_000_000" src/app/tools/tax/securities-tax/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" securities-tax/page.tsx returns match (에러 state)
- grep "text-red-500" securities-tax/page.tsx returns match (에러 스타일)
- grep "text-orange-500" securities-tax/page.tsx returns match (경고 스타일)
- grep "양도.*가액을 입력\|입력해주세요" securities-tax/page.tsx returns match (INPUT-02)
- grep "50_000_000_000\|500억" securities-tax/page.tsx returns match (INPUT-03)
- grep "2026년 기준" securities-tax/page.tsx returns match (연도 정정)
- grep "\*" securities-tax/page.tsx returns match for label (FLOW-03)
- npx tsc --noEmit passes (TypeScript 에러 없음)
  </acceptance_criteria>
  <done>securities-tax 계산기가 16개 요구사항 모두 충족. 빈값 에러, 500억 경고, 2026년 기준 텍스트 정정, 필수 필드 * 표시, 에러/경고 스타일 통일 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 두 계산기 모두 에러 메시지 패턴 동일 (text-red-500 / text-orange-500)
- grep "setError" 각 파일에서 매치 확인
- grep "\*" 각 파일 라벨에서 매치 확인 (FLOW-03)
</verification>

<success_criteria>
- vat, securities-tax 두 계산기 모두 빈값/비현실값 에러·경고 처리 완료
- 초기 상태에서 0원 결과 미노출
- 필수 필드 * 시각적 구분
- TypeScript 컴파일 에러 없음
- securities-tax 연도 표기 2026년으로 정정
</success_criteria>

<output>
After completion, create `.planning/phases/29-ux/29-01-SUMMARY.md`
</output>
