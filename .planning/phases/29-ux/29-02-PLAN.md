---
phase: 29-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/tax/year-end-tax/page.tsx
  - src/app/tools/tax/four-insurances/page.tsx
  - src/app/tools/tax/rent-tax-credit/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "year-end-tax에서 총급여액 미입력/0 시 에러 표시 후 결과 미출력"
    - "year-end-tax에서 총급여액 5억 초과 시 경고 표시 (계산 허용)"
    - "year-end-tax에서 type=number 필드(부양가족수, 자녀수)를 type=text로 변환 (INPUT-04)"
    - "year-end-tax에서 총급여 라벨에 * 표시 (FLOW-03)"
    - "four-insurances에서 월 급여 미입력/0 시 에러 표시"
    - "four-insurances에서 월 급여 5,000만원 초과 시 경고 (계산 허용)"
    - "four-insurances에서 결과 테이블 합계행이 개별 항목 합과 일치 (RESULT-03)"
    - "four-insurances에서 요율 표시가 실제 계산에 사용된 값과 일치 (국민연금 4.75%, 건보 3.595%)"
    - "four-insurances에서 법적 근거 연도/요율 텍스트 코드와 일치"
    - "rent-tax-credit에서 총급여 미입력 시 에러 표시 (현재 items 배열로 처리되나 에러 state 없음)"
    - "rent-tax-credit에서 월세 미입력 시 에러 표시"
    - "rent-tax-credit에서 필수 필드에 * 표시 (FLOW-03)"
    - "세 계산기 에러: text-red-500, 경고: text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/tax/year-end-tax/page.tsx"
      provides: "연말정산 계산기 with error/warning state, type=text inputs, * required markers"
      exports: ["YearEndTaxPage"]
    - path: "src/app/tools/tax/four-insurances/page.tsx"
      provides: "4대보험 계산기 with error/warning state, correct rate display, * required markers"
      exports: ["FourInsurancesPage"]
    - path: "src/app/tools/tax/rent-tax-credit/page.tsx"
      provides: "월세세액공제 계산기 with error state, * required markers"
      exports: ["RentTaxCreditPage"]
  key_links:
    - from: "year-end-tax/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "four-insurances/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "rent-tax-credit/page.tsx handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
year-end-tax, four-insurances, rent-tax-credit 3개 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 연말정산·4대보험·월세세액공제 계산기의 입력 검증, 결과 표시, UX 흐름, 엣지 케이스 처리 표준화
Output: 3개 계산기 모두 음수/0/비현실값 처리, 초기 상태 정리, 필수 입력 안내, 에러/경고 표시, 요율 표기 불일치 수정 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-ux/29-CONTEXT.md
@src/app/tools/tax/year-end-tax/page.tsx
@src/app/tools/tax/four-insurances/page.tsx
@src/app/tools/tax/rent-tax-credit/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: year-end-tax 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/year-end-tax/page.tsx</files>
  <read_first>src/app/tools/tax/year-end-tax/page.tsx</read_first>
  <action>
year-end-tax/page.tsx를 전체 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03):

**현황 파악 (코드 분석 결과):**
- `handleCalculate`: `if (gross <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음, `warning` state 없음 — 추가 필요
- `dependents`: `type="number"` — INPUT-04 위반
- `childrenUnder20`: `type="number"` — INPUT-04 위반
- 카드 사용액, 연금 등 필드: `type="text"` onChange에서 숫자 파싱만 하고 `replace(/[^0-9]/g, '')` 없음 — INPUT-04 위반
- 총급여 필드 onChange: `e.target.value` 그대로 저장, 숫자만 허용 처리 없음 — INPUT-04 위반
- 라벨에 * 없음 — FLOW-03 위반
- 총급여 5억 이상 비현실값 경고 없음 — INPUT-03 위반
- `{result && ...}` 패턴 — FLOW-01 이미 충족

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT-04 수정 — 숫자 필드 onChange 통일:**
금액 입력 필드(grossPay, withheldTax, creditCardUsage, debitCardUsage, pensionPaid, healthInsurance, medicalExpense, educationExpense, donation, rentPaid)는 onChange를 다음으로 변경:
```tsx
onChange={e => setGrossPay(e.target.value.replace(/[^0-9]/g, ''))}
```
각 setter에 맞게 적용. 단, 이미 `parseAmount`가 `replace(/[^0-9]/g, '')` 처리하므로 실제 계산은 영향 없음. 입력 UX 일관성을 위해 onChange에서도 필터링.

**INPUT-04 수정 — dependents, childrenUnder20:**
- `type="number"` → `type="text"` + `inputMode="numeric"`
- `onChange={e => setDependents(e.target.value.replace(/[^0-9]/g, ''))}`
- `onChange={e => setChildrenUnder20(e.target.value.replace(/[^0-9]/g, ''))}`
- `parseInt(dependents, 10) || 1` 로직 유지
- `parseInt(childrenUnder20, 10) || 0` 로직 유지

**INPUT 검증 (handleCalculate 내):**
- INPUT-01/02: `if (gross <= 0)` → `setError('총급여액을 입력해주세요.')` + `setResult(null)` + return
- INPUT-03: `if (gross > 500_000_000)` (5억) → `setWarning('총급여액이 5억원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**에러 초기화:** `handleCalculate` 시작 시 `setError(null); setWarning(null);` 호출.

**RESULT-03 확인:**
- `totalCredit = earnedCredit + childCredit + medicalCredit + educationCredit + donationCredit + rentCredit`
- `finalTax = Math.max(0, computedTax - totalCredit)`
- 결과 UI에서 세액공제 합계 행 표시 후 결정세액 표시 — 부분합 검증 통과.

**RESULT-01:** `refund = withheld - finalTax`. refund < 0이면 "추가 납부액" 표시 — 이미 처리됨. `withheld` 미입력(0)이면 `refund = -finalTax` (추가 납부로 표시). 0원 환급/납부 시 안내가 없음 — `refund === 0`이면 결과 영역에 "납부 세액 없음" 안내는 특별히 추가하지 않아도 기존 표시로 충분.

**FLOW-03: 필수 필드 * 추가:**
- "총급여액 (원)" → "총급여액 (원) *"
- 나머지는 선택 입력

**에러/경고 표시 위치:** 계산 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**EDGE-01:** gross=0 → 에러 가드. gross=1 → earnedDeduction=0.7, taxableIncome=0.3-basicDeduction < 0 → Math.max(0,...) = 0 → computedTax=0 — 정상.
**EDGE-03:** `taxableIncome = Math.max(0, ...)` — 음수 방지 있음. `incomeTax(0) = 0` — 정상.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|총급여액을 입력\|500_000_000\|inputMode" src/app/tools/tax/year-end-tax/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" year-end-tax/page.tsx returns match
- grep "text-red-500" year-end-tax/page.tsx returns match
- grep "text-orange-500" year-end-tax/page.tsx returns match
- grep "총급여액을 입력\|입력해주세요" year-end-tax/page.tsx returns match
- grep "500_000_000\|5억" year-end-tax/page.tsx returns match
- grep 'type="number"' year-end-tax/page.tsx returns 0 (number type 완전 제거)
- grep "inputMode" year-end-tax/page.tsx returns match
- grep "\*" year-end-tax/page.tsx returns match for label asterisk
- npx tsc --noEmit passes
  </acceptance_criteria>
  <done>year-end-tax 계산기가 16개 요구사항 모두 충족. 총급여 미입력 에러, 5억 초과 경고, type=number 필드 text 전환, 필수 필드 * 완료.</done>
</task>

<task type="auto">
  <name>Task 2: four-insurances + rent-tax-credit 계산기 UX·논리 감사 및 수정</name>
  <files>src/app/tools/tax/four-insurances/page.tsx, src/app/tools/tax/rent-tax-credit/page.tsx</files>
  <read_first>src/app/tools/tax/four-insurances/page.tsx, src/app/tools/tax/rent-tax-credit/page.tsx</read_first>
  <action>
four-insurances/page.tsx와 rent-tax-credit/page.tsx를 순서대로 감사·수정 (per D-03, D-04):

---

### four-insurances/page.tsx

**현황 파악 (코드 분석 결과):**
- `handleCalculate`: `if (monthlySalary <= 0) return;` — 조용한 return, 에러 없음
- `error` state 없음 — 추가 필요
- `salary` 필드 onChange: `e.target.value` 그대로 저장, 숫자만 필터링 없음 — INPUT-04 위반
- 비현실값 상한 없음 — INPUT-03 위반
- 라벨에 * 없음 — FLOW-03 위반
- **요율 불일치 버그 (Critical — D-01):**
  - 코드: `PENSION_RATE = 0.0475` (4.75%), `HEALTH_RATE = 0.03595` (3.595%), `LONGTERM_RATE = 0.1314` (13.14%)
  - 결과 rows 배열: `employeeRate: '4.5%'`, `employeeRate: '3.545%'`, `employeeRate: '12.95%*'` — 구버전 요율로 표시, 실제 계산과 불일치!
  - 법적 근거 텍스트: "국민연금 상한 월 617만원" — 코드의 `PENSION_CAP = 6_370_000` (637만원)과 불일치!
- `{result && ...}` 패턴 — FLOW-01 충족

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**INPUT-04 수정:**
- `salary` 필드 onChange: `e => setSalary(e.target.value.replace(/[^0-9]/g, ''))`

**INPUT 검증 (handleCalculate 내):**
- INPUT-01/02: `if (monthlySalary <= 0)` → `setError('월 급여를 입력해주세요.')` + `setResult(null)` + return
- INPUT-03: `if (monthlySalary > 50_000_000)` (5,000만원) → `setWarning('월 급여가 5,000만원을 초과합니다. 입력값을 확인해주세요.')` (계산 허용)

**에러 초기화:** `handleCalculate` 시작 시 `setError(null); setWarning(null);` 호출.

**요율 표시 버그 수정 (rows 배열):**
```tsx
{ name: '국민연금', ..., employeeRate: '4.75%', employerRate: '4.75%' },
{ name: '건강보험', ..., employeeRate: '3.595%', employerRate: '3.595%' },
{ name: '장기요양보험', ..., employeeRate: '13.14%*', employerRate: '13.14%*' },
```
(고용보험, 산재보험은 이미 정확)

**법적 근거 텍스트 수정:**
- "국민연금 상한 월 617만원" → "국민연금 상한 월 637만원" (PENSION_CAP = 6_370_000 기준)
- 결과 내 `* 장기요양보험 요율은 건강보험료의 12.95%` → `* 장기요양보험 요율은 건강보험료의 13.14%`

**RESULT-03 확인:**
- `totalEmployee = rows.reduce((s, r) => s + r.employee, 0)` — 개별 합산 = 표시 합계. 맞음.
- 실수령액: `parseAmount(salary) - result.totalEmployee` — salary는 raw 숫자이므로 계산 정상.

**FLOW-03: 필수 필드 * 추가:**
- "월 급여 (원)" → "월 급여 (원) *"

**에러/경고 표시 위치:** 계산 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**EDGE-01:** monthlySalary = PENSION_CAP(6_370_000) → pensionBase = 6_370_000 (상한 적용). monthlySalary > PENSION_CAP → pensionBase = PENSION_CAP (capped). 정상.
**EDGE-03:** Math.floor 사용으로 0 나누기 없음.

---

### rent-tax-credit/page.tsx

**현황 파악 (코드 분석 결과):**
- `handleCalculate`: 에러 가드 없음 — 아무 값도 입력 안 해도 result 세팅 시도
- `error` state 없음, `warning` state 없음 — 추가 필요
- `annualGross`, `monthlyRent` 필드 onChange: `e.target.value` 그대로 저장, 숫자 필터링 없음 — INPUT-04 위반
- 라벨에 * 없음 — FLOW-03 위반
- `eligible = items.every(i => i.met) && rent > 0` — rent=0 이면 ineligible 처리는 됨. 그러나 gross=0, rent=0 이면 결과 영역에 "공제 요건 미충족" 메시지가 표시됨 — FLOW-01 위반 (초기 상태에서 0원 결과 미노출 기대)
- `{result && ...}` 패턴 — 현재 버튼 클릭 시 항상 setResult 호출 → FLOW-01 위반 가능성 있음

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
```

**INPUT-04 수정:**
- `annualGross` onChange: `e => setAnnualGross(e.target.value.replace(/[^0-9]/g, ''))`
- `monthlyRent` onChange: `e => setMonthlyRent(e.target.value.replace(/[^0-9]/g, ''))`

**INPUT 검증 및 FLOW-01 수정 (handleCalculate 내):**
```tsx
const handleCalculate = () => {
  setError(null);
  const gross = parseAmount(annualGross);
  const rent = parseAmount(monthlyRent);
  if (gross <= 0) {
    setError('연간 총급여를 입력해주세요.');
    setResult(null);
    return;
  }
  if (rent <= 0) {
    setError('월세 납부액을 입력해주세요.');
    setResult(null);
    return;
  }
  // 기존 계산 로직 유지
  ...
};
```

**RESULT-03 확인:**
- `credit = Math.floor(cappedRent * rate)`. cappedRent = min(annualRent, 10_000_000). 단일 계산값으로 부분합 없음 — RESULT-03 해당 없음.

**FLOW-01 수정:** 위 검증으로 gross=0 또는 rent=0이면 `setResult(null)` + return → 초기 상태(빈 입력)에서 결과 영역 미노출.

**FLOW-03: 필수 필드 * 추가:**
- "연간 총급여 (원)" → "연간 총급여 (원) *"
- "월세 월 납부액 (원)" → "월세 월 납부액 (원) *"

**에러 표시 위치:** 계산 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
```

**CONSIST-02 확인:** 공제 요건 체크 결과 색상 `text-emerald-300`, `text-red-300` — 라이트 테마에서 낮은 대비. `text-emerald-600`, `text-red-600`으로 변경.

**EDGE-01:** gross = 55_000_000 → rate = 0.17. gross = 55_000_001 → rate = 0.15. 경계값 분기 정상.
**EDGE-03:** `Math.floor(cappedRent * rate)` — 0 나누기 없음.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; grep -c "setError\|text-red-500\|text-orange-500\|입력해주세요" src/app/tools/tax/four-insurances/page.tsx src/app/tools/tax/rent-tax-credit/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" four-insurances/page.tsx returns match
- grep "text-red-500" four-insurances/page.tsx returns match
- grep "text-orange-500" four-insurances/page.tsx returns match
- grep "4\.75" four-insurances/page.tsx returns match (요율 4.75% 표시 수정)
- grep "3\.595" four-insurances/page.tsx returns match (요율 3.595% 표시 수정)
- grep "13\.14" four-insurances/page.tsx returns match (장기요양 13.14% 표시 수정)
- grep "637만원\|6_370_000\|637" four-insurances/page.tsx returns match (국민연금 상한 수정)
- grep "setError" rent-tax-credit/page.tsx returns match
- grep "text-red-500" rent-tax-credit/page.tsx returns match
- grep "\*" four-insurances/page.tsx returns match for label
- grep "\*" rent-tax-credit/page.tsx returns match for label
- npx tsc --noEmit passes
  </acceptance_criteria>
  <done>four-insurances 요율 표시 불일치 수정 + 에러/경고 추가. rent-tax-credit FLOW-01 수정 + 에러 state 추가. 세 계산기 모두 16개 요구사항 충족.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- four-insurances 요율 행 표시가 PENSION_RATE(4.75%), HEALTH_RATE(3.595%), LONGTERM_RATE(13.14%)와 일치
- 세 계산기 모두 에러 메시지 패턴 동일 (text-red-500 / text-orange-500)
- rent-tax-credit 빈 상태에서 버튼 클릭 시 에러 표시, 결과 영역 미노출
</verification>

<success_criteria>
- year-end-tax, four-insurances, rent-tax-credit 세 계산기 모두 빈값/비현실값 에러·경고 처리 완료
- four-insurances 요율 표기 버그(4.5%→4.75%, 3.545%→3.595%, 12.95%→13.14%, 617만원→637만원) 수정
- rent-tax-credit FLOW-01 위반 수정 (초기 빈 입력 시 결과 미노출)
- 모든 type=number 필드 text/inputMode 전환
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/29-ux/29-02-SUMMARY.md`
</output>
