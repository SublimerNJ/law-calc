---
phase: 29-ux
plan: 03
type: execute
wave: 2
depends_on: [29-01, 29-02]
files_modified:
  - src/app/tools/tax/vat/page.tsx
  - src/app/tools/tax/securities-tax/page.tsx
  - src/app/tools/tax/year-end-tax/page.tsx
  - src/app/tools/tax/four-insurances/page.tsx
  - src/app/tools/tax/rent-tax-credit/page.tsx
autonomous: true
requirements: [CONSIST-01, CONSIST-02, CONSIST-03, RESULT-02, FLOW-02]

must_haves:
  truths:
    - "5개 계산기 모두 에러 메시지가 text-red-500 스타일로 통일"
    - "5개 계산기 모두 경고 메시지가 text-orange-500 스타일로 통일"
    - "5개 계산기 모두 금액 결과를 천 단위 콤마로 표시 (RESULT-02)"
    - "5개 계산기 모두 필수 입력 필드에 * 마커 존재 (FLOW-03)"
    - "5개 계산기 모두 버튼 클릭 시 입력 변경 반영 즉시 업데이트 (FLOW-02)"
    - "5개 계산기 에러 메시지 어조가 ~해주세요 체로 통일 (CONSIST-03)"
  artifacts:
    - path: "src/app/tools/tax/vat/page.tsx"
      provides: "일관성 검증 및 잔여 수정 완료"
    - path: "src/app/tools/tax/securities-tax/page.tsx"
      provides: "일관성 검증 및 잔여 수정 완료"
    - path: "src/app/tools/tax/year-end-tax/page.tsx"
      provides: "일관성 검증 및 잔여 수정 완료"
    - path: "src/app/tools/tax/four-insurances/page.tsx"
      provides: "일관성 검증 및 잔여 수정 완료"
    - path: "src/app/tools/tax/rent-tax-credit/page.tsx"
      provides: "일관성 검증 및 잔여 수정 완료"
  key_links:
    - from: "모든 계산기 에러 표시"
      to: "text-red-500 className"
      via: "error state conditional render"
      pattern: "text-red-500"
    - from: "모든 계산기 경고 표시"
      to: "text-orange-500 className"
      via: "warning state conditional render"
      pattern: "text-orange-500"
---

<objective>
5개 세금 계산기(vat, securities-tax, year-end-tax, four-insurances, rent-tax-credit) 일관성 교차 검증 및 잔여 수정

Purpose: 29-01, 29-02에서 각 계산기를 개별 수정한 결과물이 CONSIST-01~03 요구사항을 5개 전체에서 충족하는지 검증하고 불일치 항목을 수정
Output: 5개 계산기 에러/경고 스타일, 금액 표시, 필수 필드 마커, 에러 메시지 어조가 완전히 통일됨
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-ux/29-CONTEXT.md
@.planning/phases/29-ux/29-01-SUMMARY.md
@.planning/phases/29-ux/29-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5개 계산기 일관성 교차 검증 및 불일치 수정</name>
  <files>src/app/tools/tax/vat/page.tsx, src/app/tools/tax/securities-tax/page.tsx, src/app/tools/tax/year-end-tax/page.tsx, src/app/tools/tax/four-insurances/page.tsx, src/app/tools/tax/rent-tax-credit/page.tsx</files>
  <read_first>
    src/app/tools/tax/vat/page.tsx,
    src/app/tools/tax/securities-tax/page.tsx,
    src/app/tools/tax/year-end-tax/page.tsx,
    src/app/tools/tax/four-insurances/page.tsx,
    src/app/tools/tax/rent-tax-credit/page.tsx
  </read_first>
  <action>
5개 파일 전체를 순서대로 읽고 아래 체크리스트를 각 파일에 적용한다. 문제 발견 즉시 수정 (per D-03).

**CONSIST-01 체크리스트 — 동일 개념의 입력 방식 통일:**

금액 입력 필드 패턴:
- 표준: `type="text"` + `inputMode="numeric"` + onChange에서 `replace(/[^0-9]/g, '')`
- 5개 파일 전체에서 금액 입력 필드가 이 패턴을 따르는지 확인.
- 벗어나는 필드 발견 시 수정.

**CONSIST-02 체크리스트 — 결과 표시 형식 통일:**

금액 표시:
- 표준: `formatNumber(n)` = `n.toLocaleString('ko-KR')` + "원" 접미사
- 5개 파일 모두 `formatNumber` 함수 정의 있는지 확인. 없으면 추가.
- 결과 패널에서 숫자를 직접 출력하거나 `.toLocaleString()` 없이 출력하는 경우 `formatNumber()` 로 교체.

퍼센트 표시:
- `(rate * 100).toFixed(2)%` 또는 직접 상수 — 어느 쪽이든 일관성만 확인.

결과 안내 박스 색상 일관성:
- `bg-blue-500/10 border border-blue-500/30` 박스 내 텍스트: `text-blue-400` → `text-blue-600` (라이트 테마)
- `bg-emerald-500/10 border border-emerald-500/30` 박스 내 텍스트: `text-emerald-400` → `text-emerald-600`
- `bg-red-500/10 border border-red-500/30` 박스 내 텍스트: `text-red-400` → `text-red-600`
- 단, 5개 계산기 각각에서 동일 색상이 동일 방식으로 사용되는지 교차 확인. 일부만 수정된 경우 나머지도 통일.

**CONSIST-03 체크리스트 — 에러 메시지 스타일·어조 통일:**

에러 스타일:
- 표준: `{error && <p className="text-red-500 text-sm mb-3">{error}</p>}`
- 경고 표준: `{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}`
- 모든 계산기에서 이 패턴 확인. 다르면 통일.

에러 메시지 어조:
- 표준: "~을 입력해주세요." / "~을 확인해주세요."
- 모든 에러 메시지 문구를 확인하고 체로 끝나는지 확인.
- 예외: rent-tax-credit의 공제 요건 체크 항목(reason 문자열)은 informational이므로 어조 자유.

**RESULT-02 체크리스트 — 가독성 확보:**

- 결과 패널의 모든 금액이 `formatNumber()` + "원" 패턴인지 확인.
- 예외 없는 경우 통과.

**FLOW-02 체크리스트 — 입력 변경 시 업데이트 타이밍:**

- 5개 계산기 모두 `handleCalculate` 버튼 클릭 패턴 사용 (실시간 계산 아님) — CONSIST-01 기준.
- 버튼 클릭 후 결과가 최신 입력값을 반영하는지: `useState` 값이 `handleCalculate` 호출 시점에 최신값이면 충족.
- 문제 없음 확인 후 통과.

**추가 점검 — D-05/D-06 공통 패턴 추출 가능성:**

`formatNumber` 함수가 5개 파일에 모두 동일하게 정의됨. 3개 이상 계산기에서 동일 패턴 → D-06 기준 충족하나, 별도 유틸리티 추출은 scope가 크므로 이번 Plan에서는 수정 없이 기록만. (Info 수준)

**마지막 빌드 검증:**
모든 수정 후 `npx tsc --noEmit` 실행하여 TypeScript 에러 없음 확인.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; for f in src/app/tools/tax/vat/page.tsx src/app/tools/tax/securities-tax/page.tsx src/app/tools/tax/year-end-tax/page.tsx src/app/tools/tax/four-insurances/page.tsx src/app/tools/tax/rent-tax-credit/page.tsx; do echo "=== $f ==="; grep -c "text-red-500\|setError" "$f"; done</automated>
  </verify>
  <acceptance_criteria>
- 5개 파일 모두 grep "text-red-500" returns match
- 5개 파일 모두 grep "setError" returns match
- 5개 파일 모두 grep "formatNumber" returns match (RESULT-02)
- 5개 파일 모두 grep "\*" returns match for label asterisk (FLOW-03)
- grep 'type="number"' 5개 파일 모두 returns 0 (number type 제거 완료)
- npx tsc --noEmit passes
  </acceptance_criteria>
  <done>5개 세금 계산기의 에러/경고 스타일, 금액 표시, 필수 필드 마커, 에러 메시지 어조가 완전히 통일됨. Phase 29 전체 요구사항 충족.</done>
</task>

<task type="auto">
  <name>Task 2: TypeScript 빌드 최종 검증 및 Phase 29 완료 확인</name>
  <files></files>
  <read_first></read_first>
  <action>
1. `npx tsc --noEmit` 실행하여 전체 프로젝트 TypeScript 컴파일 오류 없음 확인.
2. 오류 발생 시 해당 파일 읽고 즉시 수정. 수정 후 재실행.
3. 오류 없으면 5개 파일에 대해 아래 grep 검증 실행:
   ```bash
   for f in src/app/tools/tax/vat/page.tsx \
             src/app/tools/tax/securities-tax/page.tsx \
             src/app/tools/tax/year-end-tax/page.tsx \
             src/app/tools/tax/four-insurances/page.tsx \
             src/app/tools/tax/rent-tax-credit/page.tsx; do
     echo "=== $f ==="
     echo "setError: $(grep -c 'setError' $f)"
     echo "text-red-500: $(grep -c 'text-red-500' $f)"
     echo "asterisk: $(grep -c '\*' $f)"
     echo "formatNumber: $(grep -c 'formatNumber' $f)"
     echo "type=number: $(grep -c 'type=\"number\"' $f)"
   done
   ```
4. 모든 계산기에서 각 항목 값이 0보다 크고(type=number는 0), 체크리스트 전원 통과하면 완료.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- npx tsc --noEmit exits with code 0 (에러 없음)
- 5개 파일 모두 setError count >= 1
- 5개 파일 모두 text-red-500 count >= 1
- 5개 파일 모두 type="number" count = 0
  </acceptance_criteria>
  <done>TypeScript 빌드 정상. Phase 29 요구사항 INPUT-01~04, RESULT-01~03, FLOW-01~03, CONSIST-01~03, EDGE-01~03 모두 5개 계산기에서 충족 확인 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 통과
- 5개 파일 모두 에러/경고 state 존재
- 5개 파일 모두 에러 스타일 text-red-500 / text-orange-500
- 5개 파일 모두 결과 금액 formatNumber() 사용
- 5개 파일 모두 type="number" 미사용
- four-insurances 요율 표시: 4.75%, 3.595%, 13.14%, 637만원
- securities-tax 연도 표기: 2026년 기준
</verification>

<success_criteria>
- Phase 29의 5개 세금 계산기가 16개 요구사항(INPUT-01~04, RESULT-01~03, FLOW-01~03, CONSIST-01~03, EDGE-01~03) 모두 충족
- 5개 계산기 간 에러/경고 패턴, 금액 표시, 필수 마커, 메시지 어조 완전 통일
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/29-ux/29-03-SUMMARY.md`
</output>
