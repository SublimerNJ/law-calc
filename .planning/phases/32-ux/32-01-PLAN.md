---
phase: 32-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/damages/damages-general/page.tsx
  - src/app/tools/damages/defamation/page.tsx
  - src/app/tools/damages/medical-malpractice/page.tsx
  - src/app/tools/damages/lost-income/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "damages-general에서 재산손해액 미입력 시 에러 표시 후 결과 미출력"
    - "damages-general에서 재산손해액 100억 초과 시 경고 표시 (계산 허용)"
    - "damages-general에서 과실비율 필드 type=number → type=text + inputMode=decimal 변경"
    - "damages-general에서 총 배상액 0원일 때 안내 메시지 표시"
    - "damages-general에서 재산손해액 라벨에 * 표시"
    - "defamation에서 유포기간 음수 입력 차단 (INPUT-01)"
    - "defamation에서 유포기간 필드 type=number → type=text + inputMode=decimal 변경"
    - "medical-malpractice에서 의사 과실비율 미입력/0 시 에러 표시 후 결과 미출력"
    - "medical-malpractice에서 의사 과실비율·후유장해율 필드 type=number → type=text + inputMode=decimal"
    - "medical-malpractice에서 치료비 100억 초과 시 경고 표시"
    - "medical-malpractice에서 후유장해율 100 초과 시 에러 표시"
    - "lost-income에서 나이·월순수입·노동능력상실률 필수 검증 에러 표시"
    - "lost-income에서 나이 >= 가동연한 시 에러 표시"
    - "lost-income에서 일실수입 0원일 때 안내 메시지 표시"
    - "lost-income에서 나이·월순수입·노동능력상실률 라벨에 * 표시"
    - "4개 계산기 모두 에러: text-red-500, 경고: text-orange-500 스타일 통일"
  artifacts:
    - path: "src/app/tools/damages/damages-general/page.tsx"
      provides: "일반 손해배상 계산기 with error/warning state, 필수 필드 *, 0원 안내, type=text 과실비율"
    - path: "src/app/tools/damages/defamation/page.tsx"
      provides: "명예훼손 위자료 계산기 with 음수 기간 차단, type=text 기간 입력"
    - path: "src/app/tools/damages/medical-malpractice/page.tsx"
      provides: "의료사고 손해배상 계산기 with 과실비율 에러, 장해율 상한, 고액 경고"
    - path: "src/app/tools/damages/lost-income/page.tsx"
      provides: "일실수입 계산기 with 필수 필드 에러, 나이/연한 검증, 0원 안내, 필수 * 표시"
  key_links:
    - from: "damages-general handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "defamation duration input"
      to: "음수 차단"
      via: "onChange replace(/[^0-9.]/g, '')"
      pattern: "replace.*0-9"
    - from: "medical-malpractice handleCalculate"
      to: "error/warning state display"
      via: "setError + setWarning + conditional rendering"
      pattern: "setError|setWarning"
    - from: "lost-income handleCalculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
damages-general, defamation, medical-malpractice, lost-income 4개 손해배상 계산기에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 손해배상 계산기 사용자가 잘못된 값 입력 시 명확한 안내 받고, 비현실적 값 입력 시 경고받으며, type=number 필드를 text+inputMode로 교체해 모바일 UX 개선 (per D-01, D-02, D-03)
Output: 4개 계산기 모두 에러/경고 표시, 필수 필드 *, 숫자 입력 정규화, 0원 결과 안내 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-ux/32-CONTEXT.md
@src/app/tools/damages/damages-general/page.tsx
@src/app/tools/damages/defamation/page.tsx
@src/app/tools/damages/medical-malpractice/page.tsx
@src/app/tools/damages/lost-income/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: damages-general + defamation 계산기 UX·논리 감사 및 수정</name>
  <files>
    src/app/tools/damages/damages-general/page.tsx
    src/app/tools/damages/defamation/page.tsx
  </files>
  <read_first>
    src/app/tools/damages/damages-general/page.tsx
    src/app/tools/damages/defamation/page.tsx
  </read_first>
  <action>
두 파일을 순서대로 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

---

### damages-general/page.tsx

**현황 파악:**
- `handleCalculate`: `if (property <= 0) return;` — 에러 없이 조용히 return (INPUT-02 위반)
- `error` state 없음, `warning` state 없음
- `faultRatio`: `type="number"` — INPUT-04 위반 (모바일 UX 불량)
- INPUT-03: 재산손해액 비현실값(100억 초과) 경고 없음
- FLOW-01: `result === null` 초기 → 결과 미출력 (OK)
- FLOW-03: "재산상 손해액" 라벨에 * 없음
- RESULT-01: `total === 0`일 때 안내 없음 (과실100% + 위자료없음 시)
- RESULT-03: `total = adjustedProperty + consolation` — 부분합 맞음 (OK)
- EDGE-03: 나누기 0 없음, 과실100% → adjustedProperty=0 (논리상 맞음)
- CONSIST-02: 모든 금액 `formatNumber()원` 형식 (OK)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 수정:**
```tsx
const handleCalculate = () => {
  setError(null);
  setWarning(null);
  const property = parseInt(propertyDamage.replace(/[^0-9]/g, ''), 10) || 0;
  const fault = parseFloat(faultRatio) || 0;

  // INPUT-02: 재산손해액 필수
  if (!propertyDamage || property <= 0) {
    setError('재산상 손해액을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-03: 100억 초과 경고
  if (property > 10_000_000_000) {
    setWarning('재산손해액이 100억원을 초과합니다. 입력값을 확인해주세요.');
  }
  // INPUT-01: fault 음수 차단은 min=0으로 이미 처리됨 (type=text 전환 후 replace로 처리)
  if (fault < 0 || fault > 100) {
    setError('과실비율은 0~100 사이 숫자를 입력해주세요.');
    setResult(null);
    return;
  }

  const adjustedProperty = Math.floor(property * (1 - fault / 100));
  let consolation = 0;
  if (includeConsolation) {
    consolation = Math.floor(getConsolationBase() * (1 - fault / 100));
  }
  const total = adjustedProperty + consolation;
  setResult({ adjustedProperty, consolation, total });
};
```

**INPUT-04: 과실비율 필드 type=number → type=text 변경:**
기존:
```jsx
<input type="number" min="0" max="100" value={faultRatio} onChange={(e) => setFaultRatio(e.target.value)} ... />
```
수정:
```jsx
<input
  type="text"
  inputMode="decimal"
  value={faultRatio}
  onChange={(e) => setFaultRatio(e.target.value.replace(/[^0-9.]/g, ''))}
  placeholder="0 ~ 100"
  ...
/>
```

**FLOW-03: 재산손해액 라벨 * 추가:**
- "재산상 손해액 (치료비 + 일실이익 + 기타, 원)" → "재산상 손해액 (치료비 + 일실이익 + 기타, 원) *"

**에러/경고 표시:** 계산하기 버튼 바로 위에 추가:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**RESULT-01: total=0 안내 (결과 "총 배상액" 섹션 바로 위에):**
```jsx
{result.total === 0 && (
  <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">총 배상액이 0원으로 계산되었습니다. 과실비율 또는 손해항목을 확인해주세요.</p>
  </div>
)}
```

---

### defamation/page.tsx

**현황 파악:**
- `handleCalculate`: 항상 실행됨 — 모든 입력이 select(기본값 있음)이므로 INPUT-02는 N/A
- `duration`: `type="number"` — INPUT-04 위반 (모바일 UX 불량)
- INPUT-01: `duration` 음수 시 `parseFloat(duration) || 0` → 음수 그대로 사용 → `durationW = months > 1 ? ... : 1.0` — months가 음수면 durationW=1.0이 되어 계산됨. 그러나 음수 기간은 논리상 불가 → onChange에서 차단 필요
- EDGE-01: duration=0 → `durationW=1.0` → 계산 정상 (OK)
- EDGE-03: 곱셈만 있어 나누기 0 없음 (OK)
- FLOW-01: 초기 result=null → 결과 미출력 (OK)
- FLOW-03: "유포 기간 (개월)" 라벨에 "(선택)" 명시 없음 — 기간 없는 경우도 유효하므로 선택임을 명시
- RESULT-01: mid가 0원이 될 수 없음 (BASE_AMOUNTS.mid 최소 200만원, 가중치 최소 0.7×0.7×0.7=0.343 → 최소 68만6천원) — RESULT-01 N/A
- CONSIST-02: `formatNumber()원` 형식 (OK)

**INPUT-04: duration 필드 type=number → type=text 변경 + INPUT-01: 음수 차단:**
기존:
```jsx
<input type="number" min="0" value={duration} onChange={(e) => setDuration(e.target.value)} ... />
```
수정:
```jsx
<input
  type="text"
  inputMode="decimal"
  value={duration}
  onChange={(e) => setDuration(e.target.value.replace(/[^0-9.]/g, ''))}
  placeholder="예: 3"
  ...
/>
```
(replace로 '-' 제거 → 음수 입력 불가)

**FLOW-03: duration 라벨 선택 명시:**
- "유포 기간 (개월)" → "유포 기간 (개월, 선택)"

**CONSIST-03 확인:** 현재 에러/경고 state 없음 — defamation은 모든 입력이 select이므로 별도 에러 UI 불필요. type 변경으로 INPUT-04만 처리하면 충분.
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|재산상 손해액을 입력\|100억\|총 배상액이 0원\|inputMode.*decimal\|replace.*0-9" src/app/tools/damages/damages-general/page.tsx src/app/tools/damages/defamation/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" damages-general/page.tsx returns match (에러 state 사용)
- grep "재산상 손해액을 입력" damages-general/page.tsx returns match (INPUT-02 에러 메시지)
- grep "100억\|10_000_000_000" damages-general/page.tsx returns match (INPUT-03 경고)
- grep "총 배상액이 0원" damages-general/page.tsx returns match (RESULT-01 안내)
- grep "text-red-500" damages-general/page.tsx returns match (에러 스타일)
- grep "text-orange-500" damages-general/page.tsx returns match (경고 스타일)
- grep "재산상 손해액.*\*\|\*.*재산상" damages-general/page.tsx returns match (FLOW-03 필수 *)
- grep "inputMode.*decimal" damages-general/page.tsx returns match (INPUT-04 과실비율)
- grep "replace.*0-9" defamation/page.tsx returns match (INPUT-01/INPUT-04 음수 차단)
- grep "선택" defamation/page.tsx returns match (FLOW-03 선택 명시)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep "^0$"
  </acceptance_criteria>
  <done>damages-general: 재산손해액 필수 에러, 100억 경고, 0원 안내, 필수 * 표시, 과실비율 type=text 완료. defamation: 기간 음수 차단, type=text 변경, 선택 명시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: medical-malpractice + lost-income 계산기 UX·논리 감사 및 수정</name>
  <files>
    src/app/tools/damages/medical-malpractice/page.tsx
    src/app/tools/damages/lost-income/page.tsx
  </files>
  <read_first>
    src/app/tools/damages/medical-malpractice/page.tsx
    src/app/tools/damages/lost-income/page.tsx
  </read_first>
  <action>
두 파일을 순서대로 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

---

### medical-malpractice/page.tsx

**현황 파악:**
- `handleCalculate`: `if (fault <= 0) return;` — 에러 없이 조용히 return (INPUT-02 위반)
- `error` state 없음, `warning` state 없음
- `doctorFault`: `type="number"` — INPUT-04 위반
- `disabilityRate` (후유장해율): `type="number"` — INPUT-04 위반
- INPUT-03: 치료비 합계(기지출+향후) 100억 초과 경고 없음
- EDGE-01: disabilityRate > 100 입력 허용 → consolation 과대 계산 (INPUT-03/EDGE-01 위반)
- FLOW-03: "의사 과실비율 (%)" 라벨에 * 없음
- FLOW-01: 초기 result=null → OK
- RESULT-01: total=0일 때 (모든 비용 0 + fault=100%) 안내 필요
- RESULT-03: `total = propertyDamage + consolation` — 부분합 맞음 (OK)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handleCalculate 수정:**
```tsx
const handleCalculate = () => {
  setError(null);
  setWarning(null);
  const treatment = parseInt(treatmentCost.replace(/[^0-9]/g, ''), 10) || 0;
  const future = parseInt(futureTreatmentCost.replace(/[^0-9]/g, ''), 10) || 0;
  const lost = parseInt(lostIncome.replace(/[^0-9]/g, ''), 10) || 0;
  const fault = parseFloat(doctorFault) || 0;

  // INPUT-02: 과실비율 필수 (의료과실 없으면 계산 의미 없음)
  if (!doctorFault || fault <= 0) {
    setError('의사 과실비율을 입력해주세요. (1~100% 사이)');
    setResult(null);
    return;
  }
  if (fault > 100) {
    setError('과실비율은 100%를 초과할 수 없습니다.');
    setResult(null);
    return;
  }
  // INPUT-03: 총 치료비 100억 초과 경고
  if (treatment + future > 10_000_000_000) {
    setWarning('치료비 합계가 100억원을 초과합니다. 입력값을 확인해주세요.');
  }
  // EDGE-01: 후유장해율 상한 검증
  if (hasDisability) {
    const disRate = parseFloat(disabilityRate) || 0;
    if (disRate > 100) {
      setError('후유장해율은 100%를 초과할 수 없습니다.');
      setResult(null);
      return;
    }
  }

  const propertyDamage = Math.floor((treatment + future + lost) * (fault / 100));
  let consolation = 0;
  if (hasDisability) {
    const disRate = parseFloat(disabilityRate) || 0;
    consolation = Math.floor(getConsolationBase() * (disRate / 100) * (fault / 100));
  }
  const total = propertyDamage + consolation;
  setResult({ propertyDamage, consolation, total });
};
```

**INPUT-04: doctorFault 필드 type=number → type=text 변경:**
기존: `<input type="number" min="0" max="100" value={doctorFault} onChange={(e) => setDoctorFault(e.target.value)} ... />`
수정:
```jsx
<input
  type="text"
  inputMode="decimal"
  value={doctorFault}
  onChange={(e) => setDoctorFault(e.target.value.replace(/[^0-9.]/g, ''))}
  placeholder="0 ~ 100"
  ...
/>
```

**INPUT-04: disabilityRate 필드 type=number → type=text 변경 (hasDisability 블록 내):**
기존: `<input type="number" min="0" max="100" value={disabilityRate} ... />`
수정:
```jsx
<input
  type="text"
  inputMode="decimal"
  value={disabilityRate}
  onChange={(e) => setDisabilityRate(e.target.value.replace(/[^0-9.]/g, ''))}
  placeholder="0 ~ 100"
  ...
/>
```

**FLOW-03: 의사 과실비율 라벨 * 추가:**
- "의사 과실비율 (%)" → "의사 과실비율 (%) *"

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
{warning && <p className="text-orange-500 text-sm mb-3">{warning}</p>}
```

**RESULT-01: total=0 안내 (결과 "총 배상액" 섹션 위):**
```jsx
{result.total === 0 && (
  <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">총 배상액이 0원으로 계산되었습니다. 과실비율 또는 손해항목을 확인해주세요.</p>
  </div>
)}
```

---

### lost-income/page.tsx

**현황 파악:**
- `handleCalculate`: `if (!ageVal || !income || !retAge || ageVal >= retAge) return;` — 에러 없이 조용히 return (INPUT-02 위반)
- `error` state 없음
- FLOW-03: 나이·월순수입 라벨에 * 없음 (노동능력상실률도 사실상 필수)
- RESULT-01: `disRate=0` → `lostIncomeBeforeFault=0` → `lostIncomeAfterFault=0` 안내 필요
- EDGE-01: faultRate=100% → lostIncomeAfterFault=0 — 논리상 맞음, RESULT-01로 처리
- EDGE-03: 나누기 0 없음 (OK)
- INPUT-01: age, monthlyIncome, treatmentMonths에 `replace(/[^0-9]/g, '')` 적용됨 → 음수 불가 (OK)
- CONSIST-02: 금액 `formatNumber()원` 형식 (OK)
- INPUT-04: 모든 숫자 필드 type=text + inputMode=numeric (OK, 이미 준수)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
```

**handleCalculate 수정:**
```tsx
const handleCalculate = () => {
  setError(null);
  const ageVal = parseInt(age, 10);
  const income = parseInt(monthlyIncome.replace(/,/g, ''), 10);
  const retAge = retirementOption === 'custom'
    ? parseInt(customRetirementAge, 10)
    : parseInt(retirementOption, 10);
  const treatMonths = parseInt(treatmentMonths, 10) || 0;
  const disRate = parseFloat(disabilityRate) || 0;
  const fRate = parseFloat(faultRate) || 0;

  // INPUT-02: 나이 필수
  if (!age || !ageVal || ageVal <= 0) {
    setError('사고 당시 나이를 입력해주세요.');
    setResult(null);
    return;
  }
  if (ageVal > 100) {
    setError('나이는 100세 이하로 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-02: 월순수입 필수
  if (!monthlyIncome || !income || income <= 0) {
    setError('월 순수입을 입력해주세요.');
    setResult(null);
    return;
  }
  // INPUT-02: 가동연한 custom 시 검증
  if (retirementOption === 'custom' && (!customRetirementAge || !retAge || retAge <= 0)) {
    setError('가동연한 종료 나이를 입력해주세요.');
    setResult(null);
    return;
  }
  // EDGE-01: 나이 >= 가동연한
  if (ageVal >= retAge) {
    setError(`사고 당시 나이(${ageVal}세)가 가동연한(${retAge}세) 이상입니다. 나이 또는 가동연한을 확인해주세요.`);
    setResult(null);
    return;
  }

  const remainingMonths = (retAge - ageVal) * 12;
  const hoffmanCoeff = calculateHoffmanCoefficient(remainingMonths);
  const netIncome = income * (1 - LIVING_EXPENSE_DEDUCTION);
  const lostIncomeBeforeFault = Math.floor(netIncome * (disRate / 100) * hoffmanCoeff);
  const treatmentLoss = Math.floor(netIncome * treatMonths);
  const totalBeforeFault = lostIncomeBeforeFault + treatmentLoss;
  const lostIncomeAfterFault = Math.floor(totalBeforeFault * (1 - fRate / 100));

  setResult({
    remainingMonths,
    hoffmanCoeff,
    lostIncomeBeforeFault: totalBeforeFault,
    treatmentLoss,
    lostIncomeAfterFault,
    livingExpenseDeduction: LIVING_EXPENSE_DEDUCTION,
  });
};
```

**FLOW-03: 필수 필드 * 추가 (3개):**
- "사고 당시 나이 (세)" → "사고 당시 나이 (세) *"
- "월 순수입 (원, 세후 실수령액)" → "월 순수입 (원, 세후 실수령액) *"
- "노동능력 상실률 (%, 완전 상실 = 100)" → "노동능력 상실률 (%, 완전 상실 = 100) *" — 0이면 일실수입 0원 (실질적 필수)

**에러 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mb-3">{error}</p>}
```

**RESULT-01: 일실수입 0원 안내 (결과 "과실상계 후 일실수입" 섹션 위):**
```jsx
{result.lostIncomeAfterFault === 0 && (
  <div className="mb-4 p-3 rounded-lg bg-blue-50 border border-blue-200">
    <p className="text-sm text-blue-600">일실수입이 0원으로 계산되었습니다. 노동능력상실률 또는 과실비율을 확인해주세요.</p>
  </div>
)}
```
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|text-orange-500\|과실비율을 입력\|100억\|총 배상액이 0원\|inputMode.*decimal\|후유장해율은 100" src/app/tools/damages/medical-malpractice/page.tsx && grep -c "setError\|text-red-500\|나이를 입력\|월 순수입을 입력\|가동연한\|일실수입이 0원\|나이.*\*\|\*.*나이" src/app/tools/damages/lost-income/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" medical-malpractice/page.tsx returns match (에러 state 사용)
- grep "과실비율을 입력" medical-malpractice/page.tsx returns match (INPUT-02 에러 메시지)
- grep "100억\|10_000_000_000" medical-malpractice/page.tsx returns match (INPUT-03 경고)
- grep "후유장해율은 100" medical-malpractice/page.tsx returns match (EDGE-01 상한 검증)
- grep "총 배상액이 0원" medical-malpractice/page.tsx returns match (RESULT-01 안내)
- grep "inputMode.*decimal" medical-malpractice/page.tsx returns match (INPUT-04)
- grep "의사 과실비율.*\*" medical-malpractice/page.tsx returns match (FLOW-03)
- grep "setError" lost-income/page.tsx returns match (에러 state 사용)
- grep "나이를 입력\|순수입을 입력" lost-income/page.tsx returns match (INPUT-02 에러 메시지)
- grep "가동연한" lost-income/page.tsx returns match (EDGE-01 나이 검증)
- grep "일실수입이 0원" lost-income/page.tsx returns match (RESULT-01 안내)
- grep "\*" lost-income/page.tsx returns 3+ matches for 필수 필드 (FLOW-03)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep "^0$"
  </acceptance_criteria>
  <done>medical-malpractice: 과실비율 필수 에러, 장해율 100 초과 에러, 치료비 100억 경고, 0원 안내, type=text 변경 완료. lost-income: 나이/소득 필수 에러, 나이>=가동연한 에러, 0원 안내, 필수 * 표시 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- 4개 계산기 모두 에러/경고 패턴: text-red-500 / text-orange-500 통일
- type="number" → type="text" + inputMode="decimal" 변환 확인
- grep "setError" 각 파일에서 매치 확인 (defamation 제외 — select 기반)
- 0원 결과 안내 bg-blue-50 border-blue-200 패턴 확인
</verification>

<success_criteria>
- damages-general: 재산손해액 필수 에러, 100억 경고, 0원 안내, 필수 * 표시, 과실비율 type=text 완료
- defamation: 기간 음수 차단, type=text 변경, 선택 명시 완료
- medical-malpractice: 과실비율 필수 에러, 장해율 100 초과 에러, 100억 경고, 0원 안내, type=text 완료
- lost-income: 나이/소득 필수 에러, 나이>=가동연한 검증, 0원 안내, 필수 * 표시 완료
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/32-ux/32-01-SUMMARY.md`
</output>
