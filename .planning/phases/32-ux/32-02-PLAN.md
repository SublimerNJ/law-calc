---
phase: 32-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/misc/statute-of-limitations/page.tsx
  - src/app/tools/misc/public-defender/page.tsx
  - src/app/tools/misc/legal-aid/page.tsx
  - src/app/tools/misc/certified-letter/page.tsx
autonomous: true
requirements: [INPUT-01, INPUT-02, INPUT-03, INPUT-04, RESULT-01, RESULT-02, RESULT-03, FLOW-01, FLOW-02, FLOW-03, CONSIST-01, CONSIST-02, CONSIST-03, EDGE-01, EDGE-02, EDGE-03]

must_haves:
  truths:
    - "statute-of-limitations에서 기산일 미입력 시 에러 표시 후 결과 미출력"
    - "statute-of-limitations에서 남은 기간 표시 색상 text-red-400/text-green-400 → light 색상으로 수정"
    - "statute-of-limitations에서 기산일 라벨에 * 표시"
    - "public-defender에서 결과 박스 dark 색상(bg-green-900/30, text-green-400) → light 색상으로 수정"
    - "public-defender에서 재량적 국선 소득·재산 필드에 * 표시"
    - "legal-aid에서 결과 박스 dark 색상(bg-green-900/30, text-green-400) → light 색상으로 수정"
    - "legal-aid에서 신청 안내 박스 dark 색상(bg-blue-900/20, text-blue-300) → light 색상으로 수정"
    - "legal-aid에서 월 소득·재산 필드에 * 표시 (재량 기준 적용 필드)"
    - "certified-letter에서 필수 필드(발신인·수신인 이름, 제목, 본문) 미입력 시 에러 표시"
    - "certified-letter에서 필수 필드 라벨에 * 표시"
    - "4개 도구 모두 에러: text-red-500, 결과 강조: light 테마 색상 통일"
  artifacts:
    - path: "src/app/tools/misc/statute-of-limitations/page.tsx"
      provides: "소멸시효 계산기 with 기산일 에러, * 표시, light 색상 남은기간"
    - path: "src/app/tools/misc/public-defender/page.tsx"
      provides: "국선변호인 계산기 with light 테마 결과 박스, * 표시"
    - path: "src/app/tools/misc/legal-aid/page.tsx"
      provides: "법률구조공단 계산기 with light 테마 결과/안내 박스, * 표시"
    - path: "src/app/tools/misc/certified-letter/page.tsx"
      provides: "내용증명 작성기 with 필수 필드 에러, * 표시"
  key_links:
    - from: "statute-of-limitations calculate"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
    - from: "public-defender result box"
      to: "light theme colors"
      via: "bg-green-50/bg-red-50 border-green-200/border-red-200"
      pattern: "bg-green-50|bg-red-50"
    - from: "legal-aid result box"
      to: "light theme colors"
      via: "bg-green-50/bg-red-50 border-green-200/border-red-200"
      pattern: "bg-green-50|bg-red-50"
    - from: "certified-letter handlePreview"
      to: "error state display"
      via: "setError + conditional rendering"
      pattern: "setError|error &&"
---

<objective>
statute-of-limitations, public-defender, legal-aid, certified-letter 4개 기타 법률도구에 16개 요구사항(INPUT, RESULT, FLOW, CONSIST, EDGE) 감사 및 수정

Purpose: 기타 법률도구의 dark 테마 잔재 color 일괄 수정 및 필수 입력 에러 처리 추가 — 사용자가 라이트 테마에서 가독성 저하 없이 사용 가능하게 (per D-01, D-02, D-03)
Output: 4개 도구 모두 dark 테마 색상 잔재 제거, 필수 필드 에러 처리, * 표시 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-ux/32-CONTEXT.md
@src/app/tools/misc/statute-of-limitations/page.tsx
@src/app/tools/misc/public-defender/page.tsx
@src/app/tools/misc/legal-aid/page.tsx
@src/app/tools/misc/certified-letter/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: statute-of-limitations + public-defender 계산기 UX·논리 감사 및 수정</name>
  <files>
    src/app/tools/misc/statute-of-limitations/page.tsx
    src/app/tools/misc/public-defender/page.tsx
  </files>
  <read_first>
    src/app/tools/misc/statute-of-limitations/page.tsx
    src/app/tools/misc/public-defender/page.tsx
  </read_first>
  <action>
두 파일을 순서대로 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

---

### statute-of-limitations/page.tsx

**현황 파악:**
- `calculate()`: `if (!startDate) return;` — 에러 없이 조용히 return (INPUT-02 위반)
- `error` state 없음
- CONSIST-03: 남은 기간 표시 색상 `text-red-400` (소멸) / `text-green-400` (유효) — dark 톤 색상으로 라이트 테마에서 가독성 불량
- FLOW-03: "기산일 (권리 행사 가능일 또는 피해 인지일 — 민법 제166조)" 라벨에 * 없음
- EDGE-02: 미래 날짜 기산일 입력 허용 — 법적으로 미래 기산일은 발생 가능하므로 허용 (예: 계약 만료일) → 경고만 표시
- FLOW-01: 초기 result=null → 결과 미출력 (OK)
- RESULT-01: 소멸시효 완성 시 "이미 소멸" 표시 존재 (OK)
- EDGE-01: years=1~10, 경계값에서 계산 정상 (OK)
- EDGE-03: 나누기 0 없음 (OK)

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**calculate 함수 수정:**
```tsx
const calculate = () => {
  setError(null);
  setWarning(null);
  // INPUT-02: 기산일 필수
  if (!startDate) {
    setError('기산일을 선택해주세요.');
    setResult(null);
    return;
  }
  const claim = CLAIM_TYPES[claimTypeIdx];
  const start = new Date(startDate);
  // EDGE-02: 미래 기산일 경고 (차단하지는 않음 — 계약 만료일 등 미래 기산도 유효)
  const today = new Date();
  today.setHours(0, 0, 0, 0);
  if (start > today) {
    setWarning('기산일이 오늘 이후입니다. 미래 기산일인 경우 그대로 계산합니다.');
  }
  const expiry = new Date(start);
  expiry.setFullYear(expiry.getFullYear() + claim.years);
  const expired = expiry <= today;

  setResult({
    period: `${claim.years}년`,
    expiryDate: expiry,
    remaining: diffDescription(today, expiry),
    expired,
    legalBasis: claim.description,
    note: claim.note,
  });
};
```

**에러/경고 표시:** 계산하기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mt-3 mb-0">{error}</p>}
{warning && <p className="text-orange-500 text-sm mt-3 mb-0">{warning}</p>}
```

**FLOW-03: 기산일 라벨 * 추가:**
기존: `<label className="block text-sm text-slate-600 mb-1">기산일 (권리 행사 가능일 또는 피해 인지일 — 민법 제166조)</label>`
수정: `<label className="block text-sm text-slate-600 mb-1">기산일 (권리 행사 가능일 또는 피해 인지일 — 민법 제166조) *</label>`

**CONSIST-03: 남은 기간 색상 수정 (라이트 테마):**
기존:
```jsx
<span className={`font-semibold ${result.expired ? 'text-red-400' : 'text-green-400'}`}>
```
수정:
```jsx
<span className={`font-semibold ${result.expired ? 'text-red-600' : 'text-green-600'}`}>
```

---

### public-defender/page.tsx

**현황 파악:**
- `calculate()`: 항상 실행됨 — 모든 입력이 select(기본값 있음) 또는 필수 아닌 숫자 → INPUT-02는 재량적 국선(discretionary)에서 소득/재산 미입력 시 0으로 처리되어 "자격 충족"으로 오판 가능. 단, 소득 0원 자체가 유효한 값이므로 에러보다 안내 메시지가 적절
- CONSIST-03 버그: 결과 박스 `bg-green-900/30 border border-green-700` + `text-green-400` / `bg-red-900/30 border border-red-700` + `text-red-400` — dark 테마 색상 (라이트 테마에서 가독성 불량)
- FLOW-01: 초기 result=null → OK
- FLOW-03: 재량적 국선에서 소득/재산 필드에 * 없음 — 이 값들이 판정 기준이므로 "(판정 기준)" 명시 추가
- EDGE-01: 소득=0, 재산=0 → incomeOk=true, assetsOk=true → 선정 대상 판정 — 논리상 맞음 (소득/재산 없으면 당연히 대상)
- EDGE-03: 나누기 0 없음

**CONSIST-03: 결과 박스 light 색상 수정:**
기존:
```jsx
<div className={`p-4 rounded-lg mb-4 ${result.eligible ? 'bg-green-900/30 border border-green-700' : 'bg-red-900/30 border border-red-700'}`}>
  <p className={`text-lg font-bold ${result.eligible ? 'text-green-400' : 'text-red-400'}`}>
```
수정:
```jsx
<div className={`p-4 rounded-lg mb-4 ${result.eligible ? 'bg-green-50 border border-green-200' : 'bg-red-50 border border-red-200'}`}>
  <p className={`text-lg font-bold ${result.eligible ? 'text-green-700' : 'text-red-700'}`}>
```

**FLOW-03: 재량적 국선 소득/재산 필드 라벨 수정:**
- "월 평균 소득 (원)" → "월 평균 소득 (원, 판정 기준)"
- "재산 총액 (원)" → "재산 총액 (원, 판정 기준)"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "setError\|text-red-500\|기산일을 선택\|text-green-600\|text-red-600\|bg-green-50\|bg-red-50\|text-green-700\|text-red-700\|판정 기준\|\* $\|민법.*\*" src/app/tools/misc/statute-of-limitations/page.tsx src/app/tools/misc/public-defender/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "setError" statute-of-limitations/page.tsx returns match (에러 state 사용)
- grep "기산일을 선택" statute-of-limitations/page.tsx returns match (INPUT-02 에러)
- grep "text-green-600\|text-red-600" statute-of-limitations/page.tsx returns match (CONSIST-03 light 색상)
- grep "\*" statute-of-limitations/page.tsx returns match (FLOW-03 기산일 *)
- grep "bg-green-50\|border-green-200" public-defender/page.tsx returns match (CONSIST-03 light 결과박스)
- grep "text-green-700\|text-red-700" public-defender/page.tsx returns match (CONSIST-03 light 텍스트)
- grep "판정 기준" public-defender/page.tsx returns match (FLOW-03 필드 설명)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep "^0$"
  </acceptance_criteria>
  <done>statute-of-limitations: 기산일 에러, * 표시, 남은기간 light 색상 완료. public-defender: 결과 박스 light 색상, 재량 필드 판정기준 명시 완료.</done>
</task>

<task type="auto">
  <name>Task 2: legal-aid + certified-letter 도구 UX·논리 감사 및 수정</name>
  <files>
    src/app/tools/misc/legal-aid/page.tsx
    src/app/tools/misc/certified-letter/page.tsx
  </files>
  <read_first>
    src/app/tools/misc/legal-aid/page.tsx
    src/app/tools/misc/certified-letter/page.tsx
  </read_first>
  <action>
두 파일을 순서대로 읽고 16개 요구사항 감사 후 즉시 수정 (per D-03, D-04):

---

### legal-aid/page.tsx

**현황 파악:**
- `calculate()`: 항상 실행됨 — 소득/재산 미입력 시 0으로 처리 → 소득 0원이면 항상 지원 가능으로 판정. 소득 0은 유효한 값(무직)이므로 에러보다 안내가 적절
- CONSIST-03 버그 1: 결과 박스 `bg-green-900/30 border border-green-700` + `text-green-400` / `bg-red-900/30 border border-red-700` + `text-red-400` — dark 테마 색상
- CONSIST-03 버그 2: 신청 안내 박스 `bg-blue-900/20 border border-blue-800` + `text-blue-300` / `text-yellow-300` — dark 테마 색상
- FLOW-01: 초기 result=null → OK
- FLOW-03: "월 소득 (원)" / "재산 총액 (원)" 라벨에 *(판정기준) 명시 없음
- EDGE-01: income=0 → 무직 → 지원 가능 판정 — 논리상 맞음
- EDGE-03: 나누기 0 없음

**CONSIST-03: 결과 박스 light 색상 수정:**
기존:
```jsx
<div className={`p-4 rounded-lg mb-4 ${result.eligible ? 'bg-green-900/30 border border-green-700' : 'bg-red-900/30 border border-red-700'}`}>
  <p className={`text-lg font-bold ${result.eligible ? 'text-green-400' : 'text-red-400'}`}>
```
수정:
```jsx
<div className={`p-4 rounded-lg mb-4 ${result.eligible ? 'bg-green-50 border border-green-200' : 'bg-red-50 border border-red-200'}`}>
  <p className={`text-lg font-bold ${result.eligible ? 'text-green-700' : 'text-red-700'}`}>
```

**CONSIST-03: 신청 안내 박스 light 색상 수정:**
기존:
```jsx
<div className="p-3 rounded-lg bg-blue-900/20 border border-blue-800">
  <p className="text-sm text-blue-300">
    ...
  </p>
  {supportType === 'criminal' && (
    <p className="text-xs text-yellow-300 mt-2">
```
수정:
```jsx
<div className="p-3 rounded-lg bg-blue-50 border border-blue-200">
  <p className="text-sm text-blue-700">
    ...
  </p>
  {supportType === 'criminal' && (
    <p className="text-xs text-orange-600 mt-2">
```

**FLOW-03: 소득/재산 필드 라벨 수정 (판정 기준 명시):**
- "월 소득 (원)" → "월 소득 (원, 판정 기준)"
- "재산 총액 (원)" → "재산 총액 (원, 판정 기준)"

---

### certified-letter/page.tsx

**현황 파악:**
- `handlePreview()`: `if (!senderName || !receiverName || !title || !body) return;` — 조용히 return, 에러 없음 (INPUT-02 위반)
- `error` state 없음
- FLOW-03: 발신인 이름·수신인 이름·제목·본문 라벨에 * 없음
- EDGE-02: `sendDate` — 미래 날짜 허용 (내용증명은 미래 날짜 발신 불가 → 경고 필요). 과거 날짜는 허용 (소급 발행은 현실적으로 존재)
- FLOW-01: `preview=false` 초기 → 결과 미출력 (OK)
- CONSIST-03: yellow/blue 안내 박스는 이미 light 색상 (`bg-yellow-50 border-yellow-300`, `bg-blue-50 border-blue-200`) (OK)
- INPUT-04: 이름/주소/제목/본문은 텍스트 필드 — 숫자 아님, INPUT-04 N/A
- RESULT-02: 금액 없음 — N/A

**추가할 state:**
```tsx
const [error, setError] = useState<string | null>(null);
const [warning, setWarning] = useState<string | null>(null);
```

**handlePreview 수정:**
```tsx
const handlePreview = () => {
  setError(null);
  setWarning(null);
  // INPUT-02: 필수 필드 검증
  if (!senderName) {
    setError('발신인 이름을 입력해주세요.');
    return;
  }
  if (!receiverName) {
    setError('수신인 이름을 입력해주세요.');
    return;
  }
  if (!title) {
    setError('제목을 입력해주세요.');
    return;
  }
  if (!body) {
    setError('본문을 입력해주세요.');
    return;
  }
  // EDGE-02: 미래 발신일 경고
  const today = getTodayString();
  if (sendDate > today) {
    setWarning('발신일이 오늘 이후입니다. 내용증명은 실제 발송일에 맞게 작성하세요.');
  }
  setPreview(true);
  setCopied(false);
};
```

**FLOW-03: 필수 필드 라벨 * 추가 (4개):**
- "발신인 이름" → "발신인 이름 *"
- "수신인 이름" → "수신인 이름 *"
- "제목" → "제목 *"
- "본문" → "본문 *"

**에러/경고 표시:** 미리보기 버튼 바로 위:
```jsx
{error && <p className="text-red-500 text-sm mt-3 mb-0">{error}</p>}
{warning && <p className="text-orange-500 text-sm mt-3 mb-0">{warning}</p>}
```
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && grep -c "bg-green-50\|bg-red-50\|text-green-700\|text-red-700\|bg-blue-50\|text-blue-700\|판정 기준" src/app/tools/misc/legal-aid/page.tsx && grep -c "setError\|text-red-500\|발신인 이름을 입력\|수신인 이름을 입력\|제목을 입력\|본문을 입력\|발신인 이름 \*\|수신인 이름 \*\|제목 \*\|본문 \*" src/app/tools/misc/certified-letter/page.tsx</automated>
  </verify>
  <acceptance_criteria>
- grep "bg-green-50\|border-green-200" legal-aid/page.tsx returns match (CONSIST-03 결과박스 light)
- grep "text-green-700\|text-red-700" legal-aid/page.tsx returns match (CONSIST-03 텍스트 light)
- grep "bg-blue-50\|text-blue-700" legal-aid/page.tsx returns match (CONSIST-03 안내박스 light)
- grep "text-orange-600\|orange" legal-aid/page.tsx returns match (형사변호 경고 light 색상)
- grep "판정 기준" legal-aid/page.tsx returns match (FLOW-03 필드 명시)
- grep "setError" certified-letter/page.tsx returns match (에러 state 사용)
- grep "발신인 이름을 입력\|수신인 이름을 입력" certified-letter/page.tsx returns match (INPUT-02)
- grep "제목을 입력\|본문을 입력" certified-letter/page.tsx returns match (INPUT-02)
- grep "text-red-500" certified-letter/page.tsx returns match (에러 스타일)
- grep "\*" certified-letter/page.tsx returns 4+ matches (FLOW-03 필수 *)
- cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep -c "error TS" | grep "^0$"
  </acceptance_criteria>
  <done>legal-aid: 결과/안내 박스 light 색상, 판정기준 필드 명시 완료. certified-letter: 필수 필드 에러 처리, * 표시, 미래 발신일 경고 완료.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit 전체 프로젝트 빌드 에러 없음
- dark 색상 잔재(bg-green-900/30, bg-red-900/30, bg-blue-900/20, text-green-400, text-red-400, text-blue-300, text-yellow-300) → light 색상으로 수정 확인
- grep "setError" certified-letter/page.tsx, statute-of-limitations/page.tsx 매치 확인
- 4개 도구 모두 에러: text-red-500, 경고: text-orange-500 통일
</verification>

<success_criteria>
- statute-of-limitations: 기산일 필수 에러, * 표시, 남은기간 light 색상, 미래기산일 경고 완료
- public-defender: 결과 박스 light 색상, 재량 필드 판정기준 명시 완료
- legal-aid: 결과/안내 박스 모두 light 색상, 소득/재산 필드 판정기준 명시 완료
- certified-letter: 필수 4개 필드 에러 처리, * 표시, 미래 발신일 경고 완료
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/32-ux/32-02-SUMMARY.md`
</output>
