---
phase: 38-actioninsight
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified: [
  "src/app/tools/traffic/accident-settlement/page.tsx",
  "src/app/tools/traffic/drunk-driving/page.tsx",
  "src/app/tools/traffic/fine-penalty/page.tsx",
  "src/app/tools/traffic/bail/page.tsx"
]
autonomous: true
requirements: ["UI-06"]

must_haves:
  truths:
    - "4종 교통/형사 계산기 하단에 ActionInsight 컴포넌트가 올바르게 표시된다"
    - "계산 결과가 있든 없든 팁 섹션은 항상 접근 가능하다"
  artifacts:
    - path: "src/app/tools/traffic/accident-settlement/page.tsx"
      provides: "교통사고 합의금 계산기 UI 렌더링"
    - path: "src/app/tools/traffic/drunk-driving/page.tsx"
      provides: "음주운전 처벌 계산기 UI 렌더링"
    - path: "src/app/tools/traffic/fine-penalty/page.tsx"
      provides: "과태료/범칙금 계산기 UI 렌더링"
    - path: "src/app/tools/traffic/bail/page.tsx"
      provides: "보석 보증금 계산기 UI 렌더링"
  key_links:
    - from: "src/app/tools/traffic/*/page.tsx"
      to: "src/components/ui/ActionInsight.tsx"
      via: "React Component Import"
      pattern: "<ActionInsight actionData={actionData"
---

<objective>
교통/형사 계산기 4종의 UI에 ActionInsight 컴포넌트를 연동한다.

Purpose: 사용자가 계산 전/후 언제든 실전 팁과 대응 템플릿을 확인할 수 있도록 한다.
Output: 각 페이지의 하단(CalculatorLayout 내부, 기존 요소 아래)에 ActionInsight 컴포넌트 추가
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/38-actioninsight/38-CONTEXT.md
</context>

<interfaces>
From src/components/ui/ActionInsight.tsx:
```typescript
interface ActionInsightProps {
  actionData: {
    title: string;
    tips: string[];
    scriptTemplate?: {
      title: string;
      text: string;
      buttonLabel: string;
    };
  };
  className?: string;
}
export default function ActionInsight({ actionData, className }: ActionInsightProps)
```

From src/lib/action-data.ts:
```typescript
export const actionData: CalculatorActionData
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 교통/형사 4개 계산기 UI 연동</name>
  <files>
    src/app/tools/traffic/accident-settlement/page.tsx,
    src/app/tools/traffic/drunk-driving/page.tsx,
    src/app/tools/traffic/fine-penalty/page.tsx,
    src/app/tools/traffic/bail/page.tsx
  </files>
  <action>
    위 4개 파일 각각에 대하여 다음 작업을 수행한다:
    
    1. `ActionInsight` 컴포넌트를 import 한다: `import ActionInsight from '@/components/ui/ActionInsight';`
    2. `actionData`를 import 한다: `import { actionData } from '@/lib/action-data';`
    3. `return` 문 내부, `<CalculatorLayout>`의 마지막 자식 요소로 `<ActionInsight actionData={actionData['해당도구ID']} className="mt-6" />` 를 추가한다.
    
    주의: 기존 페이지 내 로직이나 구조(result 조건부 렌더링 등)는 수정하지 않고, ActionInsight 컴포넌트는 result 조건문 바깥의 최상위 레벨 `<CalculatorLayout>` 내부에 위치해야 한다 (계산 결과가 없어도 볼 수 있도록). (per D-04)
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>4개 페이지 모두 빌드 에러 없이 정상적으로 ActionInsight 컴포넌트가 추가된다</done>
</task>

</tasks>

<verification>
- 4개 파일에 `import ActionInsight`가 포함되어 있다.
- 4개 파일에 `<ActionInsight actionData={actionData['id']} />` 가 포함되어 있다.
</verification>

<success_criteria>
- 교통/형사 도구 4종에 ActionInsight가 성공적으로 렌더링됨
</success_criteria>

<output>
After completion, create `.planning/phases/38-actioninsight/38-02-SUMMARY.md`
</output>