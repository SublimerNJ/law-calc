---
phase: 38-actioninsight
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/lib/action-data.ts"]
autonomous: true
requirements: ["ACTION-06"]

must_haves:
  truths:
    - "액션 데이터에 4개의 교통/형사 도구(accident-settlement, drunk-driving, fine-penalty, bail) 데이터가 포함된다"
    - "각 데이터는 단호하고 전문적인 톤의 팁 3개와 카톡 전송용 스크립트 템플릿을 제공한다"
  artifacts:
    - path: "src/lib/action-data.ts"
      provides: "교통/형사 특화 ActionInsight 데이터"
  key_links:
    - from: "src/lib/action-data.ts"
      to: "CalculatorActionData"
      via: "Type adherence"
      pattern: "export const actionData"
---

<objective>
교통/형사 계산기 4종에 대한 ActionInsight 템플릿 데이터를 추가한다.

Purpose: 교통/형사 사건 관련하여 법률 분쟁 전 단계에서 활용할 수 있는 실무적인 조언과 카톡/내용증명 템플릿을 제공한다.
Output: `action-data.ts` 파일 내 4개 도구 항목 추가
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/38-actioninsight/38-CONTEXT.md
</context>

<interfaces>
From src/types/action-insights.ts:
```typescript
export interface CalculatorActionData {
  [key: string]: {
    title: string;
    tips: string[];
    scriptTemplate?: {
      title: string;
      text: string;
      buttonLabel: string;
    };
  };
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: action-data.ts 교통/형사 데이터 추가</name>
  <files>src/lib/action-data.ts</files>
  <action>
    `src/lib/action-data.ts` 파일의 `actionData` 객체에 다음 4개 도구에 대한 데이터를 추가한다.
    - `accident-settlement` (교통사고 합의금): 보험사 합의, 병원 진단서, 손해사정사/변호사 상담 기준 등 팁과 합의 요구 카톡 템플릿
    - `drunk-driving` (음주운전 처벌): 초범/재범, 혈중알코올농도에 따른 대처, 행정심판(면허구제) 팁과 선처 호소(또는 반성문 제출) 관련 가이드 템플릿
    - `fine-penalty` (과태료/범칙금): 이의신청 기한, 벌점 누적 관리, 범칙금 전환의 유불리 등 팁과 이의제기 관련 템플릿
    - `bail` (보석 보증금): 보석 청구 시기, 보증보험증권 활용, 탄원서/합의서 중요성 등 팁과 피해자 합의 요청 템플릿
    
    주의: Tone은 '전문가 조언' 톤이어야 하며 팁은 핵심 3가지로 제한한다. (per D-01/D-02/D-03)
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>actionData 객체에 4개 항목이 정상적으로 추가되고 타입 검사를 통과한다</done>
</task>

</tasks>

<verification>
- `action-data.ts` 파일에 4개의 키가 존재하는지 확인
</verification>

<success_criteria>
- 4종 교통/형사 도구 데이터가 `action-data.ts`에 추가됨
</success_criteria>

<output>
After completion, create `.planning/phases/38-actioninsight/38-01-SUMMARY.md`
</output>