---
phase: 10-misc-tools
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/app/sitemap.ts
autonomous: false
requirements:
  - MISC-01
  - MISC-02
  - MISC-03
  - MISC-04

must_haves:
  truths:
    - "sitemap.ts가 존재하고 전체 70개 도구의 URL을 포함한다"
    - "npm run build가 오류 없이 완료된다"
    - "빌드 결과물에 /tools/misc/* 4개 경로가 포함된다"
  artifacts:
    - path: src/app/sitemap.ts
      provides: Next.js 15 MetadataRoute.Sitemap — 홈 + 70개 도구 URL
      exports: [default (sitemap function)]
  key_links:
    - from: src/app/sitemap.ts
      to: src/lib/tools-data.ts
      via: "import { TOOLS } from '@/lib/tools-data' — TOOLS.map(t => t.route)"
---

<objective>
sitemap.ts 생성 (전체 70개 도구 URL) + npm run build 성공 확인.

Purpose: SEO 마무리 및 전체 빌드 품질 보증.
Output: src/app/sitemap.ts 파일, 성공한 프로덕션 빌드.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-misc-tools/10-01-SUMMARY.md

<interfaces>
From src/lib/tools-data.ts:
```typescript
export const TOOLS: Tool[];
// 70 tools, each with: { id, route, name, description, category }
// route examples: "/tools/court/attorney-fee", "/tools/misc/certified-letter"

export const CATEGORIES: Category[];
// 9 categories
```

Next.js 15 sitemap API (App Router):
```typescript
// src/app/sitemap.ts
import { MetadataRoute } from 'next'

export default function sitemap(): MetadataRoute.Sitemap {
  return [
    {
      url: 'https://example.com',
      lastModified: new Date(),
      changeFrequency: 'monthly',
      priority: 1,
    },
    // ...
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sitemap.ts 생성</name>
  <files>src/app/sitemap.ts</files>
  <action>
`src/app/sitemap.ts`를 생성한다. Next.js 15 App Router의 `MetadataRoute.Sitemap` 형식을 사용한다.

```typescript
import { MetadataRoute } from 'next';
import { TOOLS } from '@/lib/tools-data';

const BASE_URL = 'https://lawcalc.kr';  // 또는 process.env.NEXT_PUBLIC_BASE_URL ?? 'https://lawcalc.kr'

export default function sitemap(): MetadataRoute.Sitemap {
  const toolUrls: MetadataRoute.Sitemap = TOOLS.map((tool) => ({
    url: `${BASE_URL}${tool.route}`,
    lastModified: new Date(),
    changeFrequency: 'monthly',
    priority: 0.8,
  }));

  return [
    {
      url: BASE_URL,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1.0,
    },
    ...toolUrls,
  ];
}
```

TOOLS 배열에 70개 항목이 있으므로 결과 sitemap은 총 71개 URL (홈 1 + 도구 70)을 포함한다.

`changeFrequency` 타입은 `'always' | 'hourly' | 'daily' | 'weekly' | 'monthly' | 'yearly' | 'never'` — 리터럴 문자열로 작성한다 (string 타입 오류 방지).
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | grep sitemap || echo "sitemap.ts: no type errors"</automated>
  </verify>
  <done>
    src/app/sitemap.ts 파일 존재. TypeScript 오류 없음. TOOLS import로 70개 URL 동적 생성. 홈 포함 71개 항목.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 10-01: 4개 misc 도구 페이지 (statute-of-limitations, public-defender, legal-aid, certified-letter)
    Plan 10-02 Task 1: src/app/sitemap.ts (71개 URL)

    Claude가 자동으로 실행한 작업:
    - npm run build (프로덕션 빌드)
    - TypeScript 타입 체크

    빌드 명령: cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npm run build
  </what-built>
  <how-to-verify>
    1. 터미널에서 빌드 실행: npm run build
       - 기대: "✓ Compiled successfully" 또는 "Route (app)" 테이블 출력, 오류 없음
       - /tools/misc/statute-of-limitations, /tools/misc/public-defender, /tools/misc/legal-aid, /tools/misc/certified-letter 경로가 빌드 출력에 포함되어야 함

    2. npm run dev로 로컬 서버 실행 후 확인:
       - http://localhost:3000/tools/misc/statute-of-limitations — 소멸시효 계산기 렌더링
       - http://localhost:3000/tools/misc/public-defender — 국선변호사 확인기 렌더링
       - http://localhost:3000/tools/misc/legal-aid — 법률구조 확인기 렌더링
       - http://localhost:3000/tools/misc/certified-letter — 내용증명 도우미 렌더링
       - http://localhost:3000/sitemap.xml — 71개 URL 포함 확인

    3. 각 도구 기능 간단 확인:
       - 소멸시효: 청구권 유형 선택 + 기산일 입력 → 만료일 출력
       - 국선변호사: 소득 입력 → 자격 판정
       - 법률구조: 가구원 수 + 소득 입력 → 지원 여부
       - 내용증명: 발신인/수신인/본문 입력 → 미리보기 버튼 → 양식 출력
  </how-to-verify>
  <resume-signal>빌드 성공 및 4개 도구 동작 확인 시 "approved" 입력. 문제 발생 시 오류 내용 설명.</resume-signal>
</task>

</tasks>

<verification>
```bash
cd "/Volumes/NJ SSD 4T/Coding/loop creation 2"
# sitemap 파일 존재
ls src/app/sitemap.ts
# TypeScript 전체 체크
npx tsc --noEmit
# 프로덕션 빌드 (checkpoint에서 실행)
npm run build
```
</verification>

<success_criteria>
- src/app/sitemap.ts 존재, TOOLS에서 70개 URL 동적 생성
- npm run build 성공 (오류 0)
- /tools/misc/* 4개 경로 빌드 출력에 포함
- /sitemap.xml 접근 시 71개 URL 반환
- 전체 70개 도구 메인 페이지에서 접근 가능
</success_criteria>

<output>
After completion, create `.planning/phases/10-misc-tools/10-02-SUMMARY.md` documenting:
- sitemap.ts 구조 및 URL 수
- 빌드 결과 (성공/실패, 경고 사항)
- Phase 10 전체 완료 확인
</output>
