---
phase: 07-traffic-calculators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/tools/traffic/accident-settlement/page.tsx
  - src/app/tools/traffic/fault-ratio/page.tsx
  - src/app/tools/traffic/drunk-driving/page.tsx
autonomous: true
requirements:
  - TRAFFIC-01
  - TRAFFIC-02
  - TRAFFIC-03

must_haves:
  truths:
    - "교통사고 합의금 계산기가 /tools/traffic/accident-settlement 라우트로 접근 가능하다"
    - "교통사고 과실비율 계산기가 /tools/traffic/fault-ratio 라우트로 접근 가능하다"
    - "음주운전 처벌 계산기가 /tools/traffic/drunk-driving 라우트로 접근 가능하다"
    - "각 계산기는 CalculatorLayout을 사용하고 입력값에 따라 결과를 표시한다"
    - "2026년 한국 법률 기준이 반영되어 있다"
  artifacts:
    - path: "src/app/tools/traffic/accident-settlement/page.tsx"
      provides: "교통사고 합의금 계산기 (치료비/위자료/휴업손해)"
      exports: ["default AccidentSettlementPage"]
    - path: "src/app/tools/traffic/fault-ratio/page.tsx"
      provides: "교통사고 과실비율 계산기 (사고유형별 기본/수정비율)"
      exports: ["default FaultRatioPage"]
    - path: "src/app/tools/traffic/drunk-driving/page.tsx"
      provides: "음주운전 처벌 계산기 (혈중알코올농도별 처벌기준)"
      exports: ["default DrunkDrivingPage"]
  key_links:
    - from: "각 page.tsx"
      to: "src/lib/tools-data.ts"
      via: "TOOLS.find(t => t.id === '...')"
      pattern: "TOOLS\\.find"
    - from: "각 page.tsx"
      to: "src/components/ui/CalculatorLayout.tsx"
      via: "import CalculatorLayout"
      pattern: "CalculatorLayout"
---

<objective>
교통/형사 계산기 6개 중 첫 3개 구현: 교통사고 합의금(TRAFFIC-01), 과실비율(TRAFFIC-02), 음주운전 처벌(TRAFFIC-03).

Purpose: 교통사고 관련 법률 비용을 한국 법률 기준에 따라 정확히 계산한다.
Output: 3개의 독립 Next.js 페이지 (`src/app/tools/traffic/[slug]/page.tsx`)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-traffic-calculators/07-CONTEXT.md

<interfaces>
<!-- src/lib/tools-data.ts 에서 필요한 항목 -->
```typescript
export interface Tool {
  id: string;       // 예: 'accident-settlement'
  name: string;     // 예: '교통사고 합의금 계산기'
  description: string;
  icon: string;
  category: string; // 'traffic'
  route: string;    // '/tools/traffic/accident-settlement'
}

export interface Category {
  id: string;    // 'traffic'
  name: string;  // '교통/형사'
  icon: string;
  description: string;
  color: string; // '#ef4444'
}

// tools-data.ts 의 traffic 항목
{ id: "accident-settlement", name: "교통사고 합의금 계산기", ... }
{ id: "fault-ratio",         name: "교통사고 과실비율 계산기", ... }
{ id: "drunk-driving",       name: "음주운전 처벌 계산기", ... }

// CATEGORIES 에서 traffic
{ id: "traffic", name: "교통/형사", color: "#ef4444" }
```

<!-- src/components/ui/CalculatorLayout.tsx 시그니처 -->
```typescript
interface CalculatorLayoutProps {
  tool: Tool;
  category: Category;
  children: React.ReactNode;
}
export default function CalculatorLayout({ tool, category, children }: CalculatorLayoutProps)
```

<!-- 참고: attorney-fee/page.tsx 패턴 -->
'use client';
const tool = TOOLS.find(t => t.id === 'attorney-fee')!;
const category = CATEGORIES.find(c => c.id === 'court')!;
// useState로 입력 관리, 계산 함수 분리, premium-card 클래스 사용
// 결과: color는 category.color 적용
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 교통사고 합의금 계산기 (TRAFFIC-01)</name>
  <files>src/app/tools/traffic/accident-settlement/page.tsx</files>
  <action>
    attorney-fee/page.tsx 패턴을 그대로 따라 'use client' 컴포넌트 작성.

    tool = TOOLS.find(t => t.id === 'accident-settlement')!
    category = CATEGORIES.find(c => c.id === 'traffic')!

    입력 항목:
    - 치료비 (원, 숫자 입력)
    - 치료 기간 (일, 입원일수 + 통원일수 각각)
    - 후유장해 등급 (없음/14급~1급 select, 없으면 0%)
    - 과실비율 (%, 0~100 슬라이더 또는 숫자 입력, 피해자 과실)

    계산 로직 (2026년 기준):
    - 위자료:
      - 입원: 85,000원/일 × 입원일수
      - 통원: 45,000원/일 × 통원일수
      - 후유장해 위자료: 장해등급별 표준 (1급 80,000,000원 ~ 14급 5,000,000원)
        [1급:80,000,000 / 2급:70,000,000 / 3급:60,000,000 / 4급:50,000,000 /
         5급:40,000,000 / 6급:30,000,000 / 7급:25,000,000 / 8급:20,000,000 /
         9급:15,000,000 / 10급:12,000,000 / 11급:9,000,000 / 12급:6,000,000 /
         13급:5,500,000 / 14급:5,000,000]
    - 합계 = 치료비 + 위자료 합산
    - 과실상계: 합계 × (1 - 피해자과실/100)
    - 최종 합의금 = 과실상계 후 금액

    결과 표시:
    - 항목별 내역 (치료비, 위자료, 소계, 과실상계, 최종 합의금)
    - 최종 합의금을 category.color(#ef4444)로 강조
    - 법적 근거: "교통사고처리특례법, 손해배상 산정기준 (2026)"

    formatNumber(n) 함수 사용 (toLocaleString('ko-KR'))
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>페이지가 TypeScript 오류 없이 컴파일되고, 입력 후 계산 버튼을 누르면 항목별 합의금 내역이 표시된다</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 교통사고 과실비율 계산기 (TRAFFIC-02)</name>
  <files>src/app/tools/traffic/fault-ratio/page.tsx</files>
  <action>
    attorney-fee/page.tsx 패턴을 따라 'use client' 컴포넌트 작성.

    tool = TOOLS.find(t => t.id === 'fault-ratio')!
    category = CATEGORIES.find(c => c.id === 'traffic')!

    입력 항목:
    - 사고 유형 (select):
      1. 신호위반 차량 vs 직진 차량 — 기본 과실: 가해(신호위반) 80%, 피해 20%
      2. 직진 vs 좌회전 — 기본: 좌회전 70%, 직진 30%
      3. 직진 vs 우회전 — 기본: 우회전 60%, 직진 40%
      4. 후진 차량 vs 직진 차량 — 기본: 후진 90%, 직진 10%
      5. 앞차 추돌 — 기본: 뒤차 100%, 앞차 0%
      6. 차선 변경 vs 직진 — 기본: 변경 70%, 직진 30%
      7. 교차로 동방향 직진 vs 직진 — 기본: 각 50%
      8. 보행자 횡단 중 충돌 — 기본: 차량 90%, 보행자 10%
    - 수정 요소 (체크박스 다중):
      - 피해 차량 과속 (-10%)
      - 가해 차량 현저한 과속 (+10% 피해자 유리)
      - 야간 사고 (-5%)
      - 우선도로 주행 (+10% 우선도로 차량 유리)
      - 어린이보호구역 (+20% 차량 불리)

    계산 로직:
    - 선택된 사고유형의 기본 과실비율을 가져옴
    - 수정 요소 적용 (가해차량 과실 기준으로 +/-)
    - 결과: 가해차량 과실 X%, 피해차량 과실 (100-X)%
    - 범위 클램프: 0~100

    결과 표시:
    - 가해/피해 차량 과실 비율 (큰 숫자로)
    - 막대 그래프 (div width %)로 시각화
    - 법적 근거: "교통사고 과실비율 인정기준 (금융감독원/법원)"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>페이지가 TypeScript 오류 없이 컴파일되고, 사고유형 선택 후 계산 버튼을 누르면 과실비율이 표시된다</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 음주운전 처벌 계산기 (TRAFFIC-03)</name>
  <files>src/app/tools/traffic/drunk-driving/page.tsx</files>
  <action>
    attorney-fee/page.tsx 패턴을 따라 'use client' 컴포넌트 작성.

    tool = TOOLS.find(t => t.id === 'drunk-driving')!
    category = CATEGORIES.find(c => c.id === 'traffic')!

    입력 항목:
    - 혈중알코올농도 BAC (%, 소수점 3자리, 0.001~0.400)
    - 측정 방법 (라디오): 호흡측정 / 혈액검사
    - 사고 여부 (체크박스): 인사사고 발생 / 재물손괴
    - 전력 (라디오): 없음 / 1회 전력 / 2회 이상 전력

    계산 로직 (도로교통법 2026년 기준):
    - BAC 0.03% 미만: 처벌 없음
    - BAC 0.03% 이상 ~ 0.08% 미만:
      - 행정처분: 면허정지 (벌점 100점)
      - 형사처벌: 1년 이하 징역 또는 500만원 이하 벌금
    - BAC 0.08% 이상 ~ 0.2% 미만:
      - 행정처분: 면허취소 (결격기간 1년)
      - 형사처벌: 1년 이상 2년 이하 징역 또는 500만원 이상 1,000만원 이하 벌금
    - BAC 0.2% 이상:
      - 행정처분: 면허취소 (결격기간 2년)
      - 형사처벌: 2년 이상 5년 이하 징역 또는 1,000만원 이상 2,000만원 이하 벌금
    - 측정 거부: 면허취소 + 1년 이상 5년 이하 징역 또는 500만원 이상 2,000만원 이하 벌금
    - 인사사고 시: 특정범죄가중처벌법 적용, 위 기준에서 가중
    - 전력 있는 경우: 가중처벌 명시

    결과 표시:
    - 행정처분 (면허정지/취소, 결격기간)
    - 형사처벌 (징역 범위, 벌금 범위)
    - 위험 등급 배지 (색상: 정지=주황, 취소=빨강, 가중=진빨강)
    - 전력/사고 가중 경고 박스
    - 법적 근거: "도로교통법 제44조, 제148조의2 (2026년 기준)"
  </action>
  <verify>
    <automated>cd "/Volumes/NJ SSD 4T/Coding/loop creation 2" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>페이지가 TypeScript 오류 없이 컴파일되고, BAC 입력 후 계산 버튼을 누르면 처벌 기준이 표시된다</done>
</task>

</tasks>

<verification>
세 페이지 모두 작성 후:
- `npx tsc --noEmit` TypeScript 오류 없음
- `npm run build` 빌드 성공
- 각 라우트 (`/tools/traffic/accident-settlement`, `/tools/traffic/fault-ratio`, `/tools/traffic/drunk-driving`) 접근 가능
- CalculatorLayout breadcrumb에 "교통/형사" 카테고리 표시
</verification>

<success_criteria>
- 3개 페이지 모두 독립 라우트로 접근 가능
- 'use client' 선언 및 CalculatorLayout 사용
- 각 계산기 입력 후 버튼 클릭 시 결과 카드 표시
- 2026년 한국 법률 기준 반영
- TypeScript 타입 오류 없음
</success_criteria>

<output>
완료 후 `.planning/phases/07-traffic-calculators/07-01-SUMMARY.md` 생성
</output>
